# TODO LIST

- [x] **[LOW]** Move the show-completed toggle into the TODO.md viewer overflow menu — Completed: 2026-06-23
  - Type: feature
  - Description: Relocate the "Show completed (N)" toggle out of the TODO.md viewer's header meta row and into the existing "More actions" (`todoMdViewerOverflowBtn`) menu in `todoMdViewer.js`, decluttering the header. It becomes a checkable item at the top of the overflow menu — above a divider, with the destructive Clear completed / Clear all below it — that flips the persisted show/hide preference and re-renders the body. The `showCompletedBtn` comes out of the meta append order so the row carries only Synced / Run backlog / Sync / ⋯ / Collapse.
  - Behavior:
    1. The toggle renders as a checkable menu item at the top of the overflow menu (`role="menuitemcheckbox"`, `aria-checked` reflecting the persisted state), separated by a divider from the Clear completed / Clear all items. Its visible label reads "Show completed (N)" / "Hide completed (N)".
    2. Tapping it flips `setTodoMdShowCompleted(...)`, re-renders the body via the existing `applyTab` path, and preserves `body.scrollTop` so the list doesn't jump — then closes the overflow menu (matching the clear items), so the re-rendered list is visible.
    3. The live count N still recomputes from `card.dataset.content` on every render (the existing `applyShowCompletedState` logic, repointed at the menu item), so opening the menu always shows the current count.
    4. At N=0 the toggle item is hidden (same intent as today's `--empty` badge-hide) — a "Show completed (0)" toggle is a no-op.
    5. Works in both placements — the desktop card and the `todoMdViewerMobileSheet` — since the overflow already renders in both.
  - Implementation notes:
    - `todoMdViewer.js`: remove the `showCompletedBtn` / `showCompletedCount` build and its `meta.appendChild(showCompletedBtn)`; add a `showCompletedItem` to `overflowMenu` above `clearCompletedItem`, with a divider element between it and the clear items. Repoint `applyShowCompletedState()` to set the item's `aria-checked`, label text, and count, and to toggle its hidden/disabled state at N=0; keep calling it on init and on every render. Move the click handler (flip pref → save scroll → `applyShowCompletedState` + `applyTab` → restore scroll) onto the item and append `closeOverflowMenu()` at its end, like the clear items.
    - The toggle stays inside the same card-build function as the overflow menu, so no listener re-registration is needed; the `prefs.js` store and the `hideCompleted: !isTodoMdShowCompleted()` render path are unchanged.
    - `style.css`: style the new checkable item (checkmark + label + count) consistent with `todoMdViewerOverflowItem`, reuse the menu's divider treatment, and carry the N=0 hide onto the item. Remove the now-unused `todoMdViewerShowCompletedBtn*` rules (button, icon, badge, `--empty`) if nothing else references them. No inline style writes from JS.
  - Out of scope: the stay-open-on-toggle alternative (close-on-toggle was chosen); any change to the Clear completed / Clear all / per-entry delete actions or the overflow menu's four-way dismissal (select / outside / Escape / re-tap); the cosmetic-vs-disk distinction of hide-completed (still render-side only — the toggle never touches TODO.md on disk). Tests: rewrite `tests/todoMdViewerShowCompleted.test.js` to assert the toggle as a menu item (open the overflow → `menuitemcheckbox` + `aria-checked` + count + close-on-select) instead of a standalone `todoMdViewerShowCompletedBtn` with `aria-pressed`, and update `tests/todoMdViewer.test.js` and `tests/mobileTodoMdViewerActionRow.test.js` for the meta row no longer holding the toggle — replacing the standalone-button assertions is intended and authorized.
  - File: `toDoList_main/src/todoMdViewer.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/todoMdViewerShowCompleted.test.js`, `toDoList_main/tests/todoMdViewer.test.js`, `toDoList_main/tests/mobileTodoMdViewerActionRow.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 0b126c11-aeac-46eb-a5e0-da2193d87ba0 -->
