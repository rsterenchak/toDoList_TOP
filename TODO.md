# TODO List

- [x] **[MEDIUM]** Add a "Run backlog" button to the TODO.md viewer card header
  - Type: feature
  - Description: Add a "Run backlog" text button to the TODO.md viewer card header, placed immediately left of the existing Sync button and styled to match it (`#161622` fill, `#2a2a38` border, `#9D93EE` text, compact, with a leading play glyph). On click it triggers the Claude Code automation routine in backlog mode (the routine selects the next eligible task). It fires by POSTing to the same Cloudflare Worker the existing inject/sync flow already uses Ã¢ÂÂ the client never calls GitHub directly or holds a token.
  - Behavior:
    1. The "Run backlog" button is always active (it does not depend on any per-todo state).
    2. On click, generate a fresh correlation_id (e.g. `crypto.randomUUID()`), then POST a backlog-mode run request to the Worker.
    3. On a successful dispatch, show a brief transient confirmation that the run was dispatched (reuse the existing inject toast pattern Ã¢ÂÂ `showInjectToast`-style Ã¢ÂÂ so styling matches), optionally including a link to the run's Actions page.
    4. On failure, show the same toast in its error variant with a short reason.
    5. While a request is in flight, disable the button (mirror the Sync button's `disabled` + loading-class pattern) so a double-click can't fire two dispatches.
  - Implementation notes:
    - The viewer card header is built in `buildTodoMdViewerCard` in `main.js`, where the `syncBtn` is created (`syncBtn.className = 'todoMdViewerSyncBtn'`, around line 5718) and appended to `meta` (around line 5744). Add the new button right before `syncBtn` in `meta` so it sits to its left. Locate with grep + offset/limit Ã¢ÂÂ `main.js` is large.
    - The existing Worker call path lives in `inject.js`: `postToWorker(payload)` is the low-level POST (reads the cached Worker URL + shared secret, sets the Bearer header). Add and export a new helper there Ã¢ÂÂ e.g. `dispatchRun({ mode, entryId, correlationId, target })` Ã¢ÂÂ that calls `postToWorker({ dispatch: true, mode, entry_id: entryId || '', correlation_id: correlationId, repo: target?.repo, filePath: target?.file_path })` and returns `{ ok, ... }` using the same `describeError` mapping the other helpers use. Import it into `main.js` alongside `readTodoMdFromWorker`.
    - The Worker's dispatch branch returns `{ ok: true, dispatched: true, ... }` on success. Use native `fetch` only (no new deps) Ã¢ÂÂ `postToWorker` already uses it.
    - On mobile the viewer card is moved into the bottom sheet (DOM move preserving listeners), so the click handler keeps working there automatically. Style the button with a class the existing viewer-header rule covers in both the inline and in-sheet instances, the same way `todoMdViewerSyncBtn` is handled.
  - Out of scope: any per-todo "Run this entry" / entry-mode control (deferred to a separate entry Ã¢ÂÂ the app has no per-row overflow menu to host it, so that work needs its own design pass and mount point). The full queued Ã¢ÂÂ running Ã¢ÂÂ PR-opened status pill and its polling (separate entry Ã¢ÂÂ this entry only shows a transient "dispatched" confirmation). The Worker-side dispatch endpoint, status branch, dedup guard, and token scope are already built and deployed.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/inject.js`, `toDoList_main/src/style.css`
  - Completed: 2026-05-29

- [ ] **[MEDIUM]** Add a run-status pill to the TODO.md viewer header
  - Type: feature
  - Description: After the "Run backlog" button dispatches a run, replace the button in the viewer card header with a status pill that polls the Cloudflare Worker and reflects the run's lifecycle (starting â queued â running â terminal). The pill uses the quiet style: a muted spinner pill for all in-flight states that only swaps its label, with color introduced only on the terminal states (success green, failure red, timed-out grey). On success the pill auto-dismisses after ~5s and the header reverts to the "Run backlog" button; on failure or timeout the pill persists with a link to the run's Actions page until the user dismisses it. Polling, dispatch, and the status read all go through the same Worker the run button already uses â the client never calls GitHub directly or holds a token.
  - Behavior:
    1. When "Run backlog" dispatches successfully (the existing button handler already generates a correlation_id and POSTs `dispatch:true`), that same correlation_id is used to start the pill, and the button is swapped out for the pill in the header `meta` container. The correlation_id is an internal tracking value used only for the dispatch/status Worker calls â it is NEVER displayed in the UI. Only one run is tracked at a time; the pill occupies the button's slot for the run's duration.
    2. The pill immediately enters a "Startingâ¦" state (muted pill, small spinner, label only â no color). This covers the post-dispatch race window where the Worker's status branch legitimately returns `{ found: false }` because the run hasn't surfaced in the runs list yet.
    3. Begin polling the Worker every 5 seconds: POST `{ status: true, correlation_id, repo, filePath }` (repo/filePath from the same resolved target the run button uses). Map the response to a pill state:
       - `{ found: false }` â keep "Startingâ¦" (race window; keep polling).
       - `status: "queued"` â "Queued" (muted spinner pill).
       - `status: "in_progress"` (conclusion null) â "Runningâ¦" (muted spinner pill).
       - `status: "completed"`, `conclusion: "success"` â "Done" terminal success state.
       - `status: "completed"`, `conclusion` anything else (failure/cancelled/timed_out/etc.) â "Failed" terminal failure state.
    4. Terminal success: pill turns green (check glyph, "Done"), stops polling, and auto-dismisses after ~5s, restoring the "Run backlog" button to the header.
    5. Terminal failure: pill turns red (alert glyph, "Failed"), stops polling, and persists. It shows an external-link affordance that opens the run's Actions URL (returned as `runUrl` in the status response). It stays until the user dismisses it (tap to dismiss â restore the button), and is NOT auto-hidden.
    6. Give-up timeout: if 10 minutes elapse from dispatch without reaching a `completed` status, stop polling and show a muted "Still running? â check Actions" state (clock glyph) that links to the runs page (use `runUrl` if a run was matched, otherwise the repo's Actions tab). This is distinct from failure â the run may still be going; the client just stopped watching. Persists until dismissed.
    7. Stop polling and clear the interval on any terminal state, on timeout, and if the viewer card is torn down / re-rendered mid-run (avoid a leaked interval firing against a detached pill).
  - Implementation notes:
    - This builds on the "Run backlog" button added to `buildTodoMdViewerCard` in `main.js` (the button sits in the header `meta` container, immediately left of `syncBtn`). The pill swap happens in that same scope; keep a reference to the button and the pill so the handler can toggle which one is mounted in `meta`.
    - The button handler must retain the generated correlation_id in a scope the pill code can read (e.g. a variable in the `buildTodoMdViewerCard` closure, or passed into a "start pill" function). This is a code-level wiring concern only â the correlation_id is internal and must NOT be rendered anywhere in the UI. If the existing handler generates it as a throwaway local, refactor minimally so the value is reachable by the pill.
    - The Worker status call should be a new exported helper in `inject.js` alongside `dispatchRun` and `readTodoMdFromWorker` â e.g. `pollRunStatus({ correlationId, target })` that POSTs `{ status: true, correlation_id, repo, filePath }` via the existing `postToWorker` and returns the parsed `{ found, status, conclusion, runUrl, runId }` (use the same `describeError` mapping for failures). Import it into `main.js`. Use native `fetch` only (no new deps); `postToWorker` already provides it.
    - Reuse the existing toast styling vocabulary where it fits (the inject toast already has ok/error variants), but the pill itself is an inline header element, not a floating toast â it lives in `meta`, mirroring how `syncBtn` is built and styled.
    - Poll with a single `setInterval` (5000ms) stored on a variable scoped to the card; clear it in all stop conditions in behavior step 7. Track dispatch time so the 10-minute give-up can be computed against `Date.now()`.
    - Style the pill states with a class the viewer-header CSS rule covers in both the inline and in-sheet (mobile bottom-sheet) instances, the same pattern `todoMdViewerSyncBtn` follows, so the pill works after the card is moved into the mobile sheet.
    - Visual reference (Void theme): in-flight pill is `#161622` fill / `#2a2a38` border / muted `#8a8699` text with a small `#534AB7`/`#cdc9ee` spinner; success is a dark-green fill (e.g. `#0f2a20` / `#1d6e56` border / `#5dcaa5` text); failure is dark-red (`#2a1414` / `#a32d2d` / `#f09595`); timeout is neutral (`#161622` / `#444441` / `#888780`). Match the existing purple accents (`#6C5DF5`/`#9D93EE`).
  - Out of scope: any per-todo "Run this entry" / entry-mode control (separate deferred entry â no per-row menu exists to host it). Tracking more than one concurrent run. Persisting pill state across a full page reload (if the user reloads mid-run the pill resets to the button; the run continues on GitHub regardless). The Worker dispatch/status endpoints, dedup guard, and token scope are already built and deployed. The correlation_id is internal plumbing and is never surfaced in the UI.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/inject.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)

- [ ] **[HIGH]** Fix the failing settingsDropdown test that blocks the full suite
  - Type: bug
  - Description: `tests/settingsDropdown.test.js` has a failing assertion at line 53 in the test "renders a ghost glyph (not a kebab) inside the trigger". The test asserts the settings trigger's `innerHTML` is set to markup containing `ghostIcon` AND does NOT contain a `<circle` (the old kebab three-circle SVG). The full suite (`npm run test:run`) fails on this, which blocks every routine PR from opening since a green suite is required. This test is unrelated to recent viewer/run-button work — it covers the nav settings dropdown's ghost-glyph trigger — so it needs its own fix.
  - Behavior:
    1. Determine whether the failure is pre-existing (fails on a clean checkout of `main` before any uncommitted work) or was introduced by a recent change. Run `npx vitest run tests/settingsDropdown.test.js` and read the actual failing assertion and the current `settingsToggle.innerHTML = ...` source in `main.js`.
    2. The two assertions are: `expect(main).toMatch(/settingsToggle\.innerHTML\s*=\s*[\s\S]*?ghostIcon/)` (passing) and `expect(main).not.toMatch(/settingsToggle\.innerHTML\s*=\s*[\s\S]*?<circle/)` (failing at line 53). The failing one means a `<circle` appears somewhere after a `settingsToggle.innerHTML =` assignment in `main.js`.
    3. The regexes are unanchored, so the `not.toMatch` can match across a large span — a `<circle` belonging to a *different* element's markup further down the file can be picked up after the `settingsToggle.innerHTML =` literal, producing a false failure even when the ghost trigger itself contains no kebab `<circle`.
    4. Decide the correct fix based on what step 1–3 reveal:
       - If `settingsToggle.innerHTML` genuinely still emits a `<circle` (the kebab wasn't fully replaced by the ghost glyph), fix the source: the trigger's markup should be the pixel-art ghost (`ghostIcon`) only, with no leftover kebab SVG.
       - If the trigger markup is correct (ghost only, no kebab) and the regex is over-matching a `<circle` from unrelated downstream markup, tighten the test so it scopes the assertion to just the `settingsToggle.innerHTML` assignment (e.g. capture the assigned string and assert on that captured group, rather than an unanchored `[\s\S]*?<circle` that can run past the assignment into other code). Do not weaken the intent — it must still catch a real kebab `<circle` inside the trigger.
    5. Run the full suite (`npm run test:run`) and confirm it is fully green before finishing.
  - Implementation notes:
    - Files are `toDoList_main/src/main.js` (if the trigger markup is wrong) and/or `toDoList_main/tests/settingsDropdown.test.js` (if the regex over-matches). `main.js` is large — grep for `settingsToggle.innerHTML` and `ghostIcon` with offset/limit rather than reading the whole file.
    - The ghost glyph is the established settings-trigger icon (the kebab three-circle SVG was intentionally replaced by it in earlier work), so the trigger containing `ghostIcon` and no kebab is the intended state — the fix should preserve that, not revert it.
    - Prefer the smallest change that makes the suite green without loosening what the test actually guards. If the source is correct, fixing the test's regex scoping is the right call; if the source regressed, fix the source.
  - Out of scope: any change to the settings dropdown's behavior, items, or styling beyond what's needed to make the trigger emit the ghost glyph without a kebab `<circle`. Adding new settings-dropdown tests beyond fixing the failing one.
  - File: `toDoList_main/tests/settingsDropdown.test.js`, `toDoList_main/src/main.js`
  - Completed: YYYY-MM-DD (PR #<number>)
