# TODO LIST

- [ ] **[MEDIUM]** Mobile description editor: lead with the phase rail and demote the manual status control
  - Type: feature
  - Description: The mobile description-editor modal is the only detail surface a task has below 1024px — the on-row status badge is CSS-hidden at that breakpoint, so this modal is where an item is actually read and worked. It currently reads as a form (body-font sentence-case controls, no pipeline state at all) rather than as the instrument panel the rest of the pipeline design uses. Restructure it to lead with a read-only phase rail rendering `derivePhase`, move the controls to the SpaceMono uppercase treatment used across the pipeline surfaces, mark Generate as the dispatched action it is, and move the manual STATUS segmented control below the actions so the derived pipeline phase and the user's own annotation are no longer stacked adjacent and read as one control.
  - Behavior: Opening a task's description on mobile shows, in order: a header line carrying the task title (uppercase display) and the close control; a four-node rail — IDEA · DRAFT · REVIEW · DONE — with nodes before the current phase filled and the current one highlighted; the entry textarea under a THE ENTRY label; Generate with its dispatch mark and a line naming which budget it spends; Inject; the Clear / Copy pair; and finally the STATUS segmented control under a label distinguishing it from the rail. The rail is display-only and cannot be tapped to change phase — phase is derived from TODO.md, so there is nothing for a tap to mean. Renaming the task still works exactly as it does today from the header. Every existing control keeps its current behavior; only order, typography, and the added rail change.
  - Implementation notes:
    - The title is NOT a label. `showDescEditorModal` builds a `titleInput` with `commitTitle`, `exitTitleEditMode`, the `titleRenameHandledByEnter` blur guard, and an Escape-to-revert path. Moving it into the header must carry that input and its full handler set — do not replace it with a static span and do not drop the pencil affordance. Render the uppercase appearance with CSS `text-transform: uppercase` so the stored value is never mutated, and remove the transform while the input is in edit mode so the user sees what they are actually typing.
    - iOS font sizes: `#descEditorModalTextarea` and the title input must stay at 16px or larger or Safari auto-zooms on focus — the `.todoStatusOption` rule carries a comment explaining exactly this. The SpaceMono button labels are a different case and are fine small, since buttons do not take text focus. Do not scale the textarea down to match the mockup's proportions.
    - Add the rail phase→display-label map to `phase.js` alongside `derivePhase` rather than defining it in `modals.js`, so the desktop `#descSibling` panel can reuse the same vocabulary later without a second copy. Map is `none`→IDEA, `draft`→DRAFT, `accept`→REVIEW, `done`→DONE. Do not add a RUN node — per-row run state is deliberately not tracked, and a permanently-empty node is worse than no node.
    - Verify the import direction before adding it: `modals.js` importing `phase.js` should be safe (`phase.js` pulls only from `inject.js` and `listLogic.js`, neither of which reaches back into `modals.js`), but `toDoRow.js` already imports `modals.js`, so confirm no cycle forms rather than assuming.
    - `#descEditorModalActions` is a wrapping flex whose current order depends on `order: -1` plus a `flex: 0 0 100%` basis on the inject button, with Generate layered on top of that. Reordering to Generate → spend line → Inject → Clear/Copy should set explicit order on every child rather than stacking another override onto the existing hack, which is already hard to reason about with three buttons in it.
    - The status control's labels and order come from `STATUS_META` / `STATUS_ORDER` — keep pulling them from there so the modal and the desktop popover cannot drift. Only its position and label styling change; the three values, their colors, and `setToDoStatus` are untouched.
    - The rail must repaint when phase changes while the modal is open — an entry can ship or be acknowledged from elsewhere mid-session. The row layer already refreshes on `TODO_RUN_STATUS_EVENT` via the `refreshDescStatusDots` sweep; subscribe the open modal to the same event and tear the listener down on close so a dismissed modal leaves nothing attached.
    - `style.css`: the rail reuses the accent tokens and the existing 10px radius and 36px-chip conventions; no new color tokens. This entry touches `style.css`, so it runs with no other style-touching entry in flight.
  - Out of scope: The desktop `#descSibling` description panel, which keeps its current layout — matching it to this is a deliberate follow-up. `derivePhase` itself and its four-value vocabulary. The on-row status badge, the desktop status popover, and the left-edge color tab. Generate's and Inject's behavior, the triage path, and the queue. Making the rail interactive in any way. The task row, the TODO.md viewer, and the chat sheet.
  - File: `toDoList_main/src/modals.js`, `toDoList_main/src/phase.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: f55f2905-2475-4768-a700-a99b4ceac8cb -->
