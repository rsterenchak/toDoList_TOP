# TODO LIST

- [x] **[HIGH]** Register the injection points the app lost when the Agent view was severed — Completed: 2026-07-31
  - Type: bug
  - Description: Flagging a task queues a `triaging` row and never fires `claude-triage.yml`, so Generate has been silently broken since `main.js` was severed from `agentView.js`. The row layer dispatches through `fireTriageSweep(projectName)` in `agentQueueStore.js`, which delegates to a `_triageDispatcher` registered by `setTriageDispatcher` — whose only caller is `agentView.js`, which nothing loads any more. So `_triageDispatcher` is null, `fireTriageSweep` resolves to null, `dispatchTriage` is never called, and `startSweepTracking` never runs, which is also why the nav dot never lights for a sweep. An audit found three further registrars in the same state: `setTriageInFlight`, also called only from the severed view, and `setItemPhaseResolver` and `setBlockedItemResolver` in `taskFilter.js`, which have NO caller in any module. This entry closes the class rather than one instance.
  - Behavior: Flagging a task from the row's Generate button or the ASKING answer path dispatches a real `claude-triage.yml` run, the nav working dot lights while it is in flight and clears when it settles, and the queue row leaves `triaging` normally. The task filter pills and the blocked chip resolve each task's phase and blocked state through their intended path. No feature gains or loses capability — this restores wiring that was silently lost.
  - Implementation notes:
    - Register a boardless triage dispatcher from `agentWiring.js`, the module that already loads on boot. Reproduce what `agentView.js` did: resolve the project id, mint a correlation id, call `dispatchTriage`, then `startSweepTracking`. Use only acyclic imports — `dispatchTriage` and `mintEntryId` from `inject.js`, `resolveDispatchTarget` from `dispatchDraft.js`, and `startSweepTracking` / `setTriageDispatcher` from the store. Do NOT import `agentView.js`, and verify no cycle forms.
    - `setTriageInFlight` is in the same state. Either register it from `agentWiring.js` alongside the dispatcher, or establish that it is genuinely board-only and remove it. Say which and why.
    - `setItemPhaseResolver` and `setBlockedItemResolver` have no caller anywhere. Determine what they were meant to inject, and whether the phase filter pills and the blocked chip are currently working through a fallback or are quietly broken. Then either wire them from the module that owns the resolution, or delete them as dead API. Report which, with evidence for the filters' current behavior.
    - Leave `agentView.js`'s own registrations in place. It still self-registers when a test mounts the board, and the deletion sequence removes them with the file.
    - THE EXISTING TESTS CANNOT VALIDATE ANY OF THIS. The triage-dispatch tests import `agentView.js` for its side effects, so they exercise a wired state the app never reaches — which is exactly why this shipped. Add tests that assert each registration happens through the boot path WITHOUT mounting the board.
    - Manual verification is required before this is done: flag a task in the app, confirm a real `claude-triage.yml` run appears in Actions, confirm the nav dot lights and clears, and click through the ALL / IDEAS / RUNNING / DONE pills and the blocked chip to confirm their counts are correct. Report all four results.
    - Audit for any remaining registrar whose only caller is `agentView.js` or which has no caller at all, and report the list even if empty. This is the fourth regression from the same severing; the goal is that it is the last.
  - Out of scope: Deleting `agentView.js`, its CSS, or any test file. Retiring the board test files. The trackers' cadence and settle logic. `derivePhase`, the phase vocabulary, and the queue pipeline's semantics.
  - File: `toDoList_main/src/agentWiring.js`, `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/taskFilter.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 34d5b79a-f145-4541-a327-ed0f650cc97d -->

- [x] **[MEDIUM]** Create a real todo row when a derive proposal is accepted — Completed: 2026-07-31
  - Type: feature
  - Description: Accepting a proposed row ships through the ONE shared funnel in `dispatchDraft` (toDoList_main/src/dispatchDraft.js), called from both `buildProposedSecondary` in agentView.js (Agent board) and `buildProposalCard` in assignmentCoverage.js (Coverage tab's proposal review modal) via `dispatchDraft(row, draftText, row.entry_id)`. That funnel calls `shipEntryForTodo({ todoId: row.todo_id, ... })`, and for a fresh derive proposal `row.todo_id` is null (per the comment at agentView.js:713-716), so the run ships straight to TODO.md/agent_queue with no visible todo item ever created in the sidebar list. Add a branch in dispatchDraft.js: when `row.todo_id` is falsy, create a real todo in the currently-selected project (dispatchDraft.js already has `getSelectedProjectName()` locally) before shipping, mirroring the existing `pushCandidate` flow in toDoList_main/src/refactorCard.js (lines ~276-297): call `listLogic.addToDo(projectName, row.context.title)`, look up the created item via `listLogic.listItems(projectName)`, backfill `created.desc = row.context.description` via `listLogic.editToDoItem(projectName, created)`, then pass `created.id` as `todoId` to `shipEntryForTodo` instead of `row.todo_id`. Persist the new id back onto the agent_queue row through the existing `listLogic.setAgentRunState(rowId, patch)` call in dispatchDraft.js so later reconciliation (`stampEntryShipped` in listLogic.js) can find and stamp it — this needs `'todo_id'` added to the `allowed` field whitelist inside `setAgentRunState` (toDoList_main/src/listLogic.js, currently omits it).
  - File: `toDoList_main/src/dispatchDraft.js`, `toDoList_main/src/listLogic.js`
  - Out of scope: rows that already carry a `todo_id` (normal flagged tasks, not derive proposals) are unaffected — this only fills the gap for proposals accepted with no source todo.
  <!-- id: 966d0fa7-6944-466f-b45f-e4dabaf02de4 -->

- [x] **[MEDIUM]** Refresh #mainList after a derive proposal creates a real todo — Completed: 2026-07-31
  - Type: bug
  - Description: When accepting a derive proposal whose queue row has no `todo_id`, `dispatchDraft()` in `dispatchDraft.js` (around lines 69-84) materializes a real todo via `listLogic.addToDo(projectName, title)` + `listLogic.editToDoItem(...)`, but never re-renders `#mainList` afterward. The new row sits in the data model but the visible list is stale until the user navigates to a different project and back (which rebuilds `#mainList` from data on selection). The two other call sites of `listLogic.addToDo` already guard against this: `refactorCard.js`'s `pushCandidate` calls its local `rebuildMainList(projectName)` helper (refactorCard.js:163, using `addToDos_restore`/`addAllToDo_DOM` from `toDoRow.js`) right after creating the todo, and `seedTasksModal.js`'s add handler does the same rebuild inline after its batch add. Fix by rebuilding `#mainList` in `dispatchDraft()` immediately after `todoId` is set from the newly created item, only when the created todo belongs to the currently-selected project (compare against `getSelectedProjectName()`, already defined in this file) so it doesn't repaint an unrelated project's list.
  - File: `toDoList_main/src/dispatchDraft.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: de80a73f-5be1-41aa-be6f-db7481b9519b -->

- [x] **[MEDIUM]** Redefine the desktop filter pills to ALL / IN PROGRESS / DONE — Completed: 2026-07-31
  - Type: feature
  - Description: The desktop pills filter purely on derived phase — ACTIVE matches `draft` or `accept`, RUNNING matches `running`, DONE matches `done` — so with 17 tasks that have no injected entry, all three read zero while ALL reads 17. The set was designed around pipeline states, which describe a small minority of a real list. Redefine it around what is actually useful: three pills, mixing manual status and derived phase deliberately, so "in progress" means work you are doing OR work the machine is doing. Drop RUNNING; an in-flight run is a kind of in-progress, not a separate category.
  - Behavior: The desktop queue rail shows three pills with live counts. ALL matches every task that is not completed — active, in progress, and idea alike. IN PROGRESS matches a task whose manual status is `in_progress`, OR whose derived phase is `draft` or `running` — an entry injected and awaiting its run, or a run in flight. DONE matches a task that is either shipped-and-acknowledged (`PHASE.DONE`) or checked off. ALL and DONE are therefore complementary rather than nested: ALL is open work, DONE is finished work. Exactly one pill is active, the selection persists, and counts update live as statuses and phases change. Mobile is unchanged — it keeps its ACTIVE / Ideas cycle pill on manual status.
  - Implementation notes:
    - `PHASE_FILTERS`' `match` currently receives a phase string. All three new predicates need more than that — manual status, derived phase, and the completed flag. Change the signature to take the ITEM and derive what each needs inside, rather than threading three arguments through. Update every call site and the comment block above the array, which documents the old phase-only contract.
    - DONE matching checked-off tasks is the sharp edge. Completed rows live in the collapsed COMPLETED section, not the main list — so filtering to DONE must reveal them, or the pill will show a count with nothing beneath it. Grep how that section's collapse works and decide deliberately: either DONE expands it, or DONE renders those rows in the main list while active. Say which, and verify the count and the visible rows agree.
    - Dropping RUNNING does NOT remove `PHASE.RUNNING` from `phase.js`. The phase stays — it drives the row badge and `derivePhase`'s ordering — and IN PROGRESS simply includes it.
    - The persisted filter key can hold `running` or `active` from the old set. Migrate on read: an unknown or retired value resolves to `all` rather than leaving the list empty on first load after deploy.
    - Update `EMPTY_MESSAGES`. The `running` entry becomes dead, `active` is shared with the mobile status filter and must keep working there, and `done` needs copy covering both senses rather than only "shipped and acknowledged".
    - Counts come from the full committed row set, as they do now — and DONE's count must include completed rows even though they sit in a separate section, or it will under-report.
    - Keep `taskFilter.js`'s dependency discipline: it deliberately holds `prefs` as its only hard dependency and inlines the phase strings rather than importing `phase.js`. If the new predicates need the completed flag or manual status, read them off the item rather than adding an import.
    - Tests: ALL matches uncompleted tasks of every manual status; IN PROGRESS matches `in_progress` status, `draft` phase, and `running` phase, and excludes idle ideas; DONE matches both an acknowledged task and a checked-off one; a stored `running` preference resolves to ALL; and the counts match what renders.
  - Out of scope: The mobile cycle pill and its manual-status filters. The blocked chip, which stays an independent overlay filter. `derivePhase`, `PHASE.RUNNING`, and the phase vocabulary. The row badges. The COMPLETED section's own behavior beyond whatever DONE needs to reveal it. The sort control.
  - File: `toDoList_main/src/taskFilter.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: f89b24df-dad7-463b-a376-8b5b9a49f089 -->

- [x] **[MEDIUM]** Coverage: let any aspect be manually confirmed, alongside its derived status — Completed: 2026-07-31
  - Type: feature
  - Description: An aspect's status is derived entirely from tagged `agent_queue` rows — a shipped run marks it covered, and nothing else can. But whether A1 actually meets the rubric's *Competent* bar is a human judgment: a run may cover an aspect partially, you may do part of it by hand, or the evaluator's bar may be higher than what shipped. Today only the manual lane's process aspects carry a tick; everything else is machine-inferred with no way to record agreement. Extend `buildCommitTick` to every aspect so each can be confirmed independently, and show confirmation ALONGSIDE the derived status rather than overriding it — so an aspect that shipped but you have not confirmed is visibly distinct from one you have signed off, which is exactly what you want to see the night before a submission.
  - Behavior: Every aspect row in the coverage breakdown carries a confirmation tick, not just the manual lane's. Ticking one records your confirmation and persists it; unticking clears it. The row continues to show its derived status — Shipped, In progress, Proposed, Blocked, Not started — with confirmation rendered as a separate mark, so the four combinations all read distinctly: shipped-and-confirmed, shipped-but-unconfirmed, unshipped-but-confirmed (work done by hand), and neither. The headline count reflects confirmation rather than derivation, since that is the number that matters at submission — with the derived coverage still visible in the bar. The manual lane keeps its existing "mark done" copy and behavior unchanged.
  - Implementation notes:
    - `buildCommitTick(item, ctx, labelText)` is already generic — it takes the label, persists via `listLogic.setAspectSubmitted`, reverts on failure, and registers in `ctx.ticks` so the modal's async submission-hydrate refreshes it. Render it on every aspect row rather than writing anything new. Choose copy that reads correctly for a rubric aspect rather than reusing "Committed to GitLab" or "mark done".
    - CONFIRMATION AND DERIVATION MUST STAY SEPARATE. Do not let a tick change the derived status or vice versa. `aspectStatus` keeps deriving from queue rows; confirmation is a second, independent axis read from `aspect_submissions`. An aspect showing Shipped with no confirmation is a legitimate and important state.
    - The headline currently reads "N of M covered" from `computeCoverage`. Decide deliberately what it counts now and state it in the PR body: confirmed, derived, or both shown. My reading is that confirmed is the useful number and derived belongs in the bar — but the bar's segments are also derived, so check the two do not end up contradicting each other on screen.
    - The tab's compact summary reads from the same computation. Whatever the headline counts, the summary must agree — one source, not two.
    - `showCoverageDetailModal` hydrates committed ticks from `aspect_submissions` and accepts a preloaded Set. That path already exists for the manual lane; confirm it covers every aspect id now rather than only process ones, and that the modal's live repaint on `onQueueChange` does not reset tick state mid-interaction.
    - Verify what `setAspectSubmitted` keys on. If `aspect_submissions` rows are scoped by project and aspect id, confirming A1 on one project must not appear on another's A1 — rubrics reuse those labels.
    - Ticks are real checkboxes inside a modal that repaints. Confirm focus is not lost when a repaint lands while a tick is focused, and that a rapid tick/untick cannot leave the UI and the store disagreeing — the optimistic-set-then-revert-on-failure shape is already there; make sure it survives a concurrent repaint.
    - Tests: every aspect renders a tick, not only manual ones; ticking persists and unticking clears; confirmation does not alter derived status; the headline and the tab summary agree; and a hydrate populates ticks for non-process aspects.
  - Out of scope: The "kept it but it made things worse" regression marker, still deferred. `aspectStatus`, `computeCoverage`'s derivation from queue rows, and the bar's segmentation. The proposal modal and Derive. The manual lane's existing copy and behavior. `assignment.md` parsing and `PROCESS_ASPECT_RE`.
  - File: `toDoList_main/src/assignmentCoverage.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 6c94edc8-ac2f-4a23-bbc2-54e172a22d39 -->

- [x] **[MEDIUM]** Copy a shipped change as a context block for an outside conversation — Completed: 2026-07-31
  - Type: feature
  - Description: Iterating on a shipped change outside the app means reassembling its context by hand — finding the entry text, the PR number, and the files touched, then pasting them somewhere. Add a control that produces that block in one tap. Put it on the detail pane's ACCEPT face, which already has every field it needs and is driven by the linked `agent_queue` row rather than local state — so unlike the RUNS tab's `localStorage` records, it works from whichever device you are holding.
  - Behavior: A task in the `accept` phase shows a copy control alongside its existing actions. Activating it copies a plain-text block to the clipboard and confirms with the existing toast. The block names the repo, the entry's marker id, the PR number and URL, the files from the entry's `- File:` line, the entry text as shipped, and a trailing placeholder line inviting a description of the desired change. The control does not alter the task, the entry, or the queue row — it only reads.
  - Implementation notes:
    - Block format, in this order, plain text with no markdown fences so it survives pasting anywhere:
      `Iterating on a shipped change in <owner>/<repo>.` then a blank line;
      `ENTRY <marker uuid>`;
      `PR #<number> — <pr_url>` (omit the line entirely when the queue row carries neither);
      `FILES <comma-separated paths from the entry's - File: line>` (omit when there is no File: line);
      a blank line, then `--- the entry as shipped ---` and the entry's full text;
      a blank line, then `--- what I want changed ---` and a single placeholder line.
    - The trailing placeholder is the point of the block — it prompts the description that makes the paste actionable. Do not omit it.
    - Every field already exists on this surface: the marker from `item.entryId`, `pr_number` / `pr_url` from the linked queue row via the shared store, and the file paths from the same tolerant `- File:` matcher the FILE readout uses. Import that matcher rather than writing a second one.
    - Entry text comes from `item.desc`. Copy it verbatim including the trailing marker comment — the marker is what lets any follow-up trace back, and stripping it would defeat the block's purpose.
    - Clipboard writes need a user gesture and can reject. Wrap `navigator.clipboard.writeText()` in try/catch AND handle the rejected promise, matching how the paste chip does it. On failure, fall back to selecting the text in a temporary element so a manual copy still works, and say so in the toast rather than failing silently.
    - Mount idempotently in the ACCEPT block alongside the existing actions, registered in `DESC_PANEL_CHILD_SELECTORS`, and excluded from `DESC_AUTHORING_GROUP_SELECTORS` for the same reason the other accept actions are — the group is hidden in `done`, and this must survive long enough to be used.
    - Style it as a ghost action beside OPEN IN TODO.MD rather than competing with ACCEPT & CLOSE. It is a secondary path, not a decision.
    - Gate on pane mode; the mobile modal's ACCEPT face keeps its single route action, per the earlier decision that inlining accept controls there costs too much height.
    - Test: the block contains every present field and omits absent ones cleanly; the marker survives verbatim; a rejected clipboard write falls back rather than failing silently; and the control renders only in the `accept` phase.
  - Out of scope: The RUNS tab's `localStorage` records and making them cross-device — a separate change, and one that may be better served by reading `agent_queue` than by syncing local state. The in-app iterate flow. The ACCEPT face's other actions. The mobile modal.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 4b179cbf-36fd-4678-a76c-5b0590be1c71 -->

- [x] **[MEDIUM]** Runs tab: read from the queue store so runs appear on every device — Completed: 2026-07-31
  - Type: feature
  - Description: The RUNS tab reads `runRecords`, an array persisted to `localStorage` under `todoapp_claudeRuns`, so it shows only runs dispatched from the browser you are holding. A run started on the phone is invisible on the desktop and vice versa — and since a shipped run record is the entry point to iterate mode, iterating is only possible from the device that dispatched. Source the list from `agent_queue` instead, which already holds `run_id`, `pr_number`, `pr_url`, `state`, and `todo_id`, is scoped per project, and is cross-device by construction. Two fields have no queue equivalent and need handling: `correlationId`, used to poll a run's status by matching the workflow run-name, and the derived `title`.
  - Behavior: The RUNS tab lists that project's runs regardless of which device dispatched them, newest first, each with its title, status, and the time it started. A shipped run still opens iterate mode. In-flight runs still poll and update to their terminal state. The list reflects runs dispatched from the row, the detail pane, and the coverage tab's Derive, not only ones started from chat — the queue is the pipeline's record, not the chat's. Switching projects switches the list. Nothing about dispatching, polling cadence, or iterate mode's behavior changes.
  - Implementation notes:
    - ESTABLISH THE FIELD MAPPING FIRST and report it in the PR body before describing anything else. For each of `entryId`, `correlationId`, `title`, `status`, `dispatchedAt`, `repo`, `project`, `agentRowId`: name the `agent_queue` column that supplies it, or state that it has none. `entryId` → `entry_id`, `project` → resolved from `project_id`, `agentRowId` → the row's own id, and `status` derives from `state`. `repo` should come from the project's routed target rather than the row.
    - `correlationId` is the sharp edge. It is minted client-side at dispatch and echoed into the workflow's run-name so status polling can find a run GitHub does not return an id for. If it is not persisted on the queue row, a run dispatched elsewhere cannot be polled from here — it can still be LISTED with its state from the row, but its live status will not update. Decide deliberately: either persist `correlation_id` on the queue row so any device can poll (a schema addition, and the honest fix), or accept that cross-device runs show their stored state without live polling and say so in the PR body. Do not silently ship the second while implying the first.
    - `title` is derived by `deriveRunTitle(entryText)` from the entry markdown. Reproduce it from the queue row's draft or the linked todo's description rather than storing a second copy — the derivation is cheap and a stored title would drift from an edited entry.
    - Keep `localStorage` as a fallback, not a parallel truth. If reading it at all after this, use it only for records with no matching queue row, and prune those once matched — two lists that can disagree is worse than one that is occasionally incomplete.
    - Repaint on `onQueueChange` so the list tracks rows arriving and settling live, the same subscription the coverage tab and the row badges already use. Drop any polling the tab does purely to refresh its own list.
    - Iterate mode's entry point takes a shipped run record. Confirm what it actually reads from that record — if it needs `correlationId`, the decision above determines whether iterate works cross-device or only for locally-dispatched runs. State the outcome plainly.
    - Verify the list is project-scoped and switches with the active project; `agent_queue` rows carry `project_id` and the store already filters by it.
    - Test: the tab lists runs from queue rows with no local records present; a row settling updates its status live; switching projects switches the list; a run dispatched from the row or from Derive appears (not just chat-dispatched ones); and iterate still opens from a shipped run.
  - Out of scope: The iterate flow's own behavior once opened. Dispatching, the reconciler, and the run trackers. The chat transcript, its tabs, and the New Chat control. Adding a `correlation_id` column to `agent_queue` — if the mapping shows it is needed, note it as a follow-up with the SQL rather than assuming the schema change is in scope here.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/agentQueueStore.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 28acb54f-d9a1-4a3d-aa03-1b47089e1886 -->

- [x] **[LOW]** Mobile: add the copy-context control to the ACCEPT face — Completed: 2026-07-31
  - Type: feature
  - Description: The control that copies a shipped change as a pasteable context block was scoped to the desktop detail pane, following the earlier decision that inlining the ACCEPT face's controls into the mobile modal costs roughly 210px and pushes the entry textarea, mode strip, picker and Generate below the fold. But that cost came from the WHAT CHANGED card, the file list, and the three decision actions — a single copy control is one button with no supporting content, closer to 40px. Add just that one to the mobile modal's ACCEPT face. The block exists to be pasted into an outside conversation, and the phone is where that most often happens, so the one surface likely to need it is currently the one without it.
  - Behavior: On mobile, a task whose phase is `accept` shows a copy-context control in its description-editor modal, beside the existing REVIEW SHIPPED CHANGE action. Activating it copies the same block the desktop control produces — repo, entry marker, PR number and URL, files, the entry as shipped, and the trailing placeholder — and confirms with the existing toast. Everything else about the mobile ACCEPT face is unchanged: acknowledging still routes to the TODO.md viewer, and the WHAT CHANGED card, file list, and decision actions stay desktop-only.
  - Implementation notes:
    - Reuse the desktop implementation, do not reimplement. The block builder should already be a function in `toDoRow.js`; if it is inlined in the desktop control's handler, extract it so both hosts call one builder — a second copy would drift from the first the next time the block's format changes.
    - Keep the clipboard handling identical: `navigator.clipboard.writeText()` wrapped in try/catch AND with the rejected promise handled, falling back to selecting the text in a temporary element so a manual copy still works. Mobile Safari is the case most likely to reject, which makes the fallback matter more here than on desktop.
    - Mount it in the modal's ACCEPT branch alongside REVIEW SHIPPED CHANGE. Style it as a ghost action so the amber route action stays the primary — acknowledging is still the main thing you do in that state.
    - `showDescEditorModal` rebuilds the modal on every open, so mount on open rather than initialising once.
    - The modal is capped at 92vh and already scrolls; one added control should not change that, but verify the ACCEPT face still fits without the body scrolling on a short viewport, since that overlap bug has appeared here before.
    - Do NOT add the WHAT CHANGED card, the FILE list, or REVERT and ACCEPT & CLOSE to the mobile modal. That decision stands — this is the one control whose value outweighs its height.
    - Test: the control renders only in the `accept` phase on mobile; it produces a block identical to the desktop control's for the same task; a rejected clipboard write falls back rather than failing silently; and the modal does not overflow its cap with the control present.
  - Out of scope: The block's format and contents, correct as landed. The desktop ACCEPT face. The mobile modal's other controls and its route to the viewer. The RUNS tab and iterate mode.
  - File: `toDoList_main/src/modals.js`, `toDoList_main/src/toDoRow.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 28a37ac2-44ac-48c3-a05a-232c023bfc6a -->

- [x] **[MEDIUM]** Open iterate mode on a shipped entry directly from the task — Completed: 2026-07-31
  - Type: feature
  - Description: Iterating on a shipped change is only reachable by tapping a SHIPPED record in the RUNS tab. That list is `localStorage`-backed, so it shows only runs dispatched from the device you are holding — and there is no path from a task to iterating on it, even though the task is where you notice the change needs adjusting. Iterate does not actually need a run record: `activeIterateEntry` is a per-workspace entry id, and `entryId` is passed on the first turn so the Worker resolves the merged diff from that marker and assembles the seed. The task already carries `item.entryId`. Add a control on the ACCEPT face that opens chat in iterate mode for that entry, scoped to that task's repo.
  - Behavior: A task whose phase is `accept` shows an iterate control alongside its existing actions. Activating it opens the chat surface — the docked pane on desktop, the sheet on mobile — switches to the CHAT tab, sets the workspace to that task's repo if it is not already there, starts an iterate session seeded with the task's entry id, and focuses the composer so you can describe the change. The first turn carries the entry id so the Worker resolves the shipped diff; later turns omit it, as they do today. Iterating from the RUNS tab is unchanged. The task, its entry, and its queue row are not modified.
  - Implementation notes:
    - Reuse the existing iterate entry point rather than reconstructing the session. Grep how the RUNS tab's shipped record opens iterate — it sets `activeIterateEntry` and drives the first turn — and expose that as a function the row layer can call with an entry id and a repo. Do not set `activeIterateEntry` directly from `toDoRow.js`.
    - `toDoRow.js` must NOT import `claudeSheet.js` — the documented `toDoRow → claudeSheet → modals → toDoRow` cycle. Use the registered-handler pattern already established for Discuss (`setDiscussTaskHandler`), registering the iterate opener from `main.js` the same way.
    - Repo scoping matters. `activeIterateEntry` is per-workspace and swapped in lockstep with `chatHistory` on a workspace change. Opening iterate for a task in a different repo than the active chat workspace must switch the workspace first, or the session will attach to the wrong thread. Verify against the existing workspace-swap path rather than adding a second one.
    - The Worker treats a 404 as "nothing to iterate on yet". A task whose entry was cleared from `TODO.md`, or whose PR was reverted, will hit that — surface it as a readable message in the transcript rather than a silent failure or a raw error.
    - Mount it in both hosts' ACCEPT faces, since the mobile modal now carries the full face. On mobile, opening chat means opening the sheet over the modal — decide whether the modal dismisses first and say which; leaving a modal stacked under an open sheet is the likely defect.
    - Style it as a ghost action beside OPEN IN TODO.MD and COPY CONTEXT. It is a third route, not a decision — ACCEPT & CLOSE stays primary.
    - Mount idempotently, register in `DESC_PANEL_CHILD_SELECTORS`, and keep it out of `DESC_AUTHORING_GROUP_SELECTORS` like the other accept actions.
    - Test: the control renders only in the `accept` phase; activating it opens chat on the CHAT tab with the task's repo active and an iterate session seeded with the entry id; a task in a non-active workspace switches the workspace first; a 404 surfaces a readable message; and RUNS-tab iterate still works.
  - Out of scope: Making the RUNS tab cross-device, which is a separate prepped entry. The iterate conversation's own behavior once open. The COPY CONTEXT block. Iterating on anything other than a shipped entry.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/main.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 57b89766-bb42-4076-af4d-9f852887380a -->

- [x] **[MEDIUM]** Runs tab: list every shipped entry, not only those with a queue row — Completed: 2026-07-31
  - Type: bug
  - Description: The RUNS tab was moved off `localStorage` and onto `agent_queue`, which fixed the device-local problem — but the queue is not the complete record of shipped work. A row only exists there for entries that went through triage or agent dispatch; an entry injected and run via Run backlog or the entry's own Run pill never gets one, so it is still missing from the list and cannot be iterated on. The complete set is the checked entries in `TODO.md`: every entry that actually shipped, regardless of how it was dispatched, cross-device by nature, each carrying the marker that iterate keys on. Use that as the list's spine and enrich from the queue where a row exists.
  - Behavior: The RUNS tab lists every shipped entry for the active project — each checked entry in that project's `TODO.md` — newest first, whether it was dispatched from chat, from the row, from Run backlog, or from the entry's Run pill. Each row shows its title derived from the entry text, and where a matching `agent_queue` row exists, its status and PR reference as well. Entries with no queue row show as shipped without run detail rather than being omitted. Tapping any of them opens iterate mode, which needs only the entry id. In-flight runs continue to appear and update as they do now. Switching projects switches the list.
  - Implementation notes:
    - The spine is the marker set the viewer already parses. `parseTodoMdMarkers` in `inject.js` returns `present` and `shipped` marker sets per repo, and `refreshShippedMarkers` populates them with a 60s TTL. Read the shipped set rather than re-fetching or re-parsing `TODO.md` — one parse, one cache.
    - Match queue rows to entries by `entry_id`. Where a row exists, take its `state`, `pr_number`, and `pr_url`. Where none exists, render the entry with its title and a plain shipped state — do not synthesise a fake run status.
    - Titles come from the entry text via `deriveRunTitle`. The marker cache holds ids, not text, so establish where the entry text comes from and say so in the PR body: the viewer's parsed tokens if they are reachable without a second fetch, or the linked todo's description. Do not add a `TODO.md` read that the viewer is already doing.
    - IN-FLIGHT RUNS ARE NOT IN THE SHIPPED SET. An entry mid-run is unchecked in `TODO.md`, so a shipped-only spine would drop it from the list exactly while you are watching it. Union the shipped entries with the queue's non-terminal rows so a run in progress still appears and settles into place when it lands.
    - Order by something stable and meaningful. The marker set has no timestamps; queue rows have `created_at` and todos have `shipped_at`. Pick one, state it, and make entries with no timestamp sort predictably rather than jumping around between repaints.
    - Repaint on both `onQueueChange` and `TODO_RUN_STATUS_EVENT` — the first covers queue transitions, the second covers the marker cache refreshing after a run merges. A list sourced from markers that only listens to the queue will go stale exactly when an entry ships.
    - Keep the list project-scoped. The marker cache is keyed by repo and queue rows by `project_id`; confirm both resolve from the active project consistently.
    - Do not reintroduce `localStorage` records as a third source. If the previous entry left a fallback path, remove it once this lands — three sources that can disagree is worse than one that is occasionally sparse.
    - Test: an entry shipped via Run backlog with no queue row appears in the list and opens iterate; an entry with a queue row shows its status and PR; an in-flight run appears before it is checked and stays in place after; the list repaints on a marker refresh; and switching projects switches the list.
  - Out of scope: Iterate mode's own behavior once opened. Dispatching, the reconciler, and the run trackers. `parseTodoMdMarkers` and the marker cache's TTL. The chat transcript and its other tabs. Adding a `correlation_id` column to `agent_queue`.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/inject.js`, `toDoList_main/src/agentQueueStore.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 99e53a1a-5b7f-4a7b-a1ba-2e6b32481875 -->

- [x] **[LOW]** DONE filter: shipped-and-acknowledged only, excluding checked-off tasks — Completed: 2026-07-31
  - Type: bug
  - Description: The DONE pill matches `!!item.completed || phaseOf(item) === 'done'`, so it includes checked-off tasks. Those live in the collapsed COMPLETED section rather than the main list, which forced the awkward requirement that DONE reveal that section — and it conflates two different ideas. The useful meaning is narrower: work the pipeline finished that is still sitting in your open list. Change DONE to match a task whose derived phase is `done` AND which is not checked off, so it selects exactly the rows carrying the shipped glyph that you have not yet filed away.
  - Behavior: DONE matches a task whose derived phase is `done` and whose `completed` flag is false — shipped, acknowledged, still open. A checked-off task is excluded regardless of its phase, so the COMPLETED section is never revealed by this filter and its count always agrees with what renders in the main list. ALL continues to match every uncompleted task, so DONE is now a strict subset of ALL rather than complementary to it. IN PROGRESS is unchanged. Mobile is unchanged.
  - Implementation notes:
    - The predicate becomes `!item.completed && phaseOf(item) === 'done'`. Both halves are already available on the item; no new import or signature change is needed.
    - REMOVE the COMPLETED-section reveal that the previous entry required. Whatever was added so DONE could show checked-off rows — expanding the section, or rendering those rows into the main list while DONE is active — is now dead and should come out, or DONE will expand a section containing nothing it matches. Grep for it deliberately; leaving it is a silent oddity rather than a visible bug.
    - Update the `EMPTY_MESSAGES` copy for `done`. The comment block above it states DONE's copy "covers both of its senses — shipped-and-acknowledged work and checked-off work"; that is no longer true and both the copy and the comment need correcting.
    - Update the comment above `PHASE_FILTERS` if it describes DONE's old two-sense contract, and the ALL/DONE relationship if it says they are complementary — DONE is now nested inside ALL.
    - Counts still come from the full committed row set, and now naturally exclude completed rows, so the under-reporting concern from the previous entry no longer applies. Verify the count matches the rendered rows exactly.
    - Tests: DONE matches a shipped-and-acknowledged task that is not checked off; excludes the same task once checked off; excludes a checked-off task whose phase is not `done`; and the count agrees with what renders.
  - Out of scope: ALL and IN PROGRESS, both correct as landed. The mobile cycle pill. The blocked chip. `derivePhase`, `PHASE.DONE`, and the phase vocabulary. The COMPLETED section's own collapse behavior. The row glyphs and badges.
  - File: `toDoList_main/src/taskFilter.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: c8b68ff0-e92e-41b0-85b7-240812504d62 -->

- [x] **[HIGH]** Desktop filters hide completed rows, so the COMPLETED section expands empty — Completed: 2026-07-31
  - Type: bug
  - Description: Expanding the COMPLETED section on desktop reveals nothing. Completed rows are governed by two independent hiding mechanisms — the section's own collapse, and the task filter's `taskFilterHidden` class — and the desktop pills now hide them unconditionally. `PHASE_FILTERS`' ALL is `!(item && item.completed)`, and IN PROGRESS and DONE both open with `if (!item || item.completed) return false`. So no desktop pill matches a completed row, every one of them stays hidden, and un-collapsing the section exposes rows the filter has already suppressed. The mobile `all` filter is `return true` and does not have this problem, which is why it only appears on desktop. The section's collapse should be the only thing governing completed rows; the filter should not have an opinion about them.
  - Behavior: Expanding the COMPLETED section on desktop reveals its rows, under every pill. Collapsing hides them again. The pills continue to filter the OPEN list exactly as they do now — ALL matches uncompleted tasks of any status, IN PROGRESS matches in-progress or a draft/running phase, DONE matches shipped-and-acknowledged tasks that are not checked off — and none of them changes what the COMPLETED section shows. The pills' counts continue to describe the open list only, so the COMPLETED header's own count remains the source for completed work. Mobile is unchanged.
    - Verify against the section's collapse state: with COMPLETED expanded, switching between all three pills must leave its rows visible throughout.
  - Implementation notes:
    - Exempt completed rows from the desktop filter rather than making each pill match them. `applyTaskFilter` should leave a completed row's `taskFilterHidden` class alone entirely, so the COMPLETED section's collapse is the sole authority. That is one exemption at the apply site rather than three predicate changes, and it cannot drift as pills are added.
    - Do NOT simply change ALL to `return true`. IN PROGRESS and DONE would still hide completed rows, so the section would populate under ALL and empty under the other two — a worse, more confusing state than the current one.
    - Leave the predicates' `item.completed` guards in place for COUNTING. The pills describe the open list, and their counts should keep excluding completed rows; it is only the visibility application that must exempt them. Confirm the counting path and the hiding path are separable — if they share one pass, split them and say so.
    - Grep how the COMPLETED section's collapse actually hides its rows. If it uses its own class or attribute, the two mechanisms are cleanly separable; if it also relies on `taskFilterHidden`, they are entangled and the fix needs to give the section its own hiding mechanism first. Report which you found.
    - `reorderToDoDOM` internally calls `applyTaskFilter` — never call both together. Any repaint added here must respect that.
    - Verify the mobile path is untouched: its `all` is `return true` and its status filters run through the same apply site, so the exemption must not change what mobile shows.
    - Test: with COMPLETED expanded, all three desktop pills leave its rows visible; collapsing hides them under every pill; the pills' counts still exclude completed rows; and mobile's status filters behave as before.
  - Out of scope: The pills' definitions and counts, correct as landed. The COMPLETED section's collapse behavior, its header count, and its position. The mobile cycle pill and the blocked chip. `derivePhase` and the phase vocabulary.
  - File: `toDoList_main/src/taskFilter.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 1c1b58a2-e316-4aaf-b897-b704cdc69acd -->

- [x] **[HIGH]** Proposal-created tasks store the summary instead of the entry — Completed: 2026-07-31
  - Type: bug
  - Description: A task created by accepting a derive proposal stores the proposal's short summary as its description rather than the TODO.md entry that was injected. Verified against the database: a task shows `description` beginning "Due date is 21 days from received date, weekend dates roll forward to Monday…" where a real entry would begin `- [ ] **[MEDIUM]** …` with its `- Type:` and `- Description:` bullets. The todo-creation block in `dispatchDraft.js` sets `created.desc = (ctx.description || '')` — the proposal's summary from `row.context` — when it should set `draftText`, the entry being injected into `TODO.md`. A previous entry called for exactly this change and it did not land; the entry-id stamp from that same entry did land, so the task is correctly linked and reaches REVIEW with its full ACCEPT face. The consequence is that everything reading `item.desc` carries the wrong text: WHAT CHANGED renders empty because it parses a `- Description:` line that is not present, and COPY CONTEXT and ITERATE both ship a one-line blurb instead of the entry.
  - Behavior: A task created by accepting a derive proposal carries the full TODO.md entry as its description — the same text injected into the file, including its headline, `- Type:`, `- Description:`, `- File:` bullets, and trailing marker comment. Its WHAT CHANGED card shows the entry's Description line. COPY CONTEXT produces a block containing the real entry, and ITERATE seeds against it. Tasks that already carry a `todo_id` are unaffected, as are tasks created by any other path.
  - Implementation notes:
    - In `dispatchDraft.js`'s creation block, set the created todo's description to `draftText`, not `ctx.description`. That is the same value passed to `shipEntryForTodo` as `entryText`, so the todo and the injected entry hold identical text.
    - Establish why the earlier change did not take and report it — whether the line was missed, or whether something downstream overwrites `desc` after creation. If a later write clobbers it, fixing only the assignment will silently fail again.
    - The description write goes through `listLogic.editToDoItem`. Confirm that path persists to Supabase rather than only localStorage; the query above shows the summary reaching the database, so it does, but verify the same holds for the longer entry text and that nothing truncates it.
    - `draftText` at that point does NOT yet carry the entry-id marker — `shipEntryForTodo` embeds it when injecting. Decide deliberately whether the todo stores the pre-marker text or is updated afterward with the marker-embedded version, and say which. An entry without its marker still works for display, but COPY CONTEXT's block is less useful without it, since the marker is what makes a follow-up traceable.
    - The block finds the created item by matching `it.tit === title` and taking the last match. Two proposals with the same title, or a pre-existing task with that title, would attach to the wrong row. Prefer the id `addToDo` returns if it provides one.
    - `todos` has no `user_id` column; `listLogicSchema.test.js` fails the build if `'todos'` and `'user_id'` appear within 200 characters of each other.
    - Test: accepting a proposal creates a todo whose description begins with the entry's `- [ ]` headline and contains its `- Type:` and `- File:` bullets; WHAT CHANGED renders the Description line; COPY CONTEXT's block contains the entry text; and a title collision does not attach to an existing task.
  - Out of scope: The entry-id stamp, correct as landed. The ACCEPT face's controls. The proposal review modal, Dismiss, and the derive workflow. `derivePhase` and the phase vocabulary.
  - File: `toDoList_main/src/dispatchDraft.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 59142ecb-f642-469d-8a70-5d34788785bd -->

- [x] **[MEDIUM]** Surface a shipped run's closing summary on the ACCEPT face — Completed: 2026-08-01
  - Type: feature
  - Description: A completed run leaves a closing summary — the agent's verdict on what it did and anything it noticed but deliberately did not fix. `fetchClosingSummary` already retrieves it and the queue store surfaces it on `no_change` rows, but a run that SHIPPED has its summary discarded, which is exactly the run whose observations matter. With the routine no longer appending follow-ups to `TODO.md`, that summary becomes the only channel for them, so it has to reach the surface where you are already deciding. Show it on the ACCEPT face, and include it in the COPY CONTEXT block so a follow-up can be handed to an outside conversation or fed straight into iterate.
  - Behavior: A task in the `accept` phase shows its run's closing summary beneath the WHAT CHANGED card, in a distinct block labelled as the run's own report rather than as part of the entry. It appears only when a summary exists; a run with none renders nothing rather than an empty block. The COPY CONTEXT block gains the summary under its own heading, so a pasted context carries what the run observed as well as what it changed. ITERATE is unchanged and already seeds from the entry, so describing a follow-up there produces a new entry against the same repo. The existing `no_change` surfacing is untouched.
  - Implementation notes:
    - Reuse `fetchClosingSummary` from `inject.js` — do not add a second fetch. Check how the `no_change` path retrieves and caches it; if the result is already stored on the queue row, read it from there rather than fetching again on every panel open.
    - The fetch is async and the panel is already mounted. Render the block only once the summary resolves, and do not block the ACCEPT face on it — the decision controls must be usable immediately.
    - A run may have no summary, or the fetch may fail. Both cases render nothing. Do NOT show an error or a placeholder; an absent summary is normal and a failed fetch is not worth interrupting a decision over.
    - Summaries can be long. Clamp the block to a few lines with an expand affordance rather than letting it push ACCEPT & CLOSE below the fold, and verify against the mobile modal's 92vh cap where the ACCEPT face is already dense.
    - Add the summary to the copy block under `--- what the run reported ---`, placed after the entry text and before the trailing `--- what I want changed ---` placeholder, so the block reads as: what shipped, what the run said about it, then your request. Omit the section entirely when there is no summary rather than emitting an empty heading.
    - Mount idempotently, register in `DESC_PANEL_CHILD_SELECTORS`, and keep it out of `DESC_AUTHORING_GROUP_SELECTORS` like the other accept-face children.
    - Render in both hosts — the desktop pane and the mobile modal, which now carries the full ACCEPT face.
    - Call `refreshViewerExpandedHeight()` when the block mounts, since it arrives async and changes the panel's height after the initial paint.
    - Test: a shipped run with a summary renders the block; one without renders nothing; a failed fetch renders nothing and does not block the decision controls; the copy block includes the summary section only when present; and the `no_change` surfacing still works.
  - Out of scope: The `no_change` path's existing summary rendering. `fetchClosingSummary` itself and the Worker's `run_result` route. ITERATE's behavior. The WHAT CHANGED card, which reads the entry's Description line and is a different thing. Creating tasks from a follow-up automatically.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 83e4595d-a343-471c-a7f5-39c678274460 -->

- [x] **[LOW]** Proposal review modal: order cards by rubric aspect — Completed: 2026-08-01
  - Type: bug
  - Description: The proposal review modal renders cards in whatever order `getProposedRows()` returns — the queue store's fetch order — so a batch lands scrambled relative to the rubric, e.g. B3 then C1 then B1. `renderList` calls `proposals.forEach` with no sort. Reviewing a derive batch means checking it against the assignment's requirements in order, so scrambled cards make it hard to see what is covered and what is missing. Sort by aspect label.
  - Behavior: Proposal cards are ordered by their rubric aspect label — A1, A2, A3, B1, B2, B3, C1, C2 — reading in the same order as the assignment. A proposal with no aspect tag sorts after all tagged ones rather than being dropped or interleaved. The order is stable across re-renders, so accepting or dismissing one card does not reshuffle the rest. Nothing else about the modal changes.
  - Implementation notes:
    - Sort in `renderList` before the `forEach`, not in `getProposedRows` — the store's order serves other consumers and should not acquire a coverage-specific opinion.
    - Aspect labels are letter-then-number (`A1`, `B10`). Sort by letter first, then by the number NUMERICALLY — a plain string sort puts `B10` before `B2`. Parse the two parts rather than comparing whole strings.
    - Get the label from wherever `buildAspectBadge` reads it, so the sort key and the rendered badge can never disagree. If the tag lives on `row.context`, read it from the same place.
    - Untagged proposals must still render. Sort them last as a group, in their existing relative order, rather than dropping them or letting a null label throw.
    - Labels may vary in case or carry stray whitespace depending on how the rubric was written. Normalise for comparison only; render the label exactly as stored.
    - The sort must be stable so the live `onQueueChange` repaint does not reorder cards while you are working through them — resolving one proposal should leave the others where they were.
    - Test: a mixed batch renders in A1, A2, B1, B2, B3, C1, C2 order; `B10` sorts after `B2`; an untagged proposal renders last; and resolving a card leaves the remaining order unchanged.
  - Out of scope: The coverage breakdown modal's own aspect ordering, which is separate. The proposal cards' contents, Accept, and Dismiss. `getProposedRows` and the queue store's fetch order. Derive and the aspect parsing.
  - File: `toDoList_main/src/assignmentCoverage.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 86b52bb7-5880-4492-a209-274b6099c1fc -->

- [x] **[LOW]** Run summary card: show the verdict and follow-ups, keep the detail behind Show more — Completed: 2026-08-01
  - Type: feature
  - Description: The WHAT THE RUN REPORTED card renders the run's closing summary verbatim, which opens with bookkeeping — "Ran in **entry** mode for target id `cd54720d…` — the sole unchecked task…" — so the part worth glancing at is buried behind Show more. The routine now emits the summary in three parts: a one-sentence verdict, a `Follow-ups:` line, then a blank line and the full detail paragraph. Render the first two in the collapsed card and keep the detail behind the existing expander. The COPY CONTEXT block continues to carry the summary in full, since verbosity is useful when handing it to another conversation.
  - Behavior: The card shows the verdict sentence and the `Follow-ups:` line by default — enough to see at a glance whether the run flagged anything. Show more reveals the rest. The card collapses back on a second tap. When a summary does not match the three-part shape, the card falls back to today's behavior and renders it clamped as-is, so older runs and any run that deviates still display. COPY CONTEXT includes the entire summary verbatim under its existing heading, unchanged.
  - Implementation notes:
    - Split on the `Follow-ups:` line rather than on paragraph position. Take everything up to and including that line as the collapsed content, and everything after the following blank line as the detail. Anchoring on the label is what makes this robust to the verdict wrapping across lines.
    - If no `Follow-ups:` line is present, treat the whole summary as detail and clamp it as the card does today. Do NOT guess at a split point — a wrong guess hides content that might matter.
    - `Follow-ups: none.` should read as reassurance, not noise. Style it at reduced emphasis; a line with actual content should read at normal emphasis so the difference is visible without reading the words.
    - The summary arrives async. Keep the existing render-when-resolved behavior and call `refreshViewerExpandedHeight()` after the split renders, since the collapsed height is now smaller than before.
    - COPY CONTEXT must not change. It takes the full summary text, including the detail paragraph — verify the split does not mutate the stored string, only what is rendered.
    - Render in both hosts, the desktop pane and the mobile modal.
    - Test: a three-part summary renders verdict plus follow-ups collapsed and the detail on expand; a summary with no `Follow-ups:` line renders clamped whole; `Follow-ups: none.` renders at reduced emphasis; and the copy block contains the full text in both cases.
  - Out of scope: `fetchClosingSummary` and the Worker's `run_result` route. The `no_change` path's own summary rendering. The WHAT CHANGED card. The routine's output format itself, which changes separately.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 3659ea51-fb4e-48a4-9c56-597e596e26b9 -->

- [x] **[MEDIUM]** TODO.md strip renders on the Structure view — Completed: 2026-08-01
  - Type: bug
  - Description: The TODO.md strip stays visible when STRUCTURE is active and peeks past the right edge of the expanded Structure view. `#mainBar[data-view="structure"]` hides the view's other queue-column children by name — `#mainList`, `#mobileProjHeader`, `#taskFilterBar`, `#bulkDescActions` — but the strip was added after that rule was written and is not in the list, so it keeps rendering. The strip belongs to the Stream view only; it is the entry point to `TODO.md` for the task queue and has no meaning alongside the source-structure map.
  - Behavior: With STRUCTURE active the TODO.md strip is not rendered. Switching back to STREAM restores it in its pinned position above the filter pills, collapsed or expanded as it was. If the viewer card was expanded into the detail pane when the view switched, returning to STREAM restores that state rather than silently collapsing it — or collapses it deliberately; state which and make it consistent.
  - Implementation notes:
    - Add the strip's selector to the existing `#mainBar[data-view="structure"]` hide rule rather than writing a separate one. One rule listing every Stream-only child is what a future addition will be checked against; two rules is how the next element gets missed the same way.
    - While you are in that rule, audit `#mainBar`'s children against it. Anything Stream-only and not listed has this bug latent — report the full list in the PR body even if the strip is the only one.
    - Check the AGENT view's equivalent gating if one still exists. The view is reached only by badge routes now, but if it has its own `data-view` rule the strip needs excluding there too.
    - Verify the expanded viewer card separately. It mounts into `#descDetailPane`, which the Structure entry hides — so it should already be gone, but confirm it is hidden rather than merely off-screen, and that `restorePaneTaskContent` is not left holding a stale reference across the view switch.
    - `#mainBar` is a three-track grid (`auto auto 1fr`) since the strip was pinned. Hiding the strip should collapse its `auto` track to zero; confirm no residual row gap remains, which would shift Structure down by a few pixels.
    - Do NOT unmount the strip in JS. Hiding it with the existing CSS rule keeps its state intact across the switch, which is what makes returning to STREAM cheap.
    - Test: the strip is absent under STRUCTURE and present under STREAM; switching back and forth preserves its collapsed/expanded state; and no residual gap appears above the Structure view.
  - Out of scope: The strip's own layout, controls, and click-to-open behavior. The Structure view's contents and its full-width split. The detail pane. The Stream view's filter pills and compose row.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: cdbea0f5-b0b3-4774-8c5b-e160fd1f0886 -->

- [x] **[MEDIUM]** Chat's Inject & run should create a task row for the entry — Completed: 2026-08-01
  - Type: feature
  - Description: Chat's Inject & run calls `injectEntry` then `dispatchRun` directly, so the entry lands in `TODO.md` and a run dispatches, but no task is created — the work ships with nothing representing it in the list, no ACCEPT face, no revert, no iterate. That is the same gap derive proposals had, and `dispatchDraft` already solves it: when a row carries no `todo_id` it creates a todo from the proposal's title and description, stamps the entry id, and repaints the list. Give chat's path the same treatment so an entry injected from chat behaves like one injected from a task.
  - Behavior: Injecting and running an entry from chat creates a task in the active project whose title is the entry's headline and whose description is the entry text as injected. The task is linked to that entry by id, so it moves through DRAFT to REVIEW as the run progresses and shows its full ACCEPT face when the run merges — ACCEPT & CLOSE, REVERT, OPEN IN TODO.MD, COPY CONTEXT, ITERATE. The list repaints so the row appears immediately rather than after a project switch. The chat run record and the RUNS tab entry are unchanged. Injecting into a repo other than the active project's still ships the entry; whether it creates a task there is covered below.
  - Implementation notes:
    - Reuse `dispatchDraft`'s creation logic rather than copying it. Extract the "no todo_id → create one, set its description, stamp the entry id, repaint" block into a shared function both paths call, so the two cannot drift. That block has already needed two corrections (`draftText` versus the summary, and the entry-id stamp); a second copy would need them again.
    - The description must be the ENTRY TEXT as injected, not a summary. That was the bug in the derive path — set it from the same `entry` string passed to `injectEntry`.
    - Derive the title from the entry's headline using `deriveRunTitle`, which chat already calls for its run record. Use one derivation, not two.
    - `injectEntry` mints `entryId` when none is supplied and embeds the marker. Stamp that id onto the created todo through `listLogic.stampTodoEntryId` so the task resolves its shipped state. Surface a failed stamp rather than swallowing it — a silently unstamped task is orphaned from work that shipped, which is the first bug this project hit.
    - REPO SCOPING IS THE OPEN QUESTION. Chat can be scoped to a workspace repo other than the active project's, and `activeChatRepo` drives the target. Creating a task in the active project for an entry injected into a different repo would attach it to the wrong list. Resolve the project from the TARGET repo via `inject_targets`, and if no project routes to it, skip creation and say so in the chat rather than creating a mismatched task. State what you chose in the PR body.
    - Chat mints a `correlationId` for its dispatch — the id the closing-summary lookup falls back to when there is no `run_id`. Persisting it on the created todo would let the WHAT THE RUN REPORTED card resolve for chat-dispatched runs too. That needs a column, so note it as a follow-up with the SQL rather than doing it here.
    - Repaint the list after creation, as `dispatchDraft` does — `#mainList` only rebuilds from data on project selection, so the row would otherwise be invisible until you navigate away and back. Guard the repaint on the created todo belonging to the currently selected project.
    - `todos` has no `user_id` column; `listLogicSchema.test.js` fails the build if `'todos'` and `'user_id'` appear within 200 characters of each other.
    - Test: Inject & run creates a task whose description is the full entry text and whose `entryId` matches the injected marker; the row reaches REVIEW when the run merges and renders the ACCEPT face; an entry injected into a repo with no routed project creates no task and reports why; and the list repaints without a project switch.
  - Out of scope: Persisting the correlation id, and the closing-summary card for chat-dispatched runs. The chat run record and the RUNS tab. `injectEntry` and `dispatchRun` themselves. The draft card's own rendering and its Create task control, which is a separate affordance.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/dispatchDraft.js`, `toDoList_main/src/listLogic.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 7c66708b-db14-4329-9415-bd7eed91eb56 -->

- [x] **[LOW]** Add a harmless comment to style.css to trigger a redeploy — Completed: 2026-08-01
  - Type: feature
  - Description: Add a single-line CSS comment (e.g. a timestamp or note) near the top of style.css with no functional or visual effect, purely to produce a new commit that triggers the deploy pipeline. No selectors, rules, or values should change. This is a throwaway trigger entry, not a real styling change.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: f2a990bc-5475-4b7e-9f18-12b0f8aae25e -->

- [x] **[HIGH]** Entry text and id are lost when a task is created for an injected entry — Completed: 2026-08-01
  - Type: bug
  - Description: A task created by `materializeEntryTodo` — from chat's Inject & run, or from accepting a derive proposal — reaches Supabase with `description` and `entry_id` both null, so its WHAT CHANGED card and FILE readout render empty and its shipped state cannot resolve from the marker. The cause is write ordering, not a missing write. `addToDo` mints the id, pushes to memory, saves to localStorage, then hands the row's INSERT to `persistMutation` and returns immediately — the insert is fire-and-forget. `materializeEntryTodo` synchronously sets `created.desc` and calls `editToDoItem`, then stamps the entry id; both issue UPDATEs keyed by an id that does not exist server-side yet, so they match zero rows and do nothing. The insert then lands carrying the original payload — empty description, no entry id. Local state looks correct, which is why the row renders with a title and nothing else.
  - Behavior: A task created for an injected entry reaches the database with its full entry text as `description` and its `entry_id` set, so WHAT CHANGED shows the entry's Description line, the FILE readout lists its paths, and the row resolves through DRAFT to REVIEW as the run progresses. This holds for both callers — chat's Inject & run and derive-proposal acceptance — and survives a reload or a switch to another device. Tasks created by any other path are unaffected.
  - Implementation notes:
    - Fix it at the creation site, not by making every later write retry. The row should be inserted ONCE carrying its final shape: title, description, and entry id together, rather than an insert followed by two updates that race it.
    - Preferred approach: give `materializeEntryTodo` a path that builds the complete item before persisting — set `desc` and `entryId` on the in-memory item after `addToDo` pushes it but BEFORE the insert is queued, or add an `addToDo` variant taking the description so the single insert carries it. Report which you chose and why.
    - If instead you await the insert before updating, establish that `persistMutation` exposes a promise to await at all. Its fire-and-forget shape is deliberate — it keeps the UI responsive — so do not make `addToDo` synchronous for every caller just to fix this one.
    - `toTodoRowPayload` already carries both `description` and `entry_id`, so a correctly-shaped item inserts them without any schema or payload change.
    - The entry-id stamp currently goes through `stampTodoEntryId`, which surfaces its own failures. If the id now travels in the insert, make sure a failure there is still visible — a silently unstamped task is orphaned from work that shipped, which is the first bug this project hit.
    - Verify against BOTH callers. The derive path was fixed once for the description already; confirm this is the reason that fix appeared to land locally but not in the database, and that both paths are correct afterward.
    - `materializeEntryTodo` finds the created item by matching `it.tit === cleanTitle` against ids not present before the call. That is sound, but if the creation path changes shape, keep the identity check rather than falling back to a title match.
    - `todos` has no `user_id` column; `listLogicSchema.test.js` fails the build if `'todos'` and `'user_id'` appear within 200 characters of each other.
    - Test with a real round trip, not just in-memory state: create a task through both callers, then read the row back from the database and assert `description` and `entry_id` are populated. An in-memory assertion passes today with the bug present, which is exactly why this shipped twice.
  - Out of scope: `persistMutation`'s fire-and-forget design and the general write queue. `injectEntry`, `dispatchRun`, and `dispatchDraft`'s dispatch logic. The ACCEPT face's rendering. The chat draft card and the RUNS tab.
  - File: `toDoList_main/src/dispatchDraft.js`, `toDoList_main/src/listLogic.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: a6c51938-3fc8-4224-b7a2-cf3d56b960cd -->

- [x] **[MEDIUM]** Mobile: use the ALL / IN PROGRESS / DONE filters instead of ALL / Active / Ideas — Completed: 2026-08-01
  - Type: feature
  - Description: Mobile filters on manual status through its own `FILTERS` array — ALL matches everything, Active matches `active` or `in_progress`, Ideas matches `idea` — while desktop uses the three phase-aware pills that describe how work actually moves: ALL is open work, IN PROGRESS is work you or the machine are doing, DONE is shipped-and-acknowledged but not yet filed away. Mobile should use the same three. The two sets are not interchangeable as written: mobile's `match` receives a status string, desktop's receives the item and derives status, phase, and the completed flag inside. Consolidate onto the desktop definitions and drive both breakpoints from one array.
  - Behavior: The mobile filter control offers ALL, IN PROGRESS, and DONE with the same meanings as desktop. ALL matches every task that is not checked off. IN PROGRESS matches manual status `in_progress`, or derived phase `draft` or `running`. DONE matches phase `done` and not checked off. The control keeps its current form — the cycle pill with its position dots — now cycling three phase-aware filters rather than three status ones. Counts update live. The selection is shared with desktop, so crossing the breakpoint keeps the same filter active. The COMPLETED section is unaffected; no filter reveals checked-off rows at either breakpoint.
  - Implementation notes:
    - Delete the mobile `FILTERS` array and point both controls at `PHASE_FILTERS`. Keep the `seg` normal-case labels — the segmented control uses them and the cycle pill uses uppercase `label` — so add `seg` values to the phase filters rather than dropping the distinction.
    - `applyTaskFilter` currently selects a predicate by breakpoint and passes a status string on mobile, an item on desktop. With one array, the call site passes the ITEM at both breakpoints. Grep every caller of the mobile predicate and update the signature — a predicate receiving a status string where it expects an item will silently match nothing.
    - THE TWO PERSISTED KEYS CAN NOW MERGE. The separate keys exist only because the vocabularies differed; with one set, one key serves both. Migrate on read: a stored `active` or `ideas` from the mobile key, or `running` from the retired desktop set, resolves to `all` rather than leaving the list empty on first load after deploy. Do not leave two keys writing to each other.
    - The desktop pills currently render only above the breakpoint and the cycle pill only below, gated in CSS. That gating stays — this changes what the filters MEAN on mobile, not which control is shown.
    - `EMPTY_MESSAGES` keys off filter key. The `active` and `ideas` entries become dead once nothing references them; remove them and confirm `inprogress` and `done` copy reads correctly on a phone, where the message has less room.
    - Completed rows must stay exempt from filtering at both breakpoints — that exemption was added when the desktop pills started excluding them, and mobile's `all` returning `true` meant it never mattered there. With mobile on the phase filters, mobile now needs the same exemption or expanding COMPLETED on a phone will reveal nothing.
    - Keep `taskFilter.js`'s dependency discipline: `prefs` as its only hard dependency, phase strings inlined rather than importing `phase.js`.
    - Test: mobile's cycle pill offers the three phase filters; IN PROGRESS matches an in-progress task and a draft-phase task on mobile; DONE excludes checked-off rows; a stored `ideas` preference resolves to ALL; expanding COMPLETED on mobile reveals its rows under every filter; and the selection persists across a breakpoint change.
  - Out of scope: The desktop pills' definitions, correct as landed. The blocked chip, which stays an independent overlay filter at both breakpoints. The sort control. `derivePhase` and the phase vocabulary. The COMPLETED section's collapse behavior.
  - File: `toDoList_main/src/taskFilter.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 37a8c4f5-47ab-4753-8d8e-b99d67adb802 -->

- [x] **[MEDIUM]** Structure tab: request a refactor scan on demand — Completed: 2026-08-01
  - Type: feature
  - Description: The post-merge refactor scan in `claude-run.yml` is now gated on the `REFACTOR_SCAN_ENABLED` repository variable and off by default, so the NEXT REFACTOR card stops advancing until someone asks for a scan. The Worker route and workflow for asking already exist and are verified: `claude-scan.yml` dispatches on demand and calls the Worker's `scan` route, and the Worker's `dispatch_scan` route fires that workflow. Add the app side — a control on the card that dispatches a scan, tracks the run, and re-reads the stored row when it settles. The card stays a pure reader: it never scans itself, because the scan takes ~90s and mobile Safari drops a connection held that long, which is the whole reason the work happens in CI.
  - Behavior: The NEXT REFACTOR card carries a control that requests a fresh scan for the active project's repo. Activating it dispatches the scan and shows a pending state; the control is disabled while a run is in flight so a second tap cannot queue another. When the run settles the card re-reads its stored scan and repaints — a new candidate if one was found, or its existing "no more candidates" state if the target file was unchanged. The pending state survives closing the Structure tab, switching projects, and returning, because it is read from the tracker rather than from card-local state. A project with no routed repo shows no control.
  - Implementation notes:
    - Add `dispatchScan(correlationId, target)` to `inject.js` beside `dispatchTriage` and `dispatchDerive`, posting `{ dispatch_scan: true, correlation_id }` through `postToWorker`. The Worker requires a non-empty correlation id and 400s without one — mint it with `mintEntryId()` as the other dispatch paths do.
    - Track the run in `agentQueueStore.js` alongside the sweep and derive trackers, polling `active_runs` with `workflow: "scan"` — the Worker's selector now accepts that value. Follow `startSweepTracking`'s shape exactly: module-level started flag, one interval, a grace window for registration lag, and a hard cap. Do NOT write a third bespoke tracker; if the sweep and derive trackers are near-identical, this is the moment to parameterise one rather than add a third copy, and say which you did.
    - There is NO queue row for a scan run — `agent_queue` is untouched by this path. The tracker's only source of truth is the `active_runs` probe, so it must not infer anything from queue state.
    - When the tracker reports the run settled, call `listLogic.loadLatestRefactorScan(repo)` again and repaint the card. The Worker writes the `refactor_scans` row at the END of the run, so re-read once on settle and once more after a short delay if `scanned_at` has not advanced — the row may land a beat after the run reports complete.
    - `refactorCard.js` mounts as a persistent sibling of the Structure tree and survives lens repaints. Keep the pending state derived from the tracker, not stored on the card element, so a lens switch or a remount picks it up unchanged.
    - Gate the control on the project having a routed inject target, the same test the availability gate and the sidebar thunderbolt use. A repo with no `src_prefix` on its registry row will 400 from the Worker's `scan` route, so consider gating on that too — check whether the card can see it, and if not, let the error surface rather than guessing.
    - The scan can legitimately report nothing new. `unchanged`, `all_under_budget`, and `no_eligible_files` are all successful outcomes; the card should return to its normal state rather than showing an error. Only a genuinely failed dispatch or a failed run warrants a message.
    - `claude-scan.yml` must exist on `main` in the target repo or the dispatch 502s from the Worker with GitHub's "workflow not found". That reads like a permissions error — if the dispatch fails, surface the Worker's `detail` rather than a generic message, so the real cause is visible.
    - Test: the control dispatches once with a minted correlation id and disables while in flight; the pending state survives a project switch and a lens change; the card re-reads and repaints on settle; an `unchanged` result returns the card to its normal state rather than an error; and a project with no routed repo shows no control.
  - Out of scope: The Worker's `scan` and `dispatch_scan` routes, `claude-scan.yml`, and the `REFACTOR_SCAN_ENABLED` gate on `claude-run.yml` — all already in place. The card's candidate rendering, Skip, and Push entry. The `refactor_scans` table and the scan's budget or model.
  - File: `toDoList_main/src/inject.js`, `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/refactorCard.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: d0b9a3a0-d476-4e25-bebb-4b766988c63f -->

- [x] **[MEDIUM]** Show API spend — a desktop panel and a mobile chat-header control — Completed: 2026-08-01
  - Type: feature
  - Description: The Worker now records every Anthropic call to `usage_events` — the chat route (Sonnet, or Opus on deep_think) and the scan route — with per-call token counts plus `route` and `model`. Nothing surfaces it, so there is no way to see what the account has spent. Add a readout with two entry points, since the surfaces differ: on DESKTOP the header's existing stats icon opens a small panel showing the month's total with a bar against a budget; on MOBILE a control in the chat view's header row, left of New Chat, opens the same panel. This covers API spend ONLY — the CI runs authenticate against the Max plan, produce no usage object, and cannot be measured. The panel must say so, or a small figure reads as "this whole pipeline is nearly free" when every run is invisible to it.
  - Behavior: Tapping the desktop header's stats icon, or the mobile chat header's spend control, opens a panel showing the current calendar month's API spend as a dollar figure, a bar filled against a configurable budget, the percentage that represents, and one line naming what the number covers and what it cannot. With no rows the panel reads $0.00 with an empty bar rather than an error or a spinner. It closes on Escape, a backdrop tap, and its own close control, returning focus to whatever opened it. The figure is account-wide, not per-project.
  - Implementation notes:
    - MOBILE ENTRY POINT: add the control to `#claudeChatHeader`, immediately BEFORE `buildClearChat()`'s button, so it sits left of New Chat in that right-aligned row. Label it with a `$` glyph and give it an `aria-label` naming what it opens. That header is structurally part of the chat view, so the control is absent on RUNS and COVERAGE without a per-tab gate, and it sits outside the scroll surface so it never scrolls away. Do NOT put it in `#claudeSheetTabs` — that strip is what the COVERAGE badge pushed past the 360px docked pane, which is why New Chat was moved out of it.
    - Match New Chat's height and radius so the pair reads as one group, but distinguish them: New Chat is the accented action, spend is a quiet readout. Prefer showing the figure inline (`$4.19`) over a bare `$` — the row holds one button today and it saves a tap for the common glance. If it wraps at 393px, fall back to the glyph alone and say so in the PR body.
    - DESKTOP ENTRY POINT: wire the header's existing stats icon. Confirm what it currently does before rebinding — if it already opens something, that is a conflict to resolve, not overwrite.
    - Build the panel ONCE and open it from both entry points. Use `wireModalDismiss` so Escape, backdrop, and focus restoration match `showCoverageDetailModal` and the proposal modal.
    - `usage_events` has RLS with a select policy on `auth.uid() = user_id`, so read it through the existing Supabase client. Sum from the first of the current month in LOCAL time — a UTC-boundary sum disagrees with what the user considers "this month" for several hours a day.
    - PRICES LIVE CLIENT-SIDE. The table stores token counts precisely so a price change is a client edit and history stays correct. Define a rate map keyed by the `model` string with SEPARATE input, output, cache-read, and cache-write rates. Cache reads are roughly a tenth of input and cache writes are above it — one blended rate would misreport badly, since the chat route is deliberately cache-heavy.
    - An unknown model must not silently contribute zero. Fall back to the most expensive known rate, and note in the PR body that this over-reports rather than under-reports — a figure that quietly ignores a new model is worse than one that errs high.
    - The budget is a user setting with a sensible default, stored in `prefs`. Handle unset or zero without dividing by it: show the total with no bar.
    - Include the honesty line in the panel: the number covers Anthropic API calls (chat and refactor scans) and NOT the pipeline runs, which bill to the Max plan. This is not optional polish — it is what stops the figure being misleading.
    - Read on open only. Do not subscribe or poll; spend moves slowly and a stale figure between opens is fine.
    - Tests: the month sum uses local-month boundaries; the rate map applies four distinct rates; an unknown model falls back to the highest known rate; a zero or unset budget renders without a bar; the panel opens from both entry points and dismisses three ways; and the mobile control renders only in the chat view.
  - Out of scope: Plan quota and Actions minutes — plan usage has no API, and Actions is free on public repos, so both would read as fiction or zero. Per-project or per-route breakdowns; one account-wide number by decision. Spend alerts or caps. The Worker's recording, already deployed.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/main.js`, `toDoList_main/src/listLogic.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 5a1579f8-d0a8-466a-9b57-131572be5ff3 -->

Reading this as: on mobile the API-spend control should be hidden everywhere except inside the chat sheet — no clarifying needed, that's clear.

- [x] **[MEDIUM]** Hide the API-spend control from the mobile top-header, keep it only in the chat sheet — Completed: 2026-08-01
  - Type: bug
  - Description: On mobile (`isMobile()` / viewport < 1024px) the API-spend button is rendering in the top-left header corner, where it shouldn't appear. It must only be shown inside the chat window (the Claude sheet) on mobile, matching its intended placement. Locate where the spend control is mounted into the header (likely `toDoList_main/src/desktopHeaderPlacement.js` or the header wiring in `toDoList_main/src/main.js`) and suppress it below the 1024px breakpoint, leaving the chat-sheet instance in `toDoList_main/src/claudeSheet.js` untouched; a CSS `display:none` under the mobile breakpoint in `toDoList_main/src/style.css` may be the cleanest fix. Verify the desktop panel and the mobile chat-header control both still render.
  - File: `toDoList_main/src/style.css`, `toDoList_main/src/desktopHeaderPlacement.js`, `toDoList_main/src/claudeSheet.js`
  <!-- id: 8ca05958-9d17-4208-bab3-432f66fd1b79 -->

- [x] **[LOW]** Hide the prev/next project navigation arrows in the mobile header — Completed: 2026-08-01
  - Type: bug
  - Description: On mobile, the `<` and `>` chevrons shown at the top corners of the project header (used to page between projects) should be hidden — the user doesn't use them for navigation. Keep the arrows visible on desktop; only suppress them under the mobile breakpoint (`isMobile()` / viewport < 1024px). Preserve any underlying swipe-to-navigate gesture between projects if one exists (likely in `toDoList_main/src/mobileProjSwipeNav.js`) — only the visible arrow buttons should disappear, not the navigation capability itself. Likely rendered in `toDoList_main/src/main.js` or `toDoList_main/src/desktopHeaderPlacement.js`, styled in `toDoList_main/src/style.css`.
  - File: `toDoList_main/src/style.css`, `toDoList_main/src/main.js`, `toDoList_main/src/mobileProjSwipeNav.js`
  <!-- id: e12c7d36-522e-4102-bf3d-67e7bd36cdd7 -->

- [x] **[MEDIUM]** API spend: daily chart and the two ratios that show where the cost is — Completed: 2026-08-02
  - Type: feature
  - Description: The spend panel shows one number for the month, which says what was spent but nothing about where it went or whether it is trending. Add a daily bar chart for the current month plus two derived figures that map onto the actual cost levers. DEEP SHARE — what fraction of spend went to Opus rather than Sonnet — is likely the largest single lever, since deep_think turns cost several times a fast turn. CACHE HIT RATE — `cache_read_tokens ÷ (input_tokens + cache_read_tokens)` — measures whether the four-breakpoint prompt-cache scheme in the chat route is actually working; it is the biggest structural investment in the Worker and is currently unverified. A drop in that ratio means something is busting the cached prefix. Month-over-month is deliberately excluded: there is one month of data, so a chart of it would be a single bar.
  - Behavior: The spend panel gains a bar chart of the current month's daily totals, one bar per elapsed day, with the tallest labelled so the scale is readable. Beneath it, two figures: the share of spend attributable to deep_think turns, and the cache hit rate across the month. Hovering or tapping a bar shows that day's date and total. Days with no usage render as empty slots rather than being omitted, so gaps are visible. With fewer than two days of data the chart is replaced by a short note that there is not yet enough history — a one-bar chart is noise. The month total, budget bar, and the line about what the number does not cover all stay as they are.
  - Implementation notes:
    - Read the same `usage_events` rows the total already reads — one query, grouped client-side by local date. Do NOT add a second round trip for the chart.
    - Group by LOCAL date, matching the month boundary the total already uses. A UTC grouping shifts several hours of every evening's usage into the next day's bar, which is exactly the kind of quiet wrongness that makes a chart worse than no chart.
    - DEEP SHARE is a share of COST, not of turns. A single Opus turn can outweigh a dozen Sonnet ones, so counting turns would understate it badly. Apply the same rate map the total uses, then split by the `deep_think` column.
    - CACHE HIT RATE is `cache_read_tokens ÷ (input_tokens + cache_read_tokens)` summed across the month — a token ratio, not a cost ratio, because it measures whether the cache is being hit, not what it saved. Say which it is in the label; "cache hit rate" reading as a cost figure would mislead.
    - The `deep_think` and `iterate_seed` columns default false, so rows recorded before the Worker change will read as fast, author-mode turns. That is wrong for any deep or iterate turn in that window. Either scope the two ratios to rows after the column's introduction, or state the caveat in the panel — do not present a figure computed partly from defaulted data as though it were measured.
    - `iterate_seed` is recorded but has no display here. Leave it unused rather than inventing a third figure; it exists so the question "is the iterate seed the expensive part" is answerable later.
    - Hand-roll the chart as inline SVG. This is ~31 bars with no interaction beyond a tooltip; a charting library would be the single largest dependency in the project for something a `<rect>` loop covers. Size the viewBox to the panel's width and scale bars to the tallest day.
    - The panel is 250px wide on desktop and near-full-width on mobile. Verify 31 bars remain distinguishable at the narrower width — if not, drop the per-bar gap before dropping bars, and never aggregate into weeks, which would hide the session-shaped spikes the chart exists to show.
    - Read on open only, like the total. No polling, no subscription.
    - Tests: daily grouping uses local dates; a day with no rows renders an empty slot; deep share is computed from cost not turn count; cache hit rate uses the token ratio; fewer than two days shows the note instead of a chart; and the month total still matches the sum of the bars.
  - Out of scope: Month-over-month comparison, until there are months to compare. Per-project or per-route breakdowns. Spend alerts, caps, or projections. The Worker's recording. Backfilling `deep_think` and `iterate_seed` for existing rows, which is not possible.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/listLogic.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 433df196-7954-4cb2-8540-6affc455970a -->

- [x] **[MEDIUM]** Daily spend chart: render the whole month, bars from a shared baseline — Completed: 2026-08-02
  - Type: bug
  - Description: The daily spend chart renders one slot per day that has data, so two days of usage fill the panel as two enormous blocks — which reads as a rendering fault rather than as early-month sparseness. The bars also do not share a baseline, so the shorter day appears to hang rather than rise, making the comparison hard to read. And the chart carries two competing labels: an inline value over the tallest bar plus a caption below naming a different day, so it is ambiguous which bar the caption describes. Render the full calendar month as a fixed axis instead, with bars rising from a common baseline and a single labelling mechanism.
  - Behavior: The chart shows one slot per day of the current calendar month — 28 to 31 depending on the month — at a fixed width regardless of how many days carry data. Bars rise from a shared bottom edge, scaled to the highest day in the month. Days with no usage render as empty slots, so a sparse early month reads as sparse and the chart does not reflow as days accumulate. Days later in the month than today render as empty slots too, rather than truncating the axis. A faint marker every seven days makes the strip readable as time. Hovering or tapping a bar shows that day's date and total; a single caption beneath names the highest day and its value. The inline label over the tallest bar and the existing per-day caption are both removed in favour of that one caption.
  - Implementation notes:
    - Build the axis from the month's day count, not from the data. Generate every day from the 1st to the month's last day, then join the usage totals onto it — a day with no rows gets zero, not omission. Use LOCAL dates for both the axis and the join, matching the month boundary the total already uses.
    - Bars share a baseline: anchor each `<rect>` to the chart's bottom edge and set its height from the value, rather than positioning from the top. Scale to the month's maximum, not to a fixed ceiling — a $0.05 day and a $5 day should both fill the chart when they are the month's peak.
    - At the panel's ~250px desktop width, 31 slots leaves roughly 6px per bar with a 1px gap. That is thin but reads correctly as a shape. If it looks cramped, reduce the gap before reducing the bar count, and NEVER aggregate into weeks — the session-shaped spikes are the whole point of a daily view.
    - Verify at the mobile panel's near-full width as well, where each bar gets considerably more room. One viewBox scaled to the container is preferable to two layouts.
    - Remove BOTH existing labels — the inline value over the tallest bar and the caption naming a day. Replace with one caption beneath the chart naming the month's peak day and value, e.g. "Peak $0.56 on Aug 1". A zero-usage month shows no caption rather than "Peak $0.00".
    - Per-bar values move to a tooltip on hover and tap. At 6px per bar a hover target is small; give each slot a full-height transparent hit area rather than relying on the bar's own height, or a low-value day becomes untappable.
    - Add a faint tick or divider every seven days from the 1st. Keep it well below the bars in contrast — it is orientation, not data.
    - The "not enough history" guard can now go: a full-month axis renders correctly with a single day of data, which is the case that guard existed to avoid. Confirm the chart reads sensibly with exactly one non-zero bar before removing it, and say in the PR body whether you did.
    - Nothing about the month total, the budget bar, the two ratio figures, or the caveat line changes.
  - Out of scope: Month-over-month comparison. The deep-share and cache-hit figures and their caveat line. The budget input. The Worker's recording. Aggregating to weeks under any circumstances.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: e7e83142-0a51-459a-b3cb-f6659a8b4c7a -->

- [x] **[MEDIUM]** Replace immediate $0.00 flash in the spend panel with a loading spinner until usage data resolves
  - Type: bug
  - Description: On mobile, openSpendPanel currently calls renderSpendReadout immediately with lastTotal (starting at 0), so users see "$0.00" flash for a few seconds before listLogic.loadMonthlyUsage() resolves and the real total populates. Replace that initial renderSpendReadout call (claudeSheet.js:1527) with a centered loading state in #usageSpendReadout: a 22px .projRunSpinner (reusing the shared `spin` keyframes) above a "Loading usage…" label in var(--text-secondary), matching the chosen mockup. Once listLogic.loadMonthlyUsage() resolves successfully (claudeSheet.js:1546-1553), call renderSpendReadout for real with the resolved total and render the chart as today; on failure or empty rows, replace the spinner state with an inline error message (e.g. "Couldn't load usage") in #usageSpendReadout instead of silently leaving $0.00. Panel-open/close and backdrop-dismiss behavior around openSpendPanel must remain unchanged.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/style.css`
  - Completed: 2026-08-03
  <!-- id: 1b8a1466-4014-43ab-9632-f6ca5dd8a86c -->

- [x] **[LOW]** Spend panel: add the Opus 5 rate — Completed: 2026-08-03
  - Type: bug
  - Description: The Worker's deep-think path now calls `claude-opus-5` rather than `claude-opus-4-8`. The spend panel's rate map is keyed by the `model` string and has no entry for the new id, so those rows price through the unknown-model fallback. That fallback resolves to the highest known rate, which happens to be the same $5/$25 as Opus 4.8 — so the figure is right today by coincidence rather than by definition, and would silently drift the moment the rate card changes. Add the key explicitly.
  - Behavior: Rows with model `claude-opus-5` price at the same input, output, cache-read, and cache-write rates as `claude-opus-4-8`. Existing 4.8 rows are unaffected, so a month spanning the switch prices both correctly. The deep-share figure continues to attribute both to deep-think turns, since it splits on the `deep_think` column rather than the model string — confirm that is what it does, and if it splits on the model name instead, fix it to use the column.
  - Implementation notes:
    - Copy the `claude-opus-4-8` entry under the new key rather than aliasing one to the other; they are the same rate today by coincidence, not by rule, and an alias would hide a future divergence.
    - Leave the unknown-model fallback in place. It exists so a model added at the Worker without a matching client rate over-reports rather than silently contributing zero, and that guard is still wanted.
    - No other change. The panel, chart, ratios, and budget are untouched.
  - File: `toDoList_main/src/claudeSheet.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 29c7c845-a6a0-468b-9f97-bc96f9ed2355 -->

- [x] **[MEDIUM]** Stop re-fetching assignment.md on save; apply the saved content directly — Completed: 2026-08-03
  - Type: bug
  - Description: After editing assignment.md via the assignment editor modal (`showAssignmentEditorModal` in `toDoList_main/src/modals.js`), the Save handler's success branch (around line 1068-1072) calls `close()` then `opts.onSaved()` with no arguments. The sole caller, `openAssignmentEditor()` in `toDoList_main/src/assignmentCoverage.js` (~line 384), wires `onSaved` to `refreshAssignment(resolveReadTarget())`, which re-fetches assignment.md from the Worker's `read` route (`readAssignmentFromWorker` in `toDoList_main/src/inject.js`) instead of using the content the user just wrote and confirmed saved. `writeAssignmentToWorker` (inject.js ~line 542) already returns `{ ok: true, sha }` on success, and the modal already holds the exact saved text in `textarea.value` — there is no need to round-trip through a fresh read to know what the file now contains. If that re-read is served stale by any layer between the browser and GitHub (Worker-side caching, GitHub API propagation lag), the coverage tab's summary and its "Derive tasks"/"Draft tasks from this" button (both driven by `_assignment.state` in `assignmentCoverage.js`) keep showing the pre-edit classification until a later read (e.g. a full page reload) happens to land after the staleness clears — matching the reported symptom of needing a manual refresh after editing, specific to the coverage tab on assignment repos.
  - Behavior: Pass the just-saved content (and new sha) forward instead of re-fetching. In `modals.js`, change the save-success branch to call `opts.onSaved(textarea.value, res.sha)` (`res` being `writeAssignmentToWorker`'s resolved value). In `assignmentCoverage.js`, add an exported function (e.g. `applyAssignmentSave(content)`) that mirrors `refreshAssignment`'s success path synchronously — sets `_assignmentProject` to the current project, sets `_assignment = describeAssignment(content)`, then calls `paint()` and `notifyAssignmentChange()` — and wire `openAssignmentEditor`'s `onSaved` to call it with the saved content instead of calling `refreshAssignment(resolveReadTarget())`. This makes the coverage tab (and the AGENT board's assignment card, which shares the same `openAssignmentEditor`) repaint immediately and correctly from the known-good saved text, with no dependency on a subsequent network read being fresh.
  - Implementation notes: Keep `refreshAssignment` itself unchanged — it's still needed for the initial mount read and project-switch read, just not for the post-save path. Do not drop the `sha` tracking; `applyAssignmentSave` only needs the content to classify, but the modal already re-opens with a fresh read+sha on the next tap regardless.
  - Out of scope: The editor modal's conflict (409) handling, which already reloads content/sha correctly on conflict. The board's `paint()` binding and `resolveReadTarget()` wiring, both correct as-is. Any Worker-side caching behavior — the Worker lives in a separate repo and isn't touched here; this fix removes the client's dependency on that read being fresh at all.
  - File: `toDoList_main/src/modals.js`, `toDoList_main/src/assignmentCoverage.js`
  <!-- id: 51dcd831-fa28-42f1-bcc2-f5ded330aca0 -->

- [x] **[HIGH]** Replace the coverage modal's dead blocked-aspect jump with an inline answer lane — Completed: 2026-08-03
  - Type: bug
  - Description: Tapping a blocked (amber) aspect row in the coverage detail modal does nothing, so there is currently NO surface anywhere in the app to answer a `needs_words` triage question raised against a rubric aspect. `jumpToBlockedAspect` in `assignmentCoverage.js` closes the modal and then depends entirely on the retired Agent board: it calls the injected `paint()` (which does `document.getElementById('agentView')` and returns immediately — no HTML template or JS creates that element anymore, see the comment at `main.js` ~line 1001 recording the AGENT pill's retirement), then queries `[data-answer-row="…"]`, whose only board-side producer was `buildSecondary` in `agentView.js`. It also calls `isBucketCollapsed`/`setBucketCollapsed`, which `agentWiring.js`'s `configureAssignmentCoverage` call never supplies, so both remain the inert module-level no-ops. Every step is dead, and the function returns silently — the modal just closes. The fallback surface (`toDoRow.js`'s ASKING block) is unreachable for these rows because it mounts per todo via `agent_queue.todo_id`, and a derive-produced aspect row that parks in `needs_words` before it is ever accepted into a todo has no `todo_id`, so no row layer renders it. Fix by deleting `jumpToBlockedAspect` entirely and expanding the blocked row in place instead. In `buildCoverageDetailRow`, the blocked branch currently returns a `.coverageDetailConfirmable` flex wrapper pairing the jump `<button>` with its confirm tick; wrap that flex row in a `.coverageDetailItem` column (the same wrapper shipped rows already use for their commit-helper lane) and mount the answer lane as its second child. Swap `.coverageDetailRow--jump` for the existing `.coverageDetailRow--expandable` treatment and add a `.coverageDetailChevron` so the affordance reads as a disclosure rather than a navigation. The lane holds: the row's `question` text as a `<p>`, a `<textarea>` (`font-size: 16px` to avoid iOS focus auto-zoom), a `role="alert"` error `<p>` hidden by default, a secondary "Discuss in chat" button, and a primary Send. Reproduce the send path from `agentView.js`'s `submitAnswer` verbatim: ignore whitespace-only input with no write, disable the textarea and button and set the label to "Sending…" while in flight, then `listLogic.answerAgentTask(row.id, text, row.thread)` (the only sanctioned `agent_queue` write path — do NOT touch Supabase from this module). On `{ ok: true }` clear the textarea, delete the row's entry from the `pendingAnswers` map exported by `agentQueueStore.js`, call `loadQueueRows(getSelectedProjectName())`, then fire `fireTriageSweep(getSelectedProjectName())` and surface a non-blocking toast via `showInjectToast` if it returns `{ ok: false }` — the answer is already saved, so a failed sweep must never block or roll back the send. On failure re-enable both controls and show the error text in the alert `<p>`. "Discuss in chat" reproduces `buildDiscussLink`'s handoff: build the seed locally (copy `buildDiscussSeed`'s shape — an opening line, then `Task: <context.title || row.title>`, the `context.description`, then `The agent asked: <row.question>`) and call `openChatWithSeed(seed, row.id)` so a ship from the resumed session still settles the row. CRITICAL: `openChatWithSeed` lives in `claudeSheet.js`, which already imports `buildCoveragePane` from this module — importing it directly forms a module cycle, so it MUST arrive through `configureAssignmentCoverage` as a new injected dep, wired in `agentWiring.js` alongside `getSelectedProjectName`/`resolveReadTarget`/`fireDeriveRun`, and must default to an inert no-op like the existing bindings so a partial wiring can't throw. Second critical constraint: the modal body is fully rebuilt by `renderBody` on EVERY `onQueueChange` (the shared listener at the bottom of the file drives `_coverageModal.onQueueChange`), which will tear down an open lane mid-typing. Hold the expanded aspect id in a module-level variable and re-expand that row on rebuild, and mirror the unsent draft into `agentQueueStore`'s exported `pendingAnswers` map on every keystroke, re-applying it when the textarea is rebuilt — same contract `paint()` honored. Restoring the caret position is not required. Also drop the now-unused `isBucketCollapsed`/`setBucketCollapsed` bindings, their `configureAssignmentCoverage` branches, and their mention in the module header comment; KEEP `paint`, which is still called at the `refreshAssignment` site.
  - Out of scope: minting a todo for orphan aspect rows (a separate concern in the derive path), the `_handedOffRows` collapsed "Continue in chat" state from `agentView.js` (the lane stays open after a handoff rather than collapsing), the proposal modal, and deleting `agentView.js` itself.
  - File: `toDoList_main/src/assignmentCoverage.js`, `toDoList_main/src/agentWiring.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 541634aa-ee0b-432c-8f82-dcc51487a3a1 -->

- [x] **[HIGH]** Fix Blocked-row action control overflowing off-screen in assignment coverage modal — Completed: 2026-08-03
  - Type: bug
  - Description: In the assignment coverage modal's "Waiting on you" section, a row in the Blocked state (e.g. task A1) renders wider than the modal on mobile — its status badge, chevron, and text-labeled action button overflow the right edge, leaving the confirm action invisible/untappable. Replace the text-labeled "Confirmed" button with a compact ~28x28px icon-only button (checkmark SVG, `stroke="currentColor"`) for Blocked rows so the full row (dot, id, truncated title, "Blocked" label, icon button) fits within the modal width without clipping. Keep the existing checkbox-style "Confirmed" behavior/state (clicking it should perform the same confirm action as the other rows) — only the visual control changes.
  - File: `toDoList_main/src/assignmentCoverage.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 1428db12-12cc-4800-a15d-d8f8f9ca5783 -->

- [x] **[MEDIUM]** Carry the TODO.md run-status pill across the mobile↔desktop viewer relocation — Completed: 2026-08-03
  - Type: bug
  - Description: The TODO.md viewer's in-flight run pill (`.todoMdViewerRunPill`, built in `todoMdViewer.js` and swapped in for `.todoMdViewerRunBtn` while a run is QUEUED/RUNNING) is missing from `VIEWER_STRIP_ACTION_SELECTORS` (todoMdViewer.js:310-316), the selector list `relocateActionsToStrip`/`restoreActionsToMeta` use to move action controls between the card's `.todoMdViewerMeta` and the desktop rail strip's `.todoMdViewerStripActions` on every mobile↔desktop breakpoint crossing (>1023px, gated by `isViewerDesktopMode`). Going mobile→desktop, `placeViewerCardDesktop` (todoMdViewer.js:502) relocates only the matched selectors into the strip, then hides the in-list card (`card.hidden = true`, line 519) — stranding the pill in the now-hidden card's meta. Going desktop→mobile, `teardownViewerDesktopChrome` (todoMdViewer.js:491) calls `restoreActionsToMeta` (which also misses the pill), then `removeViewerRailStrip()` deletes the whole strip element — destroying the pill with it. Either direction leaves an in-flight run with no visible status indicator until the run finishes or the card is otherwise re-rendered.
  - Behavior: Add `.todoMdViewerRunPill` to `VIEWER_STRIP_ACTION_SELECTORS` so the pill relocates alongside the other action controls (Run backlog button, deploy pill, sync button, overflow) in both directions, exactly as `.todoMdViewerRunBtn` already does.
  - File: `toDoList_main/src/todoMdViewer.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 8a11bb1d-f076-4478-8539-053e4f098195 -->

- [x] **[LOW]** Add a version-bump comment to style.css to trigger a redeploy — Completed: 2026-08-03
  - Type: feature
  - Description: Test entry with no functional effect, used solely to trigger the build/deploy pipeline. Add or update a single comment line near the top of the stylesheet (e.g. `/* redeploy marker: <date> */`) with no other changes. Confirms the pipeline runs end-to-end without touching app behavior.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 67412807-23ec-4386-b57e-72a804051968 -->

- [x] **[MEDIUM]** Sync chat history across devices via the chat_turns table — Completed: 2026-08-03
  - Type: feature
  - Description: A workspace repo's chat thread lives only in `todoapp_claudeChat` (localStorage), so a conversation started on one device is invisible on another. Back it with the new `public.chat_turns` table (columns `id`, `user_id`, `repo`, `role`, `content`, `created_at`; RLS scopes to `auth.uid() = user_id`, and `user_id` defaults to `auth.uid()` so the client never sends it). Add four mutations to `listLogic.js` — ALL Supabase writes route through listLogic, `claudeSheet.js` must never call supabase directly: `insertChatTurn({ id, repo, role, content })`, `fetchChatTurns(repo, limit)` (select ordered `created_at` desc, limited, returned oldest-first), `pruneChatTurns(repo, cap)` (select ids ordered `created_at` desc from offset `cap`, then delete `.in('id', ids)`; a no-op when nothing is beyond the cap), and `clearChatTurns(repo)` (delete all rows for that repo). Each returns `{ ok, error? }`. In `claudeSheet.js`, introduce a single `appendChatTurn(turn)` helper that pushes to `chatHistory`, calls `saveChatHistory()`, then fires `insertChatTurn` followed by `pruneChatTurns(repo, CHAT_HISTORY_CAP)` — reuse the existing `CHAT_HISTORY_CAP = 60` as the single source of truth for the cap on both sides, do NOT introduce a second constant. Replace all six `chatHistory.push(...)` sites with it (the send path, the assistant reply, the two note/label pushes, the iterate seed, and the run follow-up). Mint the turn's `id` with `mintEntryId()` (already imported) and pass it explicitly so the local turn and its row share an identity; also stamp a client `ts` (`Date.now()`) on the turn for ordering before its row's `created_at` is known. Both new fields must survive `stripTurnImages`, which stays exactly as-is — `images` are session-scoped and base64 must never reach Supabase any more than it reaches localStorage. Hydration is local-first: at both hydrate sites (the sheet mount-hydrate near `loadWorkspaceRepos`/`replayChatHistory`, and the workspace auto-swap that saves the outgoing thread then does `chatHistory = loadChatHistory(repo)`) keep the existing synchronous localStorage load and `replayChatHistory()` untouched so the thread paints instantly and works offline, then fire `fetchChatTurns(repo, CHAT_HISTORY_CAP)` asynchronously; on resolve, merge remote rows with the in-memory thread by turn `id` (union, remote wins on conflict), sort by `created_at` falling back to the local `ts`, slice to the last `CHAT_HISTORY_CAP`, assign to `chatHistory`, `saveChatHistory()`, and call `replayChatHistory()` a second time. CRITICAL: guard that second replay — bail without touching `chatHistory` or the surface if `activeChatRepo` changed while the fetch was in flight, or if a turn is currently being sent, because `replayChatHistory` starts with `surface.innerHTML = ''` and would destroy a pending or mid-stream assistant bubble. An `ok:false` fetch leaves the local thread exactly as hydrated and raises no toast — this is a background sync, not a user action. Every site that resets the thread must also clear the remote rows so a later hydrate can't resurrect it: `clearChatConversation` and `loadWorkspaceRepos` already call `deleteChatHistory(repo)` and should call `clearChatTurns(repo)` alongside it, and `seedIterateSession` and `startFollowUpFromRun` both assign `chatHistory = []` without any delete today — they need both. Legacy local turns predating this change carry no id and were never uploaded, so they merge in as local-only and simply won't appear on other devices; do not attempt to backfill them.
  - Out of scope: Realtime subscriptions on `chat_turns` (deliberately rejected — `replayChatHistory` is a full teardown and would kill an in-flight stream, and a turn arriving mid-compose would desync the array sent to the Worker from the server thread), a manual refresh control, syncing `todoapp_claudeIterateEntry` / `todoapp_claudeChatTask`, and any change to prompt-cache breakpoint construction.
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/src/claudeSheet.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 3dbc7c7d-cb6a-4a4a-afda-6508814f4ff6 -->

- [x] **[MEDIUM]** Add a confirm step to New Chat before wiping the thread — Completed: 2026-08-03
  - Type: feature
  - Description: `buildClearChat` wires the `+ New Chat` pill directly to `clearChatConversation` with no confirmation, and that wipe is now cross-device — it calls `deleteChatHistory` AND `clearChatTurns` for the active repo, so one stray tap permanently deletes the thread on every device with nothing recoverable. This is strictly more destructive than "Clear completed" in the RUNS tab, which already guards itself with an inline confirm even though run records are reconstructible from queue rows and TODO.md markers. Add an inline confirm line beneath the chat header row, following the `buildClearCompleted` pattern. Structure: leave `buildClearChat` returning just the pill and leave `#claudeChatHeader` untouched (it is a right-aligned flex ROW carrying the spend control and the pill, and a taller child would stretch it) — instead add a `buildClearChatConfirm()` returning a `div.claudeClearChatConfirm` with `hidden = true`, and mount it in the chat view as a sibling directly AFTER `header` and BEFORE `#claudeChatSurface`, so it spans the view width and can hold real copy. It contains a `span.claudeClearChatConfirmWarn` reading "Deletes this thread on every device.", a `button.claudeClearChatYes` labelled "Wipe", and a `button.claudeClearChatCancel` labelled "Cancel". The pill STAYS visible while the confirm is open (it is not hidden the way the runs-clear button is) so it stays obvious what is being confirmed. Rewire the pill's click to reveal the confirm instead of wiping; Cancel hides it; Wipe calls `clearChatConversation` and then hides it. Styling goes in `style.css` next to the existing `.claudeRunsClear*` rules and reuses their visual language: the row is `display: flex; align-items: center; justify-content: flex-end; gap: 10px; margin-top: 6px`, the warn text is `var(--text-secondary)` at 12px, Cancel is the muted underlined text button (`var(--text-muted)`, no background, no border), and Wipe is the same text-button shape in `#f09595` at `font-weight: 600`. CRITICAL: the confirm element needs an explicit `.claudeClearChatConfirm[hidden] { display: none !important; }` guard — the author-level `display: flex` outranks the UA `[hidden]` rule, and without it the confirm renders permanently (the `claude*` family already carries ~10 such guards; match them). Add a `syncClearChatVisibility()` helper that hides the confirm and hides the `+ New Chat` pill entirely when `chatHistory.length === 0` (nothing to wipe), and call it from `replayChatHistory`, from the new `appendChatTurn` helper, and at the end of `clearChatConversation`. Also reset the confirm to hidden whenever it could be left armed out of view: on tab change in `setActiveTab`, and on the workspace auto-swap that reassigns `chatHistory`. `clearChatConversation` itself is unchanged apart from the trailing sync call — do not alter what it deletes.
  - Out of scope: any change to what `clearChatConversation` wipes, a soft-delete `cleared_at` column on `chat_turns` (considered and rejected — this entry is the confirm step, not recoverability), confirms on any other destructive control, and the RUNS tab's existing clear confirm.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 8577a9c6-87ce-4f39-a3f6-bcbcf58bcd74 -->

- [x] **[HIGH]** Add a Retry triage action for runs stuck before a draft exists — Completed: 2026-08-03
  - Type: bug
  - Description: When `claude-triage.yml` dies before writing a verdict, `reconcileStuckTriaging` in `agentQueueStore.js` flips the row from `triaging` to `failed` with the reason "The triage sweep didn't finish for this task…". That row has no `entry_id` and no `draft`, so `buildDispatchBlock`'s retry mode computes `canRun = !!(queueRow.entry_id || draftText)` → false and mounts the RETRY button permanently disabled. The only disabled styling is `opacity: 0.6`, so it reads as a live control on both the desktop description panel and the mobile description-editor modal, and tapping it does nothing — the click handler bails on `if (btn.disabled) return`. Even if it were enabled it routes through `dispatchDraft` → `claude-run.yml`, which dispatches an entry; the wrong recovery for a row that never got one. Add a third mode that re-runs triage instead.
  - Behavior:
    - A STUCK row with no `entry_id` and no `draft` mounts a control labelled RETRY TRIAGE instead of RETRY, reusing the existing `.descDispatchButton--retry` variant (no new CSS).
    - Tapping it shows "Retrying…", clears `failure_reason` (null), sets the row back to `triaging` via `listLogic.setAgentRunState`, then calls `fireTriageSweep(projectName)` — the same store-guarded sweep the row's Generate button and the ASKING answer path fire.
    - On success the control clears on the next repaint as the row leaves STUCK (realtime + `onQueueChange`), exactly as dispatch/retry do today.
    - If `setAgentRunState` fails, surface the inline error and restore the idle label without firing a sweep.
    - If `fireTriageSweep` resolves `{ ok: false }`, the row is already back at `triaging`: surface "Triage didn't start — Run it from the Agent tab." inline rather than reverting the state.
    - If `fireTriageSweep` returns `null` (a sweep is already in flight and the shared guard swallowed the call), leave the row at `triaging` and surface "A sweep is already running — this task will be picked up on the next one." The existing reaper re-marks it failed if that sweep settles without touching it.
    - A STUCK row that DOES have an `entry_id` or a draft keeps today's RETRY behavior unchanged.
    - Update the reaper's reason string in `reconcileStuckTriaging` to point at the new control instead of "Remove it and flag the task again to retry."
  - Implementation notes:
    - `buildDispatchBlock(item, queueRow, mode)` gains a fourth param `projectName`. `syncDispatchPanel` passes `toDoChild.dataset.value`; `modals.js` `renderDispatchBlock` passes `opts.projectName || ''`.
    - Mode resolution in both hosts: for STUCK / `failed` / `no_change`, pick `'retriage'` when `!queueRow.entry_id && !(queueRow.draft || '').trim()`, else `'retry'`. The existing `data-dispatch-mode` attribute already distinguishes the mounted block, so the idempotent early return keeps working across repaints.
    - `fireTriageSweep` is already imported in `toDoRow.js` from `agentQueueStore.js`; no new import there, and `modals.js` needs none since it mounts the shared block.
    - The retriage branch must not call `dispatchDraft` at all.
    - Keep the existing anchors: retriage mounts beneath the same STUCK reason block as retry (`.descEditorModalStuck` row-side, `#descEditorModalStuck` in the modal).
  - Out of scope: the disabled-state styling of `.descDispatchButton`; any Worker or `claude-triage.yml` change; the Agent board's own retry control; per-row triage dispatch (this fires the project-wide sweep).
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/agentQueueStore.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: b101c357-598b-4acb-8b31-699041db068b -->

- [x] **[MEDIUM]** Make Run-backlog and Run-entry dispatches from the TODO.md viewer appear in the chat sheet's Runs tab
  - Type: bug
  - Description: When a run is dispatched via the TODO.md viewer's "Run backlog" button (`runBacklog()` in `toDoList_main/src/todoMdViewer.js:2017-2065`) or its per-entry "Run this entry" pill (`runEntry()`, `todoMdViewer.js:2074-2123`), the dispatch only calls `writeActiveRun()` (`runState.js`) and `startRunPill(correlationId)` (`todoMdViewer.js:1711-1752`), which drive the viewer's own local header pill. Neither path writes an `agent_queue` row nor a `localStorage['todoapp_claudeRuns']` record, so none of the three sources `getDisplayRunRecords()` unions in `claudeSheet.js:4134-4152` (`buildQueueRunRecords`, `buildShippedEntryRecords`, `runRecords`) has anything to show while the run is in flight. The chat sheet's Runs tab therefore shows nothing for these dispatches until the entry is checked off in TODO.md, at which point `buildShippedEntryRecords()` (`claudeSheet.js:3999-4041`) surfaces it directly as SHIPPED with no preceding QUEUED/RUNNING row ever shown. Fix by having `runBacklog()`/`runEntry()` also push a `runRecords`-shaped entry into the `todoapp_claudeRuns` list (mirroring the chat-ship path at `claudeSheet.js:3736-3767`: push a QUEUED record via the exported record-writing helper, then start a poller) so the same correlation id that already drives the viewer's local pill also drives a live QUEUED/RUNNING row in the chat sheet's Runs tab.
  - File: `toDoList_main/src/todoMdViewer.js`, `toDoList_main/src/claudeSheet.js`
  - Completed: 2026-08-03
  <!-- id: fe106d57-e939-4b7a-bff7-ab0c42963c86 -->

- [x] **[MEDIUM]** Add a corner drag handle to resize the assignment edit modal on desktop
  - Type: feature
  - Description: `#assignmentEditorModal` is currently fixed at max-width:520px/max-height:86vh with overflow:hidden, so the inner textarea's native `resize: vertical` handle gets clipped by the dialog and can't actually grow the surrounding chrome. Add a visible bottom-right drag handle (diagonal grip-dot affordance, ~16x16px, `cursor: nwse-resize`, positioned absolutely on `#assignmentEditorModal` which needs `position: relative`) that resizes the whole dialog box (width and height) via pointer drag, replacing the now-redundant textarea resize with `resize: none` so only the dialog-level handle drives sizing. Constrain resizing to reasonable bounds (e.g. min ~360px/300px, max ~90vw/90vh) and gate the handle to desktop only, matching the existing `@media (max-width: 480px)` breakpoint in style.css:2075 where the handle must be hidden and the mobile max-height:92vh override left untouched. Wire the drag handler in showAssignmentEditorModal alongside the dialog's existing element construction.
  - File: `toDoList_main/src/modals.js`, `toDoList_main/src/style.css`
  - Completed: 2026-08-03
  <!-- id: 70e4ad6e-f528-4592-90e8-bb52b7aa6234 -->

- [x] **[MEDIUM]** Hide the collapse/expand toggle in the TODO.md viewer strip on mobile
  - Type: bug
  - Description: On mobile, `#todoMdViewerStrip` shows a collapse/expand button, but the sheet is already always expanded automatically when opened, making the toggle redundant and confusing since it has no visible effect. Hide the collapse/expand button on mobile (matching how it's presumably already hidden on desktop), and ensure the sheet always renders expanded on open for mobile viewports. Likely lives in `todoMdViewer.js` with corresponding mobile breakpoint rules in `style.css`.
  - File: `toDoList_main/src/todoMdViewer.js`, `toDoList_main/src/style.css`
  - Completed: 2026-08-04
  <!-- id: e8ac91d1-d8b2-4711-bc44-0c48a73f1d50 -->

- [x] **[MEDIUM]** Group the coverage detail modal by rubric section and give each aspect row a two-line layout — Completed: 2026-08-04
  - Type: feature
  - Description: On mobile the coverage breakdown is unreadable at the row level and structureless at the list level. `.coverageDetailRow` (`toDoList_main/src/style.css` ~L6908) is a single-line flex and `.coverageDetailLabel` (~L6952) is `white-space: nowrap` + `text-overflow: ellipsis`, so after the dot, the `A1` id, the em-dash separator, the status word, the confirm tick and the chevron claim their fixed widths, the rubric label gets roughly 130-150px on a 390px viewport and truncates to about twenty characters — the one part of the row that says what the aspect actually is. Separately, `renderBody()` in `showCoverageDetailModal` (~L1218) groups only by lifecycle (`Waiting on you` / `Rubric aspects` / `Manual · Git & process`), so the flat middle group gives no sense of which part of the assignment is lagging even though the aspect ids already encode it (`A1`, `B2`, …) and `aspectSortKey` (~L160) already parses that letter/number split.
  - Behavior: The modal keeps its `Waiting on you` blocked group pinned at the top and its `Manual · Git & process` lane pinned at the bottom, unchanged. Between them, the flat `Rubric aspects` group is replaced by one group per rubric section letter, in first-appearance order, each headed by the bare letter plus a `1 / 3` tally and a three-segment mini progress bar (shipped / in-flight / outstanding) sized to that section's aspects. A blocked aspect renders in its home section as a dimmed, non-interactive echo row reading `Waiting on you ↑`, so a section that reports `1 / 3` shows three rows rather than two. Every aspect row becomes two lines: line one carries the dot, the id and the status as a small chip; line two carries the full label, wrapping freely instead of truncating; any tick that lives inside the row drops to a third line. Blocked and shipped rows keep their existing disclosure behaviour (answer lane, commit helper), their ticks and their amber/accent treatment.
  - Implementation notes:
    - `renderBody()` currently calls `appendGroup(blocked.length || manual.length ? 'Rubric aspects' : '', regular, null)`. Replace that single call with a section loop over `regular` **plus** the blocked items (blocked aspects must appear in their own section as echoes, so partition off `manual` only, and mark blocked items so the section renderer emits an echo rather than a live row). Derive each item's section with `aspectSortKey({ aspect: item.id })` — it already accepts any object carrying `.aspect`, so no new regex — and bucket by `.letter`, preserving the order letters first appear in `aspects` (which is rubric-file order from `parseAspects`) and preserving item order within a bucket. Items whose id doesn't parse (`aspectSortKey` returns null) collect into one trailing group labelled `Other aspects` with no letter and no bar.
    - Add `appendSection(letter, sectionItems)` alongside `appendGroup`: build a `.coverageDetailGroup coverageDetailGroup--section`, append a `.coverageSectionHead` holding a `.coverageSectionLetter` (the bare letter) and a `.coverageSectionCount` (`shipped + ' / ' + total`), then a `.coverageSectionBar` of three `.coverageSectionSeg--shipped|in-flight|outstanding` children sized by `el.style.flexGrow`, then the rows. The inline `flexGrow` write is deliberate and matches the precedent already documented on `.agentCoverageSeg` in `buildCoverageSummary` (proportions are computed per paint); everything else stays class-driven in `style.css`. Compute the per-section tally from that section's items using the same `shipped` / `in-flight` / everything-else split `computeCoverage` uses, so a section can never disagree with the header total.
    - Add `buildSectionEchoRow(item)` for blocked aspects: a `div` (never a `button`) with `coverageDetailRow coverageDetailRow--blocked coverageDetailRow--echo`, carrying the dot, the id, the label and a status chip reading `Waiting on you ↑`, no tick and no chevron, and `aria-hidden="true"` — the live actionable row in the pinned top group is the accessible one, so the echo must not double-announce.
    - Two-line layout is a CSS-only change to `buildCoverageDetailRow`'s existing DOM plus one deletion. Turn `.coverageDetailRow` into `display: grid; grid-template-columns: 8px 1fr auto auto; column-gap: 8px; row-gap: 3px; align-items: center` and place the children by class rather than by source order, since the append order (dot, id, sep, label, status, tick, chevron) no longer matches the visual order: `.coverageDetailDot` at `1 / 1`, `.coverageDetailId` at `1 / 2`, `.coverageDetailStatus` at `1 / 3`, `.coverageDetailChevron` at `grid-area: 1 / 4 / 3 / 5` so it centres against both lines, `.coverageDetailLabel` at `grid-area: 2 / 2 / 3 / 4` with `white-space: normal; overflow: visible; text-overflow: clip; line-height: 1.4`, and `.coverageDetailRow > .coverageCommitTick` at `grid-area: 3 / 2 / 4 / 5` — scope that last selector with the direct-child combinator, since `.coverageCommitTick` is also used inside `.coverageCommitTickRow` in the commit lane and must not inherit grid placement there. Restyle `.coverageDetailStatus` as a chip (SpaceMono, ~9.5px, uppercase, `--bg-active` fill, `--border-mid` hairline, 4px radius) with the shipped variant on `--accent-text` / `--accent-dim` and the in-flight and blocked variants on `--text-warning`; the existing `.coverageDetailRow--blocked .coverageDetailStatus` amber rule folds into that.
    - Remove the `.coverageDetailSep` element from `buildCoverageDetailRow` (~L714) and its CSS rule — the em dash only made sense when the id and label shared a line.
    - `.coverageDetailConfirmable` (the blocked row paired with its sibling tick) stays a flex row and is unaffected, but its `min-width: 0` comment cites the old ellipsize behaviour; update the comment so it doesn't mislead. Because the blocked row's tick is already an outside sibling, `buildCommitTick(item, ctx, 'Confirmed', item.status === 'blocked')`'s icon-only argument exists purely to save horizontal room on narrow screens — with the label now on its own line the icon-only treatment is still correct for the sibling layout, so leave that call as-is.
    - Do not touch `computeCoverage`, `aspectStatus`, `parseAspects`, `parseAspectLabels`, the `ctx.committed` / `aspect_submissions` tick path, `_expandedBlockedAspect` restoration, or the `onQueueChange` repaint contract — `renderBody` must still rebuild from live rows, preserve `body.scrollTop`, and reset `ctx.ticks` exactly as it does now.
    - Add `toDoList_main/tests/coverageSectionGroups.test.js` covering: aspects bucket into sections by letter in first-appearance order; a section's tally matches its members; a blocked aspect renders both in the pinned top group and as an `aria-hidden` echo in its section; an unparseable id lands in the trailing group. `tests/agentCoverageDetail.test.js`, `tests/coverageDetailRepaint.test.js`, `tests/coverageConfirmAspect.test.js` and `tests/agentGitlabTick.test.js` must all still pass untouched.
  - Out of scope: Human-readable section names — the rubric parse yields ids and labels only, so headers show the bare letter and tally, never an invented title. No change to the `claudeCoveragePane` summary, its headline, its counts line or the segmented `.agentCoverageBar`. No tap-to-scroll from an echo row to its live blocked row. No filtering, no collapsing sections, and no built-vs-committed second axis — those were considered and deliberately deferred.
  - File: `toDoList_main/src/assignmentCoverage.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/coverageSectionGroups.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 5b641b3e-5e65-4906-bef3-af7cfc467fdb -->

- [x] **[MEDIUM]** Preflight check in the onboard modal — Completed: 2026-08-04
  - Type: feature
  - Description: `showOnboardModal` currently offers one action — Onboard — which scaffolds and registers a repo in one irreversible step. The Worker's new `preflight` route dispatches the same `onboard.yml` in report-only mode (`ONBOARD_PREFLIGHT=1`), writing nothing and returning a resolved shape, derived commands, a `create[]` list of missing scaffold files, and a `warnings[]` array. Surface that as a Check action beside Onboard so a repo can be inspected before it is touched. The `.injectOnboardCard` and its one-tap copy stay unchanged — Check is optional, and Onboard still works without it.
  - Behavior: Add a secondary `Check` button to `#injectOnboardActions`, left of `#injectOnboardSubmit`. On click, validate the repo field with the same rules `onSubmit` uses, mint a correlation id via `mintEntryId`, subscribe with `subscribeRunOutputs`, and POST `{ preflight: true, target_repo, shape, purpose, correlation_id }` through a new `preflightRepo()` export in `inject.js` that mirrors `onboardRepo`. Render into a new `#injectOnboardVerdict` block inside `#injectOnboardBody`, below the shape field. While the row is `running`, show a spinner reusing `.injectOnboardSpinner`. On the terminal row, parse `stdout` as JSON and render collapsed: status glyph (check when `warnings` is empty, warning triangle when not), repo name, shape chip, purpose label, and — when non-zero — a `N warn` amber count and an `N missing` accent count. Tapping the collapsed strip expands it: each warning as an amber left-bordered row, each `create[]` path as an accent chip. Re-running Check replaces the prior verdict; Onboard stays enabled regardless of what the verdict says.
  - Behavior (mobile): The sub-modal already carries four fields, and an expanded verdict with a warning row plus eight file chips would push `#injectOnboardActions` below the fold. Below the 1024px breakpoint, the expanded verdict replaces the form rather than appending to it — hide `#injectOnboardBody`'s field wrappers, render the verdict full-height with a back control that restores the form. Above the breakpoint, expanded appends inline as normal. Collapsed is inline on both.
  - Implementation notes: `subscribeRunOutputs(correlationId, onRow)` is exported from `inject.js` (~line 900) and is the same channel `captureCard.js` uses; follow that module's teardown discipline and dispose on the terminal row and on `close()`, so a Check left mid-flight cannot leak a subscription. `purpose` must be sent explicitly — the Worker defaults it to `personal`, and an assignment repo expects five additional scaffold files, so a defaulted purpose under-reports `create[]`. Send the shape select value as-is; `auto` is valid. `inject.js` has no `isMobile` import today — bring in the shared helper rather than adding a local matchMedia, and keep `MOBILE_BREAKPOINT` semantics (`< 1024`). The verdict block, its expanded section, and the mobile-hidden field wrappers are all toggled, so each needs an explicit `[hidden] { display: none !important }` guard in `style.css` — the `injectOnboard*` family has no such guards today and author-level `display` declarations will otherwise win. All styling in `style.css` as classes near the existing `#injectOnboardBody` rules (~line 20610); no inline style writes.
  - Out of scope: any Apply fixes action (warnings are display-only); changes to `onboardRepo`, `pollForRegistryRow`, or the post-onboard placeholder flow; a Check action on existing target rows; a fleet view; rendering raw JSON.
  - File: toDoList_main/src/inject.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 6de6e5b5-dd4b-4579-a33f-17bec574b48e -->

- [x] **[LOW]** Drift check on inject target rows — Completed: 2026-08-04
  - Type: feature
  - Description: The four rows under INJECT TARGETS are all already-onboarded repos, and preflight against them answers a different question than pre-onboard inspection: which scaffold files have gone missing since they were onboarded. `wgu-dsa-prep` returns eight missing files (it predates triage, capture, and the assignment set) while `test-repo-only` returns none. That is a per-repo drift check with no new surface required — the same `preflightRepo()` call and verdict renderer entry 1 builds, mounted on a row.
  - Behavior: Add a third icon button to each `.injectTargetRow`, left of the existing edit pencil, using `.injectTargetIconBtn` with an `aria-label` of `Check <nickname>`. On click, mint a correlation id, subscribe, and dispatch `preflightRepo(target.repo, 'auto', purpose)`. While running, swap the icon for the spinner and disable the button. On the terminal row, expand a verdict strip beneath the row inside the same container, rendered by the shared component from entry 1 in its expanded form — collapsed adds nothing here, since the row already shows the repo name. A clean result shows a single "up to date" line; drift lists the missing files as chips. The strip persists until dismissed or another Check runs on that row. Only one row check may be in flight at a time; while one runs, the other rows' Check buttons are disabled.
  - Implementation notes: Depends on entry 1 — do not start until `preflightRepo()` and the verdict renderer exist, and extract the renderer into a shared function rather than duplicating it. `purpose` is not stored on the inject target row, so it cannot be inferred; read it from the repo's `assignment.md` presence if that is already cached, otherwise send `personal` and note in the strip that the file count assumes a personal repo. The row's `shape` IS available from the registry (`handleRepos` serializes it), so pass it rather than `auto` when present — that skips a detection step and makes the result comparable to how the repo was actually onboarded. Teardown on modal close applies here too, and with per-row channels the disposal set is now plural. New styling in `style.css` alongside `.injectTargetRow` rules; toggled elements need `[hidden] { display: none !important }` guards.
  - Out of scope: checking all rows at once; a fleet summary view; any fix or re-onboard action from the row; persisting verdicts across modal opens.
  - File: toDoList_main/src/inject.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 31e185a5-c20f-44c2-b1ea-c630d5f551c7 -->

- [x] **[MEDIUM]** Repo setup shape reference in settings — Completed: 2026-08-04
  - Type: feature
  - Description: Starting a course repo means knowing which shape it is, whether a template exists or a CLI is needed, and which gotchas apply — all of which live in `SHAPES.md` at the root of the public `claude-routine-template` repo. Surface that as a shape picker so the reference is reachable on mobile without leaving the PWA, fetched live rather than bundled so adding a shape to the file adds it to the picker with no client release.
  - Behavior: Add a `Repo setup` section to the settings menu — its own `buildSettingsMenuDivider()` and `settingsMenuSectionHeading` — placed immediately before the `Configure inject` item in the existing `Data` section. Tapping opens a new modal. On open, fetch `https://raw.githubusercontent.com/rsterenchak/claude-routine-template/main/SHAPES.md` directly (public repo, no Worker, no auth) and cache the text in a module-level variable for the session. Parse each `## <shape-name>` heading into a collapsed row showing: the shape name; a `TEMPLATE` chip when the section contains a `**Template:**` line, a dimmed `CLI` label when it does not, or a `NO SHAPE` label when it contains `**No shape exists.**`; and an amber warning glyph when the body contains `least-proven`. Tapping a row expands it inline to show the section's lead paragraph, a copyable value (the backticked repo name from `**Template:**` for template shapes, the first fenced code block for CLI shapes), the `**Onboarding adds:**` items as accent chips, and the `**Gotchas**` bullets as amber left-bordered rows — first two shown with a `SHOW N MORE` control when there are more. One row expanded at a time. A footer shows the source filename and a `VIEW FULL` control that swaps the modal body for the whole document rendered as markdown, with a back control returning to the picker.
  - Implementation notes: The file carries a `PARSING CONTRACT` HTML comment naming the four markers this depends on (`## `, `**Template:**`, `**Onboarding adds:**`, `**Gotchas**`) — parse exactly those and strip the comment before rendering the full view. As of writing the file yields 8 sections: build-pipeline (CLI, 4 gotchas), served-from-source (TEMPLATE, 4), console (TEMPLATE, 4), desktop (TEMPLATE, 3), maui (CLI, 4, warning), sql (TEMPLATE, 3), repo-only (TEMPLATE, 2), python (NO SHAPE, 0). A section with no markers must still render its lead paragraph rather than an empty body. Write a small local markdown reader; do not add a library — the full view needs headings, paragraphs, fenced code, inline code, bold, and unordered lists only. This is remote text: build every node with `textContent` and never assign `innerHTML` from the fetch. On fetch failure show an inline error with a retry control, not an empty modal. Picker rows, expanded bodies, the full-document view, and the error state are all toggled, so each needs an explicit `[hidden] { display: none !important }` guard in `style.css`. Below 1024px the full-document view replaces the picker rather than appending, matching the form-swap in the onboard modal's verdict. All styling as classes in `style.css`; no inline style writes.
  - Out of scope: editing `SHAPES.md` from the app; any action that creates a repo or copies a template; caching across sessions; wiring the picker to the onboard modal's shape select; a search field.
  - File: toDoList_main/src/settingsMenu.js, toDoList_main/src/repoSetup.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 42f4d964-d4f5-4a39-b036-f891325b13ff -->

- [x] **[MEDIUM]** File names collapse to one character in the Structure tab — Completed: 2026-08-05
  - Type: bug
  - Description: In the Structure tab's Code lens, file rows render their name truncated to a single character plus an ellipsis — `Program.cs` shows as `P.`. It does not resolve at any window width. `.structureFileName` sets `overflow: hidden` with `text-overflow: ellipsis`, which implies `min-width: 0` on a flex child, but the rule sets neither `flex` nor `min-width`. Its siblings do not shrink: `.structureExplainBtn` is `flex: 0 0 auto` with `margin-left: auto`, and the GitHub link is fixed-width. The name is therefore the only shrinkable item in the row and absorbs the full space deficit. Widening the viewport does not help because `margin-left: auto` assigns all new free space to the gap before the button rather than to the name. Nested files are worst affected — `--structure-depth` adds 14px of left padding per level, so `src/App/*.cs` at depth 2 loses another 32px before the name is measured.
  - Behavior: File names render in full when the row has room, and ellipsize from the end at a readable width when it does not — never below a few characters. The Explain button and GitHub link keep their current size and position at the right edge. Nesting indent is unchanged.
  - Implementation notes: In `style.css`, add `flex: 1 1 auto;` and a `min-width` floor (`4ch`) to the existing `.structureFileName` rule at ~line 5232. Once the name grows, `margin-left: auto` on `.structureExplainBtn` is redundant but harmless — leave it rather than risk changing button placement. Check `.structureRegionRow` and `.structureCollapsedRow` for the same pattern: they share the `position: relative` and `::before` indent-guide block at ~line 5931 and may carry the identical latent bug with longer content. Do not change the row's `display: flex`, `gap`, or padding.
  - Out of scope: the Types lens; the indent-guide gradient; truncation behavior of the Explain result body; any change to `structureView.js`.
  - File: toDoList_main/src/style.css
  - Completed:
  <!-- id: 7dd7a948-b221-4ff3-8690-30e472bc39c6 -->

- [x] **[MEDIUM]** Framework variants in the Repo setup picker — Completed: 2026-08-05
  - Type: feature
  - Description: `SHAPES.md` now splits the `build-pipeline` section into `### angular`, `### react`, and `### vue` sub-blocks, each carrying its own scaffold command and its own config edits. The picker renders only the first fenced block of a section, so it shows Angular's scaffold and nothing else — React's commands are unreachable and Vue's don't appear at all. Render variants as a segmented control inside the expanded row.
  - Behavior: When a parsed section contains `### <variant-name>` headings, render a segmented control above the section body listing each variant in file order, first selected by default. Selecting a variant swaps the body below it to that variant's content: its prose, then its fenced blocks each with its own copy control. Within a variant the first fenced block is the scaffold command and the second, when present, is the edits — label them `SCAFFOLD` and `EDITS` above their respective blocks using the existing mono label style. Copy writes the block's raw text with no label and no surrounding prose. The section's own lead paragraph, `**Onboarding adds:**` chips, and `**Gotchas**` list stay above and below the segmented control respectively — they belong to the shape, not the variant. Sections with no `###` headings render exactly as they do now.
  - Implementation notes: Split a section's body on `\n### ` after extracting the lead paragraph and before the `**Gotchas**` marker, so gotchas are never absorbed into the last variant — `vue` is currently last and its variant body ends where `**Gotchas**` begins. As of writing only `build-pipeline` has variants (angular, react, vue), each with two fenced blocks; `maui` has one fenced block and no variants and must keep rendering as it does today. Preserve fenced-block text exactly for copy — leading whitespace and blank lines are significant in the jsonc edit blocks. Do not strip the `// angular.json` style comments from jsonc blocks; those are content, unlike the bash inline comments which the file no longer contains. The segmented control, each variant body, and the copy-confirmation state are toggled, so each needs an explicit `[hidden] { display: none !important }` guard in `style.css`. Reuse the existing segmented-control pattern from the onboard modal's Purpose selector rather than new classes where the styling matches. All styling as classes in `style.css`; no inline style writes.
  - Out of scope: changes to the settings menu entry or the modal shell; the full-document view; parsing any heading level below `###`; a copy-all control that concatenates scaffold and edits.
  - File: toDoList_main/src/repoSetup.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: ed333c2e-9eac-4ee5-b7a0-bc7e11a4058f -->

- [x] **[MEDIUM]** Collapse the Structure tab's GitHub link to its arrow — Completed: 2026-08-05
  - Type: bug
  - Description: File rows in the Structure tab's Code lens still truncate names to a single character — `Greeter.cs` renders as `G.`. The earlier flex fix works but cannot help: the row's fixed-width children over-subscribe the panel. At depth 2 the indent takes 32px, the file icon and gaps ~23px, the Explain button ~145px, and `View on GitHub ↗` ~95px — more than the panel's width before the name is measured, so `.structureFileName` sits pinned at its `min-width: 4ch` floor. The GitHub label is also redundant on every row of a repo tree; the `↗` already carries the meaning.
  - Behavior: `.structureGithubLink` renders only the `↗` glyph, no text label. It keeps its current position at the right edge of the row, its muted colour, its hover and focus-visible treatment, and its existing click behavior. Its accessible name becomes `View <file path> on GitHub` via `aria-label`, so the meaning is not lost when the visible text is. File names then render in full at typical widths and ellipsize only when genuinely long.
  - Implementation notes: In `structureView.js`, `buildGithubLink` at ~line 286 sets `a.textContent = 'View on GitHub ↗'` — change to the glyph alone and add an `aria-label` built from the `file` argument. The function is shared: check every call site before changing it, since the region/line-level rows may also use it and a label-less arrow may read worse there — if so, add an optional parameter rather than changing behavior for all callers. In `style.css`, give `.structureGithubLink` a fixed hit area (min-width and centred content) so the bare glyph is still a comfortable tap target on mobile, and `flex: 0 0 auto` so it never absorbs space the name should get. Leave `.structureFileName`'s `flex: 1 1 auto; min-width: 4ch` as-is — it is correct and the floor is what should be hit only in genuinely tight cases.
  - Out of scope: resizing or relabelling the Explain button; the Types lens; the indent-guide gradient; any change to what the link points at.
  - File: toDoList_main/src/structureView.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 4d9d9a00-280c-4e61-99be-107eb6a59934 -->

- [x] **[MEDIUM]** Show the actual completed entry's title on a backlog run's Runs tab row instead of the generic "Backlog run" label — Completed: 2026-08-05
  - Type: bug
  - Description: "Run backlog" has no entry id up front (the routine picks which TODO.md entry to work on itself), so `runBacklog()` in `toDoList_main/src/todoMdViewer.js` (~line 2030) always calls `trackDispatchedRun()` (`toDoList_main/src/claudeSheet.js` ~line 689) with the hardcoded `title: 'Backlog run'` (todoMdViewer.js:2074). When the run later resolves, `reconcileSuccessConclusion()` (claudeSheet.js ~line 4928) short-circuits any record with no `entryId` straight to SHIPPED and never discovers which entry it actually was, so the Runs tab row (and the localStorage run record) is stuck showing "Backlog run" forever instead of the real task title.
  - Implementation notes: Capture a snapshot of the current TODO.md body before dispatch (the viewer already holds it in `card.dataset.content`, todoMdViewer.js ~line 1190) and thread it through `trackDispatchedRun` onto the run record. In `reconcileSuccessConclusion`, when a no-`entryId` record resolves to SHIPPED, diff the freshly-read content (`read.content`) against the stored snapshot to find the single task line whose checkbox flipped from `- [ ]` to `- [x]`, and extract its title the same way `parsePastedEntry` does in `toDoList_main/src/entryParse.js` (strip the checkbox and any leading `**[PRIORITY]**`). Update the run record's title with that before calling `setRunRecordStatus`.
  - Behavior: If the diff finds zero or more than one newly-checked entry (ambiguous), keep the existing "Backlog run" fallback rather than guessing.
  - File: `toDoList_main/src/todoMdViewer.js`, `toDoList_main/src/claudeSheet.js`
  <!-- id: b84069fa-f122-4a7f-9616-8d33de85b379 -->

- [x] **[MEDIUM]** Code viewer in the Structure tab's detail column — Completed: 2026-08-05
  - Type: feature
  - Description: In the Code lens the desktop detail column (`.structureCanvasHost`) renders nothing — the UI lens uses it for the block canvas, but Code leaves it empty. Meanwhile the refactor card reports `start_line`/`end_line` for its candidate as prose the user cannot act on. Render file source in that column, and make the scan's line span a jump target so a candidate can be read before `Push entry` drafts an entry to extract it.
  - Behavior: In the Code lens on desktop, clicking a file row loads its content via `readRepoFile(target, filePath)` and renders it into `.structureCanvasHost` with a line-number gutter and monospace text, `white-space: pre` with horizontal overflow so lines do not wrap. The clicked row stays visually selected in the tree. The viewer header shows the file path, its total line count, an external GitHub link, and a close control that returns the column to empty. The refactor card gains a jump chip reading `<file> : <start>–<end>` built from the candidate's existing `start_line`/`end_line`; clicking it opens that file and scrolls to the span with those lines highlighted, under a dismissible banner naming the candidate. Dismissing the banner clears the highlight and leaves the file open. Below 1024px the detail column does not exist — file rows keep their current behavior and no viewer is rendered.
  - Behavior (windowing): Never render a whole file. Opening cold renders the first 300 lines; jumping to a span renders from 60 lines before it to 60 after, expanded to at least 300. `Load 200 more` controls appear above and below the rendered window whenever unrendered lines remain in that direction, appending to the existing DOM rather than re-rendering. This is not optional polish: seven files in this repo exceed 3,000 lines (`style.css` is 21,719) and those are precisely the files the refactor scan targets, since it picks the largest over-budget file — so a size cap would break the jump target in almost every real case.
  - Implementation notes: New module `codeViewer.js` exporting a render/clear pair; `structureView.js` calls into it the way it already calls into `structureCanvas.js`, passing the target and file path. Do not grow `structureView.js`, already ~3,086 lines. `readRepoFile` is exported from `inject.js` and returns `{ok, content, sha}`; it routes through the Worker's `read` handler using the server-side token, so private repos work and no client auth is involved — but the repo must be in `inject_targets` or the read 400s. Cache content per repo+path+sha in a module-level Map so re-opening a file and re-jumping do not re-fetch. Build every line node with `textContent`; never assign `innerHTML` from fetched source. Keep the gutter width identical on highlighted and unhighlighted lines — the highlight's left border otherwise shifts the numbers by 2px and reads as broken while scrolling. No syntax highlighting and no highlighting library: that would mean touching the webpack config, and plain mono in two tones is legible. The viewer, its banner, the load-more controls, and the empty state are all toggled, so each needs an explicit `[hidden] { display: none !important }` guard in `style.css`. All styling as classes in `style.css`; no inline style writes.
  - Out of scope: syntax highlighting; a mobile viewer or sheet; editing; search within the file; the UI lens's use of the same column; docking the Explain result beside the code; virtualised scrolling (the chunk loader is deliberately the simpler shape, and can be replaced behind the same interface later if scrubbing large files ever matters).
  - File: toDoList_main/src/codeViewer.js, toDoList_main/src/structureView.js, toDoList_main/src/refactorCard.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 4675e0ea-6238-43cf-9555-aca1e1e3c967 -->

- [x] **[MEDIUM]** Move Explain into the code viewer, and open the viewer as a sheet on mobile — Completed: 2026-08-05
  - Type: feature
  - Description: Every file row in the Code lens carries its own `Explain with Sonnet` button — roughly eighty in this repo. That made sense when the row was the only affordance, but tapping a row now opens the file in the viewer, so the button duplicates that gesture while consuming the row width that was recently truncating file names. One control in the viewer header replaces all of them and puts the explanation beside the code it describes. The viewer is currently desktop-only because the layout has no detail column below 1024px — but the component takes its host as an argument and the chunk loader already bounds its cost, so a full-screen sheet is a valid container. Making mobile a sheet keeps one component and one Explain control rather than a second surface that does most of the same job.
  - Behavior: Remove `.structureExplainBtn` and the inline `.structureExplainResult` from file rows; a row keeps its icon, name, and GitHub arrow at every breakpoint. Add an `Explain` control to the viewer, acting on the open file — on desktop a header control left of the GitHub link, on mobile a full-width primary button in a row directly beneath the header, since on a phone it is the more likely reason the file was opened. Clicking renders the explanation in a collapsible block above the code body, pushing the code down rather than overlaying it; a chevron collapses it once read so it does not hold the screen. Opening a different file clears any shown explanation. Cache hits render instantly with no spinner, as today. The control is disabled when no file is open. Below 1024px, tapping a file row opens the viewer in a full-screen sheet over the Structure tab, with the same header, Explain control, banner, body, and load-more controls as desktop; close returns to the tree with the row still selected. The refactor card's jump chip works at both breakpoints, opening the sheet scrolled to the span on mobile.
  - Implementation notes: `renderCodeViewer(host, opts)` and `clearCodeViewer(host)` already take the host, and `resolveCanvasHost()` already returns `treeEl` on mobile — mount the sheet as a new host rather than adding breakpoint branches inside `codeViewer.js`; the module should stay unaware of which container it is in. `structureIsDesktop()` and `canvasHostDesktop` in `structureView.js` are the existing breakpoint signals; reuse them rather than a new matchMedia. `explainFile(repo, filePath, btn, resultEl)` at ~line 577 already takes its button and result element as arguments, so pass the new control and block and do not rewrite it. Keep `explainCacheKey`, `readCachedExplanation`, `writeCachedExplanation` where they are — the cache is keyed repo+path+sha and stays valid across this move, so previously explained files still hit. `currentSha` lives in `structureView.js` module scope; pass a callback down rather than exporting it, so the sha stays owned by one module. Use `onCodeViewerChange(fn)` to clear the explanation and toggle the control's disabled state. Keep `white-space: pre` with `overflow-x: auto` in the sheet — wrapping breaks the one-number-per-row gutter correspondence. The sheet, the explanation block, its loading and error states, and the collapsed state are all toggled, so each needs an explicit `[hidden] { display: none !important }` guard in `style.css`. Before deleting `.structureExplainBtn`, `.structureExplainResult`, `.structureExplainText`, `.structureExplainLoading`, `.structureExplainError`, grep for other users — the Types lens may share them.
  - Out of scope: changing the explanation prompt, model, or cache shape; explaining a selected line range rather than a whole file; syntax highlighting; virtualised scrolling; any change to the refactor card beyond the jump chip working at both breakpoints.
  - File: toDoList_main/src/structureView.js, toDoList_main/src/codeViewer.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 0a481577-4d5f-4b76-bd85-260e68bc6d56 -->

- [x] **[MEDIUM]** Types lens rows jump into the code viewer — Completed: 2026-08-05
  - Type: feature
  - Description: On C# repos the Types lens renders the manifest's class and member outline grouped by defining file, and each row carries `type.line` — but clicking a row does nothing with that number. The outline exists so you can find a declaration, and finding it currently means opening the file yourself and scrolling. The refactor card already turns `start_line`/`end_line` into a jump; the same gesture applies here, on data the manifest has been publishing all along.
  - Behavior: A type outline row becomes clickable. Clicking opens that row's `file` in the code viewer, scrolls to its `line`, and highlights it under the existing span banner naming the type — `class Greeter` or `void Main`. The banner's dismiss control clears the highlight and leaves the file open, as it does for a refactor jump. Member rows behave identically using their own line. On desktop the viewer opens in the detail column; below 1024px it opens in the sheet, matching the Code lens. Rows without a usable line stay non-interactive rather than opening the file at the top — a jump that silently lands nowhere is worse than no jump. The clicked row takes the same selected treatment a Code lens file row gets, and expanding or collapsing a file group is unchanged.
  - Implementation notes: `buildTypeOutlineRow(repo, d, depth)` at ~line 1240 already receives `file` and `line` and renders `'Line ' + d.line` as text at ~line 1250 — that string is the signal the data is present and unused. Route the click through the existing shared opener at ~line 785 (`renderCodeViewer(host, { target, filePath })`) rather than calling `renderCodeViewer` directly, so host resolution and the desktop/sheet split stay in one place; extend that helper to accept an optional highlight rather than adding a second entry point. `refactorCard.js` line 284 calls `renderCodeViewer` directly with a highlight — check its options shape and reuse it exactly so both callers pass the same thing. Guard on `typeof d.line === 'number' && d.line > 0`, the same test line 1250 already makes. The Types lens is manifest-driven, so a stale manifest yields stale lines; that is acceptable and already true of the outline itself, but do not add a freshness check in this entry.
  - Out of scope: the UI lens's region rows (they have `file`/`line` too and are a natural follow-up, but the UI lens's Locate action already does something with a selection and the interaction needs its own thought); jumping from search results; jumping to a member from a type without expanding; any change to the manifest generator.
  - File: toDoList_main/src/structureView.js
  - Completed:
  <!-- id: b555a723-c217-4f8b-b64b-2eec3dad4d3a -->

- [x] **[LOW]** Show the rendered window, and reopen the last file after a lens switch — Completed: 2026-08-05
  - Type: bug
  - Description: Two small ways the code viewer misrepresents itself. First, the header meta reads `1546 lines` while the body renders only lines 1–300, and the scrollbar sizes to the rendered slice — so the viewer looks like a complete document that inexplicably stops. The window bounds are already tracked in `state.start` and `state.end` and simply are not shown. Second, switching to the UI lens and back clears the open file, because the detail column's contents are thrown away with the host. That clear is correct — the viewer must not report a file as open after its DOM is gone — but nothing reopens the file afterward, so the lens toggle silently costs you your place.
  - Behavior: The header meta reads the rendered range against the total — `1–300 of 1,546 lines` — and updates whenever the window grows via either Load more control. When the whole file is rendered it reads `1,546 lines` with no range, so short files gain no noise. Separately, remember the last file opened per repo, and when the Code lens paints on desktop with a remembered file for that repo, reopen it in the detail column instead of the empty state, restoring any highlight span it was opened with. Switching repos does not carry a file across. Closing the viewer forgets the file, so an explicit close still returns the column to empty on the next lens switch.
  - Implementation notes: `refreshMoreControls(state)` at ~line 433 in `codeViewer.js` already runs on first paint and after each `loadMore`, and has `state.start`, `state.end`, and `state.total` in hand — update the meta there rather than at the three call sites. `refs.meta` is set at ~line 583 on initial render; that line becomes the whole-file case. Use the same thousands formatting the existing string uses, and keep the singular `line` case. For the reopen: the clear at ~line 2500 in `structureView.js` is deliberate and must stay — record the file (and highlight, if any) before clearing rather than trying to preserve the DOM, then reopen through the existing shared opener at ~line 785 after the Code lens mounts its empty state at ~line 2507. Key the memory by repo so the project switcher does not carry a file into a different tree. `getOpenCodeViewerFile()` is already exported and is the read for what to remember; `resetCodeViewer()` is where to forget it on an explicit close. Do not restore on mobile — the sheet is a deliberate open gesture and reopening it on a lens switch would trap the user in a sheet they did not ask for.
  - Out of scope: restoring scroll position within the file; persisting the open file across page reloads; virtualised scrolling; any change to the chunk size or window computation.
  - File: toDoList_main/src/codeViewer.js, toDoList_main/src/structureView.js
  - Completed:
  <!-- id: f8903aa6-d189-4d00-8195-096aa2d776a5 -->

- [x] **[MEDIUM]** Code viewer text renders at inconsistent sizes on iOS, and add a size control — Completed: 2026-08-05
  - Type: bug
  - Description: In the code viewer on mobile, lines render at visibly different font sizes within the same file — long import lines appear smaller than short comment lines despite `.codeViewerLine` setting a single `font-size: 12px`. The cause is iOS Safari's text autosizing (font boosting), which inflates text per block scaled by the block's width relative to the viewport. `.codeViewerCode` uses `white-space: pre`, so every line is a different width and lands in a different inflation cluster with a different multiplier. `style.css` sets `-webkit-text-size-adjust` nowhere, so iOS applies its `auto` default. Desktop browsers do not boost, which is why it only appears on the phone. Separately, 12px monospace is small on a phone and there is no way to adjust it.
  - Behavior: All lines in the viewer render at the same size on iOS, matching desktop. A size control in the viewer header steps the code text through a small range — 10px to 18px in 1px steps — with a minus and plus affordance and the current size between them; the controls disable at each end of the range. Stepping affects the code text and the line-number gutter together so their baselines stay aligned, and does not affect the header, banner, or Explain block. The chosen size persists per device and applies to every file opened afterward, at both breakpoints.
  - Implementation notes: Add `-webkit-text-size-adjust: 100%` (and the unprefixed `text-size-adjust: 100%`) to `.codeViewerLine` at ~line 1240 in the code viewer block. Use `100%`, not `none` — both disable automatic boosting, but `none` has historically also interfered with user-initiated zoom in some engines. Scope it to the viewer rather than `html`; boosting may be doing something desirable elsewhere in the app and this entry should not change unrelated surfaces. For the size control, drive the type from a custom property (`--code-font-size`) set on the viewer pane, with `.codeViewerLine` reading `font-size: var(--code-font-size, 12px)` so the number gutter and code inherit one value. The gutter is likely a fixed pixel width — check it and switch to a `ch`-based width if numbers clip at 18px. Persist through the same `localStorage` pattern `EXPLAIN_CACHE_KEY` uses in `structureView.js`, wrapped in try/catch since quota and privacy modes can throw; a failed write must not break the viewer. Apply the stored size when a pane is built, before first paint, so there is no visible reflow. The control belongs in `.codeViewerHeader` next to the meta; on mobile the header is already tight, so verify it does not push the path label into truncation — if it does, move it beside the Explain button in the mobile action row rather than shrinking the path.
  - Out of scope: a line-wrap toggle; changing the default size; syntax highlighting; font family choice; applying the size to the Explain block or any other surface.
  - File: toDoList_main/src/codeViewer.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 12ec9937-2971-42c7-9036-a537376ac42a -->

- [x] **[LOW]** Repo setup picker drops fenced blocks past the second — Completed: 2026-08-05
  - Type: bug
  - Description: The picker renders a variant's first fenced block as the scaffold command and its second as the edits, ignoring any beyond that. `SHAPES.md`'s angular variant now carries three — the `ng new` command, an `angular.json` edit, and a `package.json` edit — because those are edits to two different files and merging them into one copyable block is what let a `package.json` script get pasted into `angular.json`, overwriting the `test` target. The third block is silently dropped, so the picker shows the scaffold and the `angular.json` edit but not the `package.json` one.
  - Behavior: Within a variant, the first fenced block remains the scaffold and every block after it is an edit block, each rendered with its own copy control. Label each edit block from the comment on its first line when one is present — `// angular.json → projects.<name>.architect.build.options` renders as that path above the block — falling back to `EDITS` when there is no leading comment. Variants with one edit block (react, vue) render exactly as they do now. The scaffold block keeps its `SCAFFOLD` label.
  - Implementation notes: The variant parser currently takes fenced blocks positionally; change it to collect all blocks and treat index 0 as the scaffold, the remainder as edits, so no per-variant block count is assumed. As of writing: angular has 3 blocks, react 2, vue 2, and the maui section has 1 with no variants — the single-block, no-variant case must keep rendering as it does today. Read the label from the block's first line only when that line is a `//` comment, and strip it from neither the displayed code nor the copied text — the comment names the file and target path, so it is content the user needs when pasting. Do not add a copy-all control that concatenates blocks; they go to different files and concatenating them is the failure this entry exists to prevent.
  - Out of scope: rendering blocks outside variants differently; syntax highlighting in blocks; the full-document view; validating that a block's declared path exists.
  - File: toDoList_main/src/repoSetup.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: cf482413-b678-44c1-bd90-667a3dfab372 -->

- [x] **[LOW]** Repo setup picker: read block labels from the File marker — Completed: 2026-08-06
  - Type: bug
  - Description: The picker labels each fenced block from a `//` comment on the block's first line, and deliberately keeps that comment in both the rendered code and the copied text. That made every copy dirty — copying the `angular.json` edit yielded a comment naming the file that then had to be deleted, and the path appeared twice on screen, once as the caption and once inside the block. `SHAPES.md` now puts the path in a `**File:** \`path\` → \`location\`` line immediately before the fence, or `**Scaffold**` for the scaffold block, and the fences contain only what should be pasted.
  - Behavior: A fenced block's label comes from the marker line directly above it — `**File:**` renders as that line's content, `**Scaffold**` renders as `SCAFFOLD`. A block with no marker above it falls back to `SCAFFOLD` when it is the first block in the variant and `EDITS` otherwise. Copy writes the fence contents verbatim with no label and no marker line. Marker lines are not rendered as body prose; they appear only as block captions.
  - Implementation notes: As of writing, angular has three labelled blocks, react and vue two each, and the maui section has one unlabelled block with no variants — that last case must keep rendering as it does today. Match the marker as a whole line of the form `**File:** ...` or `**Scaffold**` immediately preceding a fence, allowing one blank line between; anything else is body prose. Render the marker's inline code spans as code, since the labels contain backticked paths. The previous behavior of reading the first `//` line as a label should be removed entirely rather than kept as a fallback — no block in the file has one now, and leaving both paths in invites the fenced comments coming back.
  - Out of scope: syntax highlighting; validating that a labelled path exists; a copy-all control; the full-document view.
  - File: toDoList_main/src/repoSetup.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: f5c3ab7e-ff60-4e8b-b9a4-aaa2d82c7050 -->

- [x] **[LOW]** Worker error messages are discarded in favour of the bare status — Completed: 2026-08-06
  - Type: bug
  - Description: When the TODO.md read failed for a freshly onboarded repo, the viewer showed `Couldn't load TODO.md — HTTP 400`. The Worker's response body carried `{"error":"Target not in allowlist","repo":...}`, which names the cause exactly — the registry cache had not yet picked up the new `inject_targets` row. Discarding it turned a self-explanatory 60-second cache window into an opaque failure. Every Worker route returns a JSON body with an `error` field on failure, so this loses real diagnostic information on every non-2xx response, not just this one.
  - Behavior: When a Worker call fails, parse the response body and surface its `error` field in the message shown to the user, keeping the status as secondary detail — `Couldn't load TODO.md — Target not in allowlist (400)`. When the body is absent, unparseable, or has no `error` field, fall back to the current bare-status message so a truly opaque failure still reports something. Applies wherever a Worker response's status is currently checked without reading the body.
  - Implementation notes: Grep `inject.js` for `res.ok`, `HTTP ' +`, and `status` to find the call sites — the fetch wrapper, if there is a single one, is the right place to do this once rather than at each caller. The Worker's shape is consistent: `json({ error, ...context }, status)` from `http.js`, so `error` is a string on every failure path. Some routes add useful context alongside it (`repo`, `filePath`, `detail`); include `detail` when present, truncated, since the 502 paths carry upstream API text there. Do not surface the raw body when parsing fails — it may be an HTML error page from Cloudflare rather than the Worker, and dumping that into the UI is worse than the status alone. Reading the body consumes the stream, so clone or read once and reuse if any caller already reads it on the failure path.
  - Out of scope: retry logic; changing any Worker response shape; a global error toast; surfacing errors from non-Worker fetches.
  - File: toDoList_main/src/inject.js
  - Completed:
  <!-- id: 48d037cf-b18d-4096-8dbe-2f5076eb352d -->

- [x] **[MEDIUM]** Row-level drift check hardcodes personal purpose — Completed: 2026-08-06
  - Type: bug
  - Description: The inject target row's drift check calls `preflightRepo` with a hardcoded `'personal'` because `inject_targets` had no purpose to read. Purpose determines whether the five assignment-purpose files — `assignment.md`, `.claude/derive.md`, `claude-derive.yml`, `.claude/style.md`, `.claude/commenting-style.md` — belong in the expected set, so on an assignment repo the row check undercounts missing files by exactly five and reports drift as clean when it is not. Every WGU coursework repo is assignment, which is most of what the check is for. `inject_targets` now carries a `purpose` column and `handleRepos` serializes it, so the value is available on the registry row.
  - Behavior: The row-level drift check sends the target's own purpose instead of the hardcoded string, falling back to `'personal'` when the field is absent so rows predating the column behave as they do today. A drift check on an assignment repo then reports the full expected set, and its count matches what the onboard modal's Check reports for the same repo with Assignment selected.
  - Implementation notes: The hardcoded `'personal'` is in the row check's `preflightRepo` call in `inject.js` (~line 2899, alongside `target.shape || 'auto'`); change it to `target.purpose || 'personal'`, matching the shape fallback's shape. `handleRepos` now returns `purpose` on each repo entry — confirm the client's target objects carry it through from that response rather than from a separate source, since the workspace list and the inject target rows may be built from different places. Remove the note in the row's verdict, if one was added, saying the count assumes a personal repo — it is no longer true. Do not change the onboard modal's Check, which asks the user directly and is already correct.
  - Out of scope: backfilling purpose on existing rows (done by hand in SQL); editing purpose from the app; changing what the five assignment files are; the onboard modal.
  - File: toDoList_main/src/inject.js
  - Completed:
  <!-- id: 1234b678-6e2c-40f3-8e16-7c85392035ec -->

- [x] **[MEDIUM]** Fix black bar rendering above the TODO.md viewer header on mobile — Completed: 2026-08-06
  - Type: bug
  - Description: On mobile (`< 1024px`), the TODO.md viewer renders a full-width dark band directly above the rounded "TODO.md" header card, which does not appear on desktop and is not part of the header card's own styling. Diagnose whether this is an empty/zero-content wrapper element above the header, a sticky-header or scroll-container element painting a darker background token (e.g. `--bg-base` over `--bg-surface`), or leftover padding/margin plus a background on the viewer's outer container; then remove or make transparent whichever element is painting it so the area above the header card matches the surrounding page background with no visible band. The header card itself — its rounded corners, "TODO.md" label, and the run / ship / refresh / overflow buttons — must render unchanged, and the desktop layout must not regress. Likely locations are the viewer's container markup in `toDoList_main/src/todoMdViewer.js` and its mobile breakpoint rules in `toDoList_main/src/style.css`.
  - File: `toDoList_main/src/todoMdViewer.js`, `toDoList_main/src/style.css`
  - Completed:
  <!-- id: fc5b8547-8443-4520-8be6-72338a748964 -->

- [x] **[MEDIUM]** Make the mobile TODO.md launcher read as a card instead of a flat band — Completed: 2026-08-06
  - Type: bug
  - Description: Follow-up to the earlier black-bar fix, which repainted `#mainBar` / `#mainList` / `#taskFilterBar` to `--bg-elevated` on mobile. That removed the `--bg-base` seam but left a new one: `.todoMdViewerHeader` also paints `background: var(--bg-elevated)`, and on the collapsed in-list launcher (`#mainList > #todoMdViewerCard`, `height: var(--item-h)`, body hidden) the header is effectively the whole card — so the launcher now paints the exact same `#14151b` as the canvas behind it and only shows as two ~3px slivers of `--bg-surface` plus a 1px `--border-dim` edge. Against the todo rows it reads as a flat dark band rather than a card. Inside the existing `@media (max-width: 1023px)` block, in the same rule group that already scopes `#mainList > #todoMdViewerCard .todoMdViewerHeader` (drops its vertical padding and bottom border), also set that header's `background` to `transparent` so the launcher paints one contiguous `var(--bg-surface)` from its own card background, and raise the card's border from `var(--border-dim)` to `var(--border-mid)` so the edge is legible against the elevated canvas. Scope both to `#mainList > #todoMdViewerCard` only — the full-screen mobile sheet (`#todoMdViewerMobileSheet .todoMdViewerCard`), the desktop pane-hosted card (`#descDetailPane .todoMdViewerCard`), and the desktop rail strip must all render unchanged, and the expanded in-list card must keep its header/body divider. Do not change the `--bg-elevated` repaint of `#mainBar` / `#mainList` / `#taskFilterBar`, the launcher's `height` / `margin` / `max-height` sizing, the `::before` "TODO.md" label, or the icon-only run / deploy / sync / overflow buttons.
  - File: `toDoList_main/src/style.css`
  - Completed:
  <!-- id: 81bbafeb-80d3-414e-a5b1-87164260db93 -->

- [x] **[LOW]** Match the mobile TODO.md launcher fill to the task rows — Completed: 2026-08-06
  - Type: bug
  - Description: Follow-up to the launcher-surface fix. The collapsed in-list launcher now paints a single contiguous fill, but it uses the base `.todoMdViewerCard` background `var(--bg-surface)` (`#191a22`), while the todo rows beside it use `var(--bg-row)`, which the mobile block re-declares to `#20222e` for the interactive-layer contrast pass. The launcher therefore sits a step darker than the rows it lines up with. In the `@media (max-width: 1023px)` block, in the existing `#mainList > #todoMdViewerCard` rule that already sets the launcher's `height` / `margin` / `max-height` / border, add `background: var(--bg-row)` so the launcher reads on the same interactive layer as the rows and picks up the mobile re-declaration automatically. Keep the `:active` feedback rule (`#mainList .todoMdViewerCard:active { background: var(--bg-hover) }`) working — `--bg-hover` is also re-declared at this breakpoint, so the pressed state should still read as a lift rather than a drop; verify it does and adjust only if it no longer contrasts. Scope the change to `#mainList > #todoMdViewerCard` so the expanded in-list card, the full-screen sheet (`#todoMdViewerMobileSheet .todoMdViewerCard`), the desktop pane-hosted card (`#descDetailPane .todoMdViewerCard`), and the desktop rail strip keep `--bg-surface` unchanged. Do not alter the launcher's transparent header, border, sizing, `::before` label, or its icon-only run / deploy / sync / overflow buttons.
  - File: `toDoList_main/src/style.css`
  - Completed:
  <!-- id: f6fd63eb-53b1-4184-8044-614b80fb3731 -->

- [x] **[LOW]** Darken the mobile TODO.md launcher and add a chevron affordance — Completed: 2026-08-06
  - Type: bug
  - Description: Two follow-ups to the launcher fill change. First, `var(--bg-row)` puts the launcher on the same layer as the task rows, which reads muddy in practice because the rows the user usually sees are `todo-row--in_progress` (painted `var(--bg-hover)`, `#262838`) rather than plain `--bg-row` — so the launcher lands in an awkward near-match. In the `@media (max-width: 1023px)` block, change the `#mainList > #todoMdViewerCard` background from `var(--bg-row)` back to `var(--bg-surface)` so it sits one clear step below the row layer while staying lifted off the `--bg-elevated` canvas; keep the `var(--border-mid)` border added earlier. Second, the launcher does not read as tappable even though the whole card opens the bottom sheet. Add a chevron after the `TODO.md` label: the existing `#mainList > #todoMdViewerCard .todoMdViewerHeader::before` rule supplies the label text, and `::after` is currently unused on that header, so add a `::after` carrying a right-pointing chevron glyph at ~14px in `#7a74a8` (the muted purple-neutral `#completedHeader` already uses) with a small left gap. Because the header is `display: flex` and the visible child is `.todoMdViewerMeta` (which holds the right-aligned run / deploy / sync / overflow buttons), a default `::after` would render after that group at the far right — so pin the order explicitly, e.g. `order: -2` on `::before` and `order: -1` on `::after`, leaving `.todoMdViewerMeta` at its default so it stays right-aligned via the header's existing spacing. Do not use `content` string concatenation on `::before` for the chevron, so its color and size stay independent of the label. Scope both changes to `#mainList > #todoMdViewerCard` — the expanded in-list card, the full-screen sheet (`#todoMdViewerMobileSheet .todoMdViewerCard`), the desktop pane-hosted card (`#descDetailPane .todoMdViewerCard`), and the desktop rail strip must render unchanged, and no chevron may appear on any of them. Keep the launcher's transparent header, `height` / `margin` / `max-height` sizing, the `:active` press feedback, and the icon-only button set as they are; the chevron is decorative, so it must not become a separate tap target or be announced to screen readers.
  - File: `toDoList_main/src/style.css`
  - Completed:
  <!-- id: b6cf8d1a-c003-4344-ac59-1bf84e58574f -->

- [x] **[MEDIUM]** Repo setup picker shows only the first command block on variant-less sections — Completed: 2026-08-06
  - Type: bug
  - Description: `parseShapesDoc` computes `copyValue = firstFencedBlock(lines)` for a CLI section with no `### ` variants, so such a section renders exactly one copyable block and drops the rest. That was correct when a variant-less CLI section carried a single scaffold command, but `SHAPES.md`'s maui section is now a seven-step sequence — install the workload, scaffold, create the solution, add a class library, add the test project, ignore build output, commit — each with its own `**N · Label**` caption and fenced block. The picker shows step 1's command and silently discards steps 2 through 7, so the on-device reference is missing most of the setup it exists to document. `parseVariantBody` already parses exactly this shape; it is simply never called for a section without variants.
  - Behavior: A CLI section with no `### ` variants renders its body as an ordered sequence of captioned blocks and prose, the same way a variant body renders today — every fenced block gets its caption from the `**N · Label**` line above it and its own copy control. Sections whose body holds a single unlabelled block keep rendering as one plain copyable value, so nothing regresses for a section that never had steps. Template sections are unaffected: their `copyValue` is the template repo name and they have no command sequence. The `**Onboarding adds:**` chips and `**Gotchas**` list keep rendering above and below the body as they do now.
  - Implementation notes: The cleanest shape is to treat a variant-less section as one implicit variant — run its lines through `parseVariantBody` and store the result as `parts` on the section, rather than duplicating the block/caption walk. `parseVariantBody` must not absorb the `**Gotchas**` list or the `**Onboarding adds:**` line into the trailing block; the variant path already guards this by bounding the variant region, so the section path needs the same bound applied against the first of those markers. As of writing: maui has 7 captioned blocks and no variants, build-pipeline has 3 variants (angular 4 blocks, react 2, vue 2), and every template section has none — those three cases must all render correctly. Keep `copyValue` for the single-unlabelled-block case so the existing render path for simple sections is untouched. Fenced-block text must stay verbatim for copy, including leading whitespace in the jsonc blocks.
  - Out of scope: the full-document view; syntax highlighting; changing `SHAPES.md`; a copy-all control; rendering `####` or deeper headings.
  - File: toDoList_main/src/repoSetup.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 51398b4c-b5e9-4f16-ae43-8ea72b8b6701 -->

- [x] **[MEDIUM]** Preflight spinner never resolves on mobile — Completed: 2026-08-06
  - Type: bug
  - Description: Running Check from the phone leaves the spinner running indefinitely; the same check on desktop settles in a few seconds. Both surfaces subscribe with `subscribeRunOutputs` and then wait for a terminal row, with no read of the row and no timeout, so a single missed realtime event hangs forever with no error. Realtime rides a WebSocket, and iOS Safari drops those when the tab backgrounds, the screen locks, or the network transitions between cellular and wifi — without reconnecting. Desktop holds the socket for the few seconds a preflight takes and receives the event; mobile frequently does not. The row itself settles correctly in `run_outputs` either way, so the result exists and is simply never read.
  - Behavior: After `preflightRepo` resolves, read the `run_outputs` row for that correlation id once directly. If it is already terminal, render the verdict and dispose the channel. If it is still `running`, keep waiting on realtime as today. Start a give-up timer when the dispatch resolves; on expiry read the row once more — render it if terminal, and if it is still `running` or absent, render the existing error state with a message distinguishing the two (the run may still be in flight, versus nothing was recorded) and offer a retry that re-reads rather than re-dispatching. Additionally, re-read the row when the document becomes visible again, since returning to the tab is the moment a backgrounded socket is known to have missed events. Both terminal paths dispose the channel. Applies identically to the onboard modal's Check and the inject target row's drift check.
  - Implementation notes: Add a read helper alongside `preflightRepo` in `inject.js` that selects the `run_outputs` row by correlation id and returns it or null; `extractPreflightReport` already parses a terminal row's `stdout`, so reuse it. `renderPreflightError` already exists — pass it the distinguishing reason rather than adding a surface. Both call sites already track their channel and have disposal paths; route the new terminal conditions through those so a timeout cannot leak a channel. The row check tracks channels per row in a set, so its timer must be per row. Guard against double-render: the immediate read, a visibilitychange re-read, and a realtime event can all deliver the same terminal row — keep a settled flag per correlation id and ignore repeats. Do not re-dispatch on retry; a second run against the same correlation id 409s on the unique constraint. Remove the `visibilitychange` listener on dispose, or a closed modal keeps re-reading.
  - Out of scope: reconnecting the realtime socket; changing `subscribeRunOutputs`; the capture card; interval polling.
  - File: toDoList_main/src/inject.js
  - Completed:
  <!-- id: 60b18433-68fe-49ea-8bdf-206d2ffe4fb4 -->

- [x] **[LOW]** View code action for UI region rows — Completed: 2026-08-07
  - Type: feature
  - Description: Published UI region rows carry `region.file` and `region.line` from the manifest, and `selectHandle` already receives both — the shared action toolbar even renders "Line N." as context. But nothing acts on that number. The Types lens rows already jump into the code viewer from the same line data; regions have the same shape and no equivalent. This was deferred earlier out of a concern that a region row's click already means "select", which drives the toolbar. Putting the jump in the toolbar rather than on the row resolves that: selection keeps its meaning and the jump becomes an explicit second action.
  - Behavior: Add a `View code` action to the shared handle toolbar, beside the existing `Reference in chat` and `Copy selector` actions, as a secondary control. It appears only when the selected handle carries a usable `file` and a `line` greater than zero — a handle without them keeps today's toolbar exactly. Clicking opens that file in the code viewer scrolled to the line, highlighted under the span banner naming the region by its label. The banner's dismiss clears the highlight and leaves the file open. On desktop the viewer opens in the detail column; below 1024px in the sheet. The row stays selected and the toolbar stays visible, so a second region can be inspected without reselecting.
  - Implementation notes: `openFileInCodeViewer(repo, filePath, highlight)` at ~line 847 is the shared opener and already takes `{ startLine, endLine, banner }` — pass `startLine` and `endLine` both as the region's line, since a region is a point rather than a span, and let the viewer render a single highlighted line. Add the action inside `appendReferenceCopyActions` (~line 1265) or a sibling helper, so live and published rows get identical treatment; live regions measured from the running DOM have no `file`/`line`, so the guard naturally hides it there. The toolbar already computes the "Line N." context string at ~line 1250 with the same `typeof d.line === 'number' && d.line > 0` test — reuse that condition rather than writing a second one, so the label and the action can never disagree. Do not change what clicking the row does. New styling as classes in `style.css` alongside the existing toolbar action rules; the action's hidden state needs an explicit `[hidden] { display: none !important }` guard.
  - Out of scope: live region rows gaining file/line (they are DOM-measured, not manifest-derived); jumping to a selector's definition in CSS rather than its region's source line; changing the Types lens jump; any change to the manifest generator.
  - File: toDoList_main/src/structureView.js, toDoList_main/src/style.css
  - Completed:
  <!-- id: 07f97165-ab4d-4790-b94b-ddba26da334b -->

- [x] **[HIGH]** Chat wipe does not replicate across devices — Completed: 2026-08-07
  - Type: bug
  - Description: Clearing a chat on desktop leaves the thread intact on mobile. The delete does reach Supabase — `clearChatTurns` removes the repo's `chat_turns` rows — but the receiving device never acts on their absence. Two causes compound. First, `hydrateChatTurnsFromRemote` bails at `if (!rows.length) return;` (~line 924), so a successful fetch returning zero rows — precisely what a wipe produces — is discarded as if it were a failed read. The surrounding comment correctly justifies bailing on `ok:false`, but zero rows on `ok:true` is the opposite: positive proof the thread is empty. Second, even past that guard the merge is a pure union — `chatHistory.forEach(put)` then `rows.forEach(put)` — so a local turn absent from the remote set is kept, because nothing distinguishes "deleted on another device" from "not yet synced up". The merged thread is then re-saved to localStorage, so the wiped turns persist indefinitely.
  - Behavior: A successful fetch returning zero rows clears the thread on the receiving device rather than returning early. More generally, a local turn that was previously persisted and is now absent from the remote set is dropped, while a local turn that has never been persisted is kept. After a wipe on one device, opening the chat on another shows an empty thread and the New Chat pill hides itself, matching the wiping device. A turn sent moments ago on the receiving device, whose insert has not yet completed, survives the hydrate exactly as it does today. A failed or offline fetch still returns without touching anything.
  - Implementation notes: `created_at` is the discriminator and is already reliable — `appendChatTurn` (~line 840) stamps only `id` and a client `ts`, never `created_at`, and the merge is the sole place it is assigned, from `row.created_at`. So a local turn carrying `created_at` came from a row and its absence from a fresh fetch means it was deleted remotely; a turn without one has never round-tripped and must be preserved. Replace the union's unconditional `chatHistory.forEach(put)` with a filter on that rule, and remove the `!rows.length` early return so the empty case runs the same path. Keep both existing guards immediately after — the `repo !== activeChatRepo` and `chatTurnInFlight` bails are load-bearing, since `replayChatHistory` opens with `surface.innerHTML = ''` and would paint the wrong repo's thread or destroy a mid-stream bubble. Keep the images carry-over on conflict. Call `syncClearChatVisibility()` after the merge so the New Chat pill disappears when the thread empties, and make sure the emptied thread is written through `saveChatHistory` so a reload does not restore it. Note `clearChatTurns` in `listLogic.js` (~line 1569) deletes by `repo` with no `user_id` filter — out of scope here, but worth checking that RLS on `chat_turns` scopes the delete, or one user's clear could reach another's rows in a multi-tenant future.
  - Out of scope: realtime for `chat_turns` (the sync is fetch-on-open by design); a tombstone table; changing `clearChatTurns`; the prune path.
  - File: toDoList_main/src/claudeSheet.js
  - Completed:
  <!-- id: 486bbe23-bf1b-4a84-a447-fed62d7a4104 -->

- [x] **[MEDIUM]** Backlog runs show a generic title instead of the task row's name — Completed: 2026-08-07
  - Type: bug
  - Description: Dispatching Run backlog from the TODO.md viewer card creates a Runs-tab row titled `Backlog run` for every project, so several concurrent runs are indistinguishable. The hardcoded title at `todoMdViewer.js:2074` is justified by a comment reasoning that a backlog dispatch names no entry — true, since the routine picks the task itself — but that answers a different question. The row does not need the entry's name; it needs the task row's name, and `projectName` is already in scope and already passed to the same `trackDispatchedRun` call as `project`. The per-entry Run button gets this right via `runEntryTitle`, and the chat ship path via `deriveRunTitle`; only the backlog path discards an identity it holds.
  - Behavior: A backlog dispatch titles its Runs-tab row with the task row's name, falling back to `Backlog run` only when that name is empty. The row shows its live status beside the title as it already does for other dispatches — `QUEUED` on dispatch, then running, then terminal. Once the run lands and the TODO.md diff reveals which entry was checked off, the row still swaps to that entry's real title, exactly as it does now; the task row's name is the label before that resolves, not instead of it.
  - Implementation notes: Use `projectName` from the enclosing scope at ~line 2074, the same value handed to `trackDispatchedRun` as `project` two lines below, trimmed and guarded so an empty name falls back to the existing literal. Do not read it from `card.dataset.projectName` — the closure value is what the rest of the dispatch already uses and cannot drift from the run being started. Leave `todoSnapshot` and the post-run diff rename untouched; that path resolves the entry title later and must keep overriding this one. Update the comment above the call: it currently explains why the label is generic, which will no longer be true, and its reasoning about the entry being unknown belongs with the `todoSnapshot` line beneath it.
  - Out of scope: the per-entry Run button and the chat ship path (both already title correctly); the diff-based rename after a run lands; the status pill on the viewer card; any change to dispatch mode.
  - File: toDoList_main/src/todoMdViewer.js
  - Completed:
  <!-- id: 249774a8-4f02-4b8a-9466-257e2d190e40 -->

- [x] **[MEDIUM]** Runs tab shows oldest-first on the device that dispatched the runs — Completed: 2026-08-07
  - Type: bug
  - Description: The Runs tab lists newest-first on one device and oldest-first on another for the same project. `getDisplayRunRecords` picks between two orderings at ~line 4251: when queue or shipped-spine records are present it sorts everything `byDispatchedDesc`, but when only local records exist it returns `runRecords` unsorted via an early return, commented as keeping its "existing newest-first insertion order". That comment is wrong — `runRecords` is appended to on dispatch, so its insertion order is oldest-first. Because `runRecords` lives in per-device localStorage, the device a run was dispatched from has local records and takes the unsorted path, while any other device has none and takes the sorted one. The split is by dispatch origin, not by breakpoint — it will flip if the runs are started from the phone instead.
  - Behavior: The Runs tab lists newest-first on every device and in every combination of sources. A local-only list orders by dispatch time descending like any other, so a run started moments ago appears at the top rather than beneath older completed rows. The Clear-completed affordance stays pinned beneath the last row.
  - Implementation notes: Delete the early return at ~line 4251 and let the single sorted path handle every case — `byDispatchedDesc` already exists and the concat is a no-op when the other two arrays are empty, so this is a removal rather than a new branch. Two orderings selected by which sources happen to be loaded is the bug's actual shape; leaving the branch and sorting inside it would preserve the hazard. Correct the comment rather than deleting it: the useful fact is that `runRecords` is append-on-dispatch and therefore oldest-first, which is the opposite of what it currently claims. Verify `byDispatchedDesc` handles a record with no `dispatchedAt` — shipped-spine records are built from a different source and a missing key must not throw or scatter rows to one end.
  - Out of scope: `buildQueueRunRecords`, `buildShippedEntryRecords`, and `pruneMatchedLocalRecords`; the Clear-completed control; any change to what a run row displays.
  - File: toDoList_main/src/claudeSheet.js
  - Completed:
  <!-- id: 16626c80-8425-44c0-b6f3-22843772a059 -->

- [x] **[MEDIUM]** Fix "couldn't link this task to its entry" error when accepting a proposal races a re-hydrate — Completed: 2026-08-07
  - Type: bug
  - Description: Accepting a proposal in the Agent board's Proposed bucket (buildProposedSecondary in toDoList_main/src/agentView.js) calls dispatchDraft (toDoList_main/src/dispatchDraft.js), which materializes a brand-new todo via materializeEntryTodo/listLogic.addEntryTodo before shipping. The new todo is pushed into allProjects[project].items optimistically and its INSERT is fired fire-and-forget through persistMutation; shipEntryForTodo (toDoList_main/src/shipEntry.js) then calls listLogic.stampTodoEntryId(todoId, entryId), a synchronous in-memory scan of allProjects that returns { ok:false, error:'Todo not found.' } if the id is missing, which surfaces as the "Run dispatched, but couldn't link this task to its entry" toast. If listLogic.hydrateFromSupabase() (toDoList_main/src/listLogic.js, ~line 3609) runs in that window — via the visibilitychange re-hydrate or the 5-minute backstop interval wired in toDoList_main/src/index.js — it rebuilds allProjects[name].items for an EXISTING project purely from the latest Supabase SELECT (~line 3737), which does not yet include the unconfirmed INSERT. Unlike brand-new local-only projects, whose items are explicitly preserved via local.items.slice() (~line 3808), a freshly materialized todo under an already-synced project has no equivalent preservation, so it is silently dropped from local state and the later stampTodoEntryId lookup fails even though the run itself dispatched successfully.
  - Behavior: Expected — accepting a proposal should never lose the freshly created todo out from under itself, even if a re-hydrate lands mid-flight; the "couldn't link" toast should only fire for a genuine failure, not this race.
  - Implementation notes: In hydrateFromSupabase's per-project merge (toDoList_main/src/listLogic.js, the remoteProjects.forEach block around line 3698), carry over any local todo item that is not yet present in remoteTodos for that project id but was created in the current session (e.g. track pending-insert ids, or fold in local-only items the same way the local-only-project branch already does), rather than dropping it silently.
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/src/dispatchDraft.js`, `toDoList_main/src/shipEntry.js`
  <!-- id: c4c93b3e-3f98-4151-bc11-a3357df5f89f -->

- [x] **[MEDIUM]** Add a visible spinner to the Derive button while a derive run is in flight — Completed: 2026-08-07
  - Type: bug
  - Description: The Derive action in the coverage pane only flips its label to "Deriving…" and disables itself while isDeriveActive() is true, with no visible spinner glyph, so an in-flight claude-derive.yml run doesn't clearly read as "working." Add a small 12px spinning ring (reusing the shared spin keyframes and #9D93EE accent already used by .projRunSpinner) inline before the label text, centered in the button, shown whenever isDeriveActive() is true and hidden otherwise. Update buildDeriveAction and fireDeriveFromPane in assignmentCoverage.js to render/remove the spinner span alongside the existing label/disabled toggling, and add the spinner styling (reusing the existing spin keyframes) to style.css near the .claudeCoverageDerive rules.
  - File: `toDoList_main/src/assignmentCoverage.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 5ab7dccc-7723-4037-b167-44893e03d9a1 -->

- [x] **[MEDIUM]** Fix extra bottom gap under the Stream/Structure tab bar in iOS standalone PWA mode — Completed: 2026-08-07
  - Type: bug
  - Description: When the app is launched from an iOS "Add to Home Screen" icon (`display-mode: standalone`), the bottom tab bar (STREAM / STRUCTURE) sits noticeably higher than it does in Safari, leaving a dead black band below it; in Safari the same bar renders correctly. The cause is a double-counted bottom inset — the tab bar's fixed bottom padding is being added on top of `env(safe-area-inset-bottom)` (and/or a `100vh` container that doesn't account for `viewport-fit=cover`), which Safari's own chrome already absorbs. Fix so the standalone rendering matches the Safari rendering exactly: audit the bottom-nav padding/height rules and any `env(safe-area-inset-bottom)` usage in `toDoList_main/src/style.css`, the viewport meta / `viewport-fit=cover` handling in `toDoList_main/src/index.js` and `toDoList_main/src/viewport.js`, and prefer `100dvh` over `100vh` for the app shell. Acceptance criteria: (a) in standalone mode the tab bar sits flush at the bottom with no extra gap, visually identical to Safari; (b) Safari rendering is unchanged and the labels are never clipped by the home indicator; (c) the floating assistant FAB and the TODO.md footer row keep their current spacing relative to the tab bar in both modes; (d) desktop (>= 1024px) layout is untouched.
  - File: `toDoList_main/src/style.css`, `toDoList_main/src/viewport.js`, `toDoList_main/src/index.js`
  - Completed:
  <!-- id: 7b30a9b3-7597-4186-989f-adff6a1f7dff -->

- [x] **[MEDIUM]** Remove the safe-area color band below the Stream/Structure tab bar in iOS standalone mode — Completed: 2026-08-07
  - Type: bug
  - Description: In the installed iOS PWA (`display-mode: standalone`) a solid band renders below the STREAM/STRUCTURE tab bar, pushing the tabs up off the bottom edge; Safari renders correctly. The band is painted in the same elevated/sheet surface color as the Claude sheet tab, so it is a container whose background bleeds through its own `env(safe-area-inset-bottom)` padding — NOT the previously-assumed double padding on the tab bar itself, and NOT page background showing beneath a short `100vh` shell (a prior entry already changed the tab bar's own padding and the band persisted, so do not repeat that fix). Start by grepping the whole of `toDoList_main/src/` for `safe-area-inset-bottom`, `viewport-fit`, and `display-mode: standalone` to enumerate every element that applies a bottom inset, including inline paddings set from JS, then identify which one paints the band and stop it from adding inset on top of the layout the tab bar already reserves. Acceptance criteria: (a) in standalone the tab bar sits at the same visual offset it has in Safari with no colored band beneath it; (b) tab labels are never overlapped or clipped by the iOS home indicator; (c) Safari rendering is byte-for-byte unchanged; (d) the Claude sheet's own bottom chrome and the composer row keep their current safe-area handling and are not clipped in standalone; (e) desktop (>= 1024px) layout is untouched.
  - File: `toDoList_main/src/style.css`, `toDoList_main/src/viewSwitcher.js`, `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/viewport.js`
  - Completed:
  <!-- id: 27deda97-77c1-4af4-8a7e-9f2444b01b2f -->

- [x] **[MEDIUM]** Stamp `shippedAt` for entries that ship without an `agent_queue` row so Runs-tab marker records sort by real ship time — Completed: 2026-08-08
  - Type: bug
  - Description: The Runs tab's unified sort (`getDisplayRunRecords` → `sort(byDispatchedDesc)`, `toDoList_main/src/claudeSheet.js` ~line 4258) orders every source by `dispatchedAt`. For shipped-entry marker records (`buildShippedEntryRecords`, ~line 4100), `dispatchedAt` is `parseIsoMs(item.shippedAt)`, but `item.shippedAt` is only ever stamped by `listLogic.js`'s `stampEntryShipped` (~line 1917), which is called exclusively from `agentQueueStore.js`'s `settleShipped` (~line 609) and `reconcileShippedStamps` (~line 276) — both gated on an actual `agent_queue` row reaching state `shipped`. Entries shipped via the TODO.md viewer's "Run backlog" button or a per-entry "Run this entry" pill never create an `agent_queue` row (per the comment at `claudeSheet.js` ~line 4085), so `shippedAt` is never stamped for them unless `trackDispatchedRun` (`claudeSheet.js` ~line 708) happened to track that exact dispatch locally in the same browser session that shipped it. Any shipped entry that reaches the Runs tab only via the marker-cache fallback (a cross-device ship, or a same-device ship from a session where the sheet wasn't tracking it) keeps `item.shippedAt` unset, so `dispatchedAt` resolves to 0 and the entry always sorts to the very bottom — regardless of how recently it actually shipped. This is why the newest-first ordering fix already in place (the `byDispatchedDesc` sort, well-commented in `getDisplayRunRecords`) does not fully hold: it correctly orders queue-derived and locally-tracked records, but the marker-only fallback path it was partly designed to cover never gets a real timestamp. Fix by stamping `shippedAt` the first time a todo's `entryId` is observed in the shipped marker set, not only when an `agent_queue` row settles — e.g. in `inject.js`'s `refreshShippedMarkers` (~line 681), diff the newly-fetched `shipped` set against the previous cache entry and call a new entryId-keyed stamp helper in `listLogic.js` (`stampEntryShipped` is keyed by `todoId`, so a matching entryId-keyed variant or lookup is needed) for every entryId newly seen as shipped.
  - File: `toDoList_main/src/inject.js`, `toDoList_main/src/listLogic.js`, `toDoList_main/src/claudeSheet.js`
  <!-- id: 03dc34c2-ab66-46c2-9fe3-cc9b9f22fab4 -->

- [x] **[MEDIUM]** Fix task-row and description-panel dispatches to appear in the chat sheet's Runs tab — Completed: 2026-08-08
  - Type: bug
  - Description: Entries dispatched via the shared `dispatchDraft()` core in `toDoList_main/src/dispatchDraft.js` (used by the Agent board's Dispatch/Retry in `agentView.js` and the todo description panel's Dispatch/Retry block in `toDoList_main/src/toDoRow.js` around `buildDispatchBlock`/line 987) never register a run record, so they never show a QUEUED/RUNNING pill under the chat sheet's Runs tab while in flight. The other two dispatch surfaces already do this correctly: `todoMdViewer.js`'s "Run backlog" and "Run this entry" buttons call `trackDispatchedRun()` (exported from `claudeSheet.js`, lines 708-740) right after a successful `dispatchRun()`, and chat's own "Inject & run" path builds its run record inline in `claudeSheet.js` (~line 3837). `dispatchDraft()` (`toDoList_main/src/dispatchDraft.js:60-147`) should call `trackDispatchedRun()` after `shipEntryForTodo` succeeds (right after the `listLogic.setAgentRunState(rowId, patch)` call), passing `{ correlationId: res.correlationId, entryId: res.entryId, title: <row's context.title, already computed as `title` in the existing `!todoId` branch, or derived from `draftText` when the row already had a todo_id>, repo: target ? target.repo : null, project: getSelectedProjectName(), dispatchedAt: Date.now() }`, mirroring the shape used at `todoMdViewer.js:2161-2168`.
  - Behavior: A run dispatched from either the Agent board or a todo's description panel must show a QUEUED pill in the Runs tab immediately after dispatch, then progress RUNNING → SHIPPED/FAILED exactly like the two already-working paths, without creating a duplicate record if the same correlation id is tracked twice (`trackDispatchedRun` already dedups via `findRunRecord`).
  - Implementation notes: `claudeSheet.js` already statically imports `materializeEntryTodo` from `dispatchDraft.js` (line 64), so a static top-level `import { trackDispatchedRun } from './claudeSheet.js'` in `dispatchDraft.js` would form an import cycle. Use a dynamic `import('./claudeSheet.js')` inside `dispatchDraft()` at the point of use, mirroring the existing dynamic import of `./toDoRow.js` in this same file's `rebuildSelectedList` (lines 206-220), which was added for the identical cycle reason.
  - File: `toDoList_main/src/dispatchDraft.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 5496a964-4956-46f5-b045-bf2446508aa7 -->

- [x] **[MEDIUM]** Coverage tab reads project.md on personal repos — Completed: 2026-08-08
  - Type: feature
  - Description: The coverage tab is hardcoded to `assignment.md`, so a personal repo shows no card — `classifyAssignment` returns `absent` and the tab hides. Personal projects need the same surface fed by a `project.md` brief instead. Most of the rendering already supports it: `appendSection(null, items)` renders a group with a plain heading and no tally or bar, `buildAspectBadge` returns null for rows carrying no aspect, and `aspectSortKey` groups untagged proposals last. So a brief with no rubric IDs routes through existing paths — what is missing is reading the right file and accepting its shape.
  - Behavior: On a repo whose registry purpose is `personal`, the coverage tab reads `project.md` beside the routed TODO.md instead of `assignment.md`; on `assignment` it reads `assignment.md` exactly as today. A `project.md` classifies as `filled` when it holds any non-comment content beyond its heading — it has no `## Requirements` section and must not be judged against one — and as `unfilled` when only the seeded comment hints remain. The card summarises it the same way (title from the first non-comment line, word count, section count) but reports no aspects, so every proposal renders in the untallied group with no coverage fraction and no progress bar. User-visible copy on a personal repo says "brief" where it currently says "assignment". The editor opens and saves `project.md`, and its conflict text names that file. Nothing changes for assignment repos.
  - Implementation notes: `purpose` is not available client-side today. `handleRepos` now returns it per repo, and `onboardRepo`/`preflightRepo` in `inject.js` only ever send it outbound — so first thread it into the stored target descriptor that `findTargetById` returns, then read it in `resolveReadTargetFor`. Treat anything unrecognized as `personal`, matching the Worker's own normalization, so a stale cached descriptor cannot make a personal repo look for a file that will never exist. `classifyAssignment` (~line 271) and `describeAssignment` (~line 285) both key off `extractRequirementsSection`; branch on document kind rather than adding a second section name, since `project.md` has no fixed section contract and requiring one would make an ordinary brief classify as unfilled. `parseAspects` must return `[]` for a project brief — do not invent keys from its `##` headings; the untallied path depends on the absence of aspects and inventing them would produce a fraction with a meaningless denominator. Do NOT rename the module or its exports: `assignmentCoverage.js` is ~1,997 lines and `refreshAssignment`/`getAssignmentState`/`applyAssignmentSave` are called from several modules; a rename touches every call site for no behavioural gain. The Worker already authorizes and accepts writes to `project.md` — `siblingDocPaths` in `registry.js` and the basename guard in `todo.js` both cover it — so no Worker change is needed here.
  - Out of scope: the derive pass that produces project proposals (separate, and server-side); seeding `project.md` at onboard time; any coverage fraction or progress bar for personal repos; renaming the module.
  - File: toDoList_main/src/assignmentCoverage.js, toDoList_main/src/inject.js
  - Completed:
  <!-- id: 5af3675c-80df-418d-bd5b-5cbfcec63e54 -->

- [x] **[MEDIUM]** Coverage tab stays empty until the project is switched — Completed: 2026-08-08
  - Type: bug
  - Description: On first paint the Coverage tab shows no card; switching to another project and back makes it appear. `main.js:2904` calls `initInjectTargets()` without awaiting it, so the targets cache is often still empty when the chat pane mounts and calls `refreshAssignmentForActiveProject()` at `claudeSheet.js:5505`. With no targets loaded, `resolveReadTargetFor` returns null and `refreshAssignment` resolves synchronously to `absent` without a Worker call — but it still records `_assignmentProject` for that project. The double-fetch guard then sees the cache already belongs to the active project and no-ops every later call, so nothing ever re-reads. A project switch is the only thing that invalidates it. This is not specific to the brief path; an assignment repo races identically and has since the tab was built.
  - Behavior: The Coverage tab renders its card on first paint without a project switch, for both assignment and personal repos. A project whose target cannot yet be resolved does not settle as `absent` — it stays unresolved, so the next call retries rather than being suppressed by the double-fetch guard. Once the targets cache loads, the tab reads and paints on its own. A project that genuinely has no registry target still resolves to `absent` and renders no card, exactly as today.
  - Implementation notes: Two halves, and both are needed. First, in `refreshAssignment` (~line 314 region) distinguish "no target because the registry has not loaded" from "no target because this project has none" — only record `_assignmentProject` in the second case. The registry-not-loaded signal is the targets cache being empty rather than the lookup missing; `inject.js` already exposes enough to tell those apart, so read that rather than inferring from a null descriptor. Second, have the targets warm-up notify the coverage module when it completes: `initInjectTargets` at `inject.js:1487` already awaits `ensureRepoPurposes()` then `loadInjectTargets()` and then calls `refreshAllInjectButtons()`, which dispatches `mainListRendered` — either extend that path or add an explicit refresh call, so a tab that mounted early repaints. Do not fix this by awaiting `initInjectTargets()` at `main.js:2904`; that serialises app boot behind a network call for a surface that should tolerate arriving late. Keep the double-fetch guard — it exists to stop the board and the pane both reading, and the bug is that an unresolved state was allowed to satisfy it, not that the guard is wrong.
  - Out of scope: `ensureRepoPurposes` and the warm-up ordering (both correct); the classification logic; the card's rendering; any change to how `purpose` is stamped.
  - File: toDoList_main/src/assignmentCoverage.js, toDoList_main/src/inject.js
  - Completed:
  <!-- id: d69aff6e-34a4-435e-ac10-ca5ae4af1c17 -->

- [x] **[MEDIUM]** Untagged proposals render in arbitrary order — Completed: 2026-08-08
  - Type: bug
  - Description: Derive emits project proposals foundation-first — data layer, then the surfaces that read it, then the PWA shell — and its closing summary tells the user to accept them top to bottom for a sane build order. The coverage tab discards that order. `compareProposalsByAspect` (~line 199) returns `0` when both rows have a null aspect, which every project proposal does, so the sort is a no-op; `Array.sort` is stable, so the rendered order is the fetch order, and the `agent_queue` select in `agentQueueStore.js` (~line 225) carries no `.order()`, leaving Postgres free to return rows however it likes. On the first real project derive the service-worker proposal — inserted seventh — rendered first, so the top of the list was the last thing that should be built.
  - Behavior: Two proposals with no aspect order by their insertion time, oldest first, so the list matches the order derive wrote them and accepting top to bottom gives derive's intended build order. Aspect-tagged proposals keep sorting by aspect exactly as they do today, and a tagged proposal still sorts ahead of an untagged one. The ordering is stable across repaints and identical on every device.
  - Implementation notes: Extend `compareProposalsByAspect`'s final `return 0` to compare `created_at` ascending, falling back to `id` when timestamps tie — seven rows insert within about two seconds, so equal timestamps are possible if the column has coarse precision. Confirm `agent_queue` actually selects `created_at`; the store uses `select('*')` so it should be present, but the comparator must tolerate a missing or unparseable value by treating those rows as equal rather than throwing or sorting them all to one end. Do not fix this by adding `.order()` to the Supabase query instead — the comparator is where ordering is decided for both tagged and untagged rows, and splitting that across two places is how they drift. Leave `aspectSortKey` alone; it is correct and its "untagged sorts last" contract is what keeps tagged proposals ahead.
  - Out of scope: the coverage tally and section grouping; the Runs tab ordering; `buildQueueRunRecords`; adding an explicit sequence column to `agent_queue`.
  - File: toDoList_main/src/assignmentCoverage.js
  - Completed:
  <!-- id: 399e3328-f112-4ec9-9fa8-51ba195c212a -->

- [x] **[MEDIUM]** Shape reference is missing from the mobile settings modal — Completed: 2026-08-08
  - Type: bug
  - Description: The Repo setup section — the `Shape reference` row that opens the `SHAPES.md` picker — was added to `settingsMenu.js`, the gear-button dropdown. On mobile the settings surface is `settingsModal.js`, opened from `#drawerSettingsBtn`, which `main.js` describes as drawer-bound and therefore mobile-only via CSS. That modal lists View, Appearance, About, Help, Data, and Account, and carries `Configure inject` under Data — but has no Repo setup section and no way to reach the picker. So the shape reference, whose whole purpose is being readable from a phone while setting up a repo, is desktop-only.
  - Behavior: The mobile settings modal gains a `Repo setup` section containing `Shape reference`, matching the dropdown's section on desktop. Tapping it opens the same picker via `showRepoSetupModal()`. The section sits immediately before the existing `Configure inject` control, and `Configure inject` moves under the same heading so the two halves of pointing the app at a repo read as one cluster, as they do in the dropdown. Nothing else in the modal moves or changes.
  - Implementation notes: `settingsModal.js` builds its sections with local heading and item construction rather than importing `buildSettingsMenuItem`/`buildSettingsMenuSectionHeading` from `settingsMenu.js` — follow the modal's own existing pattern (see how the Data section at ~line 196 builds its heading and the `Configure inject` row at ~line 213) rather than importing the dropdown's builders, which are shaped for a dropdown and would need their own styling reconciled. Import `showRepoSetupModal` from `repoSetup.js` the same way the modal already imports `showInjectSettingsModal` from `inject.js` at line 22. Do not add the section to `settingsMenu.js` again — it is already there and correct. Do not attempt to unify the two surfaces in this entry: they are separately built by design and merging them is a much larger change than this bug warrants.
  - Out of scope: unifying `settingsMenu.js` and `settingsModal.js`; the picker's own rendering; the desktop dropdown; any other section of the mobile modal.
  - File: toDoList_main/src/settingsModal.js
  - Completed:
  <!-- id: 49856619-d84e-463b-a366-e706fca58785 -->

- [x] **[MEDIUM]** Extend bottom tab bar background into the iOS safe-area inset instead of leaving it as bare page background — Completed: 2026-08-08
  - Type: bug
  - Description: In iOS standalone (Add to Home Screen) mode, the STREAM/STRUCTURE tab bar correctly reserves space for the home-indicator safe area, but the strip of screen below the bar renders as plain black page background instead of continuing the bar's own surface color — visually reading as a separate dead band under the tabs. Two prior entries fixed padding on the wrong elements; screenshot evidence confirms the tab bar row itself has a background that stops above the safe-area padding rather than filling through it. Find the bottom tab bar's container (likely in `viewSwitcher.js` and its CSS rules in `style.css`) and change it so its background color/element extends the full height including `env(safe-area-inset-bottom)` — e.g. apply the surface background to the outer fixed-position wrapper and add the safe-area padding *inside* that same background, rather than padding an element whose background doesn't cover the inset. Acceptance criteria: (a) in standalone mode there is no visible color seam or band between the tab bar and the screen edge; (b) STREAM/STRUCTURE labels remain fully visible and untouched by the home indicator; (c) Safari (non-standalone) rendering is pixel-identical to before; (d) the floating assistant FAB position is unaffected; (e) desktop (>=1024px) layout is untouched.
  - File: `toDoList_main/src/viewSwitcher.js`, `toDoList_main/src/style.css`
  - Completed:
  <!-- id: cd58852e-b87c-4d60-88ad-bb06fe691be2 -->

- [x] **[MEDIUM]** Heal the shrunken standalone viewport after keyboard close so the bottom tab bar returns flush to the screen edge — Completed: 2026-08-08
  - Type: bug
  - Description: The persistent band under the STREAM/STRUCTURE tab bar in the installed iOS app is not a styling defect — it is the known iOS standalone-PWA viewport bug: the first time the software keyboard opens, the layout viewport shrinks by ~59px and stays shrunk for the whole session (`window.innerHeight` and `100dvh` never recover until force-quit), so the fixed `#mobileTabBar` with `bottom: 0` rides 59px above the physical screen bottom. Screenshot measurement confirms the bar's border-box ends exactly 59 CSS px short of the edge while its own 64px height (`--mobile-tab-h` + capped inset) is correct. The three prior entries were all static CSS patches and could never fix a runtime viewport-state bug. Fix per the documented workaround (https://dev.to/cederhook/fixing-the-ios-standalone-pwa-keyboard-bug-that-shrinks-your-viewport-for-good-63d): create a new module `viewportHeal.js` exporting an init function, called from the `index.js` bootstrap, gated at init on `matchMedia('(display-mode: standalone)').matches`. Inside: track `maxVH` as the max of `window.innerHeight` across `resize` events; add a document-level capture-phase `focusout` listener that, ~140ms after blur, runs the heal when `maxVH - window.innerHeight > 4`: capture `#mainList.scrollTop`, set `#outerContainer.style.display = 'none'`, force a synchronous reflow with `void el.offsetHeight`, restore `display` to `''`, then restore the scrollTop. (`#outerContainer` is `height: 100dvh` on mobile, satisfying the workaround's full-viewport-height requirement.) As a secondary trigger, run the same stuck-check debounced ~200ms on `visualViewport` `resize` when no element is focused, covering keyboard dismissal without blur. The heal is a strict no-op when not stuck, so Safari, desktop, and fresh sessions are untouched. Also remove the dead `#mobileTabBar::after` safe-area paint-through block in `style.css` shipped by the previous entry — with the bar at `bottom: 0` that pseudo-element hangs entirely below the viewport edge and paints nothing — and delete its regression test `mobileTabBarSafeAreaPaint.test.js`, replacing it with `viewportHeal.test.js` covering: the standalone gate, the >4px stuck threshold, the display flip with sync reflow between the writes, and scroll restoration. Acceptance criteria: (a) in standalone, after opening the keyboard via the Add-a-task field and dismissing it, the tab bar sits flush against the physical screen bottom with no band or seam; (b) the heal never fires when `innerHeight` is within 4px of its session max; (c) `#mainList` scroll position is preserved across a heal; (d) non-standalone Safari rendering and behavior are unchanged; (e) desktop (>=1024px) is untouched.
  - File: `toDoList_main/src/viewportHeal.js`, `toDoList_main/src/index.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/viewportHeal.test.js`, `toDoList_main/tests/mobileTabBarSafeAreaPaint.test.js`
  - Completed:
  <!-- id: c360cbf6-d0d1-4f3c-9439-e47c3499f907 -->

- [x] **[MEDIUM]** Heal an already-shrunken standalone viewport at launch by comparing against the physical screen instead of a session maximum — Completed: 2026-08-08
  - Type: bug
  - Description: The viewport heal shipped in `viewportHeal.js` never fires in the field because its stuck-detection is session-relative: `maxViewportHeight` seeds from `window.innerHeight` at init, so a session that begins already shrunken (the common case — iOS keeps the installed app resident and a reload inside a shrunken web process inherits the short viewport; only a force-quit resets it) records the shrunken height as the maximum and reports a deficit of zero forever. Replace the reference with an absolute one: an `expectedViewportHeight()` helper returning `screen.height` in portrait and `screen.width` in landscape (orientation via comparing `window.innerWidth`/`window.innerHeight`; `screen.*` are CSS px on iOS and do not shrink with this bug — in standalone with `viewport-fit=cover` the layout viewport should equal the full screen dimension). `isViewportStuck()` becomes `expectedViewportHeight() - window.innerHeight > STUCK_THRESHOLD_PX` with the threshold raised from 4 to 24 (the real deficit is ~59px; the margin absorbs minor UA quirks without thrashing), keeping the existing `< 1024px` width gate. Delete `maxViewportHeight`, `trackViewportHeight`, and the `resize` listener that fed it. Add three triggers alongside the existing focusout and visualViewport ones, all routing through the same `healViewport()`: (a) an initial check ~300ms after `initViewportHeal()` arms, so a session that boots stuck heals immediately; (b) `visibilitychange` when `document.visibilityState === 'visible'`, settle-delayed, covering resume from the app switcher; (c) `pageshow`, covering bfcache restores. Add an ineffective-heal cooldown: after a flip, re-measure, and if the deficit is unchanged record the timestamp and refuse further attempts for 5 seconds, so platforms where expected legitimately differs from actual (e.g. iPad windowed standalone) get one harmless flip rather than a loop. Teardown must clear the new listeners and timers. Update `viewportHeal.test.js`: expectation now derives from `screen` dimensions, plus coverage for the launch-time trigger, the visibility trigger, the raised threshold, and the cooldown. Acceptance criteria: (a) with a mocked `screen.height` of 852 and `innerHeight` of 793 at init, a heal runs within the launch window without any focus event; (b) no heal runs when `innerHeight` is within 24px of the expected dimension; (c) an ineffective flip suppresses re-attempts for the cooldown window; (d) existing focusout/visualViewport behavior and the standalone gate are unchanged; (e) desktop (>=1024px) untouched.
  - File: `toDoList_main/src/viewportHeal.js`, `toDoList_main/tests/viewportHeal.test.js`
  - Completed:
  <!-- id: b1a4d77f-7a79-4698-b101-9a032ab0441a -->

- [x] **[MEDIUM]** Add a Diagnostics section to settings and arm the viewport heal via navigator.standalone as well as display-mode — Completed: 2026-08-08
  - Type: feature
  - Description: Four viewport fixes have shipped with no way to observe their behavior on the affected device, and the leading suspect is that `initViewportHeal()`'s gate — `matchMedia('(display-mode: standalone)').matches` — never passes inside the current iOS installed-app container, silently no-opping every fix. Two changes. First, in `viewportHeal.js`: broaden the gate to also arm when `navigator.standalone === true` (the legacy iOS flag), keeping the existing try/catch; and add instrumentation — a module-level status object recording both gate readings (populated even when arming rejects, so a dead gate is visible), whether the module armed, the last stuck-check timestamp and deficit, the expected height used, and counts of heals attempted vs effective — exposed via a new `getViewportHealStatus()` export that returns a copy. Second, a new `settingsDiagnostics.js` module exporting a builder that `settingsModal.js` mounts as a section (id `settingsDiagnosticsSection`, class `settingsSection`, heading "Diagnostics", mirroring the existing section scaffolding exactly), placed after Repo setup. Read-only label/value rows, values in SpaceMono, recomputed every time the modal opens (not at module load): app version from `changelog[0]` in `changelog.js`; `display-mode: standalone` match; `navigator.standalone`; `screen.width×screen.height`; `window.innerHeight`; `visualViewport.height` and `offsetTop` (guard for absence); `document.documentElement.clientHeight`; service worker controller present; and the full `getViewportHealStatus()` readout (armed, gate readings, last deficit, attempts/effective). Include one 36×36 chip-standard copy button that writes the entire readout to the clipboard as JSON via the same clipboard approach the task-row copy chips use. Acceptance criteria: (a) opening settings twice after a viewport change shows updated values; (b) the copy chip yields parseable JSON containing every displayed field; (c) when the gate rejects, the section still shows both gate readings and armed:false; (d) `initViewportHeal()` arms when `navigator.standalone` is true even if the display-mode query is false, with existing heal behavior otherwise unchanged; (e) tests cover the broadened gate, the status export including the rejected-gate path, and the section's field completeness; (f) desktop rendering of the settings modal is unaffected beyond the new section appearing.
  - Out of scope: any change to heal or fallback mechanics beyond the gate broadening; the pending bottom-chrome reseat entry stays uninjected until this ships and a device screenshot decides whether it is needed.
  - File: `toDoList_main/src/viewportHeal.js`, `toDoList_main/src/settingsDiagnostics.js`, `toDoList_main/src/settingsModal.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/settingsDiagnostics.test.js`, `toDoList_main/tests/viewportHeal.test.js`
  - Completed:
  <!-- id: 749ac000-c1b9-4c95-8a90-63c13413e920 -->

- [x] **[MEDIUM]** Reseat the bottom-fixed mobile chrome by the measured viewport deficit when the display-flip heal fails to restore — Completed: 2026-08-09
  - Type: bug
  - Description: Field diagnostics from the installed iOS app confirm the shrunken-viewport state (screen 852, innerHeight 793, deficit 59) with the heal armed and the display-flip attempted but ineffective — WebKit never re-measures. Add a deterministic CSS fallback driven by the measurement JS already has. In `viewportHeal.js`, restructure every trigger to: measure the deficit; if stuck, attempt the flip (respecting the existing cooldown); re-measure; then reconcile fallback state — if still stuck AND the deficit is within a plausibility band of 30–90px (guards against legitimately windowed environments like iPad Stage Manager), write the deficit via `document.documentElement.style.setProperty('--vh-deficit', deficit + 'px')` and add class `vhDeficit` to `document.body`; if not stuck, remove both. Reconciliation runs on every trigger even when the flip is on cooldown, and teardown clears the class and property. Extend the module's status object and `getViewportHealStatus()` with `fallbackActive` (boolean) and `fallbackDeficitPx` (number or null), updated during reconciliation. In `settingsDiagnostics.js`, add a `heal fallback` row rendering those two fields (e.g. `active · 59px` / `off`) so the next device screenshot verifies the fallback without guesswork. In `style.css`, inside the `@media (max-width: 1023px)` block, add a rule group gated on `body.vhDeficit` that re-anchors each compact bottom-fixed element by subtracting the deficit from its existing `bottom` expression — deliberately `bottom: calc(...)` composition rather than `transform: translateY(...)`, so no stacking contexts or fixed-position containing blocks are introduced and no element's transition machinery is disturbed: `#mobileTabBar` to `calc(0px - var(--vh-deficit, 0px))`; `#claudeLauncher`, `#bottomSheet[data-state="IDLE"]`, `#bottomSheet[data-state="PEEK"]`, `#undoToast`, and `#mobileUpdatePill` each to their current live base expression minus `var(--vh-deficit, 0px)` — read each base expression from the stylesheet at implementation time rather than trusting any list, and comment the block stating these calcs shadow the base rules and must move with them. Fixed elements escape `#outerContainer`'s `overflow: hidden` (no transform on it), so the reseated chrome renders in the strip below the shrunken viewport; grep to confirm none of the targeted elements contain `position: fixed` descendants. Extend `viewportHeal.test.js`: fallback applied when a flip leaves the deficit unchanged; cleared when a later measurement reads healthy; band guard rejects a 200px deficit; healthy sessions never carry the class; status export includes the two new fields on both paths. Extend `settingsDiagnostics.test.js` for the new row. Acceptance criteria: (a) in a stuck standalone session the tab bar, FAB, sheet nub/peek strip, undo toast, and update pill all sit at their designed offsets from the physical screen bottom, no band or seam below the bar; (b) if the viewport later recovers, the fallback clears and nothing is double-offset; (c) healthy sessions, Safari, and desktop (>=1024px) carry no `vhDeficit` class and are pixel-identical to before; (d) the diagnostics section reports the fallback state; (e) expanded bottom sheet and chat sheet untouched.
  - Out of scope: the EXPANDED bottom sheet and the full-screen chat sheet (stretched overlays need a height-side correction, not a bottom offset — separate entry if the residual band under them matters in practice).
  - File: `toDoList_main/src/viewportHeal.js`, `toDoList_main/src/settingsDiagnostics.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/viewportHeal.test.js`, `toDoList_main/tests/settingsDiagnostics.test.js`
  - Completed:
  <!-- id: 1a36b407-66ef-4518-a715-ccca24803b54 -->

- [x] **[MEDIUM]** Stop reseating bottom chrome into the clipped strip; make the shrunken viewport bottom read as the designed edge — Completed: 2026-08-09
  - Type: bug
  - Description: Device evidence shows WebKit composites the stuck standalone session as a surface exactly as tall as the shrunken layout viewport: after the vhDeficit reseat, `#mobileTabBar`'s box extends below that line and only the sliver above it renders (the 2px active-tab indicator is visible; icons and labels below the boundary are not), while every reseated element whose box stayed above the line renders normally. Nothing in-page can paint below the shrunken viewport bottom, so the reseat strategy inverts: while `.vhDeficit` is active, all bottom-fixed chrome keeps its base anchors against the effective (shrunken) viewport, and the fallback's job becomes making the un-paintable strip below look intentional. In `style.css`, rewrite the vhDeficit rule group: delete every `bottom: calc(... - var(--vh-deficit))` override added by the previous entry — `#mobileTabBar`, `#claudeLauncher`, `#bottomSheet[data-state="IDLE"]`, `#bottomSheet[data-state="PEEK"]`, `#undoToast`, `#mobileUpdatePill` all revert to their base expressions — and replace the group with seam cleanup only: identify by grep which element paints the downward box-shadow that gets sheared flat at the surface boundary (check `#mobileTabBar`, the `#bottomSheet` IDLE/PEEK states, and any bar-adjacent scrim), and while `body.vhDeficit`, suppress that shadow's downward component so the bar's bottom edge meets the OS canvas extension with no gradient dip; the bar, body canvas, and strip are all `--bg-elevated` already, so with the shadow gone the strip reads as a uniform apron. Update the block's banner comment to state the compositor-clip finding and why offsets below the effective viewport are futile. In `viewportHeal.js`, keep detection, flip, cooldown, reconciliation, and the class/var publishing unchanged — the var may go unused by CSS but stays published for diagnostics — and update comments to describe the fallback as seam mitigation rather than reseating. `settingsDiagnostics.js` needs no field changes. Update `viewportHeal.test.js` expectations that assert reseat CSS coupling if any, and adjust any test reading the vhDeficit stylesheet block. Acceptance criteria: (a) in a stuck session the STREAM/STRUCTURE icons and labels are fully visible and tappable at the bar's base position, with the FAB, sheet nub, undo toast, and update pill at their designed offsets above it; (b) below the bar, the strip to the physical edge is uniform `--bg-elevated` with no gradient dip or hard seam at the bar's edge; (c) healthy sessions, Safari, and desktop are pixel-identical to before the vhDeficit block existed; (d) diagnostics still reports `heal fallback: active · 59px` in a stuck session.
  - Out of scope: any attempt to render content below the shrunken viewport; expanded sheet and chat sheet; upstream WebKit behavior.
  - File: `toDoList_main/src/style.css`, `toDoList_main/src/viewportHeal.js`, `toDoList_main/tests/viewportHeal.test.js`
  - Completed:
  <!-- id: 5250158f-6a7e-456c-8514-8fd8ff7556d2 -->

- [ ] **[HIGH]** Fix backlog runs always settling to Shipped even when nothing was checked off
  - Type: bug
  - Description: `reconcileSuccessConclusion` in `toDoList_main/src/claudeSheet.js` (~line 5028) branches on `rec.entryId`. For an entry-mode run it correctly reads main's TODO.md via `readTodoMdFromWorker` and keys off `entryCheckboxState` to choose SHIPPED vs NOCHANGE (the NOCHANGE branch is at ~line 5074-5088). But the `!rec.entryId` branch (~line 5035-5054), which handles every backlog dispatch from `todoMdViewer.js`'s "Run backlog" button, unconditionally calls `setRunRecordStatus(correlationId, 'SHIPPED')` — it only uses `resolveBacklogRunTitle`/`newlyCheckedTitle` to try to recover a nicer title, never to decide the verdict. So a backlog run that finishes green but genuinely changed nothing (the routine reports every open entry ineligible and exits clean) is shown as "Shipped" in the Runs tab even though the task row it should describe is still pending, because no entry was actually checked off in TODO.md.
  - Behavior: mirror the entry-mode branch — use the existing dispatch-time `rec.openTitles` snapshot to detect whether any previously-open title actually flipped to checked in the fresh TODO.md read already fetched for `resolveBacklogRunTitle`. Zero titles flipped means a genuine no-change backlog run: call `setRunRecordStatus(correlationId, 'NOCHANGE')` (and `logConceiveRun(project, rec, 'nochange', runId, target)`) instead of SHIPPED, and still surface the picked task's title on that row when the routine's closing summary or diff makes it resolvable, instead of leaving the generic "Backlog run" label.
  - Implementation notes: `newlyCheckedTitle` (claudeSheet.js:4948-4959) currently returns `null` for BOTH zero matches and 2-or-more ambiguous matches, so the caller can't tell "definitely nothing changed" from "can't tell which of several changed." It needs to distinguish those two cases (e.g. return the found-titles array, or an object with a count) so the backlog branch can drive NOCHANGE only on a confirmed zero-match, while an ambiguous multi-match still fails safe to SHIPPED per the function's documented fail-safe policy.
  - File: `toDoList_main/src/claudeSheet.js`
  <!-- id: 8ef06590-9ea8-4ffa-b103-36ef3e59d75d -->
