# TODO List

## Bugs

- [ ] **[MEDIUM]** Let ArrowDown off the bottom calendar row descend into the day-detail row list
  - Description: Currently the Calendar arrow-nav handler clamps ArrowDown at the bottom row of `#calendarGrid` — pressing ArrowDown on any cell in the last rendered row (which is usually a mix of in-month and `.calendarCell.outOfMonth` trailing days, e.g., May 31 and June 1–6 when viewing May 2026) does nothing, even though there's a focusable `#calendarDayList` containing `.todayRow.todoRowCard` rows immediately below the grid. Extend the Calendar ArrowDown branch so when the focused cell is in the last rendered grid row AND `#calendarDayList` has at least one `.todayRow.todoRowCard` child, focus jumps to the first row in the day-detail list (`#calendarDayList .todayRow.todoRowCard`) instead of clamping. "Last row" is computed from cell index — `index >= totalCells - 7` where totalCells is `#calendarGrid.children.length` — so the rule works whether the month renders 5 rows or 6, and treats in-month and `outOfMonth` cells in that trailing row identically (the visual difference is opacity, not navigability). The row-nav extension already gives day-panel rows `tabindex="-1"` and wires ArrowUp/ArrowDown between them; this entry only adds the grid→panel boundary. Symmetric ArrowUp from the first day-panel row jumps back to the previously-focused calendar cell (resolved via `calendarSelectedKey` if set, falling back to the cell whose `data-date` matches today, falling back to the last cell — same fallback chain the Enter-rebuild-focus fix uses). Both transitions preventDefault and stopPropagation. Test: with the May 2026 month rendered and a `.calendarCell.outOfMonth` cell focused in the last row, dispatch ArrowDown and assert `document.activeElement` is the first `.todayRow.todoRowCard` inside `#calendarDayList`; dispatch ArrowUp on that row and assert focus returns to a `.calendarCell`.
  - File: `toDoList_main/src/main.js`, `toDoList_main/tests/todoRowKeyboardNav.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
     
- [ ] **[MEDIUM]** Replay welcome tour switches to Projects view and seeds a sample project when none exists
  - Description: Triggering "Replay welcome tour" from the settings menu while on the Today or Calendar view causes every coachmark callout to misalign — the desktop tour's STEPS anchor against `.selectedProject`, todo row chrome (due pill, description chevron), `#projButton`, and the right-side icon cluster, but `#mainBar[data-view="today"] #mainList { display: none }` (and the Calendar equivalent) hides the project view's content surfaces, so the seeded sample row, due pill, and description chevron either don't exist or return zero-sized bounding rects. The existing fall-through ("centered callout when target() returns null") fires for those steps, which the user reads as "callouts don't line up with the page content." Separately, on a fresh load where the user has deleted the seeded sample project, replaying the tour also produces centered fallbacks for the row-anchored steps because `isSampleSeeded()` is true and the seeder doesn't re-run. Fix the Replay row handler in the settings menu (`main.js`, inside the `buildSettingsMenuItem('Replay welcome tour', …)` activation callback): before dispatching to `startWelcomeCarousel()` / `startCoachmarkTour()`, (1) call `applyActiveView('projects')` so the Projects surfaces are visible and their targets have real geometry, and (2) check `listLogic.getProjects()` — if the array is empty, call `listLogic.seedSampleProject()` (overriding the `isSampleSeeded()` once-per-install guard for the replay path specifically, since the entire point of replaying is to give the tour real targets) and then re-render the project sidebar + main list so the seeded row / sample todos exist in the DOM by the time `startCoachmarkTour` resolves its targets. Same fix applies symmetrically to the mobile carousel branch — `applyActiveView('projects')` first so the carousel's similar in-page references resolve correctly, though the carousel itself is less DOM-anchored than the desktop tour. Both branches should defer the tour kickoff by one tick (microtask or `requestAnimationFrame`) after the view switch and re-render so the layout passes have settled before the tour reads bounding rects for the spotlight cut-out. Tests: replaying with `data-view="today"` set on `#mainBar` flips it to `"projects"` before the overlay mounts; replaying with `listLogic.getProjects().length === 0` produces a seeded project before the overlay mounts; replaying when projects already exist does NOT re-seed (skip the seed call when `getProjects().length > 0` so users with their own projects aren't surprised by a sample appearing).
  - File: `toDoList_main/src/main.js`, `toDoList_main/tests/coachmarkTour.test.js`, `toDoList_main/tests/welcomeCarousel.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)

## Features

- [ ] **[MEDIUM]** Add selectable ambient visualizer to focus music popover for when YouTube video is blocked
  - Description: When YouTube video is blocked on corporate networks the audio still plays but the 240×135 iframe shows nothing, leaving the popover visually inert. Add a generative ambient visualizer that occupies the iframe's footprint while music is playing, toggled by a new checkbox in the popover and styled by a dropdown next to it. Important constraint: the visualizer CANNOT react to the YouTube audio waveform — YouTube iframes don't expose their audio stream to Web Audio API across origins, so any analyser-driven design is impossible without abandoning the YouTube embed. The five visualizer styles are decorative loops on fixed timings, not audio-reactive: STARFIELD (slow horizontal star drift over a deep purple void, most "Void theme"), BLOBS (three slow-drifting blurred lavender/purple blobs on long sine paths, lava-lamp feel), RINGS (concentric purple rings expanding from a central core on a 4-second cycle), BARS (16 staggered equalizer-style bars with offset CSS keyframes — looks the most "music visualizer" while being decorative), GHOST (companion ghost mascot bobs gently with music notes rising past, ties to the existing ghost SVG). Picker layout follows PICK-A from the mockup round: a single row below the station list containing the checkbox (left), a STYLE label, and a native `<select>` dropdown listing the five styles by name. New row's vertical position sits between the curated stations list and the Pause + slider controls row so the popover keeps its existing top-down rhythm. New module `musicVisualizer.js` mirrors `music.js`'s singleton pattern: exports `ensureVisualizer()` (mount into the iframe-footprint container), `destroyVisualizer()` (tear down, stop all animations), `setVisualizerStyle(styleId)` (swap the active style without remounting the container). The iframe and visualizer share the same wrapper element with absolute-positioned children — visualizer on top with a higher z-index when enabled, iframe behind. When the user unchecks the box, the visualizer detaches and the YouTube iframe is revealed again (useful at home where video isn't blocked). Critical to honor the prior visualizer-failure root cause noted in past notes: any CSS-only bar visualizer (BARS option) MUST set an explicit parent height on the visualizer wrapper because flex-end alignment of percentage-height bars collapses to zero against an unsized parent. Set `height: 100%` on the wrapper and let aspect-ratio: 16/9 on the iframe container drive the actual pixel height. Persistence: two new preferences in `prefs.js` — `todoapp_musicVisualizerEnabled` (boolean, defaults to false so first-time users keep current behavior) and `todoapp_musicVisualizerStyle` (string, one of "starfield" / "blobs" / "rings" / "bars" / "ghost", defaults to "starfield"). Visualizer only runs when music is actively playing — when the user pauses, animations pause too (set `animation-play-state: paused` on the active style's root element, no need to tear down). When the user resumes, animations resume from where they left off. Visualizer also bails on `prefers-reduced-motion: reduce` even when the checkbox is on — substitute a static frame (single rendered snapshot of the chosen style, no animation) so the popover doesn't look broken. Tests: each style mounts a root element with the expected class (e.g., `.musicViz--starfield`), the checkbox writes the pref and toggles visualizer presence, the dropdown writes the style pref and swaps the active style without unmounting the wrapper, prefers-reduced-motion swaps the animated style for a static fallback.
  - File: `toDoList_main/src/musicVisualizer.js` (new), `toDoList_main/src/music.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/musicVisualizer.test.js` (new)
  - Completed: YYYY-MM-DD (PR #<number>)

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
