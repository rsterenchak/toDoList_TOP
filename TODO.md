# TODO List

## Bugs

- [x] **[HIGH]** Cleanup pass — Remove Drive sync code entirely
  - Description: Drive sync is being cut as part of the backend migration wrap-up. Supabase now owns multi-device sync via its realtime + auth layer, making Drive's parallel sync system redundant. The Drive code is also currently half-disabled (the boot-time `autoSyncOnAppLoad()` call in main.js is commented out due to OAuth `origin_mismatch` errors in Codespaces), so leaving it in the codebase is the worst case: full complexity tax with zero functional benefit. The actual backup story is handled separately by a forthcoming JSON import/export feature (tracked in a sibling entry) which gives users a real escape hatch in a portable format. Server-side, Supabase's own automated backups cover the data-loss recovery story without any app involvement. This entry is pure deletion — no behavioral changes to the app's data model or sync layer, just removing dead/redundant code paths and their UI surfaces.
  - Files to delete entirely: `toDoList_main/src/driveAuth.js`, `toDoList_main/src/driveExport.js`, `toDoList_main/src/driveImport.js`, `toDoList_main/src/driveAutoSync.js`, and all corresponding test files under `toDoList_main/tests/` (e.g. `driveAuth.test.js`, `driveExport.test.js`, `driveImport.test.js`, `driveAutoSync.test.js`, and any other files matching `drive*.test.js`).
  - Removals from main.js: (1) All imports from the four Drive modules at the top of the file. (2) The `migrateLegacyDriveSyncMarker()` call and its import. (3) The entire Drive-related state machinery: `_driveSyncState`, `_driveSyncTooltip`, `computeDriveSyncState`, `syncStateTooltip`, `syncStateToGlyphClass`, `SYNC_GLYPH_SVG`, `paintSyncBadge`, `paintAllSyncBadges`, `setDriveSyncState`, `refreshDriveSyncState`, `recomputeDriveSyncStateLocal`. (4) All event listeners for `driveSyncStateChanged`, `autoSyncStateChanged`, `driveManualActionSuccess`, `driveConnectionChanged`. (5) The settings menu's entire Drive section (the `driveHeading`, `driveHeadingBadge`, the `buildDriveSyncRow` function, the call that appends it to the menu, and the `driveMenuRowNeedsRefresh` helper). (6) The `onDriveSyncClick` function and the `computeDriveSyncLabel` function. (7) The `openDriveConflictPopover` function and any CSS associated with `#driveConflictBackdrop`, `#driveConflictPopover`, `#driveConflictHeader`, etc. (8) `settingsToggleSyncBadge`, `drawerSettingsBtnSyncBadge`, and all paint/refresh calls that target them. (9) `buildSettingsModalDriveSyncCard`, `computeSettingsModalDriveSyncVerb`, `computeSettingsModalDriveSyncSubLabel`, `refreshSettingsModalSyncCard`, and the Data section of the Settings modal (the `dataSection`, `dataGrid`, `dataHeading`). (10) The `installAutoSyncBackgroundTriggers()` call, the `registerAutoSyncRebuild` call, and the commented-out `setTimeout` block that previously called `autoSyncOnAppLoad`. (11) Inside `showSettingsModal`'s open handler, the listeners `onDriveSyncStateChangedForModal` and `onDriveConnectionChangedForModal` and their corresponding `removeEventListener` calls in the close handler.
  - Removals from prefs.js: The `readLastDriveSyncedAt`, `readLastLocalMutationAt`, `migrateLegacyDriveSyncMarker` exports, and the underlying localStorage key constants for `todoapp_lastDriveSyncedAt`, `todoapp_lastDriveExportedAt`, and the local mutation marker. The `formatRelativeExportedAt` export in exportImport.js should also go since its only caller is Drive-related UI; if any non-Drive caller remains after the cleanup, keep it.
  - Removals from style.css: All CSS rules targeting `.settingsToggleSyncBadge`, `.settingsMenuDriveSyncBadge`, `.driveConflictBackdrop`, `#driveConflictPopover`, `.driveConflictAction`, `.settingsModalDataTile--driveSync`, `.settingsModalDataTileIcon--driveSync`, `.settingsModalDataTileIcon--spinning`, `.drawerSettingsBtnSyncBadge`, `body.driveExportInProgress`, `body.driveImportInProgress`, and any `[data-sync-state="..."]` rules. Also remove any `--proj-accent` rules that exclusively serve the Drive sync UI.
  - Removals from index.js: Any auto-sync arming, sign-out-clears-Drive-token logic, or Drive-related boot sequence calls.
  - Removals from package.json: The Google APIs / OAuth dependencies if they exist as direct dependencies (search for `googleapis`, `gapi`, `@google/oauth`, etc.); remove the corresponding lockfile entries. If GIS (Google Identity Services) is loaded via CDN script tag rather than npm, remove that script tag from index.html.
  - Removals from .env / .env.example: The `OAUTH_CLIENT_ID` entry, and its corresponding `DefinePlugin` entry in webpack.config.js.
  - Behavior after cleanup: The app boots, hydrates from Supabase, and behaves exactly as it does today minus the Drive UI surfaces. The settings menu no longer has a Drive section. The mobile Settings modal no longer has a Data section. The ghost icon no longer has a sync-state badge overlay. The drawer Settings button on mobile no longer has a sync badge. The footer is unchanged. No Drive-related localStorage keys are read or written. No Drive-related events are dispatched or listened for. Sign-in, sign-out, project CRUD, todo CRUD, hydration — all continue to work exactly as they do now.
  - Acceptance criteria: (1) No file in `toDoList_main/src/` imports from `driveAuth`, `driveExport`, `driveImport`, or `driveAutoSync` — verified by `grep -r "drive" toDoList_main/src/ | grep -v ".test.js"` returning zero matches (case-insensitive); (2) No CSS rule references any drive-related class — verified by `grep -i drive toDoList_main/src/style.css` returning zero matches; (3) The app boots and the settings menu opens without any Drive UI; (4) `npm test` passes with the Drive test files removed (no orphan tests); (5) Webpack build completes successfully with no missing-import errors; (6) Sign in and verify all Phase 5 mutation paths still work — add/edit/complete/delete a todo, add/rename/delete a project, sign out, sign back in; (7) Run `grep -ri 'autoSync\|driveSync\|oauthClient\|gapi' toDoList_main/src/` and confirm zero matches outside of comments that are being explicitly preserved for historical context (delete those too if any remain).
  - Out of scope: The JSON import/export feature (separate sibling entry); any refactoring of non-Drive code; touching the Supabase realtime subscription (that's a separate concern); removing any non-Drive feature even if it shares utility code with Drive (audit each utility — if it has non-Drive callers, keep it).
  - File: All files listed under "Removals" sections above; `package.json`, `package-lock.json`, `.env.example`, `webpack.config.js`, possibly `index.html`
  - Completed: 2026-05-25

## Features

- [x] **[MEDIUM]** Re-enable drag-and-drop JSON import with redesigned full-window overlay
  - Description: The drag-and-drop import code in `exportImport.js` is fully intact (`attachDragDropImport` is exported, `#importDropOverlay` CSS is present, validation routes through the shared `importTodosFromString` pipeline with the destructive-overwrite confirm modal), but the boot-time call from `main.js` is missing — dragging a `.json` file onto the window currently does nothing. Re-wire the call alongside the other restore-from-storage hooks (passing the same `rebuildAfterImport` callback the file picker and Drive pull paths use), and redesign the overlay to a full-window dashed perimeter: replace the small centered `#importDropOverlayInner` card with an inset dashed border (`inset: 18px; border: 2px dashed var(--accent); border-radius: 8px; box-shadow: 0 0 24px var(--accent-glow)`) over a slightly darker base wash (`background: rgba(14,15,20,0.72)`), centered inside it a 44px Tabler-style file-arrow glyph (vanilla inline SVG or a unicode glyph — no new icon-font dependency), the existing "DROP JSON TO IMPORT" label in SpaceMono uppercase letterspaced, and a 12px `var(--text-secondary)` subline reading "Replaces all current projects & todos" so the destructive nature is legible before the confirm modal opens. Keep the pointer-coarse early-return (touch browsers skip drag listeners entirely), the existing `dragDepth` enter/leave counter, and the file-type guard. The overlay stays `pointer-events: none` so the window-level `drop` listener still fires.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/exportImport.js`, `toDoList_main/src/style.css`
  - Completed: 2026-05-24

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
