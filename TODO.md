# TODO LIST

- [x] **[MEDIUM]** Paint the 16px strip below #desktopViewSubBand with --bg-base via downward box-shadow
  - Type: bug
  - Description: After PR #419 painted `#desktopViewSubBand` with `var(--bg-base)` and the follow-up commit `b17a6372` added a `box-shadow: 0 -16px 0 0 var(--bg-base)` on the sub-band to cover the 16px margin-top gap ABOVE it, a 16px grey strip remains visible BELOW the sub-band. Console probe confirms: `#desktopViewSubBand` rect is `y: 89, height: 32` (bottom at y=121), `#outerContainer`'s row 3 is 48px tall, so 16px of slack remains at the bottom of the row. `elementsFromPoint` at 8px below the sub-band returns `#mainSec` (transparent), then `#mainSplit` (transparent), then `#outerContainer` painting `rgb(20, 21, 27)` — the greyer chrome bleeding through. Fix by mirroring the existing upward shadow with a symmetric DOWNWARD shadow on the sub-band: extend its current `box-shadow: 0 -16px 0 0 var(--bg-base)` to `box-shadow: 0 -16px 0 0 var(--bg-base), 0 16px 0 0 var(--bg-base)`. Hard-edged (no blur, no spread), additive, lives on the sub-band so it disappears with the sub-band at <1024px, and uses the same token the existing shadow uses so the two extensions match exactly. This parallels the two-shadow pattern already established on `#desktopChatPane` (which has both an upward `--bg-base` shadow for its own top-strip cover and a separate `-1px 0` shadow for the vertical separator). The box-model rewrite was correctly aborted in a prior run because it breaks `chatPaneSubHeaderAlign` — this entry preserves all sub-band geometry (`min-height: 32px`, `margin-top: 16px`) untouched and only extends the existing box-shadow declaration.
  - Behavior:
    1. At ≥1024px: no visible grey strip below the view tabs. The painted area between the project-switcher row's chrome and the filter+sort band's first content is one continuous `--bg-base` region, with the sub-band's existing upward shadow handling the top strip and the new downward shadow handling the bottom strip.
    2. `getComputedStyle(sub).boxShadow` includes BOTH `rgb(14, 15, 20) 0px -16px 0px 0px` (existing) AND `rgb(14, 15, 20) 0px 16px 0px 0px` (new). Pin both via behavioral test.
    3. `sub.offsetHeight` is unchanged at 32px. `getComputedStyle(sub).marginTop` is unchanged at `16px`. `sub.parentElement.id === 'outerContainer'` and `grid-row: 3` are unchanged. The sub-band's geometry is intentionally untouched — only its paint extends.
    4. The chat pane's existing shadow stack (`rgb(14, 15, 20) 0px -16px 0px 0px, rgba(108, 93, 245, 0.18) -1px 0px 0px 0px`) is unchanged and the three pinned chat-pane tests (`chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, `navbarGapPaneBackground`) remain unchanged and passing.
    5. The `(d2)` test in `subBandBackground.test.js` that pins the existing upward shadow stays green — the new downward shadow is additive, not a replacement.
    6. At <1024px the sub-band is `display:none` and the shadow is inert.
    7. The chat pane's CHAT/RUNS tab strip is unaffected. The downward shadow paints into `#mainSec`'s territory (the task pane), not into `#desktopChatPane`'s territory. Since `#mainSec`'s bg at the affected region is transparent (per probe), the new shadow simply replaces what `#outerContainer` was painting there.
  - Test-first regression set:
    1. Source-pattern: the `#desktopViewSubBand` rule's `box-shadow` declaration contains BOTH `0 -16px 0 0 var(--bg-base)` and `0 16px 0 0 var(--bg-base)` (or color-equivalent tokens). Assert both shadow segments are present.
    2. The existing upward shadow assertion in `subBandBackground (d2)` continues to pass — do NOT modify it.
    3. The sub-band's `min-height: 32px` and `margin: 16px 0 0` (or whichever current form) are preserved — pin them so a future "tidy up" doesn't quietly break the chat-pane alignment.
    4. Chat pane non-regression: `getComputedStyle(chatPane).boxShadow` is unchanged; chat pane rect top/bottom unchanged within 1px; `elementsFromPoint` at the CHAT/RUNS tab center still returns a `.claudeTab` first.
    5. Optional behavioral guard: assert `elementsFromPoint` at 8px below the sub-band's bottom returns `#mainSec` or `#mainSplit` first (not `#outerContainer`), proving a closer-stack element is now covering the strip. May not work in jsdom; if so, the source-pattern test is sufficient.
  - Implementation notes: Grep `style.css` for `box-shadow: 0 -16px 0 0 var(--bg-base)` to find the existing declaration on `#desktopViewSubBand`. Extend it in place: `box-shadow: 0 -16px 0 0 var(--bg-base), 0 16px 0 0 var(--bg-base);` — same token, mirrored vertical offset. Do NOT touch the sub-band's `min-height`, `margin`, `height`, `padding`, `grid-row`, `position`, `z-index`, or DOM placement. Do NOT touch `#desktopChatPane`, `#mainSec`, `#mainSplit`, `#outerContainer`, `#mainBar`, `#taskFilterBar`, or the chat-pane alignment tests. The `(d2)` test's existing assertion that matches the upward shadow will continue to match (it greps for the substring, which is unchanged); add a NEW assertion alongside it that pins the downward shadow segment.
  - Out of scope: any change to the sub-band's geometry (`min-height`, `margin`, `height`, `padding`); any change to `#desktopChatPane`, `#mainSec`, `#mainSplit`, `#outerContainer`, `#mainBar`, `#taskFilterBar`; any change to the top project-switcher row's chrome; the `--bg-base` token; mobile chrome; the three pinned chat-pane alignment tests; the existing `(d2)` upward-shadow assertion.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/subBandBackground.test.js`
  - Completed: 2026-06-07
  <!-- id: eed22fd6-6b4b-4119-9051-e6cb12509177 -->

- [x] **[MEDIUM]** Add 'Show completed (N)' toggle to TODO.md viewer, default off
  - Type: feature
  - Description: The in-app `#todoMdViewer` (header `#todoMdViewerHeader`, body `#todoMdViewerBody`) currently renders every entry in `TODO.md` including completed ones (`- [x]`), so the list grows monotonically as the pipeline ships PRs and the active backlog gets buried under shipped work. Add a "Show completed (N)" toggle to `#todoMdViewerHeader` (next to whatever header chrome is already there) that defaults OFF; when off, every completed entry is hidden from the rendered body — both the top `- [x] **[PRIORITY]** Title` line AND ALL of its nested sub-bullets (Type, Description, Behavior, Implementation notes, Out of scope, File, Completed, the `<!-- id: ... -->` marker, and any other indented continuation lines belonging to that entry). The count `N` reflects the number of completed top-level entries currently hidden. When the toggle is ON, completed entries render exactly as they do today — no change to their visual treatment. Toggle state persists across sessions via `localStorage` under the `todoapp_` prefix convention (e.g. `todoapp_todoMdShowCompleted`). Default state is OFF (i.e. completed hidden) on first load and after a localStorage clear. This is a render-side change only — `TODO.md` on disk is untouched, the pipeline agent still reads the full file (preserving institutional memory: aborted entries, design decisions, prior probes), and the `Completed: YYYY-MM-DD (PR #N)` lines stay intact for the changelog cross-reference workflow.
  - Behavior:
    1. On first load (no localStorage key present): toggle is OFF, completed entries are hidden, body renders only `- [ ]` entries and their sub-bullets. The header shows "Show completed (N)" where N is the count of hidden completed top-level entries.
    2. Clicking the toggle flips state and persists `'true'` or `'false'` to `localStorage.todoapp_todoMdShowCompleted` (or whichever exact key — match existing `prefs.js` conventions; grep `todoapp_` to pick the right pattern). The body re-renders immediately; no full page reload.
    3. When ON: every entry renders exactly as it does today. Completed entries' visual state (strikethrough or whatever's already applied to `- [x]` lines) is unchanged.
    4. When OFF: the entire range from a `- [x]` line through the LAST sub-bullet belonging to that entry (everything indented under it, including the `<!-- id: ... -->` HTML comment and the trailing blank line if present) is hidden. The next `- [ ]` or `- [x]` line marks the start of the next entry and bounds the hide range.
    5. The (N) count reflects ACTIVE state of the file (recalculated on each render), not a cached value — if a `Completed:` line is added/removed between renders, N updates.
    6. If `TODO.md` has zero completed entries, the toggle is still visible but reads "Show completed (0)" and is functionally a no-op when clicked. (Alternative: hide the toggle entirely at 0. I'd suggest keeping it visible so the affordance doesn't appear and disappear; flag if you'd prefer it auto-hidden.)
    7. Page header (`#todoMdViewerHeader`) layout doesn't reflow noticeably when the toggle's N changes from one digit to two digits — give the count a fixed-width container or right-align the toggle so the rest of the header doesn't shift.
    8. The toggle is keyboard-accessible (Tab focusable, Enter/Space activates), and has `aria-pressed` reflecting state.
    9. Existing scroll position in `#todoMdViewerBody` is preserved across toggle clicks — clicking the toggle should NOT jump to the top of the body. (If preserving exact scroll is hard, at minimum: when toggling from OFF→ON, the previously-visible top entry stays at the same y-position; when toggling from ON→OFF, the topmost still-visible entry stays at the same y-position.)
  - Test-first regression set:
    1. Default state: with no `localStorage.todoapp_todoMdShowCompleted` key set, the toggle reads as OFF (`aria-pressed="false"` or equivalent) and the rendered body contains no `- [x]` lines.
    2. Toggle persistence: clicking the toggle writes the expected string to `localStorage`; on next render with that key set to `'true'`, completed entries are visible.
    3. Hide range correctness: a completed entry's sub-bullets (Type, Description, etc.) and its `<!-- id: ... -->` marker are all hidden when the toggle is OFF. Use a fixture with at least one completed entry that has the full sub-bullet structure and one with minimal structure to cover both cases.
    4. Count correctness: with a fixture containing K completed entries, the toggle label includes `(K)`. With K=0, label is `(0)`.
    5. Re-render reactivity: after `TODO.md` content changes (simulated by feeding new markdown into the viewer), N updates without a manual reload.
    6. Pipeline non-regression: the file on disk is NOT modified by toggling. Assert that no write to `TODO.md` (via worker, via fs, via fetch PUT) is dispatched when the toggle is clicked.
    7. Accessibility: toggle has `aria-pressed` reflecting state; focus is reachable via keyboard.
  - Implementation notes: Find the current `#todoMdViewerBody` rendering function (grep `todoMdViewer` in `main.js`, `inject.js`, and any module that owns the viewer — the render likely takes the raw markdown string and produces HTML). Add a filter pass over the markdown before render: parse into entries (split on `^- \[` at the start of a line, accumulating continuation lines until the next top-level `- \[` or end-of-file), tag each entry as completed or active based on its first line's `[x]` vs `[ ]`, then conditionally drop completed entries based on the toggle state. The (N) count is `entries.filter(e => e.completed).length`. The toggle DOM goes into `#todoMdViewerHeader` — match the existing header's element style (likely a button or label; grep what's already there). For persistence, follow `prefs.js` patterns — grep for `todoapp_` key reads in `prefs.js` and use the same `get`/`set` helpers if they exist. CRITICAL: do NOT change `TODO.md` on disk and do NOT change how the pipeline reads it. The pipeline reads `TODO.md` server-side via GitHub API, NOT through the viewer's DOM — so the viewer's render-side filtering is purely cosmetic and the pipeline keeps seeing the full file with all completed entries. Add a comment in the implementation explicitly noting this so a future refactor doesn't "consolidate" the filter into a place that also affects the pipeline read path. The toggle's existence is a render-time control; the file is the source of truth.
  - Out of scope: any change to `TODO.md` on disk; any change to how the pipeline agent reads `TODO.md` (it should continue to read the full file including completed entries — preserving the institutional memory that's already saved us from regressions twice this session); a "Clear completed" button that prunes entries from the file (deliberately not included — see entry description rationale); changes to how completed entries are styled when visible; the pipeline's selection logic (which already skips completed entries by checking `- [x]` vs `- [ ]`); the `Completed:` line format; the `<!-- id: ... -->` marker format; changelog generation.
  - File: whichever module owns the `#todoMdViewer` render (grep `todoMdViewer` to find it — likely in `main.js` or a dedicated `todoMdViewer.js` module if it exists), `toDoList_main/src/style.css` (if the toggle needs new chrome), `toDoList_main/src/prefs.js` (for the localStorage key if a centralized pref helper is used), `toDoList_main/tests/` (add a new test file `todoMdViewerShowCompleted.test.js` — grep existing viewer tests first to see if there's a natural file to extend)
  - Completed: 2026-06-07
  <!-- id: 7f01fc9c-666f-408a-9af9-6bb60adc161a -->

- [x] **[MEDIUM]** Swap "Show completed (N)" text pill for 32×32 icon button with count badge
  - Type: bug
  - Description: The previous "Show completed (N)" toggle entry (the show-completed-toggle work that shipped to `#todoMdViewerHeader`) added a text-pill button that reads "Show completed (2)" or similar. On mobile, that pill combined with the `TODO.md` title, "▶ Run backlog" pill, and "Sync" pill overflows the viewer header and gets clipped on the right edge — confirmed visually in the user-supplied screenshot. Replace the text-pill form with a 32×32 icon button: a checked-checkbox glyph (option C from the approved mockup — an SVG combining a rectangle outline with a checkmark inside, conveying "completed item") inside a square button with the existing purple border and corner radius, and a small floating count badge at the top-right of the button showing the number of currently-hidden completed entries. Everything else from the original toggle entry stays: state persists in `localStorage` (same key, do NOT migrate or rename), toggle defaults OFF, OFF hides every completed entry's full block (the `- [x]` line, sub-bullets, `<!-- id: ... -->` marker), ON shows everything, the file on disk is never mutated, and the pipeline agent still reads the full `TODO.md` server-side. Only the trigger's visual form changes; behavior and persistence are unchanged.
  - Behavior:
    1. The text-pill button is REMOVED from `#todoMdViewerHeader`. In its place sits a 32×32 square icon button with `border: 1px solid var(--purple)` (or whichever purple border token the previous text pill used — match it), `border-radius: 8px`, and `background: transparent` in OFF state.
    2. The icon inside is an SVG combining a rounded-rectangle outline with a checkmark inside it (the checked-box glyph from mockup option C). Stroke color matches the existing purple-light token (`var(--purple-lt)` / `#9D93EE` or whichever the rest of the viewer header uses), stroke-width approximately 2px, the glyph centered in the button at roughly 16×16 inside the 32×32 container.
    3. A small floating count badge sits at the top-right corner of the icon button: purple background (`var(--purple)`), white text, ~16px tall, `border-radius: 9px` (or full pill), min-width 16px, padding `0 4px`, font monospace, font-size ~10px, bold. The badge sits with a small negative offset (top:-5px, right:-5px) so it visually overlaps the icon button's top-right corner, with a thin border in the parent's background color (the viewer header's bg) so it reads as separated from the button's own border. Badge displays the count of currently-hidden completed entries (N).
    4. When N is 0: the badge is either dimmed (background swapped to `var(--muted-dim)`, opacity ~0.85) OR hidden entirely. Pick one and pin it via test. Suggest: hide the badge at 0 since "show completed (0)" is a no-op and a visible 0 invites confusion.
    5. ON state (toggle pressed, completed entries visible): icon button background inverts to `rgba(108, 93, 245, 0.18)` (a subtle purple-tinted fill) so the active state reads against the OFF transparent background. `aria-pressed="true"`. Icon and badge unchanged.
    6. OFF state (toggle not pressed, completed entries hidden — default): icon button background is transparent. `aria-pressed="false"`. Icon and badge unchanged.
    7. The button's `aria-label` is "Show completed (N)" or "Hide completed (N)" depending on state — keep it descriptive for screen readers since the visible glyph has no text label. The `title` attribute mirrors `aria-label` for hover tooltip on desktop.
    8. Keyboard interaction is unchanged: Tab focusable, Enter/Space activates, focus ring visible (use whatever focus treatment the rest of the viewer header's interactive elements use — grep for a `:focus-visible` rule).
    9. On mobile widths (~380px and below): the `#todoMdViewerHeader` row containing `TODO.md` title + icon button + "▶ Run backlog" + "Sync" no longer clips on the right edge. Specifically: the rightmost element ("Sync" pill) is fully visible without horizontal scroll within the viewer's container. If there's still a fit problem after the icon swap, do NOT shrink the other pills as part of this entry — flag as a follow-up. The expected outcome is that swapping the ~140px-wide text pill for a ~40px icon button (32px button + ~8px badge overhang) frees ~100px of horizontal space, which should be sufficient.
    10. On desktop widths (≥1024px): the icon button renders identically. No special desktop-only chrome.
    11. Toggle state persistence is UNCHANGED — same `localStorage` key, same default OFF, same read/write timing. The icon button's click handler does exactly what the old text pill's click handler did; only the rendered DOM differs.
  - Test-first regression set:
    1. The text-pill form is gone: no element in `#todoMdViewerHeader` has the literal string "Show completed" as visible text content (it's now only in `aria-label` / `title`).
    2. The new icon button exists in `#todoMdViewerHeader`: a `<button>` with the SVG child and the count badge child. Pin by selector — e.g. assert there's exactly one button in the header with `aria-pressed` reflecting the toggle state and an SVG child.
    3. Default state: with no `localStorage.todoapp_todoMdShowCompleted` (or whichever key the previous entry used — DO NOT change it) key set, the button's `aria-pressed === "false"` and the rendered body contains no `- [x]` lines.
    4. Badge count reflects hidden completed entries when OFF; shows the same count (or whatever the design pinned in behavior #4) when ON. Pin behavior #4's choice explicitly: at N=0, badge is `display: none` (or has CSS that hides it).
    5. Click flips state, writes to `localStorage` (same key as before — assert the key name matches the previous entry's choice; if the previous entry used `todoapp_todoMdShowCompleted`, this entry uses the same), updates `aria-pressed`, updates the rendered body, updates the badge count.
    6. Click does NOT trigger a write to `TODO.md` on disk (assert no fetch/PUT/worker dispatch to the file path).
    7. Layout regression test (best-effort in jsdom — may need to be a CSS pattern assertion): the icon button's computed width is ≤ 36px (32px + a small border/badge allowance), not the 140-ish px of the previous text pill. Pin via a getComputedStyle/getBoundingClientRect check OR via a source-pattern check on the CSS rule (button width is explicit, not auto from text content).
    8. Existing scroll-preservation behavior from the previous entry stays: clicking the icon button doesn't jump `#todoMdViewerBody` to the top.
    9. Existing accessibility from the previous entry: `aria-pressed`, keyboard-focusable, keyboard-activatable. Pin all three.
  - Implementation notes: Find the previous entry's rendering of the "Show completed" pill (grep `todoMdViewerHeader` and `Show completed` in the viewer module — likely a button created via `createElement` in `main.js` or wherever the viewer header is built). Replace the button's DOM construction:
    - Strip the text content "Show completed (N)" from being inside the button.
    - Append an SVG element as the button's child. Inline SVG is fine (no asset file needed); use the checked-checkbox glyph from the approved mockup — combine a rounded-rectangle path (the box outline, missing its top-right corner to indicate "open" for the check to enter) with a polyline path (the checkmark). Stroke `currentColor` so it inherits from the button's `color`. Width/height attributes `16` and `16`, viewBox `0 0 24 24`.
    - Append a `<span class="todoMdShowCompletedBadge">` (or similar — match existing class naming conventions in the viewer) as the button's second child. Set its text content to `N` when N > 0; hide the badge via `display: none` (or set a `data-empty` attribute and target it in CSS) when N === 0.
    - Update `aria-label` and `title` on each render to reflect the current N and the verb (Show vs Hide based on state).
    - Set CSS for `.todoMdShowCompletedButton` (or the existing button selector) to: `width: 32px; height: 32px; position: relative; padding: 0; display: inline-flex; align-items: center; justify-content: center; border: 1px solid var(--purple); border-radius: 8px; background: transparent; color: var(--purple-lt); cursor: pointer; flex-shrink: 0;`. Add `&[aria-pressed="true"] { background: rgba(108, 93, 245, 0.18); }` for the active state.
    - Set CSS for the badge: `position: absolute; top: -5px; right: -5px; background: var(--purple); color: #fff; border-radius: 9px; min-width: 16px; height: 16px; padding: 0 4px; font-family: var(--font-mono, monospace); font-size: 10px; font-weight: bold; display: inline-flex; align-items: center; justify-content: center; line-height: 1; border: 1.5px solid var(--header-bg);` — use whichever bg token the viewer header paints so the badge's border tucks the badge visually away from the button edge.
    - Reuse the EXISTING click handler from the previous entry — do NOT add a new one. The button's behavior is unchanged; only its DOM is.
    - Do NOT change the localStorage key. Do NOT change the default OFF state. Do NOT change the entry-filter logic. Do NOT change `TODO.md` on disk. Do NOT change how the pipeline reads the file.
    - If the existing test file `tests/todoMdViewerShowCompleted.test.js` (or whatever the previous entry created) asserts the text content "Show completed", update that assertion to match the new DOM (icon + badge) — but DO NOT delete the entire test file; extend it. Add new assertions for the icon button form (SVG child present, badge child present with correct text, computed width ≤ 36px).
  - Out of scope: changing the localStorage key or its persistence behavior; changing the default OFF state; changing the entry-filter parsing or the (N) calculation logic; changing the "Run backlog" pill or "Sync" pill (if there's STILL a clip after the icon swap, that's a separate follow-up — do not preemptively shrink those); changing the `TODO.md` on-disk format; changing how the pipeline agent reads the file; any change to the chat pane, the sub-band, or other unrelated viewer chrome.
  - File: whichever module owns the `#todoMdViewer` rendering (grep `todoMdViewer` to find it — same file as the previous Show-completed entry touched), `toDoList_main/src/style.css` (CSS for the icon button and badge), `toDoList_main/tests/todoMdViewerShowCompleted.test.js` (extend, do not replace)
  - Completed: 2026-06-07
  <!-- id: 8941f60e-fa1d-4b1f-8fa4-b61a74ea7206 -->

- [x] **[MEDIUM]** Swap show-completed icon to checkmark glyph, eliminate the collapse button, and relocate show-completed to that slot
  - Type: bug
  - Description: The prior "icon button with badge" entry shipped a 32×32 button with a checked-checkbox glyph and a count badge into `#todoMdViewerHeader`, but the resulting layout still feels crowded on mobile and the button sits in a sub-optimal position between the tabs and the "synced just now" text. Two paired changes: (1) swap the checked-checkbox SVG glyph for the simpler standalone checkmark glyph (option A from the original mockup — just a polyline checkmark, no surrounding rectangle), which reads as "completed" with less visual weight; (2) eliminate the collapse button currently at the far-right of `#todoMdViewerHeader` (the down-chevron / collapse-viewer toggle) and move the show-completed icon button into that slot. The button keeps its 32×32 dimensions (preserve tap target — do NOT shrink below 32px), keeps its purple border, keeps the floating count badge at the top-right corner, and keeps all behavior from the prior entry (state, localStorage key, default OFF, click handler, accessibility). Only the glyph and the DOM position change.
  - Behavior:
    1. The icon glyph inside the button is now a checkmark-only SVG: a single polyline (e.g. `points="20 6 9 17 4 12"` in a `0 0 24 24` viewBox) with `stroke="currentColor"`, stroke-width ~2.5, stroke-linecap and linejoin "round". No rectangle, no checkbox outline — just the check. Sized 16×16 inside the 32×32 button, centered.
    2. The collapse button (the down-chevron at the far right of `#todoMdViewerHeader` that toggled the viewer's collapsed state) is REMOVED — not just hidden via `display: none`, but fully removed from the DOM construction and any associated event listeners are cleaned up. If the collapsed-viewer state was persisted in localStorage under a `todoapp_` key, that key is left as-is on disk (no migration, no cleanup) so reverting this change in the future doesn't lose user state, but it's no longer read or written.
    3. The show-completed icon button is positioned in the slot the collapse button vacated — i.e. as the rightmost element in `#todoMdViewerHeader`, or wherever the collapse button used to render. Whatever neighboring chrome was to the LEFT of the collapse button (the "Sync" pill, the "▶ Run backlog" pill, etc.) stays in its current relative order; only the show-completed button moves to the far right.
    4. After the relocation, `#todoMdViewerHeader` on mobile widths (~380px and below) renders all its remaining elements without horizontal clipping. The freed space is: previously occupied by the show-completed button (left of center) AND the collapse button (right edge), now reduced to just one button on the right edge.
    5. Button dimensions stay at 32×32. Do NOT shrink to 24×24 or below — that would push the tap target below the comfortable mobile floor.
    6. The count badge stays at the top-right of the icon button with the same purple background, white text, ~16px size, and the same N-counting behavior from the prior entry (N = hidden completed entries when OFF). At N=0, the badge remains hidden as before.
    7. All other behavior from the prior entry is unchanged: localStorage key is the SAME (do not rename), default state is OFF, click flips state and persists, `aria-pressed` reflects state, `aria-label` and `title` are descriptive, keyboard interaction unchanged, file on disk is never mutated, pipeline reads the full file.
    8. The viewer's body (`#todoMdViewerBody`) is unaffected. The filter-completed-entries logic from the prior entry stays intact. Only `#todoMdViewerHeader`'s DOM changes.
    9. If the collapse button had any associated CSS rules in `style.css` (e.g. `#todoMdViewerCollapseButton { ... }` or a class-based selector), those rules are removed as part of this entry to avoid orphan CSS. Similarly any handler registrations in `main.js` or the viewer module.
  - Test-first regression set:
    1. Source-pattern: the prior entry's checked-box SVG markup is gone (no `<path d="M21 12v7...">` or whichever multi-path structure encoded the checkbox-with-tick); the new single-polyline checkmark SVG is present.
    2. Source-pattern: the collapse button's DOM construction (likely a `createElement` call producing a `<button>` with the down-chevron content) is gone. Grep `Collapse` or `collapse` in the viewer module and confirm no live references remain.
    3. Source-pattern: the show-completed button's parent insertion site has changed from its prior position (left/center of the header) to the far-right slot. Pin by either checking `Array.from(header.children).indexOf(showCompletedButton) === lastIndex`, or by asserting the button is the last child of `#todoMdViewerHeader`.
    4. The icon button is still 32×32 (computed width and height === 32px, or the CSS rule explicitly declares 32px).
    5. Behavior preservation: clicking the icon button still flips the same localStorage key (same name as the prior entry — do NOT use a new key), still updates `aria-pressed`, still re-renders the body without scroll jump, still doesn't mutate `TODO.md` on disk.
    6. Badge behavior unchanged: N reflects hidden completed entries when OFF, badge hides at N=0.
    7. Layout regression: on viewport 380px wide, all children of `#todoMdViewerHeader` are within the header's bounds (no overflow, no horizontal scroll). May be hard to verify in jsdom without a layout engine — fall back to source-pattern (assert the header's children count is one less than before, since collapse is gone) if computed bounds aren't reliable.
    8. Collapse functionality is fully gone: no element in `#todoMdViewerHeader` has the collapse behavior wired up. If a `toggleCollapsed()` function existed and was only called from the now-removed button, it can be left in place for safety OR removed; the test should NOT pin whether it's still in source, only that the button isn't.
  - Implementation notes: Three coordinated edits in the viewer module that owns `#todoMdViewerHeader`'s construction:
    - (1) Find the prior entry's button-DOM code (grep `todoMdShowCompleted` or whichever class/id the prior entry used). Replace the SVG child with the single-polyline checkmark from option A: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>`, sized 16×16. Keep the badge child unchanged.
    - (2) Find the collapse button construction (grep for it — likely a sibling `createElement` in the same function that builds `#todoMdViewerHeader`, with a class like `todoMdViewerCollapseButton` or similar). Delete the DOM creation, the event listener registration, and the appendChild that inserts it into the header. Delete any CSS rules in `style.css` that targeted only that button. If the collapse functionality is referenced from somewhere else (a keyboard shortcut, a menu item), do NOT delete those references in this entry — flag as follow-up. The current entry's scope is removing the header button only.
    - (3) Move the show-completed button's `appendChild` site to be the LAST child of `#todoMdViewerHeader`. If the existing code does `header.appendChild(showCompletedButton)` somewhere in the middle of header construction and then appends other children after it, reorder so it's last. If the existing code already appends it before the collapse button, just removing the collapse button (step 2) is sufficient — the show-completed button automatically becomes the rightmost element.
    - The localStorage key from the prior entry is preserved exactly. Do NOT use a new key, do NOT migrate. The click handler is the same handler.
    - If the existing test file `tests/todoMdViewerShowCompleted.test.js` (or whichever file the prior entry's tests live in) pins the prior button's position with an assertion like "icon button is in position N", update that assertion to "icon button is the last child of `#todoMdViewerHeader`". Do NOT delete the file; extend it.
  - Out of scope: changing the localStorage key or default state; changing the badge appearance or color; changing the button's dimensions (32×32 stays); changing any other element in `#todoMdViewerHeader` ("Sync" pill, "▶ Run backlog" pill, "Rendered" / "Raw markdown" tabs, "synced just now" status text — all unchanged); changing `#todoMdViewerBody`; changing the filter-completed-entries logic; the `TODO.md` on-disk format; the pipeline agent's behavior; restoring collapse functionality elsewhere in the UI (separate entry if desired); any change to the chat pane or other unrelated viewer chrome.
  - File: whichever module owns `#todoMdViewer` rendering (same file as the prior Show-completed entries touched), `toDoList_main/src/style.css` (remove collapse button rules, no new CSS needed for the glyph swap unless the new SVG needs different sizing), `toDoList_main/tests/todoMdViewerShowCompleted.test.js` (extend, do not replace)
  - Completed: 2026-06-07
  <!-- id: a528c484-fa06-4220-874a-3d6185ff78b6 -->
