# TODO LIST

- [x] **[MEDIUM]** Move stuckReasonText from agentView.js into agentQueueStore.js
  - Type: feature
  - Description: `stuckReasonText(row)` is the single resolver for the copy a STUCK (`failed` / `no_change`) row surfaces, but it lives in `agentView.js` — the module being retired — while two of its three consumers sit outside the board. It is a pure function over `agent_queue` row fields (`row.failure_reason`, `row.state`) with no dependency on anything else in the view, so its natural home is `agentQueueStore.js`. Move the function and its leading comment there. The move creates no new import edges and no cycle risk: `agentQueueStore.js` imports only `supabaseClient.js` and `listLogic.js`, and every consumer already imports from it. In `modals.js`, add `stuckReasonText` to the existing `agentQueueStore.js` import on line 30 and delete the `agentView.js` import on line 31 outright — that line is `modals.js`'s only dependency on the view, so this severs the edge entirely. In `main.js`, move the symbol from the `agentView.js` import block (`~101–107`) into the `agentQueueStore.js` block directly below it (`~108–115`); the `setStuckReasonResolver(stuckReasonText)` registration at `~3119` is unchanged, but its comment claiming the row layer can't import `agentView` needs rewriting — the constraint is now satisfied by the store, and `toDoRow.js`'s own comment above `setStuckReasonResolver` should be reworded the same way. In `agentView.js`, add `stuckReasonText` to its existing `agentQueueStore.js` import (the block ending at `~46`) because `buildStuckSecondary` at `~809` still calls it, and drop the "Exported so the task-row description editor…" note since the export no longer originates there. Update the comment at `modals.js:424` that describes the resolver as agentView's. Tests: `tests/mobileDescEditorStuckBlock.test.js` needs two edits — repoint its import on line 6 from `../src/agentView.js` to `../src/agentQueueStore.js`, and update the source-text assertion at `~56`, whose regex `/import\s*\{[^}]*stuckReasonText[^}]*\}\s*from\s*['"]\.\/agentView\.js['"]/` hardcodes the old specifier and will fail once `modals.js` imports from the store; retarget it to `agentQueueStore\.js` so the test keeps pinning the single-resolver guarantee rather than being loosened. The call-site assertion at `~74` matches `stuckReasonText(queueRow)` and needs no change. Also check `tests/agentStuckActions.test.js`, which imports a block from `../src/agentView.js` — if `stuckReasonText` is among those symbols it must be repointed too. This is a pure internal code move with no user-visible effect: identical strings resolve for identical rows, no surface changes. Per the changelog skip clause in `.claude/routine.md`, do not merge, prepend, or prune a changelog bullet — note "No changelog entry — change has no user-visible effect" in the PR body instead.
  - File: `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/agentView.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/main.js`, `toDoList_main/tests/mobileDescEditorStuckBlock.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 05bb249e-68b9-4630-bb22-b1b6a15af77e -->

- [x] **[HIGH]** Stamp shipped_at as a repairable invariant, not a one-shot at settle time
  - Type: bug
  - Description: `shipped_at` is null on every row in the `todos` table — 100 out of 100 sampled — while `agent_queue` holds 29 rows at `shipped`, 26 of which carry a `todo_id`. The consequence is that the derived REVIEW phase (`PHASE.ACCEPT`) rests entirely on the TODO.md marker, so every `clear all entries` wipes it; only acknowledged tasks keep state, via the `entryReviewedAt` terminal gate at `phase.js:118`. The write path is not missing and must not be rebuilt: `settleShipped` at `agentQueueStore.js:595` calls `stampShippedTodo(row.todo_id)` on line 601, which calls `listLogic.stampEntryShipped`, which sets `shippedAt`, calls `saveToStorage()`, and mirrors through `persistMutation` — and both the insert (`~3183`) and update (`~3247`) branches correctly forward `if (payload.shipped_at) row.shipped_at = payload.shipped_at`. The column exists (it returns null, not an error) and the identical path succeeds for `entry_reviewed_at` via `markEntryReviewed`. The defect is that the stamp is attempted exactly once, at settle, and its failure is permanent by design: `settleShipped` intentionally does not gate the ship on the stamp, and the reconciler only settles rows drawn from the `dispatched`/`running` in-flight set, so a row that reaches `shipped` with an unstamped todo is never revisited. Any transient failure in that one moment — most plausibly the startup backlog reconcile (`kickDispatchReconciler`) settling rows before `allProjects` has hydrated from Supabase, which makes `stampEntryShipped`'s in-memory scan return `{ok:false, error:'Todo not found.'}` — is unrecoverable. Fix by making the stamp a repairable invariant. Add a reconcile pass that runs wherever all queue rows are already loaded (`fetchAllQueueRows`, `agentQueueStore.js:242`, is the natural host since it needs no new fetch): for every row at `shipped` carrying a `todo_id` whose todo has no `shippedAt`, call `listLogic.stampEntryShipped(todo_id)`. `stampEntryShipped` is already idempotent — an already-stamped todo keeps its original time — so this is safe to run on every load and needs no separate migration for the existing 26 rows; they repair on the next pass. Gate the pass on todos actually being hydrated so it cannot re-run the same failed lookup: skip and retry on the next load rather than warning per row. Keep the ship un-gated on the stamp — that decision is correct and its comment explains why; this entry changes only whether a missed stamp is recoverable. Do NOT make the reconciler re-settle already-shipped rows to achieve this: settling has other effects (PR link resolution, `refreshQueueAndNotify`) and re-running it would be a far wider change than stamping a missing field. Do NOT touch `derivePhase` — its gates are correct and the fix is to populate the field they read. While implementing, confirm the transient cause: the failure path logs `settleShipped: failed to stamp shipped_at on todo <id>: <reason>`, so report in the PR body which reason actually fires, since the answer determines whether a follow-up entry is needed to order the boot sequence. Tests: a shipped row with a todo_id whose todo lacks `shippedAt` gets stamped by the pass; a row whose todo is already stamped is left untouched with its original timestamp; a row with a null todo_id is skipped without error; and the pass is a no-op when todos are not yet hydrated rather than stamping nothing and consuming its one chance.
  - File: `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/listLogic.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 668c8baa-beca-48b6-90bd-6c2966034a38 -->

- [x] **[MEDIUM]** Replace the per-row description chevron with the copy-title button on desktop
  - Type: feature
  - Description: On desktop the `#descToggle` `▾` chevron is dead chrome — clicking or keyboard-focusing a row already opens its description in the detail pane via `openRowInDetailPane()` (which itself drives `descToggle.click()`), so the caret is a second path to something the row already does. Meanwhile `.copyTitleBtn` is built for every committed row but only paints at ≤1023px. Retire the chevron's paint at all widths and surface the copy button in its slot on desktop.
  - Behavior: Desktop committed rows show a trailing cluster of due pill → stats toggle (recurrence rows only) → copy icon → `×`. The copy icon uses the chevron's resting treatment it replaces — `var(--text-secondary)`, `var(--bg-hover)` on hover — rather than the receded `#4a4b58` the mobile rule uses, so the meta cluster stays one visual weight with `#statsToggle`. Copy behavior is unchanged: click writes `item.tit` to the clipboard and swaps the glyph to a checkmark for ~1s. Mobile is visually untouched. The description panel still opens on row click/focus, on Ctrl+Enter (bulk), and on Ctrl+Delete (focused row).
  - Implementation notes: `#descToggle` stays in the DOM as a headless toggle — do NOT delete the element or `wireDescToggle`. It is the mechanism, not just the affordance: `openRowInDetailPane()`, `todoStatus.js`, `mobileTaskCreate.js`, `main.js`'s bulk/Ctrl+Delete paths, and the mobile tap-to-read path all call `descToggle.click()` (77 references across six files). Also keep the inline `descToggle.style.display` writes in `buildToDoRow` and the first-commit reveal — `main.js` uses `descToggle.style.display === 'none'` as its blank-placeholder guard during bulk dispatch, and that reads the inline property, not computed style. (1) In `style.css`, promote the existing `#descToggle { display: none !important; }` rule out of the `@media (max-width: 1023px)` block into default scope and delete the now-duplicated mobile copy; `!important` is required to beat the inline `display: "flex"` writes. Strip the `#descToggle::after` `▾` content rule and the `.open` rotate rule (the `.open` class itself is still set/read by JS — leave the classList calls alone). (2) In `toDoRow.js`, drop `descToggle.setAttribute("tabindex", "0")` and `setAttribute("role", "button")` so it stops being an invisible tab stop on every row; leave the `aria-label` and the Enter keydown handler harmless-but-unreachable, or remove the keydown handler too. Desktop keyboard users reach the pane through the delegated `focusin` listener on `#mainList` (`ensureDetailPaneFocusListener`) and toggle via Ctrl+Delete, so no keyboard path is lost. (3) Move `toDoChild.appendChild(copyBtn)` from its current position (before `duePill`) to sit between the `statsToggle` and `descToggle` appends. Do not add desktop `order` rules instead — the mobile block already sets explicit `order` on `#duePill`/`.copyTitleBtn`/`#statsToggle`, so mobile layout is DOM-position-independent and stays identical. Keep `.todoStatusLabel` inserted before `#descIndicator` where it is: the mobile badge's `order: 2` ties `.copyTitleBtn`'s and resolves by DOM order. (4) Flip `.copyTitleBtn`'s base `display: none` to `inline-flex` and set its base `color` to `var(--text-secondary)`; the `@media (max-width: 1023px)` reveal rule becomes redundant and can go, but keep the mobile `order` rules and the mobile-dark `color` override. (5) Update `toDoList_main/tests/mobileDescToggleHidden.test.js` — its third case asserts the desktop `#descToggle` block still carries 24×24 sizing and the `::after` `▾` glyph, which this entry deliberately removes. Rewrite that case to assert the chevron is hidden in default scope with `!important` and carries no `::after` content; keep the other four cases (they pin the still-load-bearing `descToggle.click()` routing and the inline-style placeholder guard).
  - Out of scope: Any change to mobile row chrome or the mobile copy button's receded color. Any change to the detail pane, its header, or the description panel's contents. Removing `#descToggle` from the DOM or refactoring the callers off `descToggle.click()`. Touching `#statsToggle` or `#closeButtonToDo`.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/mobileDescToggleHidden.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 7cde4937-a5c9-4b56-a37b-f9a53d5b6a92 -->

- [x] **[HIGH]** TODO.md strip stretches to fill the rail when the task list is short — Completed: 2026-07-31
  - Type: bug
  - Description: The pinned TODO.md strip grows to fill most of the queue rail when few tasks are present, with its contents floating in the vertical middle of an oversized box. `#mainBar` is `grid-template-rows: auto 1fr` — the `auto` track holds `#taskFilterBar`, the `1fr` track holds the scrollable list. The strip declares no `grid-row`, so it auto-places into the `1fr` track and stretches to fill it, and its own `align-items: center` then centres the file name and actions inside that height. With a full list the effect is hidden because the list consumes the space; with one or two rows the strip takes over the rail. This is the same unplaced-child-in-an-explicit-grid defect that has hit `#descSibling` repeatedly.
  - Behavior: The strip renders at its natural content height — one row of controls, or two when wrapped — pinned directly beneath the filter bar, regardless of how many tasks are in the list. An empty or nearly empty project shows the strip at the same compact height as a full one, with the task list and its empty state occupying the remaining space beneath it. Everything else about the strip is unchanged.
  - Implementation notes:
    - Add a third track to `#mainBar` rather than squeezing the strip into an existing one: `grid-template-rows: auto auto 1fr`, with `#taskFilterBar` and the strip in the two `auto` tracks and `#mainList` in the `1fr`. Give each child an explicit `grid-row` so nothing depends on source order or auto-placement.
    - CHECK EVERY EXISTING `grid-row` AGAINST `#mainBar` BEFORE CHANGING THE TEMPLATE. The comment notes the Agent view overlays with `grid-row: 1 / -1`, which stays correct with three tracks, but any rule using a numeric row index will silently shift. Grep every `grid-row` in the stylesheet and report which ones reference this grid.
    - The strip is `display: none` by default and only shown inside the desktop media block, so the third track must not change the mobile layout — either scope the template change to the same media block, or confirm an empty `auto` track collapses to zero height when the strip is hidden. Verify rather than assume; an `auto` track with a `display: none` child should collapse, but the row gap may not.
    - Do not fix this with `align-self: start` on the strip alone. That stops the stretch but leaves the strip sharing the `1fr` track with the list, so the list's own height calculation is still wrong — `#mainList` has `overflow-y: auto` and needs the full track to size its scroll region correctly.
    - Verify `#mainList`'s scrolling still works after the template change: with a long list it must scroll within its track rather than growing the rail, and the strip must stay pinned above it rather than scrolling away.
    - Confirm the empty-state and the ghost spacer inside `#mainList` still position correctly, since they were laid out against a `1fr` track that previously started higher.
    - Add a test asserting the strip resolves to a row other than the list's, so a future unplaced child cannot reintroduce this.
  - Out of scope: The strip's internal padding, margins, controls, and wrapped-row behavior. The click-to-open behavior and the viewer card in the detail pane. The filter bar and the sort control. The mobile layout and the bottom sheet.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 7053ca8f-6920-4f1e-8953-f61ea7b8108e -->

- [x] **[MEDIUM]** Move the working watch out of agentView.js — Completed: 2026-07-31
  - Type: feature
  - Description: `agentView.js` is down to one export worth keeping. `stuckReasonText` has already moved, and `main.js` is now the only importer, taking `renderAgentView`, `subscribeAgentView`, `unsubscribeAgentView` — all board rendering that dies with the view — plus `startAgentWorkingWatch`, which drives the nav working dot and has nothing to do with the board. Relocate that one function into `agentQueueStore.js`, beside the dispatch reconciler and the availability gate it already sits with conceptually. This is the first of three small steps to retire the view; earlier attempts bundled relocation, removal, and test cleanup into a single run and did not complete, so each step now lands on its own.
  - Behavior: Identical to today. The nav working dot lights while a triage sweep or a dispatched run is in flight and clears when neither is, with the same grace window and hard cap. It continues to work with no board mounted, as it already does. Nothing else changes — the board still renders, its subscribe/unsubscribe still run, and no call site outside the watch is touched.
  - Implementation notes:
    - Move `startAgentWorkingWatch` and everything it closes over: its module-level started flag, the poll interval handle, the poll function, and any deadline or cadence constants it reads. Leaving state behind while the function moves is how this kind of extraction silently breaks — grep every identifier the function references and move or import each.
    - `agentQueueStore.js` already hosts the dispatch reconciler and the availability gate, and the gate calls into the watch. If that call currently crosses module boundaries, this move makes it local — check for it and simplify if so.
    - Match the shape already established there: module-level started flag, one interval, no dependence on a mounted view. `startDispatchReconciler` is the reference.
    - Verify no import cycle forms. `agentQueueStore.js` must not gain anything reaching back into a view; if the watch reads something from `agentView.js`, that dependency has to move too or be inverted.
    - Update `main.js` to take `startAgentWorkingWatch` from the new location, leaving the three board imports untouched — they are removed in the next entry. Its call site at roughly `main.js:2773` (`setTimeout(startAgentWorkingWatch, 0)`) stays as it is; only the import path changes.
    - Move any test covering the watch out of `tests/agentView.test.js` into `tests/agentQueueStore.test.js`, updating its import path. Do NOT delete the rest of that file; the board is still live and its remaining tests are still valid coverage.
    - Do not "improve" the watch while moving. No renames beyond what export requires, no cadence changes, no restructuring of the grace or hard-cap logic. A pure move keeps the diff reviewable.
    - Report in the PR body: the before/after line count of both source files, which module-level state moved, how many test blocks relocated, and the suite's wall-clock runtime.
  - Out of scope: `renderAgentView`, `subscribeAgentView`, `unsubscribeAgentView`, and their call sites in `main.js` — removed in the next entry. The `#agentView` container and the view-switching wiring. Deleting `agentView.js`, its CSS, or the bulk of its test file. The watch's own logic, cadence, and settle behavior.
  - File: `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/agentView.js`, `toDoList_main/src/main.js`, `toDoList_main/tests/agentView.test.js`, `toDoList_main/tests/agentQueueStore.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 92ca938e-b67e-44f3-acb9-8d61cb6563a0 -->

- [x] **[MEDIUM]** Sever the Agent view from main.js and update the tests that assert on it — Completed: 2026-07-31
  - Type: feature
  - Description: `agentView.js` now exports nothing anyone needs — `stuckReasonText` and `startAgentWorkingWatch` have both been relocated to `agentQueueStore.js`, leaving only `renderAgentView`, `subscribeAgentView`, and `unsubscribeAgentView`, all board rendering. Sever `main.js` from the module: remove the import block, the call sites, the `#agentView` container it owns at runtime, and the view-switch handling that still accepts `'agent'`. THIS IS NOT A SMALL DIFF: roughly 31 test files reference the agent view, and while `tests/agentView.test.js` keeps passing (it imports the module directly, and the module survives this entry), any test asserting on `main.js`'s wiring — the container, the view being selectable, the switch accepting `'agent'` — will fail and must be updated as part of this work. `agentView.js` itself, its CSS, and its own test file are deleted in the next entry, so this step leaves the module orphaned but present and a failure here is recoverable.
  - Behavior: The Agent view is unreachable. Nothing navigates to it, no code path renders it, its DOM container is gone, and the view switcher no longer accepts `'agent'` — a persisted or restored `agent` value falls back to the default view rather than leaving a blank pane. Every capability it held remains reachable exactly as today: dispatching, retrying, answering a triage question, choosing a mockup variant, deriving from a rubric, reviewing proposals, and reviewing a shipped entry all happen on the row, in the detail pane, in the coverage tab, or in the mobile modal. The nav working dot still lights and clears via the relocated watch. Task rows, phases, badges, filters, Structure, the chat pane, and the queue store are unchanged.
  - Implementation notes:
    - START BY MAPPING THE TESTS, before changing any source. Grep `tests/` for `agentView`, `data-view="agent"`, `#agentView`, and `getActiveView` and classify each hit: (a) asserts on `agentView.js`'s own exports — leave alone, the module survives; (b) asserts on `main.js` wiring that this entry removes — must be updated or deleted; (c) incidental mention with no dependency — leave alone. Report the three counts in the PR body before describing the source changes.
    - `main.js` IS THE BOOT PATH. A throw there renders a blank app, and the in-app revert controls live inside the app that will not load — recovery is a GitHub PR revert. Prefer several small verified edits over one sweeping one, and run the suite between them rather than only at the end.
    - Remove the import block (around `main.js:101–105`) and every call site. Grep each of `renderAgentView`, `subscribeAgentView`, `unsubscribeAgentView` individually; line numbers shift as the file changes.
    - The activate/deactivate pair (roughly `3755`/`3756` and `3760`) sits inside a view switch. Remove the whole agent branch, not just the calls, or an empty selectable case remains.
    - `main.js:3697` and `3702` still handle `view === 'agent'`, and `2325` reads `getActiveView() === 'agent'`. All three go. Check whether `3702`'s `safe = 'agent'` is part of a fallback that sanitises a restored view value — if so, `'agent'` must resolve to the default rather than being dropped silently.
    - Remove the `#agentView` container created around `main.js:1032`, its `data-view="agent"` gating in `main.js` and `style.css`, and any `body[data-view="agent"]` rules. Confirm nothing stale remains in `viewFocusNav.js` or the arrow-key tab order.
    - If a constant or array enumerates valid views, remove the agent entry there too — a lingering value can be reached by restored state and would render nothing.
    - Update tests in category (b) to match the new reality rather than deleting them wholesale. A test asserting "switching to agent renders the board" becomes "agent is not a selectable view"; a test asserting the container exists is removed. Do NOT weaken an assertion to make it pass — if a test cannot be updated coherently, that indicates the severing lost something and it should be reported, not skipped.
    - Do NOT delete `agentView.js`, its `agent*` CSS, or `tests/agentView.test.js`. The next entry removes all three together, and keeping the module here means the sever can be verified independently of the deletion.
    - Do NOT change queue semantics. The `agent_queue` states, the reconciler, the working watch, the realtime channel, `dispatchDraft`, the `shipped_at` stamp, and `derivePhase` all stay exactly as they are. This severs a view, not a pipeline.
    - Report in the PR body: the test classification counts, every call site removed with its line, whether a view-list constant existed, how a persisted `agent` value now resolves, how many tests were updated versus removed, and the suite's wall-clock runtime.
  - Out of scope: Deleting `agentView.js`, its CSS, or `tests/agentView.test.js` — the next and final entry. The `agent_queue` table and everything downstream of it. `derivePhase`, badges, filters, and the phase vocabulary. The Structure view, the chat pane, and the coverage tab.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: ae8c7e9f-b539-48a5-b50f-1fff9f23e603 -->

- [ ] **[MEDIUM]** Retire the 11 board test files that import agentView.js
  - Type: feature
  - Description: `agentView.js` cannot be deleted while 11 test files still import it and mount the board: `agentTriageDispatch`, `agentAssignmentEditor`, `agentCoverageDetail`, `agentAssignmentCard`, `agentBucketCollapse`, `agentGitlabTick`, `agentWorkingDotSeed`, `agentMockupGen`, `agentDispatch`, `agentStuckActions`, `agentRevert`. A previous run correctly aborted the deletion on this. These test board rendering of behavior that has since moved to the row, the detail pane, the coverage tab, and the queue store, and successor suites already exist for much of it — `dispatchDraftShared`, `toDoRowDispatch`, `toDoRowMockupPane`, `claudeCoverageTab`, `claudeCoverageProposals`, `coverageDetailRepaint`, `agentDispatchReconciler`, `agentRunTrackers`. Retire the 11, but only after establishing per-behavior that the coverage survives somewhere. The risk is not breaking the build — it is silently dropping the only test for something that still ships.
  - Behavior: No test file outside `tests/agentView.test.js` imports `agentView.js`. Every behavior those files covered that STILL EXISTS in the app is covered by a test against the module that now owns it. The suite passes from a clean checkout. Nothing in `src/` changes.
  - Implementation notes:
    - Work file by file, not in bulk. For each of the 11, list its test blocks and classify each: (a) covers board rendering only — bucket ordering, card chrome, the paint loop, the in-view unavailable message, collapse state — DELETE, the behavior is gone; (b) covers behavior that moved and is already asserted in a successor suite — DELETE, and name the successor file and the test that covers it; (c) covers behavior that moved and is NOT covered anywhere — RELOCATE the block to the module's current test file, updating imports and any board-mounting setup.
    - Category (c) is the point of this entry. Do not assume a successor covers something because its name suggests it — open the successor and find the assertion. If you cannot point at a specific test, treat it as (c) and relocate.
    - `agentGitlabTick` and `agentBucketCollapse` are the likeliest to be pure (a) — GitLab commit ticking and bucket collapse were board features. `agentWorkingDotSeed` is the likeliest (c) risk: the working watch moved to `agentQueueStore.js` and `agentRunTrackers.test.js` exists, but seed-specific coverage may not have travelled with it.
    - `agentAssignmentEditor`, `agentAssignmentCard`, and `agentCoverageDetail` cover the assignment/coverage subsystem, which is live in the chat pane. `claudeCoverageTab` and `coverageDetailRepaint` exist — check them for overlap before deleting anything here.
    - A relocated test must exercise the module that owns the behavior now, not mount the board. If a block cannot be rewritten without the board, that means it was testing board rendering after all — reclassify it as (a) and say so.
    - Do NOT touch `tests/agentView.test.js`; it goes with the module in the next entry. Do NOT touch `src/` at all.
    - Do NOT weaken or skip a relocated test to make it pass. If it cannot pass against its new home, the relocation lost something and that must be reported rather than papered over.
    - Run the full suite after each file is retired, not only at the end, so a failure names the file that caused it.
    - Report in the PR body: for each of the 11, the count of blocks deleted as (a), deleted as (b) with the successor named, and relocated as (c) with the destination. Also report the suite's test count and wall-clock runtime before and after.
  - Out of scope: Deleting `agentView.js`, `tests/agentView.test.js`, or any `agent*` CSS — the next and final entry, which becomes safe once nothing but `agentView.test.js` imports the module. Any change to `src/`. Renaming surviving `agent*` classes or test files.
  - File: `toDoList_main/tests/`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 47e045a3-6ee3-4b23-9b46-647443180fc8 -->
