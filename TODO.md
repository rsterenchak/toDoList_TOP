# TODO LIST

- [ ] **[MEDIUM]** Unify project selector pattern across breakpoints; migrate 700px → 1024px
  - Type: feature
  - Description: Migrate the existing 700px responsive breakpoint to 1024px throughout the codebase, AND unify the project selector pattern so all viewport widths use the mobile pattern (workspace pill in header opens a slide-in drawer). At desktop widths (≥1024px), the persistent left-column sidebar presentation is replaced with the same pill+drawer pattern mobile already uses. The same `#sideBar` element serves both presentations via CSS — at all widths it becomes a slide-in overlay drawer, opened by tapping the workspace pill in the header. The mobile workspace pill (`#mobileProjHeader`) is revealed at desktop widths with desktop-appropriate header placement and styling. All project-switching JS continues to work unchanged — the existing tap-pill → drawer-opens flow handles both modes. After this entry ships: at all widths, the workspace pill in the header shows the active project; tapping it opens the project drawer; the main task area fills the full content width with no persistent left column.
  - Implementation notes:
    - **Key codebase reality (per the agent's prior investigation of this work area):**
      - `#sideBar` (the element `main1` in main.js) is SHARED — it serves as the desktop persistent left column AND the mobile slide-in drawer via different CSS at different breakpoints. Same DOM element, different presentations. **DO NOT DELETE IT.**
      - `#mobileProjHeader` is currently `display:none` above 700px (style.css:~6687). Comment at main.js:~2887 explicitly states: "Hidden on desktop via CSS — desktop relies on the sidebar rail/full pattern to surface the active project name." This entry changes that reliance.
      - The existing responsive breakpoint throughout style.css is 700px / 701px. This entry migrates all of those to 1023px / 1024px.
      - All project-switching logic (clicking a project name in the sidebar, swiping the mobile pill, etc.) already exists and works; this entry only changes the CSS presentation, not the JS behavior.
    - **The transformation is primarily CSS:**
      - Find every media query in `style.css` using `(max-width: 700px)` or `(min-width: 701px)`. Migrate them to `(max-width: 1023px)` and `(min-width: 1024px)` respectively. The boundary between "mobile pattern" and "desktop pattern" moves from 700px to 1024px.
      - At the new desktop breakpoint (≥1024px), change `#sideBar` from persistent-left-column to slide-in overlay drawer. Mirror the mobile pattern: `position:fixed`, transformed off-screen by default, slides in when `.sidebar-open` class is applied.
      - At the new desktop breakpoint, REVEAL `#mobileProjHeader` (remove its `display:none` rule or override). The pill should be visible in the header at desktop widths, just as it is on mobile.
      - Adjust the desktop styling of `#mobileProjHeader` so it fits the desktop header layout cleanly. The mobile styling may be centered or full-width in a way that doesn't fit alongside the existing pomodoro/music/hamburger affordances. At desktop, the pill should sit on the left of the header area (matching the desktop mockup), not occupy the full row. Use existing design tokens.
      - Adjust the main content area's CSS at desktop widths so it fills the full viewport width (no offset/margin for a sidebar that's now an overlay). Find the rules that previously offset main area for the persistent sidebar and remove or update them.
      - Below 1024px, everything stays exactly as the current mobile behavior. No observable changes to mobile UX.
    - **JS breakpoint checks (if any exist):** Search main.js and other JS files for any literal `700`, `701`, or `<= 700` / `>= 701` width checks. If found, migrate them to 1023 / 1024 to match the CSS. Likely candidates: viewport-conditional logic in `component()` or any resize listeners. If grep returns no matches, that's fine — the responsiveness is purely CSS-driven.
    - **The chat slide-up sheet is untouched.** It remains a slide-up sheet at all widths after this entry. D2 (the next entry) converts it to a right pane on desktop.
    - **What stays the same (do NOT change):**
      - `listLogic.js` — all project query and selection functions unchanged
      - Project switching JS — taps and swipes still work via the existing handlers
      - The DOM structure — `#sideBar` and `#mobileProjHeader` both still exist; nothing deleted
      - Mobile UX at widths < 1024px — identical to current behavior
      - Chat sheet (claudeSheet.js) — slide-up behavior unchanged
      - Pomodoro, music, INBOX, filter pills, status indicators, voice mic — all unchanged
    - **Critical**: do NOT delete `#sideBar`. It is the slide-in drawer element at all breakpoints after this entry. Deleting it would break project switching everywhere. Change its CSS only.
    - **Critical**: do NOT create new DOM elements for the desktop workspace pill. `#mobileProjHeader` already exists; just reveal and re-style it.
    - **Critical**: do NOT modify the TODO.md viewer.
    - **Critical**: do NOT add a two-pane chat layout. That's D2's work.
    - **Critical**: do NOT use inline JS styles for the responsive presentation. Pure CSS, media queries, and class toggles only.
    - **Critical**: the chat slide-up sheet stays unchanged at all widths. Don't preemptively convert it to a pane.
    - **Acceptance test scenarios** (the agent should verify each):
      - At ≥1024px window width:
        - Workspace pill (`#mobileProjHeader`) visible in header showing active project name
        - No persistent left column / sidebar visible
        - Main task area fills full viewport width
        - Tapping the workspace pill opens the project drawer (`#sideBar` slides in as overlay)
        - Drawer can be closed via existing close mechanisms (whatever mobile uses)
        - Project switching from the drawer works correctly
      - At <1024px window width:
        - Everything looks and works exactly as the current mobile UX (no observable change)
      - Resize across the 1024px boundary:
        - Layout transitions cleanly without broken intermediate states
        - Active project persists
    - **Test additions:**
      - (a) At width ≥1024px, `#mobileProjHeader` is visible (CSS-computed `display` is not 'none')
      - (b) At width ≥1024px, `#sideBar` is positioned as an overlay (not a persistent column) — verify via computed CSS or visual class assertion
      - (c) At width ≥1024px, opening the drawer (clicking the pill or its trigger) adds `.sidebar-open` class to `#sideBar`
      - (d) At width <1024px, all current mobile behavior is preserved
      - (e) Project switching via the drawer updates the active project correctly at both breakpoints
      - (f) No new console errors at either breakpoint
      - (g) Existing tests (1990 passing per the prior run) all continue to pass
  - Visual reference: `desktop-final-idle.svg` from the design session — the desktop layout shows no persistent left column; workspace pill sits in the header on the left, where the user taps to switch projects.
  - Out of scope: the two-pane chat layout (D2), the chat collapse/expand toggle (D3), removal of `#sideBar` entirely (it remains as the drawer element), any new DOM creation, any project-logic changes, any chat-sheet behavior changes. **Do NOT modify the TODO.md viewer.**
  - File: `toDoList_main/src/style.css`, possibly `toDoList_main/src/main.js` (only if there are JS-side breakpoint checks at 700px that need migration), `toDoList_main/tests/`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 313aed68-3476-4bd3-8c90-dead4cf9548f -->
