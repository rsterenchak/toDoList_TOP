# TODO List

## Bugs

- [x] **[HIGH]** Guard `listLogicHydrated` listener registration in main.js to fire exactly once
  - Description: After landing the single-flight guard on `hydrateFromSupabase` in `toDoList_main/src/listLogic.js`, the console diagnostic shows the function body now runs exactly once per sign-in (only one `[hydrateFromSupabase] called` log, only one dispatch log) — but the `listLogicHydrated` event listener in `toDoList_main/src/main.js` is being invoked TWICE per dispatch. First invocation logs `projects count at listener time: 4` and rebuilds the sidebar correctly; second invocation logs `projects count at listener time: 0` and wipes the sidebar via `restoreFromStorage`'s early-return-with-empty-state branch (which clears `#sideMa` and `#mainList` before bailing). Manual refresh still works because the boot path doesn't double-fire the listener. The double-invocation indicates the `document.addEventListener('listLogicHydrated', ...)` registration at the bottom of `main.js` is running twice during app boot — most likely because main.js is being loaded twice by the webpack-generated HTML (the app has four entry bundles: index, main, toDo, list), or because the module is being re-evaluated by some other import path. The deeper root cause (why main.js evaluates twice) should be a separate follow-up entry; this entry is a one-shot guard so the listener registration is idempotent regardless of how many times the module body runs. Once landed, the second invocation no longer fires, the sidebar stays populated on sign-in, and no manual refresh is needed.
  - Behavior: Sign out and sign back in via magic link in a new tab. The sidebar renders the user's projects via the first listener invocation and stays populated. The console shows `[main.js] listLogicHydrated listener fired` exactly once per sign-in, not twice. No regression in initial-boot hydration, manual refresh, or any other code path that fires `listLogicHydrated`.
  - Implementation: In `toDoList_main/src/main.js`, locate the existing module-level `document.addEventListener('listLogicHydrated', function onHydrate() { ... })` block (search for `listLogicHydrated` — there is only one match in main.js, near the bottom of the file alongside the `export { component, ... }` line). Wrap the registration in a one-shot guard using a `window`-scoped flag so a second module evaluation short-circuits before re-registering. The guard pattern: `if (typeof document !== 'undefined' && !window.__hydrateListenerRegistered) { window.__hydrateListenerRegistered = true; document.addEventListener('listLogicHydrated', function onHydrate() { /* existing body unchanged */ }); }`. Do not modify the listener body itself — only the registration wrapper. Keep the existing console.log calls inside the body in place for now (they remain useful for the follow-up entry that chases the duplicate-evaluation root cause). The `window.__hydrateListenerRegistered` flag name should be unique enough to avoid collision with any other code; if there's a project convention for these guards (e.g. an existing `__listenerRegistered` namespace on window), follow it instead.
  - Acceptance criteria: (1) Sign out and sign in via magic link in a fresh tab — sidebar populates with the user's projects and stays populated (no wipe); (2) Console shows `[main.js] listLogicHydrated listener fired` exactly once per sign-in cycle, never twice; (3) Manual refresh still works as before and the sidebar still renders correctly; (4) The flag `window.__hydrateListenerRegistered` is set to `true` after first registration (verifiable in DevTools console: `window.__hydrateListenerRegistered` returns `true` after the app has loaded).
  - Out of scope: Investigating why `main.js`'s module body evaluates twice (separate entry — likely related to the four-entry-bundle webpack config); refactoring the listener body; changing the `hydrateFromSupabase` single-flight guard landed in the prior entry; touching any other event listener registration in main.js.
  - File: `toDoList_main/src/main.js`
  - Completed: 2026-05-25

## Features

- [x] **[MEDIUM]** Re-enable drag-and-drop JSON import with redesigned full-window overlay
  - Description: The drag-and-drop import code in `exportImport.js` is fully intact (`attachDragDropImport` is exported, `#importDropOverlay` CSS is present, validation routes through the shared `importTodosFromString` pipeline with the destructive-overwrite confirm modal), but the boot-time call from `main.js` is missing — dragging a `.json` file onto the window currently does nothing. Re-wire the call alongside the other restore-from-storage hooks (passing the same `rebuildAfterImport` callback the file picker and Drive pull paths use), and redesign the overlay to a full-window dashed perimeter: replace the small centered `#importDropOverlayInner` card with an inset dashed border (`inset: 18px; border: 2px dashed var(--accent); border-radius: 8px; box-shadow: 0 0 24px var(--accent-glow)`) over a slightly darker base wash (`background: rgba(14,15,20,0.72)`), centered inside it a 44px Tabler-style file-arrow glyph (vanilla inline SVG or a unicode glyph — no new icon-font dependency), the existing "DROP JSON TO IMPORT" label in SpaceMono uppercase letterspaced, and a 12px `var(--text-secondary)` subline reading "Replaces all current projects & todos" so the destructive nature is legible before the confirm modal opens. Keep the pointer-coarse early-return (touch browsers skip drag listeners entirely), the existing `dragDepth` enter/leave counter, and the file-type guard. The overlay stays `pointer-events: none` so the window-level `drop` listener still fires.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/exportImport.js`, `toDoList_main/src/style.css`
  - Completed: 2026-05-24

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
