# TODO LIST

- [ ] **[MEDIUM]** Move stuckReasonText from agentView.js into agentQueueStore.js
  - Type: feature
  - Description: `stuckReasonText(row)` is the single resolver for the copy a STUCK (`failed` / `no_change`) row surfaces, but it lives in `agentView.js` — the module being retired — while two of its three consumers sit outside the board. It is a pure function over `agent_queue` row fields (`row.failure_reason`, `row.state`) with no dependency on anything else in the view, so its natural home is `agentQueueStore.js`. Move the function and its leading comment there. The move creates no new import edges and no cycle risk: `agentQueueStore.js` imports only `supabaseClient.js` and `listLogic.js`, and every consumer already imports from it. In `modals.js`, add `stuckReasonText` to the existing `agentQueueStore.js` import on line 30 and delete the `agentView.js` import on line 31 outright — that line is `modals.js`'s only dependency on the view, so this severs the edge entirely. In `main.js`, move the symbol from the `agentView.js` import block (`~101–107`) into the `agentQueueStore.js` block directly below it (`~108–115`); the `setStuckReasonResolver(stuckReasonText)` registration at `~3119` is unchanged, but its comment claiming the row layer can't import `agentView` needs rewriting — the constraint is now satisfied by the store, and `toDoRow.js`'s own comment above `setStuckReasonResolver` should be reworded the same way. In `agentView.js`, add `stuckReasonText` to its existing `agentQueueStore.js` import (the block ending at `~46`) because `buildStuckSecondary` at `~809` still calls it, and drop the "Exported so the task-row description editor…" note since the export no longer originates there. Update the comment at `modals.js:424` that describes the resolver as agentView's. Tests: `tests/mobileDescEditorStuckBlock.test.js` needs two edits — repoint its import on line 6 from `../src/agentView.js` to `../src/agentQueueStore.js`, and update the source-text assertion at `~56`, whose regex `/import\s*\{[^}]*stuckReasonText[^}]*\}\s*from\s*['"]\.\/agentView\.js['"]/` hardcodes the old specifier and will fail once `modals.js` imports from the store; retarget it to `agentQueueStore\.js` so the test keeps pinning the single-resolver guarantee rather than being loosened. The call-site assertion at `~74` matches `stuckReasonText(queueRow)` and needs no change. Also check `tests/agentStuckActions.test.js`, which imports a block from `../src/agentView.js` — if `stuckReasonText` is among those symbols it must be repointed too. This is a pure internal code move with no user-visible effect: identical strings resolve for identical rows, no surface changes. Per the changelog skip clause in `.claude/routine.md`, do not merge, prepend, or prune a changelog bullet — note "No changelog entry — change has no user-visible effect" in the PR body instead.
  - File: `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/agentView.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/main.js`, `toDoList_main/tests/mobileDescEditorStuckBlock.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 05bb249e-68b9-4630-bb22-b1b6a15af77e -->
