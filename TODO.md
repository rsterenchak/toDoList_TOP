# TODO LIST

- [x] **[MEDIUM]** Paint #desktopViewSubBand with --bg-base so it matches the filter band
  - Type: bug
  - Description: The view-tabs sub-band (`#desktopViewSubBand`) is currently `background: transparent` and lives under `#outerContainer`, which paints `rgb(20, 21, 27)` — the greyer chrome. The filter+sort band (`#taskFilterBar`) is also transparent but lives under `#mainBar`, which paints `var(--bg-base)` (`#0e0f14`). So the two bands look mismatched even though both are "transparent" — they inherit different parent backgrounds. The previous attempt to fix this by relocating the sub-band into `#mainBar` was correctly aborted by the pipeline agent: `#mainBar` is a CSS grid with three explicit tracks (`auto auto 1fr`), the sub-band has no assigned grid-row in that grid, and `#desktopChatPane` uses a `margin-top:-32px` + `box-shadow` hack to overhang into the row `#desktopViewSubBand` currently occupies under `#outerContainer` to keep its CHAT/RUNS tabs aligned with PROJECTS/INBOX/CALENDAR (three pinned tests: `chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, `navbarGapPaneBackground`). Moving the sub-band out breaks that alignment. Fix instead by leaving the sub-band exactly where it is and giving it an explicit `background: var(--bg-base);` so it paints the same color the filter band falls through to. Pure paint change — no DOM relocation, no grid edits, no chat-pane interaction, no layout shift. The top project-switcher row sits as a sibling of the sub-band under `#outerContainer` and is unaffected; its greyer chrome stays exactly as today.
  - Behavior:
    1. At ≥1024px: `getComputedStyle('#desktopViewSubBand').backgroundColor` equals `getComputedStyle('#mainBar').backgroundColor` equals `rgb(14, 15, 20)` (the resolved value of `--bg-base`). The view-tabs row and the filter+sort row paint identically — no visible seam.
    2. The top project-switcher row (the project pill + open/done stats row) keeps its current greyer chrome from `#outerContainer`'s `rgb(20, 21, 27)` — explicitly pinned with a computed-style assertion so a future sweep can't take it down.
    3. `#desktopViewSubBand`'s DOM parent is UNCHANGED — still a child of `#outerContainer` at `grid-row: 3`. Pin this so a future "actually move it" refactor that re-introduces the chat-pane collision is caught at test time.
    4. `#desktopChatPane`'s `margin-top: -32px` + `box-shadow` overhang stays intact and CHAT/RUNS tabs stay aligned with the view tabs — verify by leaving the three pinned chat-pane tests untouched and passing.
    5. At <1024px the sub-band is `display:none` already; no mobile change. The new `background:` declaration is harmless at mobile since `display:none` makes it inert.
  - Implementation notes: One-line CSS change in `style.css` — add `background: var(--bg-base);` to the existing `#desktopViewSubBand { ... }` rule (or whichever rule owns its desktop styling). Grep `style.css` for `#desktopViewSubBand` to find the block; there should be exactly one main rule, with the desktop-specific overrides either inline at the same selector or inside the `@media (min-width: 1024px)` block — putting the declaration on the base rule is fine since the sub-band is `display:none` at mobile. Do NOT hardcode `#0e0f14` — use the `var(--bg-base)` token already defined at `:root` (confirmed present via console: four `--bg-base: #0e0f14` definitions live in the cascade). Do NOT touch `#mainBar`, `#outerContainer`, `#desktopViewSubBand`'s DOM parent, the chat pane, the grid templates, or any chat-pane alignment CSS. Behavioral test (extends the existing band-layout test file — grep `desktopViewSubBand` in `tests/` first): assert `getComputedStyle(viewSubBand).backgroundColor === getComputedStyle(mainBar).backgroundColor` AND assert it equals the resolved value of `var(--bg-base)`; assert `viewSubBand.parentElement.id === 'outerContainer'` (DOM parentage unchanged); assert `getComputedStyle(outerContainer).backgroundColor` is preserved at its current value (top-row chrome guard). Source-pattern alone is insufficient — the prior round shipped the inverse because the test was equality-without-anchor; this round's test pins against `--bg-base` AND `#mainBar` AND parentage, so the only passing implementation is the intended one.
  - Out of scope: any change to `#desktopViewSubBand`'s DOM parent or its grid placement; any change to `#mainBar`, `#outerContainer`, or `#desktopChatPane`; any change to the top project-switcher row's chrome; the page-bg variable itself; the filter band's existing transparent background; mobile band chrome. The three pinned chat-pane tests (`chatPaneSubHeaderAlign.test.js`, `twoPaneSubHeaderPolish.test.js`, `navbarGapPaneBackground.test.js`) must remain unchanged and passing.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/taskFilter.test.js` (or the closest existing band-layout test file — grep first)
  - Completed: 2026-06-07
  <!-- id: 0c0f26f1-7287-450d-8885-7d6fac7db322 -->

- [x] **[MEDIUM]** Have #desktopViewSubBand fill its grid slack so --bg-base paints the whole row
  - Type: bug
  - Description: After painting `#desktopViewSubBand` with `var(--bg-base)`, two thin grey strips still show above and below the view tabs because the sub-band has layout slack inside its grid track and `#outerContainer`'s greyer `rgb(20, 21, 27)` paints through that slack. Confirmed by console probe: the sub-band has `margin: 16px 0 0` (16px outside the painted area at the top) and computed `height: 32px` while `#outerContainer`'s row 3 (the row the sub-band occupies — third track of `grid-template-rows: 44px 0px 48px 1087px 36px`) is 48px tall, leaving a 16px strip at the bottom where `#outerContainer`'s bg shows through. Fix by absorbing both strips into the sub-band itself: convert the 16px top margin into 16px top padding so `--bg-base` paints the strip above the view-tab buttons, and add 16px bottom padding (or set `height: 100%` and re-center the buttons) so the sub-band fills the entire 48px row. Do NOT change the row's height; do NOT change `#outerContainer`'s background; do NOT change the sub-band's DOM parent. The chat pane's `margin-top: -32px` / `box-shadow` overhang and its three pinned alignment tests (`chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, `navbarGapPaneBackground`) all key off the sub-band's row position and dimensions; this entry preserves both — only the sub-band's own internal box model shifts.
  - Behavior:
    1. At ≥1024px: there's no visible grey strip above or below the view tabs. The view-tabs row (PROJECTS / INBOX / CALENDAR) paints `--bg-base` continuously from where the top project-switcher row ends to where the filter+sort band begins.
    2. The view-tab buttons (`.viewPill`) are vertically centered in the painted band exactly where they are today — the visual position of the buttons does not shift. The buttons are currently 28px tall in a 32px sub-band with a 16px gap above; after the fix they're 28px tall in a 48px sub-band with the same effective gap from the top row (the 16px formerly outside the bg, now inside).
    3. `#desktopViewSubBand.offsetHeight` is 48px after the change (matches `#outerContainer`'s row 3 height). `getComputedStyle(sub).marginTop` is `0px`. The 16px formerly held by margin is now top padding; the additional 16px formerly grey at the bottom is now bottom padding.
    4. `#desktopChatPane`'s overhang stays correct — its CHAT / RUNS tabs remain aligned with the view tabs. Pin this by leaving the three chat-pane alignment tests untouched and passing.
    5. The top project-switcher row keeps its `rgb(20, 21, 27)` chrome from `#outerContainer` — unchanged.
    6. The filter+sort band (`#taskFilterBar`) is unaffected — still transparent, still inherits `#mainBar`'s `--bg-base`.
    7. At <1024px the sub-band is `display:none` and the change is inert.
  - Implementation notes: One rule in `style.css`, on whatever block already sets `#desktopViewSubBand`'s desktop styles (grep `desktopViewSubBand` to land on it — likely in the `@media (min-width: 1024px)` section). Change `margin: 16px 0 0` to `margin: 0` and add `padding: 16px 16px` (or `padding-top: 16px; padding-bottom: 16px;` preserving the existing `padding: 0 16px` horizontal value — actual current declaration TBD by the grep). The `background: var(--bg-base)` declaration from the prior entry stays. Do NOT touch `#outerContainer`'s `grid-template-rows`, `#mainBar`, the chat pane, or any other selector — only the sub-band's own padding/margin changes. Behavioral test: assert `getComputedStyle(sub).marginTop === '0px'`; assert `sub.offsetHeight` is at least the row height (48 in current snapshot — anchor against `getComputedStyle(outerContainer).gridTemplateRows.split(' ')[2]` so a future row-height tweak doesn't strand the assertion); assert `getComputedStyle(sub).backgroundColor === getComputedStyle(mainBar).backgroundColor` (paint anchor still holds); assert `sub.parentElement.id === 'outerContainer'` (DOM parentage unchanged — explicit guard against re-attempting the move that was correctly aborted by the agent last round); and assert the first `.viewPill` child's `getBoundingClientRect().top - sub.getBoundingClientRect().top` is within 1px of where it sits today (button vertical position preserved). Capture the current value with a one-shot probe and inline it as the baseline if needed. The chat-pane tests stay untouched — verify they still pass.
  - Out of scope: any change to `#outerContainer`, `#mainBar`, `#desktopViewSubBand`'s DOM parent or grid placement, `#desktopChatPane`, `#taskFilterBar`, the top project-switcher row's chrome, the `--bg-base` token itself, mobile band chrome, or the three pinned chat-pane alignment tests.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/taskFilter.test.js` (or whichever band-layout test file currently owns sub-band assertions — grep first)
  - Completed: 2026-06-07
  <!-- id: a299cdfc-a465-4f3c-a8df-04cc09792301 -->

- [x] **[MEDIUM]** Restore a visible boundary between #desktopChatPane and #desktopViewSubBand
  - Type: bug
  - Description: After PR #419 painted `#desktopViewSubBand` with `var(--bg-base)`, the view-tabs band and the chat pane's CHAT/RUNS tab band visually merged into one continuous bar — both surfaces resolve to `rgb(14, 15, 20)` at the same `top: 89`, and the chat pane's `box-shadow: rgb(14, 15, 20) 0px -16px 0px 0px` lift now paints the same color as the surface beneath it, erasing the seam that previously distinguished the two panes' header rows. Console probe confirms: sub-band and chat pane both at `rect.top: 89`, chat pane's first tab at `top: 93`, chat pane shadow color `rgb(14, 15, 20)`, sub-band bg `rgb(14, 15, 20)` — identical. Restore the visible boundary by giving the chat pane's lifted region a slightly different surface treatment from the sub-band's bg. The cleanest move is a 1px vertical separator between the two panes rather than a color change: a `border-left: 1px solid var(--line)` (or whichever token the rest of the chat pane uses for its left edge) on `#desktopChatPane`, OR an explicit 1px-wide bg-token-different vertical stripe in `#mainSplit`'s gutter. Do NOT change the sub-band's bg back — that re-introduces the seam between sub-band and filter band that PR #419 fixed. Do NOT change the chat pane's `margin-top: -32px` or `box-shadow` color — those are load-bearing for the CHAT/RUNS tab alignment with the view tabs and are pinned by `chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, and `navbarGapPaneBackground`. The fix is additive — a new separating element or border, not a paint swap.
  - Behavior:
    1. At ≥1024px with the chat pane open, there's a visible 1px vertical line (or equivalent visual treatment) separating the view-tabs band from the chat pane's CHAT/RUNS tab band. The two surfaces no longer read as one continuous bar.
    2. The chat pane's CHAT/RUNS tabs stay at their current `top: 93` (within 1px) — vertical alignment with the view tabs is preserved. The three pinned chat-pane tests (`chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, `navbarGapPaneBackground`) remain unchanged and passing.
    3. `#desktopViewSubBand`'s background stays `var(--bg-base)`. The contract pinned in PR #419's `subBandBackground` test stays green.
    4. The chat pane's `margin-top: -32px` and `box-shadow: 0 -16px 0 0 var(--bg-base)` are unchanged.
    5. With the chat pane collapsed/hidden, the boundary treatment is also gone — no orphan 1px line painted on an empty surface.
  - Implementation notes: Grep `style.css` for `#desktopChatPane` to find the rule block. The treatment can be either `border-left: 1px solid var(--line)` directly on `#desktopChatPane` (cleanest), or a `::before` pseudo-element on `#desktopChatPane` painting a 1px-wide colored band at `left: 0; top: 0; bottom: 0;` if the existing rule has a `border` that would conflict. Confirm by checking what `border` declarations `#desktopChatPane` already has. The `var(--line)` token is used elsewhere in the codebase (e.g. `#projectPickerDropdown`'s 1px purple border uses a token from the same family) — grep `--line` to confirm the right token, fall back to `rgba(108, 93, 245, 0.18)` if `--line` doesn't exist as a CSS variable. Source-pattern test: assert the `#desktopChatPane` rule contains the new boundary declaration. Behavioral test if jsdom can resolve it: `getComputedStyle(chatPane).borderLeftWidth` is `1px` and `borderLeftColor` is not transparent. Do NOT touch `#desktopViewSubBand`, `#mainSplit`, `#outerContainer`, `#mainBar`, or the chat pane's `margin-top` / `box-shadow`.
  - Out of scope: any change to `#desktopViewSubBand`'s background or its DOM placement; any change to the chat pane's overhang geometry; the slack strips above and below the view-tabs band (separate follow-up entry); the filter band; mobile chat-pane chrome; the chat pane's collapse/expand toggle.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/chatPaneSubHeaderAlign.test.js` (or the closest existing chat-pane visual test file — grep first; do NOT modify the three existing alignment assertions, only add the boundary one alongside them)
  - Completed: 2026-06-07
  <!-- id: 8c79a30f-126c-4c46-9db5-318830b40c1c -->

- [ ] **[MEDIUM]** Have #desktopViewSubBand fill its grid slack so --bg-base paints the whole row
  - Type: bug
  - Description: `#desktopViewSubBand` paints `var(--bg-base)` (per PR #419) but only occupies 32px of its 48px grid row in `#outerContainer`, plus has a 16px top margin that lives outside the painted area. Two grey strips (`rgb(20, 21, 27)` from `#outerContainer`'s chrome) show through above and below the view-tab buttons. Console probe confirms: sub-band `height: 32px`, `margin: 16px 0 0`, `grid-template-rows: 44px 0px 48px 1087px 36px` (row 3 = 48px = sub-band's track). Fix by absorbing both strips into the sub-band's own box: convert `margin: 16px 0 0` to `margin: 0` plus add equivalent top padding, and add bottom padding so the painted area spans the full 48px row. The view-tab buttons stay at their current visual position (the 16px gap above them is preserved as padding instead of margin, so `--bg-base` paints through it). This entry lands AFTER the chat-pane boundary entry — once a visible separator is back between the view-tabs band and the chat pane, expanding the painted area of the view-tabs band is safe: the chat pane has its own boundary treatment and can't be confused for an extension of the view-tabs band any longer.
  - Behavior:
    1. At ≥1024px: no visible grey strip above or below the view tabs. The view-tabs band paints `--bg-base` continuously from where the top project-switcher row ends to where the filter+sort band begins.
    2. The view-tab buttons (`.viewPill`) sit at the same `getBoundingClientRect().top` they do today (within 1px) — visual position preserved.
    3. `#desktopViewSubBand.offsetHeight` matches `#outerContainer`'s row 3 height (currently 48px — anchor the assertion against `getComputedStyle(outerContainer).gridTemplateRows.split(' ')[2]` rather than the literal so a future row-height tweak doesn't strand the test).
    4. `getComputedStyle(sub).marginTop` is `0px`.
    5. `#desktopViewSubBand.parentElement.id` is still `outerContainer`; `grid-row: 3` is preserved.
    6. The chat pane's CHAT/RUNS tabs stay at their current alignment with the view tabs — the three pinned chat-pane tests remain unchanged and passing.
    7. The chat-pane boundary from the previous entry stays visible — no regression.
    8. At <1024px the sub-band is `display:none`; this change is inert there.
  - Implementation notes: One rule in `style.css` on whatever block sets `#desktopViewSubBand`'s desktop styles. Change `margin: 16px 0 0` to `margin: 0` and extend the padding from the current `padding: 0 16px` to `padding: 16px 16px 16px 16px` (preserving the horizontal 16px, adding 16px vertical so the buttons stay centered in the now-taller band). The `background: var(--bg-base)` declaration from PR #419 stays. Do NOT touch `#outerContainer`'s `grid-template-rows`, `#mainBar`, `#desktopChatPane`, the chat-pane boundary added in the previous entry, or any other selector. Behavioral test: assert `getComputedStyle(sub).marginTop === '0px'`; `sub.offsetHeight` matches `#outerContainer`'s row 3 height; `getComputedStyle(sub).backgroundColor` is still `--bg-base`-resolved; `sub.parentElement.id === 'outerContainer'`; the first `.viewPill` child's offset from the sub-band's top is within 1px of its baseline value (capture-from-current-render the first time the test runs and inline it).
  - Out of scope: any change to `#outerContainer`, `#mainBar`, `#desktopViewSubBand`'s DOM parent or grid placement, `#desktopChatPane`, `#taskFilterBar`, the top project-switcher row's chrome, the `--bg-base` token itself, mobile band chrome, the three pinned chat-pane alignment tests, or the chat-pane boundary from the previous entry.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/subBandBackground.test.js` (extend the existing file rather than create a new one — the geometry assertions belong next to the paint assertions already there)
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 5f2fed3b-1334-4b1c-87b6-4e22897ce6db -->

- [ ] **[HIGH]** Raise #desktopChatPane above #desktopViewSubBand's stacking layer
  - Type: bug
  - Description: After PR #419 painted `#desktopViewSubBand` with `var(--bg-base)`, the chat pane's CHAT/RUNS tab strip (`.claudeTabGroup` at `top: 91, height: 31`) is now visually covered by the sub-band. Console probe confirms the cause: `#desktopViewSubBand` has `z-index: 9` and `position: relative`, occupying y=89→121; `#desktopChatPane` has `z-index: auto` and `position: relative`. The sub-band's `z-index: 9` beats the chat pane's auto layer, so the sub-band's `--bg-base` paint sits ON TOP of the chat pane's tab region where they overlap horizontally. The previous "boundary" entry tried to add a vertical separator on the chat pane, which didn't address the actual stacking problem and was correctly identified as ineffective. The fix is a single declaration: give `#desktopChatPane` `z-index: 10` so it stacks above the sub-band. The chat pane already has `position: relative`, so `z-index` is honored immediately. This restores the chat pane's CHAT/RUNS tabs to visibility without disturbing anything else — the sub-band's paint, the chat pane's `margin-top: -32px` overhang, the `box-shadow: rgb(14, 15, 20) 0 -16px` lift, and the alignment of CHAT/RUNS with the view tabs all remain unchanged. The visible "boundary" between the chat pane and the sub-band that was the secondary concern follows naturally: the chat pane's `.claudeTabGroup` paints `rgb(21, 21, 30)` (its own slightly-lighter chrome), so once it's no longer covered, it reads as its own distinct surface against the view-tabs band's `--bg-base`.
  - Behavior:
    1. At ≥1024px with the chat pane open, the `.claudeTabGroup` (CHAT / RUNS tabs) is fully visible — not covered, not partially covered.
    2. `getComputedStyle(chatPane).zIndex` is `'10'`.
    3. `elementsFromPoint` at the tab group's center returns `BUTTON.claudeTab` (or whichever tab is centered) at index 0, and no `#desktopViewSubBand` appears anywhere in the first 3 entries of that stack.
    4. The sub-band's own paint (`var(--bg-base)`) and z-index (`9`) are unchanged. The chat pane's `margin-top: -32px`, `box-shadow`, position, and DOM placement are all unchanged.
    5. The three pinned chat-pane tests (`chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, `navbarGapPaneBackground`) remain unchanged and passing. The CHAT/RUNS tabs still align vertically with the view tabs (the sub-band's z-9 covering them was a paint regression, not an alignment regression — the tabs were always at `top: 91` per probe, the view tabs at the same band, just buried under the sub-band's paint).
    6. If the chat pane is collapsed/hidden, the `z-index` declaration is inert (no visible content to layer).
    7. The previous "boundary" entry's `border-left` or pseudo-element (whatever shipped) should be reverted if it added visible chrome that isn't desired in this final state — confirm by checking what's currently on `#desktopChatPane`'s border declarations and removing them if they only existed for the boundary goal that this stacking fix now subsumes.
  - Test-first regression set:
    1. Source-pattern: `#desktopChatPane` rule contains `z-index: 10` (or higher, but `10` is the conventional minimum-just-above-9 value — assert the literal so a future drift catches).
    2. Behavioral if jsdom resolves it: `getComputedStyle(chatPane).zIndex` is `'10'`.
    3. Stacking probe at the tab group center using `elementsFromPoint`: returns `.claudeTab` (or whatever the actual tab element is) before any `#desktopViewSubBand`. If `elementsFromPoint` isn't reliable in jsdom, fall back to asserting the numeric z-index ordering: chat pane's z-index > sub-band's z-index.
    4. Sub-band paint unchanged: `getComputedStyle(viewSubBand).backgroundColor` still resolves to `--bg-base` (PR #419 contract preserved).
    5. Chat pane geometry unchanged: `chatPane.getBoundingClientRect().top` matches its current value within 1px; `getComputedStyle(chatPane).marginTop === '-32px'`; `getComputedStyle(chatPane).boxShadow` includes `'rgb(14, 15, 20)'` and `'-16px'`.
  - Implementation notes: Grep `style.css` for `#desktopChatPane` to find the rule block. Add `z-index: 10;` to the desktop rule (or whichever rule already has `margin-top: -32px` — keep the new declaration adjacent to the existing geometry tweaks). The chat pane already has `position: relative` per the probe (`pcs.position === 'relative'`), so `z-index` will take effect without a position change. If the previous "boundary" entry shipped a `border-left` or `::before` pseudo on `#desktopChatPane`, REVERT IT in the same change — the stacking fix subsumes the boundary goal because the tab group's own `rgb(21, 21, 30)` chrome now reads against the sub-band's `--bg-base`. Source-inspection is insufficient on its own for this kind of bug — the prior boundary entry passed source-pattern checks while leaving the actual visible defect unfixed. The regression set above pins the visible outcome (`elementsFromPoint` stacking) alongside the source.
  - Out of scope: `#desktopViewSubBand`'s background or geometry; the chat pane's overhang (margin-top, box-shadow); the slack strips above and below the view-tabs band (separate follow-up); the three pinned chat-pane alignment tests; the filter band; mobile chrome.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/chatPaneSubHeaderAlign.test.js` (or wherever the prior boundary entry's tests landed — extend that file; do NOT modify the three existing alignment assertions, only add the stacking ones)
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 0fd29be0-412e-48df-8fa2-c9da09ef831a -->
