# TODO LIST

- [x] **[MEDIUM]** Paint the 16px strip below #desktopViewSubBand with --bg-base via downward box-shadow
  - Type: bug
  - Description: After PR #419 painted `#desktopViewSubBand` with `var(--bg-base)` and the follow-up commit `b17a6372` added a `box-shadow: 0 -16px 0 0 var(--bg-base)` on the sub-band to cover the 16px margin-top gap ABOVE it, a 16px grey strip remains visible BELOW the sub-band. Console probe confirms: `#desktopViewSubBand` rect is `y: 89, height: 32` (bottom at y=121), `#outerContainer`'s row 3 is 48px tall, so 16px of slack remains at the bottom of the row. `elementsFromPoint` at 8px below the sub-band returns `#mainSec` (transparent), then `#mainSplit` (transparent), then `#outerContainer` painting `rgb(20, 21, 27)` — the greyer chrome bleeding through. Fix by mirroring the existing upward shadow with a symmetric DOWNWARD shadow on the sub-band: extend its current `box-shadow: 0 -16px 0 0 var(--bg-base)` to `box-shadow: 0 -16px 0 0 var(--bg-base), 0 16px 0 0 var(--bg-base)`. Hard-edged (no blur, no spread), additive, lives on the sub-band so it disappears with the sub-band at <1024px, and uses the same token the existing shadow uses so the two extensions match exactly. This parallels the two-shadow pattern already established on `#desktopChatPane` (which has both an upward `--bg-base` shadow for its own top-strip cover and a separate `-1px 0` shadow for the vertical separator). The box-model rewrite was correctly aborted in a prior run because it breaks `chatPaneSubHeaderAlign` — this entry preserves all sub-band geometry (`min-height: 32px`, `margin-top: 16px`) untouched and only extends the existing box-shadow declaration.
  - Behavior:
    1. At ≥1024px: no visible grey strip below the view tabs. The painted area between the project-switcher row's chrome and the filter+sort band's first content is one continuous `--bg-base` region, with the sub-band's existing upward shadow handling the top strip and the new downward shadow handling the bottom strip.
    2. `getComputedStyle(sub).boxShadow` includes BOTH `rgb(14, 15, 20) 0px -16px 0px 0px` (existing) AND `rgb(14, 15, 20) 0px 16px 0px 0px` (new). Pin both via behavioral test.
    3. `sub.offsetHeight` is unchanged at 32px. `getComputedStyle(sub).marginTop` is unchanged at `16px`. `sub.parentElement.id === 'outerContainer'` and `grid-row: 3` are unchanged. The sub-band's geometry is intentionally untouched — only its paint extends.
    4. The chat pane's existing shadow stack (`rgb(14, 15, 20) 0px -16px 0px 0px, rgba(108, 93, 245, 0.18) -1px 0px 0px 0px`) is unchanged and the three pinned chat-pane tests (`chatPaneSubHeaderAlign`, `twoPaneSubHeaderPolish`, `navbarGapPaneBackground`) remain unchanged and passing.
    5. The `(d2)` test in `subBandBackground.test.js` that pins the existing upward shadow stays green — the new downward shadow is additive, not a replacement.
    6. At <1024px the sub-band is `display:none` and the shadow is inert.
    7. The chat pane's CHAT/RUNS tab strip is unaffected. The downward shadow paints into `#mainSec`'s territory (the task pane), not into `#desktopChatPane`'s territory. Since `#mainSec`'s bg at the affected region is transparent (per probe), the new shadow simply replaces what `#outerContainer` was painting there.
  - Test-first regression set:
    1. Source-pattern: the `#desktopViewSubBand` rule's `box-shadow` declaration contains BOTH `0 -16px 0 0 var(--bg-base)` and `0 16px 0 0 var(--bg-base)` (or color-equivalent tokens). Assert both shadow segments are present.
    2. The existing upward shadow assertion in `subBandBackground (d2)` continues to pass — do NOT modify it.
    3. The sub-band's `min-height: 32px` and `margin: 16px 0 0` (or whichever current form) are preserved — pin them so a future "tidy up" doesn't quietly break the chat-pane alignment.
    4. Chat pane non-regression: `getComputedStyle(chatPane).boxShadow` is unchanged; chat pane rect top/bottom unchanged within 1px; `elementsFromPoint` at the CHAT/RUNS tab center still returns a `.claudeTab` first.
    5. Optional behavioral guard: assert `elementsFromPoint` at 8px below the sub-band's bottom returns `#mainSec` or `#mainSplit` first (not `#outerContainer`), proving a closer-stack element is now covering the strip. May not work in jsdom; if so, the source-pattern test is sufficient.
  - Implementation notes: Grep `style.css` for `box-shadow: 0 -16px 0 0 var(--bg-base)` to find the existing declaration on `#desktopViewSubBand`. Extend it in place: `box-shadow: 0 -16px 0 0 var(--bg-base), 0 16px 0 0 var(--bg-base);` — same token, mirrored vertical offset. Do NOT touch the sub-band's `min-height`, `margin`, `height`, `padding`, `grid-row`, `position`, `z-index`, or DOM placement. Do NOT touch `#desktopChatPane`, `#mainSec`, `#mainSplit`, `#outerContainer`, `#mainBar`, `#taskFilterBar`, or the chat-pane alignment tests. The `(d2)` test's existing assertion that matches the upward shadow will continue to match (it greps for the substring, which is unchanged); add a NEW assertion alongside it that pins the downward shadow segment.
  - Out of scope: any change to the sub-band's geometry (`min-height`, `margin`, `height`, `padding`); any change to `#desktopChatPane`, `#mainSec`, `#mainSplit`, `#outerContainer`, `#mainBar`, `#taskFilterBar`; any change to the top project-switcher row's chrome; the `--bg-base` token; mobile chrome; the three pinned chat-pane alignment tests; the existing `(d2)` upward-shadow assertion.
  - File: `toDoList_main/src/style.css`, `toDoList_main/tests/subBandBackground.test.js`
  - Completed: 2026-06-07
  <!-- id: eed22fd6-6b4b-4119-9051-e6cb12509177 -->

- [ ] **[MEDIUM]** Add 'Show completed (N)' toggle to TODO.md viewer, default off
  - Type: feature
  - Description: The in-app `#todoMdViewer` (header `#todoMdViewerHeader`, body `#todoMdViewerBody`) currently renders every entry in `TODO.md` including completed ones (`- [x]`), so the list grows monotonically as the pipeline ships PRs and the active backlog gets buried under shipped work. Add a "Show completed (N)" toggle to `#todoMdViewerHeader` (next to whatever header chrome is already there) that defaults OFF; when off, every completed entry is hidden from the rendered body — both the top `- [x] **[PRIORITY]** Title` line AND ALL of its nested sub-bullets (Type, Description, Behavior, Implementation notes, Out of scope, File, Completed, the `<!-- id: ... -->` marker, and any other indented continuation lines belonging to that entry). The count `N` reflects the number of completed top-level entries currently hidden. When the toggle is ON, completed entries render exactly as they do today — no change to their visual treatment. Toggle state persists across sessions via `localStorage` under the `todoapp_` prefix convention (e.g. `todoapp_todoMdShowCompleted`). Default state is OFF (i.e. completed hidden) on first load and after a localStorage clear. This is a render-side change only — `TODO.md` on disk is untouched, the pipeline agent still reads the full file (preserving institutional memory: aborted entries, design decisions, prior probes), and the `Completed: YYYY-MM-DD (PR #N)` lines stay intact for the changelog cross-reference workflow.
  - Behavior:
    1. On first load (no localStorage key present): toggle is OFF, completed entries are hidden, body renders only `- [ ]` entries and their sub-bullets. The header shows "Show completed (N)" where N is the count of hidden completed top-level entries.
    2. Clicking the toggle flips state and persists `'true'` or `'false'` to `localStorage.todoapp_todoMdShowCompleted` (or whichever exact key — match existing `prefs.js` conventions; grep `todoapp_` to pick the right pattern). The body re-renders immediately; no full page reload.
    3. When ON: every entry renders exactly as it does today. Completed entries' visual state (strikethrough or whatever's already applied to `- [x]` lines) is unchanged.
    4. When OFF: the entire range from a `- [x]` line through the LAST sub-bullet belonging to that entry (everything indented under it, including the `<!-- id: ... -->` HTML comment and the trailing blank line if present) is hidden. The next `- [ ]` or `- [x]` line marks the start of the next entry and bounds the hide range.
    5. The (N) count reflects ACTIVE state of the file (recalculated on each render), not a cached value — if a `Completed:` line is added/removed between renders, N updates.
    6. If `TODO.md` has zero completed entries, the toggle is still visible but reads "Show completed (0)" and is functionally a no-op when clicked. (Alternative: hide the toggle entirely at 0. I'd suggest keeping it visible so the affordance doesn't appear and disappear; flag if you'd prefer it auto-hidden.)
    7. Page header (`#todoMdViewerHeader`) layout doesn't reflow noticeably when the toggle's N changes from one digit to two digits — give the count a fixed-width container or right-align the toggle so the rest of the header doesn't shift.
    8. The toggle is keyboard-accessible (Tab focusable, Enter/Space activates), and has `aria-pressed` reflecting state.
    9. Existing scroll position in `#todoMdViewerBody` is preserved across toggle clicks — clicking the toggle should NOT jump to the top of the body. (If preserving exact scroll is hard, at minimum: when toggling from OFF→ON, the previously-visible top entry stays at the same y-position; when toggling from ON→OFF, the topmost still-visible entry stays at the same y-position.)
  - Test-first regression set:
    1. Default state: with no `localStorage.todoapp_todoMdShowCompleted` key set, the toggle reads as OFF (`aria-pressed="false"` or equivalent) and the rendered body contains no `- [x]` lines.
    2. Toggle persistence: clicking the toggle writes the expected string to `localStorage`; on next render with that key set to `'true'`, completed entries are visible.
    3. Hide range correctness: a completed entry's sub-bullets (Type, Description, etc.) and its `<!-- id: ... -->` marker are all hidden when the toggle is OFF. Use a fixture with at least one completed entry that has the full sub-bullet structure and one with minimal structure to cover both cases.
    4. Count correctness: with a fixture containing K completed entries, the toggle label includes `(K)`. With K=0, label is `(0)`.
    5. Re-render reactivity: after `TODO.md` content changes (simulated by feeding new markdown into the viewer), N updates without a manual reload.
    6. Pipeline non-regression: the file on disk is NOT modified by toggling. Assert that no write to `TODO.md` (via worker, via fs, via fetch PUT) is dispatched when the toggle is clicked.
    7. Accessibility: toggle has `aria-pressed` reflecting state; focus is reachable via keyboard.
  - Implementation notes: Find the current `#todoMdViewerBody` rendering function (grep `todoMdViewer` in `main.js`, `inject.js`, and any module that owns the viewer — the render likely takes the raw markdown string and produces HTML). Add a filter pass over the markdown before render: parse into entries (split on `^- \[` at the start of a line, accumulating continuation lines until the next top-level `- \[` or end-of-file), tag each entry as completed or active based on its first line's `[x]` vs `[ ]`, then conditionally drop completed entries based on the toggle state. The (N) count is `entries.filter(e => e.completed).length`. The toggle DOM goes into `#todoMdViewerHeader` — match the existing header's element style (likely a button or label; grep what's already there). For persistence, follow `prefs.js` patterns — grep for `todoapp_` key reads in `prefs.js` and use the same `get`/`set` helpers if they exist. CRITICAL: do NOT change `TODO.md` on disk and do NOT change how the pipeline reads it. The pipeline reads `TODO.md` server-side via GitHub API, NOT through the viewer's DOM — so the viewer's render-side filtering is purely cosmetic and the pipeline keeps seeing the full file with all completed entries. Add a comment in the implementation explicitly noting this so a future refactor doesn't "consolidate" the filter into a place that also affects the pipeline read path. The toggle's existence is a render-time control; the file is the source of truth.
  - Out of scope: any change to `TODO.md` on disk; any change to how the pipeline agent reads `TODO.md` (it should continue to read the full file including completed entries — preserving the institutional memory that's already saved us from regressions twice this session); a "Clear completed" button that prunes entries from the file (deliberately not included — see entry description rationale); changes to how completed entries are styled when visible; the pipeline's selection logic (which already skips completed entries by checking `- [x]` vs `- [ ]`); the `Completed:` line format; the `<!-- id: ... -->` marker format; changelog generation.
  - File: whichever module owns the `#todoMdViewer` render (grep `todoMdViewer` to find it — likely in `main.js` or a dedicated `todoMdViewer.js` module if it exists), `toDoList_main/src/style.css` (if the toggle needs new chrome), `toDoList_main/src/prefs.js` (for the localStorage key if a centralized pref helper is used), `toDoList_main/tests/` (add a new test file `todoMdViewerShowCompleted.test.js` — grep existing viewer tests first to see if there's a natural file to extend)
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 7f01fc9c-666f-408a-9af9-6bb60adc161a -->
