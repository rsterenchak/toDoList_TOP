# TODO List

## Bugs

- [x] **[LOW]** Remove diagnostic console.log calls added during auth/hydration debugging
  - Description: During the Phase 4-5 sign-in hydration bug hunt, four temporary `console.log` calls were added to instrument the hydration event flow. They served their purpose (confirming the single-flight guard on `hydrateFromSupabase` landed correctly and identifying the duplicate listener registration in main.js) and the fixes are now in place — the logs are no longer needed and clutter the browser console for every authenticated user on every sign-in. Remove them. The `[persistMutation] called:` log inside `persistMutation` and its `console.warn` error branches in `listLogic.js` should be KEPT — they remain useful as ongoing debugging hooks for the Phase 5 INSERT/UPDATE/DELETE flow and don't fire on normal idle use. Only the four hydration-specific diagnostic logs should be removed.
  - Behavior: Sign out and sign in via magic link. The browser console shows no `[hydrateFromSupabase] called`, no `[hydrateFromSupabase] dispatching listLogicHydrated event`, no `[main.js] listLogicHydrated listener fired`, and no `[main.js] projects count at listener time:` messages during the sign-in flow or on any subsequent navigation. The `[persistMutation] called:` log continues to fire on data mutations (it's not in scope for this entry). Other behavior — sidebar populating on sign-in, single listener fire, single hydration call — remains unchanged.
  - Implementation: In `toDoList_main/src/listLogic.js`, find and delete the line `console.log('[hydrateFromSupabase] called');` at the top of the `hydrateFromSupabase` function, and the line `console.log('[hydrateFromSupabase] dispatching listLogicHydrated event');` immediately before the `listLogicHydrated` dispatch. In `toDoList_main/src/main.js`, find the `document.addEventListener('listLogicHydrated', ...)` registration (now wrapped in the `window.__hydrateListenerRegistered` one-shot guard) and delete the two lines at the top of the listener body: `console.log('[main.js] listLogicHydrated listener fired');` and `console.log('[main.js] projects count at listener time:', listLogic.listProjectsArray().length);`. Do NOT remove the `console.log('[persistMutation] called:', op, table, JSON.stringify(payload));` line in `persistMutation` or any of the `console.warn('[persistMutation] ... error:', result.error);` calls — those stay. Do NOT remove the `window.__hydrateListenerRegistered` guard wrapper itself; only the two log lines inside the listener body. Do NOT remove the `console.warn('[listLogicHydrated] re-render failed:', e);` line in the catch block — that's a real-error warning, not diagnostic noise.
  - Acceptance criteria: (1) Sign-in flow produces a clean browser console with no `[hydrateFromSupabase]` or `[main.js] listLogicHydrated listener fired` / `projects count at listener time` log entries; (2) `[persistMutation] called:` logs still fire when adding/editing/completing/deleting todos; (3) `[persistMutation] insert error:` / `update error:` / etc. warnings still fire on Supabase response errors (verifiable by temporarily inducing an error, e.g. by signing out then attempting a mutation); (4) `[listLogicHydrated] re-render failed:` warning still fires if `restoreFromStorage` throws; (5) Sign-in still populates the sidebar correctly with no regression.
  - Out of scope: Removing or changing the `persistMutation` debug logs (those are kept intentionally); refactoring any of the listener bodies; touching the `window.__hydrateListenerRegistered` guard added in the prior entry; adding a debug-mode flag system to toggle logs on/off (separate consideration if console-noise becomes a recurring concern).
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/src/main.js`
  - Completed: 2026-05-25

## Features

- [x] **[MEDIUM]** Re-enable drag-and-drop JSON import with redesigned full-window overlay
  - Description: The drag-and-drop import code in `exportImport.js` is fully intact (`attachDragDropImport` is exported, `#importDropOverlay` CSS is present, validation routes through the shared `importTodosFromString` pipeline with the destructive-overwrite confirm modal), but the boot-time call from `main.js` is missing — dragging a `.json` file onto the window currently does nothing. Re-wire the call alongside the other restore-from-storage hooks (passing the same `rebuildAfterImport` callback the file picker and Drive pull paths use), and redesign the overlay to a full-window dashed perimeter: replace the small centered `#importDropOverlayInner` card with an inset dashed border (`inset: 18px; border: 2px dashed var(--accent); border-radius: 8px; box-shadow: 0 0 24px var(--accent-glow)`) over a slightly darker base wash (`background: rgba(14,15,20,0.72)`), centered inside it a 44px Tabler-style file-arrow glyph (vanilla inline SVG or a unicode glyph — no new icon-font dependency), the existing "DROP JSON TO IMPORT" label in SpaceMono uppercase letterspaced, and a 12px `var(--text-secondary)` subline reading "Replaces all current projects & todos" so the destructive nature is legible before the confirm modal opens. Keep the pointer-coarse early-return (touch browsers skip drag listeners entirely), the existing `dragDepth` enter/leave counter, and the file-type guard. The overlay stays `pointer-events: none` so the window-level `drop` listener still fires.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/exportImport.js`, `toDoList_main/src/style.css`
  - Completed: 2026-05-24

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
