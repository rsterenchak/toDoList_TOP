# TODO List

## Bugs

- [x] **[MEDIUM]** Fix unclickable Sign in button in Focus Music YouTube iframe
  - Description: When the embedded YouTube player in the Focus Music popover shows the "Sign in to confirm that you're not a bot" gate, clicking the Sign in link inside the iframe does nothing — the OAuth popup never opens, so the user can't authenticate and the player stays gated. The curated track list outside the iframe works normally, so this is scoped to clicks landing inside the YouTube embed itself. Most likely cause is a restrictive `sandbox` attribute on the iframe missing `allow-popups` and `allow-popups-to-escape-sandbox` (YouTube's sign-in flow opens `accounts.google.com` in a popup window, which a sandboxed iframe silently blocks); a secondary suspect is browser third-party cookie blocking, which would let the popup open but prevent the sign-in from persisting. Investigate the iframe element created for Focus Music in `main.js` — confirm whether a `sandbox` attribute is set and, if so, add `allow-popups allow-popups-to-escape-sandbox allow-scripts allow-same-origin allow-forms`, or remove the attribute entirely so YouTube's default permissions apply. As a fallback for users on browsers that still block the popup (or who decline third-party cookies), add a small "Open in YouTube ↗" link next to each curated track that opens the stream on youtube.com in a new tab so the user can sign in there and return.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`
  - Completed: 2026-05-09

## Features

- [x] **[MEDIUM]** Add YouTube IFrame Player music feature with visualizer button and curated lofi/ambient stations
  - Description: Add a focus-music player to the navbar's right cluster, sitting between `#pomodoroToggle` and `#settingsToggle`. The trigger is a 36×36 button whose icon is a 5-bar equalizer that animates while playing and settles flat when paused or idle. Click opens an anchored popover containing a visible 240×135 YouTube iframe (so the lofi/ambient stream artwork shows through, satisfying YouTube's TOS and giving a small ambient visual element), a station picker (curated list of ~6 starter stations + a "paste URL" entry), a play/pause primary button, a volume slider, and a "now playing" track-title line populated from `player.getVideoData()`. Audio is driven by the YouTube IFrame Player API — `<script src="https://www.youtube.com/iframe_api">` loaded lazily on first popover open, single player instance reused for the lifetime of the page. No API key, no OAuth, no backend. Persist last-station, volume, and any user-added custom stations in localStorage. Do not auto-resume on page load (mobile autoplay restrictions block it and unexpected audio is hostile). Pause the player when the Pomodoro `pomodoro-alert` body class lands so the chime isn't drowned out, and resume on acknowledgment if the user was playing before. Network-required and offline-fail are accepted compromises. Pre-roll ads on non-live content are an accepted compromise.
    - Behavior:
      1. Visualizer bars animate via CSS-only `@keyframes` (scaleY pulse, staggered delays) while the button carries `data-music-status="PLAYING"`; flatten to a static 30% scaleY when `IDLE`, `PAUSED`, or `BUFFERING`. `prefers-reduced-motion` flattens the bars unconditionally.
      2. While playing, the button picks up the accent color treatment used by `#pomodoroToggle[data-pomo-status="RUNNING"]` (accent border + accent fill text) so the right-cluster buttons read as kin.
      3. Click toggles the popover open/closed. Popover dismisses on outside click, Escape, button re-click, viewport resize, and scroll — mirror the existing `hidePomodoroPopover` plumbing for the listener attach/detach pattern, but **do not** remove the popover from the DOM on close (the iframe inside it would be destroyed and audio would stop). Hide via a CSS class instead; the popover is built once on first open and reused thereafter.
      4. Station list is a vertical list of buttons; the active station gets an accent-tinted row (mirror `.pomodoroTab.active`). Genre tag right-aligned in muted SpaceMono uppercase.
      5. Selecting a station while paused stages it but doesn't auto-play; selecting a station while playing performs a seamless swap (`player.loadVideoById(id)` for video / live, `player.loadPlaylist(playlistId)` for playlist).
      6. "Paste YouTube URL" row at the bottom of the station list opens an inline input. On Enter, parse the URL to extract videoId or playlistId, validate, prepend to the user's custom-stations list, persist, and select it. Custom stations render at the top of the picker under a "Your stations" subheader; curated stations sit beneath under a "Curated" subheader. Each custom-station row carries a `×` to remove it.
      7. Now-playing line under the iframe: poll `player.getVideoData()` on `onStateChange` (state 1 = playing) and update the displayed title + author. Empty when idle.
      8. Pomodoro coordination: subscribe to the pomodoro controller via `ensurePomodoro().subscribe(...)`. When `status` transitions into `COMPLETE_UNACKED`, capture `wasPlaying` and call `player.pauseVideo()`. On any subsequent transition out of `COMPLETE_UNACKED`, call `player.playVideo()` only if `wasPlaying` was true.
    - Implementation notes:
      - **New module** `toDoList_main/src/music.js` mirrors the shape of `pomodoro.js` and `companion.js`: `createMusic(doc)` factory returning a controller (`play`, `pause`, `setStation`, `setVolume`, `addCustomStation`, `removeCustomStation`, `subscribe`, `getState`, `destroy`), plus module-level `ensureMusic()` / `destroyMusic()` singleton accessors. State machine: `IDLE → BUFFERING → PLAYING → PAUSED → PLAYING → IDLE`. Internal `_loadIframeApi()` helper appends the YT API `<script>` to `document.head` once and resolves on the global `window.onYouTubeIframeAPIReady` callback (idempotent — subsequent calls return the same in-flight promise).
      - YT player config: `{ height: '135', width: '240', playerVars: { autoplay: 0, controls: 0, disablekb: 1, modestbranding: 1, rel: 0 }, events: { onReady, onStateChange, onError } }`. The target div lives inside the popover so the iframe is visible when the popover is open; CSS hides the popover (and therefore the iframe) when closed.
      - Curated starter list (hardcoded constant in music.js):
        - Lofi Girl — beats to relax/study to (live, `jfKfPfyJRdk`)
        - Lofi Girl — synthwave radio (live, `4xDzrJKXOOY`)
        - Chillhop Music — jazzy beats radio (live, look up videoId at curation time)
        - The Bootleg Boy — chill lofi hip hop (live, look up videoId at curation time)
        - Ambient drone — pure ambient playlist (curated playlist ID)
        - Synthwave radio — beats to chill/drive to (live, look up videoId at curation time)
      - URL parsing for the paste-URL flow: handle `youtube.com/watch?v=ID`, `youtu.be/ID`, `youtube.com/playlist?list=ID`, `youtube.com/embed/ID`, `youtube.com/live/ID`. Extract videoId (11-char alphanumeric + `-_`) or playlistId (`PL`/`OL`/`UU`/`LL` prefix + alphanumeric). Reject anything else with a brief inline error under the input.
      - localStorage key under the `todoapp_` prefix: `todoapp_music_state` for `{ activeStationId, volume, customStations: [{ id, name, kind, sourceId }] }`. Writes are best-effort (try/catch).
      - `main.js`: import from `music.js` (`ensureMusic`, plus any helper exports), add `const musicToggle = document.createElement('button')`, wire it between `pomodoroToggle` and `settingsToggle` in the navbar (`nav.appendChild` order). Functions to add mirror the pomodoro plumbing: `getMusicController()`, `syncMusicIcon()`, `hideMusicPopover()`, `showMusicPopover()`, `onMusicOutsideClick()`, `onMusicKeydown()`. Subscribe at controller-level on the post-init `setTimeout(0)` so the icon's playing/paused state stays in sync regardless of whether the popover is open.
      - The existing `margin-left: auto` lives on `#pomodoroToggle` and pushes the whole right cluster to the navbar's right edge. The new `#musicToggle` should NOT carry `margin-left: auto` — it inherits the navbar's `gap: 8px` between siblings, sitting flush against the pomodoro button.
      - **Visualizer (CSS-only, lock in the implementation correctly):** `.musicVizBars` parent gets `display: inline-flex; align-items: flex-end; height: 14px; gap: 1.5px;` (explicit height is critical — without it the children's percentage heights resolve to 0 and the bars are invisible). Each `.musicVizBars span` gets `display: block; width: 2px; background: currentColor; border-radius: 1px;` plus an explicit `height` (40% / 80% / 60% / 90% / 50% across the five bars). Animation: `@keyframes musicVizBar { 0%, 100% { transform: scaleY(0.3); transform-origin: bottom; } 50% { transform: scaleY(1.0); transform-origin: bottom; } }` with staggered `animation-delay` (0s / 0.2s / 0.4s / 0.6s / 0.8s) and 1.4s ease-in-out infinite. Animation only runs when the parent button has `data-music-status="PLAYING"` — drive via `#musicToggle[data-pomo-status="PLAYING"] .musicVizBars span { animation: musicVizBar ... infinite; }` (default state has no animation). `@media (prefers-reduced-motion: reduce)` flattens the bars to scaleY(0.6) with no animation.
      - `style.css`: add `#musicToggle` styled like `#pomodoroToggle`, the `.musicVizBars` rules above, `#musicPopover` styled like `#pomodoroPopover`, and `body:has(#musicPopover.open) #helpFab { display: none; }` (note the `.open` class — the popover is always in the DOM, so the FAB-hide rule must check the open state, not just element presence). Also add a `display: none` rule for `#musicPopover` when it lacks the `.open` class so the iframe isn't visible at rest.
      - `modals.js`: extend `isAnyModalOrPopoverOpen()` to include `document.querySelector('#musicPopover.open')` (again, presence-only is wrong for this popover). Add a "Music" topic section to `HELP_TOPICS` describing the button, the curated/custom stations split, the paste-URL flow, and the network-required nature.
      - `template.html`: no changes — the YT API script is loaded lazily by music.js, not via a top-level `<script>` tag, so initial paint isn't blocked.
      - **TOS note:** rendering the iframe visibly in the popover satisfies YouTube's embed-visibility terms. Do not hide the iframe via `display: none` or off-screen positioning while playing — the popover's visibility toggle is the right level of "visible while in use."
      - **Ad disclosure (optional but considerate):** in the help modal's Music topic, mention "live streams play continuously without interruption; non-live playlists may include YouTube ads." Sets expectation for users who hit a pre-roll on a playlist.
      - Mobile: tapping the play button on a 1px-wrapped iframe sometimes fails on iOS Safari — having the iframe visible at 240×135 inside the popover sidesteps this. The play button still must be a synchronous click handler within the user-gesture tick.
      - No new npm dependencies. The YT IFrame Player API is loaded directly from `youtube.com` at runtime.
  - File: `toDoList_main/src/music.js`, `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/src/modals.js`
  - Completed: 2026-05-07

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
