# TODO LIST

- [x] **[MEDIUM]** Remove the Calendar view, leaving Projects, Inbox, and Conceive
  - Type: feature
  - Description: Retire the unused Calendar view (a month grid of todos by due date). `calendarView.js` is a standalone module, so this is a clean deletion of the module plus its nav wiring, keyboard-nav handlers, CSS, and tests. Preserve everything the calendar merely surfaced: due dates, due-date pills, and recurring-task behavior all live in the Projects view and stay untouched — only the month-grid visualization is dropped. After this entry the tab bar carries Projects, Inbox, and Conceive; the Inbox view is removed in a separate follow-up.
  - What to remove:
    1. Module: delete `toDoList_main/src/calendarView.js` in full (this also removes its internal `buildTodayRow`, `handleTodayCheckboxToggle`, `jumpToProjectTodo`, `renderCalendarDayPanel`, `parseCalendarKey`).
    2. `main.js` imports + wiring: the `calendarView.js` import (`renderCalendarView`, `resetCalendarStateToToday`, `shiftCalendarMonth`, `formatCalendarKeyForDate`, `getCalendarSelectedKey`, `initCalendarView`); the `initCalendarView({ applyActiveView })` call at module load; the `'calendar'` branch in `applyActiveView`; `viewPillCalendar` in `viewSwitcher` and its mobile-tab-bar counterpart; the `main2.appendChild(calendarView)` line and the `calendarView` container built in `component()`; the calendar month prev/next buttons (`calendarPrevBtn`, `calendarNextBtn`) and their click handlers; the `calendarNavArrowKey` function and its two `keydown` bindings; the Calendar arrow-key re-focus block that reads `getCalendarSelectedKey()`; and the `'calendar'` branch in `firstFocusableInActiveMainView`. After `calendarNavArrowKey` is gone, no nav-order array referencing `viewPillCalendar` / `calendarPrevBtn` / `calendarNextBtn` should remain.
    3. `prefs.js`: drop `'calendar'` from the `ACTIVE_VIEW_KEY` known-token allowlist so a stored `'calendar'` value falls back to `'projects'`. Leave `'inbox'` in place — it's removed in the follow-up.
    4. `style.css`: remove the calendar selectors (`#calendarView`, `#calendarGrid`, calendar cells, `.todayRow*`, the prev/next button styles, `[data-view="calendar"]`, day-panel styles). Before deleting any class, grep it to confirm it isn't shared with Projects rows — preserve `.todoRowCard` if project rows still use it; only the `.todayRow`-specific rules go.
    5. Tests: delete the calendar tests (`calendarKeyboardNav.test.js` plus any calendar CSS-only tests under `tests/`). Don't touch the Projects-view or inbox tests.
  - Acceptance criteria:
    - `main.js` no longer imports from or references `calendarView.js`, and the module file is gone; no dangling reference to any removed symbol in source or tests.
    - The tab bar shows Projects, Inbox, and Conceive; restoring with a stale `'calendar'` pref lands on Projects.
    - Projects view unchanged (due-date pills and the recurring checkbox still work) and the Inbox view still works — its module and the `'inbox'` view path are untouched.
    - Full Vitest suite (`npm run test:run`) green.
  - Implementation notes: `main.js` is over 25k tokens — find the blocks with grep + offset/limit (the `applyActiveView` switch, `viewSwitcher` / mobile-tab construction, `main2.appendChild`, `calendarNavArrowKey`, the prev/next handlers, the arrow re-focus block, `firstFocusableInActiveMainView`), never a full read. Do NOT remove `advanceRecurringTodo` or the listLogic date helpers — the Projects view uses recurrence and due-date logic. The calendar key formatter is calendar-local, so it goes with the module; if grep shows an imported calendar symbol is referenced outside the removed calendar code, replace that use with listLogic's equivalent rather than leaving a dangling import. Tear down additively: strip the wiring, confirm green, then delete the module file and CSS. This also retires the open "first Enter on Calendar jumps focus to today" bug.
  - Out of scope: any change to the Projects view, due-date or recurrence logic, the Inbox view, the Conceive view, or the TODO.md viewer; removing the Inbox view (separate follow-up).
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`, `toDoList_main/src/calendarView.js`, `toDoList_main/tests/calendarKeyboardNav.test.js`
  - Completed: 2026-06-21
  <!-- id: d99ce555-8d9d-4ec0-bcb6-e4315093b80a -->

- [x] **[MEDIUM]** Rebind Conceive to per-project stages and remove the standalone concept store
  - Type: feature
  - Description: Reshape the Conceive tab (shipped in Run 1) from a standalone collection of concepts into a per-project lifecycle surface. Each project carries its own ordered `stages` (seeded SDLC) and a `lifecycle` label, and the Conceive view renders the *currently selected* project's stages. The standalone concept store goes away entirely — no index, no "+ New concept," no concept create/rename/delete, and no graduate-to-project step (the project already exists). Projects and Conceive become two lenses on the same selected project: its tasks, and its lifecycle thinking. The Run 1 concept data is throwaway, so `conceptsLogic.js` and its `todoapp_concepts` key are removed outright with no migration. The stacked-collapsible-section editor itself is unchanged — only its data source and header change.
  - What changes:
    1. Data model (`listLogic.js`): add `stages` (ordered `[{ id, label, body }]`, seeded with the SDLC set — Why, Concept, Requirements, Design, Build plan) and `lifecycle` (default `'SDLC'`) to the per-project entry, alongside the existing `{ id, items, color, sortByDue, target_id }`. Seed both when a project is created, and backfill them in the load-time migrate/sanitize block (any project missing `stages` gets the default SDLC list) the same way `color` / `sortByDue` are backfilled. Thread the two new fields through every place that constructs or copies a project entry so they aren't silently dropped: `editProject` (rename moves the entry to a new key — it must carry `stages`/`lifecycle`), `replaceAllProjects` (the `allProjects[name] = { ... }` rebuild), `snapshotProjects` (the export `{ name, items, color }` shape), and `toProjectRowPayload`. Add `setProjectStageBody(projectName, stageId, text)` and a `getProjectStages(projectName)` accessor, routing writes through the existing `saveToStorage` funnel (the Supabase mirror is Run 3).
    2. View (`conceiveView.js`): rewrite to render the currently selected project's stages instead of a concept list. Remove the index, the "+ New concept" action, and concept rename/delete. Resolve the selected project the same way the Projects view does — the `.selectedProject` sidebar row and its `#projInput` value — read its stages via `getProjectStages`, and render them with the existing stacked-section layout, headed by the project name plus the `lifecycle` chip. Editing a stage body autosaves through `setProjectStageBody`. When no project is selected (or none exist), show a gentle empty state ("Select a project to plan its stages").
    3. Selection wiring (`main.js`): when the selected project changes via the sidebar, refresh the Conceive view if it's the active view. Make the project-click handler's forced `applyActiveView('projects')` conditional — clicking a project while Conceive is active stays on Conceive and re-renders it for the newly selected project; clicking while any other view is active behaves exactly as today. Apply this at the canonical project-selection click handler; if a duplicate/legacy selection handler exists, reconcile both.
    4. Remove: delete `conceptsLogic.js` and its `todoapp_concepts` persistence (optionally clear the stale key on load, since the data is throwaway); delete `tests/conceptsLogic.test.js`; remove any concept-index-specific CSS in `style.css` (the index list rows and the "+ New concept" control) while keeping the stacked-section styles.
  - Behavior:
    1. Selecting a project renders its todos in the Projects list as today; if the Conceive view is open at that moment, it stays open and re-renders to that project's stages instead of switching away.
    2. Opening Conceive shows the selected project's five SDLC stages as collapsible sections with filled/empty dots, headed by the project name + SDLC chip; editing a body autosaves.
    3. With no project selected, Conceive shows the empty state and no editor.
    4. Renaming a project keeps its stages attached (they move with the entry to the new name); deleting a project removes its stages with it.
    5. Import/export and any bulk project replace round-trip `stages` + `lifecycle` without dropping them.
  - Implementation notes:
    - Project entries are keyed by name in `allProjects`; the selected project is the `.selectedProject` `#projChild`, and its name is the `#projInput` value — reuse that, don't invent a parallel selection store. `main.js` is over 25k tokens; grep the project-selection click handler(s), `applyActiveView`, and the Conceive view-append/refresh sites with offset/limit, never a full read.
    - Seed stage ids with `crypto.randomUUID()`; the ordered `stages` array serializes directly into the existing `allProjects` localStorage blob (no separate key) and maps cleanly to a future `projects.stages` jsonb column in Run 3.
    - Keep stage text inputs at `font-size: 16px+` (iOS zoom). No new dependencies. Styling stays in `style.css` — no inline style writes from the view. All project-model mutations route through `listLogic.js`.
    - Tests: replace `conceptsLogic.test.js` with `listLogic` coverage — a newly-created project seeds the five SDLC stages in order plus `lifecycle: 'SDLC'`; `setProjectStageBody` targets a stage by id; `editProject` (rename) preserves `stages`; `snapshotProjects` / `replaceAllProjects` round-trip the new fields. If any existing `listLogic` test asserts the exact project-entry shape, update it to include `stages` / `lifecycle`.
  - Out of scope: Supabase persistence/sync for project stages (Run 3 adds the `projects.stages` jsonb column and threads it through `toProjectRowPayload` + hydrate); seeding todos into a project from its Build-plan stage (later); per-stage "Ask Claude" assist (later); the lifecycle/methodology preset picker and per-project add/reorder/remove of stages (later — this entry seeds and renders the SDLC list and edits bodies only); any change to the Projects list, Inbox, the status model, or the TODO.md viewer.
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/src/conceiveView.js`, `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/src/conceptsLogic.js`, `toDoList_main/tests/conceptsLogic.test.js`, `toDoList_main/tests/listLogic.test.js`
  - Completed: 2026-06-21
  <!-- id: b0d338bc-60af-4aa5-82c8-ebe073733b2b -->

- [ ] **[MEDIUM]** Sync per-project Conceive stages to Supabase
  - Type: feature
  - Description: Wire the per-project `stages` and `lifecycle` (added in the per-project Conceive reshape) into the Supabase persistence path so lifecycle thinking syncs across devices, exactly the way project `color` / `sortByDue` already do. The schema columns are added manually first (the `alter table` above); this entry is the client-side threading only. Because stages live on the existing `projects` row, there's no new table and no RLS change — the row's existing per-user policy covers the new columns, and stages ride the project's existing reconciliation (last-write-wins on `updated_at`). All changes are in `listLogic.js`.
  - What changes:
    1. Write path — `toProjectRowPayload(entry, name, idx)`: add `stages: entry.stages || null` and `lifecycle: entry.lifecycle || 'SDLC'` to the returned row object. This one change covers every write that mirrors a project — the `replaceAllProjects` rebuild and all per-row project updates already route through it.
    2. Stage mutator — the stage-body setter from the reshape (`setProjectStageBody`): after its existing `saveToStorage` write, mirror to Supabase with `persistMutation({ op: 'update', table: 'projects', payload: toProjectRowPayload(entry, name, idx) })`, mirroring the existing color/sortByDue mutators (compute the project's index the same way they do). Going through `toProjectRowPayload` bumps `updated_at` the same way those edits do, so a stage edit wins cross-device LWW.
    3. Read path — `hydrateFromSupabase`: the projects pull already uses `select('*')`, so the columns return automatically — no query change. In the remote→local mapping (both the remote-only adopt branch and the intersection LWW-adopt branch), copy `row.stages` and `row.lifecycle` onto the entry; when `row.stages` is null (rows predating the column), seed the default SDLC stage list using the same seeding helper the local load-time backfill uses, so server-null and fresh-local behave identically.
    4. Realtime — `handleProjectsRealtime`: apply `stages` / `lifecycle` from the incoming project row onto the `allProjects` entry using the same mapping as hydrate (with the same null→SDLC backfill), so a stage edit made on another device appears live without a reload.
  - Behavior:
    1. Editing a stage on device A writes locally and mirrors to the project's Supabase row; on device B it appears on the next hydrate and live via realtime.
    2. A project pulled from Supabase with no stored stages (an existing row) backfills to the default SDLC list on hydrate — never an empty or broken Conceive view.
    3. Color, sort, rename, todos, and stages all continue to reconcile as one project row on `updated_at` (coarse per-row last-write-wins, unchanged) — near-simultaneous edits to two project-level fields across two devices resolve by whole-row recency, the same as color/sortByDue today.
  - Implementation notes:
    - This is a `listLogic.js`-only change; `select('*')` means no query edits. The file is large — grep `toProjectRowPayload`, the stage-body setter (`setProjectStageBody`), `hydrateFromSupabase`, and `handleProjectsRealtime` with offset/limit, never a full read. Do NOT add `user_id` to any todos query or payload (the schema test enforces this) and do not touch the todos path — stages live only on `projects`.
    - Keep the null→SDLC backfill in one shared helper so local-load, hydrate, and realtime all seed identically — don't duplicate the seed array.
    - Tests: update the projects-payload/schema test (`listLogicSchema.test.js`) to expect `stages` and `lifecycle` in `toProjectRowPayload`'s output; add a round-trip test using the jsdom supabase stub — a project with stages serializes through `toProjectRowPayload`, a stubbed server row's `stages` / `lifecycle` map back into `allProjects` on hydrate, and a null-stages server row backfills to the SDLC default. Keep the todos-no-`user_id` test green.
  - Out of scope: the lifecycle/methodology preset picker and per-project add/reorder/remove of stages (later); seeding todos into a project from its Build-plan stage (later); per-stage "Ask Claude" assist (later); field-level (sub-row) merge of stages vs other project fields (intentionally not done — whole-row LWW matches existing project-field behavior); any schema change beyond the two columns added manually; any change to the todos table or the Projects/Inbox views.
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/tests/listLogicSchema.test.js`, `toDoList_main/tests/listLogicStagesSync.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 68af6035-796e-419b-8cb7-5aed6c51bce8 -->
