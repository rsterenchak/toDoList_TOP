# TODO List

## Bugs

- [x] **[MEDIUM]** Surface LOCAL + DRIVE Export/Import in the mobile Settings modal as a 2×2 button grid — Completed: 2026-05-23
  - Description: All four data-transfer actions from the desktop ghost menu (Local Export, Local Import, Drive Export, Drive Import) are unreachable on mobile because `#settingsToggle` is `display: none` at the ≤700px breakpoint — the dropdown that houses them is the trigger itself. Add a new "Data" section as the FIRST section in the mobile Settings modal (above View / Appearance / About), rendered as a 2×2 button grid: top row local, bottom row Drive; columns are Export then Import. Each tile is a 56px tap-target carrying an icon (↓ / ↑ for local, ☁↓ / ☁↑ for Drive), the verb ("Export" / "Import"), and a sub-label ("to file" / "from file" / "to Drive" / "from Drive"). Beneath the grid, a single caption row preserves the stale-time signal that the desktop rows carry as right-side pills: "Last local: 2 hr ago · Last Drive: yesterday", formatted via the existing `formatRelativeExportedAt` helper reading `readLastExportedAt()` and `readLastDriveExportedAt()`. Each tile invokes the same handler the corresponding desktop menu row already invokes — `exportTodosToFile()`, `importFileInput.click()`, `exportTodosToDrive()`, `importTodosFromDrive(rebuildAfterImport)` — so no new business logic is introduced. The existing `body.driveExportInProgress` / `body.driveImportInProgress` CSS hooks already drive the dim/disabled loading state and are reused to dim the corresponding Drive tile during in-flight uploads. The desktop `#settingsMenu` is untouched.
  - Behavior:
    1. Open Settings on mobile (drawer → Settings button). The modal opens with Data as the first section, followed by View, Appearance, About.
    2. Data section renders a 2×2 grid: row 1 = Export to file / Import from file; row 2 = Export to Drive / Import from Drive.
    3. Tapping a tile invokes the same handler as the corresponding desktop menu row (no new orchestration logic added — pure DOM-only wiring).
    4. While a Drive export is in flight, the body class `driveExportInProgress` dims the "Export to Drive" tile via CSS; same for `driveImportInProgress` and the "Import from Drive" tile.
    5. The caption row below the grid reads "Last local: <relative> · Last Drive: <relative>" using `formatRelativeExportedAt`. If either value is null (never exported), that half of the caption reads "Last local: never" / "Last Drive: never".
    6. Caption is re-rendered each time the Settings modal opens so stale values can't linger across long sessions; the existing `refreshFooterExportLabel` pattern is the template for the refresh wiring.
  - Acceptance criteria:
    - Mobile Settings modal opens with Data section visible at the top — Local Export, Local Import, Drive Export, Drive Import all reachable.
    - Tapping each tile triggers the same flow the corresponding desktop menu row does: local Export downloads a JSON file, local Import opens the file picker, Drive Export runs the OAuth + multipart upload, Drive Import runs the OAuth + most-recent-file fetch.
    - In-flight Drive uploads / downloads visibly dim their corresponding tile (the existing `body.driveExportInProgress` and `body.driveImportInProgress` classes drive this).
    - Stale-time caption renders correctly on first open AND after a successful Export (local or Drive) without needing the modal to be reopened twice.
    - Desktop ≥701px behavior is unchanged — the ghost menu still renders Local/Drive sections with their right-side pills exactly as before.
  - Implementation notes:
    - The four handler functions (`exportTodosToFile`, `importFileInput.click`, `exportTodosToDrive`, `importTodosFromDrive`) are already imported into `main.js` for the desktop `showSettingsMenu` path. Reuse those references directly inside `showSettingsModal` — do not re-import or duplicate.
    - The tile builder is a new helper (e.g. `createDrawerDataTile(icon, verb, sub, onClick, extraClass)`) sitting next to `createDrawerToggleRow` / `createDrawerActionRow` in `main.js`. The `extraClass` parameter is for the `settingsModalDataTile--driveExport` / `--driveImport` anchors that the existing dim-loading CSS hooks pivot on.
    - The caption row reuses `formatRelativeExportedAt` from `exportImport.js` — already imported. The "never" fallback string should match whatever the desktop stale-export hint uses for first-run consistency.
    - Caption refresh should be triggered from the same place the About section's project-count refresh runs (modal-open hook), not from inside the per-tile click handlers — successful exports already call `refreshFooterExportLabel`, and the same `setTimeout 0` pattern can drive a new `refreshSettingsModalDataCaption` from inside the existing post-export refresh paths to update the caption while the modal is open.
    - `main.js` is over 25k tokens — navigate with grep for `showSettingsModal`, `showSettingsMenu`, `exportTodosToDrive`, `importTodosFromDrive`, `formatRelativeExportedAt`, and `createDrawerToggleRow` rather than reading the file in full.
  - Out of scope: Changing the desktop `#settingsMenu` layout, label text, or row order; adding new Drive functionality (folder picker, multi-account, two-way sync); changing the underlying OAuth scope or token cache; styling tweaks to the existing toggle / About / Help rows. The 2×2 grid is the only new visual surface this entry introduces.
  - Depends on: "Remove mobile #footBar and relocate version label into Settings modal About section" entry (MEDIUM, above) — this entry assumes the Settings modal's About / View / Appearance section structure from the bottom-gap entry is already in place.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/settingsDropdown.test.js`, `toDoList_main/tests/driveExport.test.js`, `toDoList_main/tests/driveImport.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[LOW]** Show "last exported to Drive" timestamp on Export to Drive menu row — Completed: 2026-05-22
  - Description: Mirror the existing "EXPORTED N HOURS AGO" relative-time label on the Export JSON row, but for the new Export to Drive row. After a successful Drive upload, persist a `lastDriveExportAt` ISO timestamp to localStorage, and render it on the right side of the Export to Drive menu row using the same dimmed-uppercase styling as the existing Export JSON timestamp. Use the same relative-time formatter that powers the Export JSON label so the two rows stay visually consistent (e.g., "JUST NOW", "5 MINUTES AGO", "2 HOURS AGO", "YESTERDAY", "3 DAYS AGO").
  - Behavior:
    1. Before the user has ever exported to Drive, the right side of the row is empty (no "NEVER" placeholder — match how Export JSON behaves on a fresh install).
    2. On successful Drive upload, write `lastDriveExportAt` to localStorage as an ISO 8601 timestamp.
    3. Each time the ghost popover menu opens, recompute and re-render both timestamp labels (Export JSON and Export to Drive) from their stored values so they don't stale out across long sessions.
    4. Failed Drive uploads do not update the timestamp.
    5. Importing a JSON backup does not overwrite `lastDriveExportAt` (it is a local-device fact about this device's sync state, not part of the user's data).
  - Implementation notes:
    - Reuse the existing relative-time formatter rather than duplicating it. Locate it (likely in `main.js` near the Export JSON timestamp rendering) and extract to a small shared helper if it isn't already standalone.
    - Persist `lastDriveExportAt` as a top-level localStorage key (not nested inside the todo data JSON), so it stays per-device and is not round-tripped through export/import.
    - The timestamp DOM update should be triggered from the Drive upload success handler in `driveExport.js`, calling a render function exported from `main.js` (or via a small custom event, matching whatever pattern Export JSON already uses).
  - Out of scope: showing the destination file's Drive URL or filename on the row; "last imported" timestamps; per-day export history.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/src/driveExport.js`
  - Depends on: "Add 'Export to Google Drive' option to ghost menu" entry (MEDIUM, above)
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[LOW]** Group ghost menu export/import rows under LOCAL and DRIVE section headers — Completed: 2026-05-22
  - Description: Once Export to Drive and Import from Drive land in the ghost popover menu, the four data-transfer rows (Export JSON, Export to Drive, Import JSON, Import from Drive) read as redundant when stacked flat. Reorganize them into two labeled sections — LOCAL and DRIVE — using the same dim-uppercase section-header styling already used by the HELP section further down the menu. Within each section, Export comes before Import. The row labels themselves shorten to "Export" and "Import" since the section header now provides the local/Drive context, eliminating the "Export JSON / Export to Drive" naming redundancy.
  - Behavior:
    1. Ghost popover menu opens. From top to bottom: LOCAL section header, Export row, Import row, divider, DRIVE section header, Export row, Import row, divider, Theme, Toggle floating ghost, HELP section header, Replay welcome tour, Help.
    2. Section headers (LOCAL, DRIVE) match the styling of the existing HELP header — same dimmed uppercase typography, same vertical spacing, same indentation. They are visual labels only — not clickable, not focusable, skipped by keyboard navigation.
    3. The Export row under LOCAL still shows the "EXPORTED N HOURS AGO" relative-time label on its right side, same as today.
    4. The Export row under DRIVE shows its "EXPORTED N HOURS AGO" label (per the separate "Show last exported to Drive timestamp" entry) on its right side.
    5. Keyboard navigation (arrow keys) moves through actionable rows only, skipping section headers — Export (Local) → Import (Local) → Export (Drive) → Import (Drive) → Theme → ...
    6. Clicking each row triggers the same handler it did pre-grouping — only the labels and visual grouping change, no behavior changes.
  - Implementation notes:
    - Reuse whatever the HELP section header uses today (likely a `<li>` with a `settingsMenuItem--sectionHeader` modifier class or similar). Do not introduce a second section-header style.
    - Row text changes: "Export JSON" → "Export" (under LOCAL), "Export to Drive" → "Export" (under DRIVE), "Import JSON" → "Import" (under LOCAL), "Import from Drive" → "Import" (under DRIVE). The section header disambiguates.
    - Update any tests that assert on the exact menu item text strings (likely in `tests/settingsDropdown.test.js`, `tests/driveExport.test.js`, and any import-related tests) — they will break on the label change and need to look up rows by section + position or by a stable selector rather than by literal text.
    - Update the `Export to Drive sits between Export JSON and Import JSON` assertion in `tests/driveExport.test.js` — the new ordering is Export (Local), Import (Local), Export (Drive), Import (Drive), so that test's structural claim needs to be rewritten to match the section grouping.
    - Keyboard nav: confirm the existing focus-traversal logic in `main.js` (or wherever arrow-key handling lives for the settings dropdown) already skips elements without an action handler. If it walks every `<li>`, add a `[data-skip-nav]` attribute or equivalent to the section-header elements and respect that in the traversal loop.
    - Section dividers: a horizontal rule between DRIVE and Theme keeps the existing visual rhythm. Confirm the existing menu CSS already inserts dividers between sections via `border-top` on section headers or similar, and reuse that pattern rather than adding a one-off `<hr>`.
  - Out of scope: collapsible / accordion sections, reordering Theme and Toggle floating ghost into a new section header (PREFERENCES or similar — could be a follow-up), tooltips on the shortened row labels, icons on each row.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `tests/settingsDropdown.test.js`, `tests/driveExport.test.js`
  - Depends on: "Add 'Export to Google Drive' option to ghost menu" entry (MEDIUM), "Add 'Import from Drive' option to ghost menu" entry (MEDIUM)
  - Completed: YYYY-MM-DD (PR #<number>)

## Features

- [x] **[MEDIUM]** Add selectable ambient visualizer to focus music popover for when YouTube video is blocked
  - Description: When YouTube video is blocked on corporate networks the audio still plays but the 240×135 iframe shows nothing, leaving the popover visually inert. Add a generative ambient visualizer that occupies the iframe's footprint while music is playing, toggled by a new checkbox in the popover and styled by a dropdown next to it. Important constraint: the visualizer CANNOT react to the YouTube audio waveform — YouTube iframes don't expose their audio stream to Web Audio API across origins, so any analyser-driven design is impossible without abandoning the YouTube embed. The five visualizer styles are decorative loops on fixed timings, not audio-reactive: STARFIELD (slow horizontal star drift over a deep purple void, most "Void theme"), BLOBS (three slow-drifting blurred lavender/purple blobs on long sine paths, lava-lamp feel), RINGS (concentric purple rings expanding from a central core on a 4-second cycle), BARS (16 staggered equalizer-style bars with offset CSS keyframes — looks the most "music visualizer" while being decorative), GHOST (companion ghost mascot bobs gently with music notes rising past, ties to the existing ghost SVG). Picker layout follows PICK-A from the mockup round: a single row below the station list containing the checkbox (left), a STYLE label, and a native `<select>` dropdown listing the five styles by name. New row's vertical position sits between the curated stations list and the Pause + slider controls row so the popover keeps its existing top-down rhythm. New module `musicVisualizer.js` mirrors `music.js`'s singleton pattern: exports `ensureVisualizer()` (mount into the iframe-footprint container), `destroyVisualizer()` (tear down, stop all animations), `setVisualizerStyle(styleId)` (swap the active style without remounting the container). The iframe and visualizer share the same wrapper element with absolute-positioned children — visualizer on top with a higher z-index when enabled, iframe behind. When the user unchecks the box, the visualizer detaches and the YouTube iframe is revealed again (useful at home where video isn't blocked). Critical to honor the prior visualizer-failure root cause noted in past notes: any CSS-only bar visualizer (BARS option) MUST set an explicit parent height on the visualizer wrapper because flex-end alignment of percentage-height bars collapses to zero against an unsized parent. Set `height: 100%` on the wrapper and let aspect-ratio: 16/9 on the iframe container drive the actual pixel height. Persistence: two new preferences in `prefs.js` — `todoapp_musicVisualizerEnabled` (boolean, defaults to false so first-time users keep current behavior) and `todoapp_musicVisualizerStyle` (string, one of "starfield" / "blobs" / "rings" / "bars" / "ghost", defaults to "starfield"). Visualizer only runs when music is actively playing — when the user pauses, animations pause too (set `animation-play-state: paused` on the active style's root element, no need to tear down). When the user resumes, animations resume from where they left off. Visualizer also bails on `prefers-reduced-motion: reduce` even when the checkbox is on — substitute a static frame (single rendered snapshot of the chosen style, no animation) so the popover doesn't look broken. Tests: each style mounts a root element with the expected class (e.g., `.musicViz--starfield`), the checkbox writes the pref and toggles visualizer presence, the dropdown writes the style pref and swaps the active style without unmounting the wrapper, prefers-reduced-motion swaps the animated style for a static fallback.
  - File: `toDoList_main/src/musicVisualizer.js` (new), `toDoList_main/src/music.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/musicVisualizer.test.js` (new)
  - Completed: 2026-05-21
     
- [x] **[MEDIUM]** Add slide-out fade completion animation to todo row checkboxes — Completed: 2026-05-22
  - Description: Clicking the row checkbox to mark a todo complete currently produces no completion animation — the row's "done" styling (strikethrough title, muted color) applies instantly, which reads as no feedback at all and makes the interaction feel flat. Add a CSS-driven slide-out fade animation (ANIM-D from the mockup round): on click, the row briefly translates ~40px to the right while fading to ~40% opacity and the title strikethrough draws in, then settles back to its final position in the standard "done" appearance. Total duration ~280ms. The animation runs in parallel with the underlying data mutation — `listLogic.toggleToDo` (or whatever the existing toggle path is) fires immediately on click without waiting on the animation, so the persisted state and the visual completion stay in sync even if the user navigates away mid-animation. Wire by adding a transient class (e.g., `.todoCompleting`) to the row element on the click handler that triggers the toggle from open → done; the class drives a CSS `@keyframes` rule that animates `transform: translateX()` and `opacity` from 0 → 40px / 100% → 40% across the first ~140ms, then back to 0 / 100% across the remaining ~140ms while the row's actual `.done` class lands. Remove the transient class via an `animationend` listener so subsequent renders don't re-fire it. Animation applies in three places — the project view `#mainList` rows, the `.todayRow.todoRowCard` rows in the Today view, and the same row style in the Calendar day-detail panel — all three already share the row markup, so a single CSS rule scoped to `.todoCompleting` covers all three surfaces. Animation only fires on the open → done transition, NOT on done → open (un-completing); un-completing applies its end state instantly so users who misclick and immediately fix it don't watch the row dance backwards. Honor `prefers-reduced-motion: reduce` — wrap the keyframes block in `@media (prefers-reduced-motion: no-preference)` so the animation is skipped entirely for users who've opted out, and the row just snaps to its done state as it does today. Test pins: clicking an open row's checkbox adds `.todoCompleting` and removes it after `animationend`; clicking a done row's checkbox does NOT add the class; with `prefers-reduced-motion: reduce` matched, the keyframes rule is not in the active stylesheet (assert via media query gate or by checking computed `animation-name` on `.todoCompleting` resolves to `none`); the data layer's toggle method fires synchronously on click regardless of animation state.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/todoCompletionAnimation.test.js` (new)
  - Completed: 2026-05-22

- [x] **[MEDIUM]** Add "Export to Google Drive" option to ghost menu — Completed: 2026-05-22
  - Description: Add a new "Export to Drive" menu item directly below the existing "Export JSON" row in the ghost popover menu. Clicking it triggers a Google OAuth 2.0 flow (via Google Identity Services, using PKCE) prompting the user to sign in and authorize the app with the non-sensitive `drive.file` scope, then uploads the same JSON payload that the existing Export JSON produces to the user's Drive root via a multipart POST to `https://www.googleapis.com/upload/drive/v3/files`. On success, show a toast matching the existing toast styling with an "Open in Drive" link to the uploaded file's `webViewLink`. Re-prompt OAuth on each session — do not persist the access token to `localStorage`. Reuse the existing export filename pattern (`todoapp-export-YYYY-MM-DD.json`) and the same JSON serialization path so both export methods produce byte-identical files.
  - Behavior:
    1. User opens ghost popover menu, sees "Export to Drive" as a new row below "Export JSON".
    2. First click in a session opens Google sign-in popup; subsequent clicks in the same session reuse the in-memory token until it expires (~1 hour), at which point re-auth runs silently if possible, otherwise re-prompts.
    3. While the upload is in flight, the menu row shows a subtle loading state (text dim + spinner) and is disabled to prevent double-uploads.
    4. On success: toast "Exported to Drive" with "Open in Drive" link (opens `webViewLink` in a new tab).
    5. On failure (denied consent, network error, API error): toast with a clear error message; no partial state left behind.
  - Implementation notes:
    - Requires a Google Cloud project with an OAuth 2.0 Client ID configured for the GitHub Pages origin. Document the setup steps in a comment block at the top of the new driveExport module so a fresh fork knows what to provision.
    - Use the Google Identity Services library loaded lazily on first click via a `<script>` tag injection (mirrors the lazy-load pattern from the music player's YouTube IFrame API), not bundled with Webpack — avoids a hard dependency on a Google CDN at build time.
    - Use the `drive.file` scope only (not `drive`) so the app stays in the non-sensitive scope tier and avoids Google's security review process.
    - Extract the JSON serialization logic from the existing Export JSON handler into a shared helper so both code paths produce identical output.
    - Build the OAuth client ID into a module-level constant; do not hardcode in `main.js`.
  - Out of scope: folder picker (always dumps to Drive root), token persistence across sessions, two-way sync, import-from-Drive (separate future entry), accounts/multi-user.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/src/driveExport.js`
  - Completed: YYYY-MM-DD (PR #<number>)

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
