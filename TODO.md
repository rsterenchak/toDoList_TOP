# TODO List

## Bugs

- [x] **[MEDIUM]** Fix project row drag failing when clicking empty space after short titles
  - Description: On projects with short titles (e.g. "Project 1"), clicking in the empty area to the right of the title text and attempting a drag-reorder does nothing — the drag never initiates. The entire project row should act as a drag handle regardless of title length. Root cause: `#projInput` uses `width: 100%` and fills the full `1fr` grid column of `#projChild`, so the visually-empty space after a short title is actually inside the input element. Once a user clicks an already-selected project (which sets `titleInput.style.pointerEvents = "auto"` in the project click handlers in `main.js`), that state is never reset — the `blur` handlers only reset `cursor`, not `pointerEvents`. From then on, mousedowns in the empty area of the input are captured by the input instead of `#projChild`, blocking `dragstart`. Fix by either re-locking `pointerEvents` back to `"none"` on blur, switching to a CSS-class-driven edit mode so the default pointer-events state is reliable, or wrapping the title in a non-stretching element so `#projChild` owns the trailing empty space directly.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[LOW]** `projectLength` should return 0 for null or undefined project names
  - Description: `projectLength(project)` in `listLogic.js` currently guards against falsy input with `if (!project || !allProjects[project]) return 0;`, which is fine — but the function's behavior when passed a non-string value (e.g., `projectLength(42)` or `projectLength({})`) is undefined. In practice nothing in the app calls it with non-string values today, but the guard is permissive enough to let weird inputs through and return `undefined` or throw depending on what `allProjects[project]` does with them. Tighten the guard so the function returns 0 for any input that isn't a non-empty string. Preserve existing behavior for valid string inputs. Add a regression test covering non-string inputs (number, object, array, boolean) alongside the existing null/undefined cases.
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/tests/listLogic.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)

## Features

- [ ] **[MEDIUM]** Replace inline M/D/Y date chip with a pill-triggered calendar popover
  - Description: Swap the current triple-input M/D/Y date chip (and its three quick-date shortcut buttons) on each todo row for a single pill-shaped button that opens an anchored month-view calendar popover. **Match the app's existing aesthetic exactly** — the VOID dark theme with its cool-neutral light counterpart, SpaceMono monospace typography for dates and small-caps labels (uppercase, letter-spaced), the 0.5px borders used throughout the app, `var(--radius-sm)` / `var(--radius-md)` corner radii, the purple accent (`var(--accent)` / `var(--accent-dim)` / `var(--accent-text)`), the warning/urgent color ramps already defined (`var(--text-warning)` for due-soon, `var(--text-urgent)` for overdue), and the subtle `box-shadow: 0 6px 18px rgba(0, 0, 0, 0.5)` / modal-style surface treatment from `#projContextMenu` and the changelog modal. The popover should feel like it belongs to the same family as those two components, not like a third-party date picker shoehorned in. Every color, border, font, and spacing value comes from the existing CSS custom property tokens in `:root` — no new hex values, no new font stacks. The pill reads "Set date" with a small calendar glyph when empty (muted secondary text); when a date is set it reads absolutely as "Apr 30" in normal states, switches to relative ("Due in 2d", "3d overdue") when the existing due-soon (≤3 days) or due-overdue thresholds from `applyDueUrgency` fire, and carries the same warning/urgent color treatment the chip does today by reusing the `.due-soon` / `.due-overdue` classes on `#toDoChild`. Clicking the pill opens a popover floated next to it with a small arrow pointing back to the trigger (CSS-drawn, same surface fill and border as the popover itself — mirror the arrow pattern from elsewhere in the app); the popover contains a month header with previous/next arrows (match the caret treatment from `.completedCaret`), a 7-column weekday grid with single-letter S M T W T F S labels in SpaceMono uppercase muted text, date cells for the visible month plus leading/trailing greyed-out neighbors, today's cell marked with a ring outline using `box-shadow: 0 0 0 1px var(--accent)` (no fill so it's distinguishable from selection), the currently-selected date filled with `var(--accent)` and white numerals, a compact strip of Today / Tomorrow / +1w quick-date chips above the calendar styled identically to the existing `.quickDateBtn` class (reuse it directly, don't fork), and a subtle "Clear" text button at the bottom in muted secondary color with a dotted-underline hover matching the footer version label's treatment. Cell hover uses `var(--bg-hover)`; keyboard-focused cell uses a dashed `var(--accent-dim)` outline distinct from the selected fill. Clicking a date writes through to the existing `item.due` "M-D-YYYY" storage format via the same path `wireDateInputs` and `setRowDateOffset` take today (so `applyDueUrgency`, persistence, and the footer counter all keep working), closes the popover, and updates the pill label. The popover dismisses on outside click, Escape, and on selection (mirror the dismissal pattern in `showProjectContextMenu` with capture-phase listeners); supports keyboard navigation (arrow keys move day-by-day across weeks and months, Enter selects, Escape closes) with the currently-focused cell visually distinguished from the selected cell. On mobile the popover stays anchored rather than becoming a bottom sheet — the app scales down cleanly already and a bottom sheet is extra complexity; do ensure the pill and popover inherit the 16px font-size-minimum inside inputs that the mobile media query enforces so iOS Safari doesn't auto-zoom. In light mode (`:root[data-theme="light"]`) every surface, border, text, and accent token automatically retargets because you're using variables throughout; verify the popover reads cleanly in both themes without any mode-specific overrides. Preserve the existing row-level hide rule: blank placeholder rows still hide the pill and its popover affordance until the title is committed, exactly as the current chip behaves. Scope: replace the `#dueInput` chip (including `#dateText`, `#month`, `#day`, `#year`, `#dash` siblings) and the `#quickDates` strip with the new pill and popover in `buildToDoRow`; remove the now-dead CSS for those IDs and add styling for the pill's four states (empty, set, due-soon, due-overdue), the popover chrome, the calendar grid, and the quick-date strip inside it. Don't touch the underlying due-date storage format, `applyDueUrgency` thresholds, `daysUntilDue` math, the completed-row muted styling, or `#descToggle` / `#checkToDo` / `#closeButtonToDo` — this is a trigger-and-picker swap, not a model change.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
     
- [ ] **[LOW]** Add home screen icon and PWA manifest using favicon.svg as source
  - Description: When users add the app to their home screen on iOS or Android, the icon currently defaults to a generic browser screenshot instead of a branded app icon. Use the existing `favicon.svg` as the single source asset: reference it via `<link rel="icon" type="image/svg+xml">` for modern browsers, and generate PNG variants at build time for platforms that don't accept SVG — a 180×180 `apple-touch-icon.png` for iOS, plus 192×192 and 512×512 for Android, with a separate maskable 512×512 variant that bakes in ~10% safe-zone padding so Android's adaptive icon crop doesn't clip the artwork. Wire `favicons-webpack-plugin` into `webpack.config.js` rather than committing pre-generated PNGs, so the SVG stays the single source of truth and future icon edits regenerate every size automatically. First check `webpack.config.js` for an existing `HtmlWebpackPlugin` — if it already has a `template:` option pointing to an HTML file, edit that file to add the meta/link tags below; if `HtmlWebpackPlugin` has no `template:` option (or the plugin is using its default built-in template), create `toDoList_main/src/template.html` with a standard HTML5 shell (DOCTYPE, `<html lang="en">`, `<head>` including `<meta charset>` and `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`, empty `<body>` — the webpack plugin injects script/style tags automatically) and add `template: './src/template.html'` to the `HtmlWebpackPlugin` options. Add a `manifest.webmanifest` declaring `name: "Task Management"`, `short_name: "Tasks"`, `start_url: "/"`, `scope: "/"`, `display: "standalone"`, `background_color: "#0e0f14"`, `theme_color: "#0e0f14"` (matching `--bg-base`), and the generated icons array including a `purpose: "maskable"` entry. Reference the manifest from the template with `<link rel="manifest" href="manifest.webmanifest">`, add `<meta name="theme-color" content="#0e0f14">` so the iOS/Android status bar tints match the app chrome, and add the iOS standalone metas (`apple-mobile-web-app-capable: "yes"`, `apple-mobile-web-app-status-bar-style: "black-translucent"`, `apple-mobile-web-app-title: "Tasks"`). Also add `CopyWebpackPlugin` (or the equivalent) config to copy `manifest.webmanifest` into the `dist/` output so the referenced path actually resolves at runtime. No in-app install prompt or service worker — keep scope to the install-experience polish only; offline support is a separate feature.
  - File: `toDoList_main/src/favicon.svg`, `toDoList_main/src/manifest.webmanifest`, `toDoList_main/src/template.html`, `toDoList_main/webpack.config.js`, `toDoList_main/package.json`
  - Completed: YYYY-MM-DD (PR #<number>)

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
