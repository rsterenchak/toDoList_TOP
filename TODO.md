# TODO LIST

- [x] **[MEDIUM]** Project switcher: per-project triage question count, without the observer loop
  - Type: feature
  - Description: Third attempt. PR #830 shipped this and hung the app on load; it was reverted in #831. Cause is now known exactly: `updateAllProjectQuestionCounts` writes into `sideMain` (creates a `.projQuestionCount` span and sets `badge.textContent`), while `questionCountObserver` observes `sideMain` with `childList: true, subtree: true`. Setting `textContent` is a childList mutation, so every paint re-triggered the observer, which repainted, forever — an infinite microtask loop that pinned the main thread. Synchronous chrome rendered; async hydration never ran; the app showed an empty list with no project header. The store half of #830 was correct and is reinstated unchanged. Rebuild the switcher half with no self-triggering observer.
  - Behavior: Each project in the switcher shows an amber count of its `agent_queue` rows in `needs_words`. A project with none shows nothing — no zero badge. The open project shows its own count the same way. The count is live: an `agent_queue` realtime push repaints it without a project switch. It is labelled as triage questions, not "needs you", since shipped-but-unreviewed entries and landed drafts are excluded. Tapping a project switches to it as it does today. The switcher renders its project list normally whether or not any count resolves, and painting counts must never re-enter itself.
  - Implementation notes:
    - Reinstate the `agentQueueStore.js` half of #830 as-is: `_allRows`, `getAllQueueRows`, `getWaitingQuestionCounts`, `fetchAllQueueRows`, `loadAllQueueRows`, and the `Promise.all` in the realtime handler. That code was defensive and correct — do not redesign it.
    - DO NOT observe `sideMain` (or any ancestor of the badges) with `childList` or `subtree`. The paint mutates that subtree, so such an observer is guaranteed to re-enter. Instead, call the paint explicitly from the places that rebuild project rows — grep the project add, rename, delete, and restore paths in `main.js` and call it at the end of each, the same way other post-mutation refreshes are already invoked there. If a single shared rebuild helper exists, one call site is enough.
    - Make the paint idempotent as a second line of defence, independent of how it is triggered: read the badge's current `textContent` and return without writing when it already equals the new value; skip `classList` work when the class state already matches. A paint that changes nothing must perform zero DOM writes.
    - Add a re-entrancy guard — a module-level boolean set on entry and cleared in a `finally` — so that if any future caller does trigger it recursively, the second call returns immediately instead of hanging the tab. Cheap, and the failure mode it prevents is unrecoverable in-app: every revert control lives inside the app that will not render.
    - `observe()` throws on a null target. The existing function opens with `if (!sideMain) return;`, which implies `sideMain` may be absent — any observer or wiring added here must guard the same way before touching it.
    - Keep the count resolution outside the row loop: build the name→count map once inside a try/catch that falls back to `{}`, then have the loop only read the map with a default of zero. No calls into the store or `listLogic` from inside the loop.
    - TESTS ARE THE POINT OF THIS ENTRY, and source-text assertions are what let #830 through — every switcher test there grepped `main.js` for strings and never mounted a DOM. Add real DOM regression tests: mount a switcher with at least two project rows and a non-zero count, run the paint, and assert (a) the paint function executes a bounded number of times — spy on it and assert the call count does not grow after a microtask flush; (b) a second paint with unchanged counts performs no DOM writes; (c) the project list still renders in full when `getWaitingQuestionCounts` throws. A test that only asserts the observer's option object is not a substitute.
    - `style.css`: reinstate #830's rules unchanged — the hidden-by-default pill and the `.hasQuestionCount` reveal were not implicated. Existing `#ffbd5e` amber, no new tokens. This entry touches `style.css`, so it runs with no other style-touching entry in flight.
  - Out of scope: Counting landed drafts (`drafted` with no `draftSeenAt`) cross-project — needs other projects' todos in memory. Counting REVIEW cross-project — needs a per-repo TODO.md read for all seven projects on load. Any notification outside the app, a global aggregate badge on the chat button, auto-switching to a project with waiting work, the task rows' own badges, and the Agent board. The `footObserver` on `mainList` and any other existing observer.
  - File: `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/main.js`, `toDoList_main/src/style.css`
  - Completed: 2026-07-23
  <!-- id: 3b0854b6-8641-4cd7-80a2-5a84afd920f7 -->

- [ ] **[MEDIUM]** Task filter: a blocked-on-you toggle beside the status pill
  - Type: feature
  - Description: The task filter keys entirely on the manual `status` field — ALL / Active / Ideas — so there is no way to filter to the three states that are genuinely waiting on a human: REVIEW (shipped, unacknowledged), ASKING (a parked triage question), and DRAFTED (a generated entry not yet looked at). Those are now first-class phases with a shared vocabulary and a shared amber treatment, but finding them means scrolling the list looking for amber words. Add one amber toggle to the filter bar that shows only blocked rows, kept deliberately separate from the status pill so a derived state never joins a control that is otherwise pure manual status — the same separation already settled on the row badge and the description-editor rail.
  - Behavior: The filter bar carries an amber chip showing the count of blocked tasks in the current project — tasks whose `derivePhase` is `accept`, `asking`, or `drafted`. The chip is ALWAYS present, not conditionally mounted: at zero it renders dimmed and inert with a `0`, so the bar's geometry never shifts and "nothing needs me" is readable rather than inferred from an absence. Tapping it when the count is non-zero filters the list to blocked rows only and simultaneously snaps the status pill to ALL, so the two controls are never both filtering — no invisible AND, and the pill never displays a value it is not applying. Tapping again releases the filter, leaving the pill on ALL. While the blocked filter is active and the count falls to zero (the last item acknowledged or answered), the filter auto-releases and the full list returns, so the user can never be stranded in an empty filtered view. The blocked filter persists across reload alongside the existing filter preference. Count and membership both update live as phases change, without a re-render or a project switch.
  - Implementation notes:
    - Add `isBlockedPhase(phase)` to `phase.js` as the single definition of the blocked set — `PHASE.ACCEPT || PHASE.ASKING || PHASE.DRAFTED`. `derivePhase` already returns `ACCEPT` only when unreviewed (it returns `DONE` once `entryReviewedAt` is set), so no extra unreviewed check is needed. Do not inline the set in `taskFilter.js`; a fourth blocked state later must land in one place.
    - `taskFilter.js` documents that it deliberately keeps `prefs` as its only dependency to avoid a cycle. Adding `phase.js` appears safe — `phase.js` imports only `inject.js` and `agentQueueStore.js`, and `taskFilter.js` is imported only by `toDoRow.js`, `main.js`, and `viewFocusNav.js`, none of which sit in that chain. VERIFY this before adding the import rather than trusting the analysis, and if a cycle does form, resolve the phase at the `applyTaskFilter` call sites and pass it in rather than importing.
    - Reuse the existing hide mechanism exactly — the `taskFilterHidden` class and `isCommittedRow`, so the blank placeholder row always stays visible and no second hiding path is introduced. Do not use inline `style.display`; the module's header comment explains why the class exists.
    - Compute the count from the FULL committed row set, matching how the existing pill counts are computed, not from the currently visible subset.
    - Persist via a second key in `prefs.js` alongside `TASK_FILTER_KEY`, following the same read-with-fallback and write shape. A stored `true` whose count is zero on boot must resolve to released, so a reload can never land in an empty filtered view.
    - Live updates: the count and membership must repaint on both `TODO_RUN_STATUS_EVENT` (marker/review changes) and `onQueueChange` from `agentQueueStore` (asking/drafted changes). Grep how `applyTaskFilter` is currently re-invoked from `toDoRow.js` and hang the repaint off the same signals rather than adding a new event.
    - The auto-release on zero must not re-enter: releasing changes the filter, which triggers a repaint, which recomputes the count. Guard it so a release performs one pass and stops — and make the repaint idempotent (no DOM writes when the class state already matches), for the same reason the switcher count needed it.
    - Placement: the chip goes in `#taskFilterBar` immediately before the Sort control. That bar is grid-placed and switches to `justify-content: flex-start` with tighter padding under the narrow-phone media block; there is also an existing note in `style.css` about that row running out of room on narrow phones. Check both before adding a third control, and verify at 390px that the cycle pill, the chip, and Sort all fit without wrapping.
    - Both filter controls share one persisted state and are CSS-gated so exactly one is visible (the segmented row is `display: none` on mobile, leaving the cycle pill). The blocked chip is a single control visible at BOTH breakpoints — do not build a second copy of it.
    - Add an empty-state message for the blocked filter following the `EMPTY_MESSAGES` pattern, for the brief window before auto-release fires.
    - Tests: assert the blocked set matches exactly the three phases; that tapping the chip snaps the status pill to ALL; that auto-release fires once and does not loop; that a stored-active preference with a zero count boots released; and that the placeholder row stays visible under the blocked filter.
    - `style.css`: reuses the existing `#ffbd5e` amber and the 36px-chip and radius conventions; no new tokens. This entry touches `style.css`, so it runs with no other style-touching entry in flight.
  - Out of scope: Replacing the status pill's ALL / Active / Ideas vocabulary with phase-based filters, and the prototype's four-across pill row — the segmented control is already hidden on narrow phones for space, and re-adding it there is a separate decision. Composing the blocked filter with a non-ALL status filter. Sorting by phase. The usage meters, which have no data source. Merging the Tasks and Agent tabs. The row badge, the description editor, and the TODO.md viewer.
  - File: `toDoList_main/src/phase.js`, `toDoList_main/src/taskFilter.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: a5093bc6-7841-4d66-a877-3f806472d663 -->
