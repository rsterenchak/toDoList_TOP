# TODO LIST

- [x] **[MEDIUM]** Desktop panel: phase-switch skeleton — hide authoring controls in terminal phases — Completed: 2026-07-25
  - Type: feature
  - Description: The desktop description panel (`#descSibling`) opens into the same authoring layout — entry textarea, file picker, Generate, Inject — for every phase, including a shipped-and-acknowledged item whose entry is already in TODO.md and merged. This is the first slice of turning the panel into a phase face (option B): introduce a single phase-driven visibility switch, and use it only to hide the authoring controls once they no longer apply. The REVIEW and STUCK faces that fill that space with phase-appropriate content are the next two slices; this entry does not add new content, it only stops showing authoring controls in phases where they are meaningless, so the change is small and reversible.
  - Behavior: When a task's derived phase is `done` (shipped and acknowledged), the panel hides the entry textarea, the file-picker trigger and panel, Generate, and Inject, leaving the phase rail, the section context, and Discuss. In `none`, `draft`, `accept`, `asking`, `drafted`, `stuck`, and `mockup`, the panel renders exactly as it does today — every authoring control visible and functional. The ASKING and STUCK blocks continue to appear in their states unchanged. The switch is derived from `derivePhase` and repaints live: if an open panel's task transitions to `done` (its entry acknowledged elsewhere), the authoring controls disappear on the next refresh; if it leaves `done`, they return. Mobile is untouched.
  - Implementation notes:
    - Add a single function — `applyPhaseLayout(descSibling, phase)` — that toggles the authoring group's visibility from one derived phase, and call it from the same place `mountDescRail` / `syncAskingPanel` / `syncStuckPanel` are called, both on open and on the live-refresh path (the row's `TODO_RUN_STATUS_EVENT` and `onQueueChange` handlers around lines 908-918). Do not scatter per-control conditionals through the mount block; one switch keyed by phase, one place.
    - Hide via the `[hidden]` attribute, and add explicit `[hidden] { display: none }` guards for each affected child. This codebase has repeatedly hit author-level `display` outranking the UA `[hidden]` rule (the `claude*` family and the file-picker panel both carry explicit guards); a hidden control that still shows is the predictable failure here. Do NOT use inline `style.display`.
    - The authoring group is: `#descInput`, `.filePickTrigger`, `.filePickPanel`, `.generateBtn`, `.generateFailure`, `.injectBtn`. Rail (`.phaseRail`), entry label (`.descSiblingEntryLabel`), ASKING/STUCK blocks, and Discuss are NOT in the group. Note that hiding `.injectBtn` here must not fight `refreshInjectButton`, which independently hides/shows it based on description emptiness — `applyPhaseLayout` gates the whole group by phase, `refreshInjectButton` gates inject within the shown case; the phase gate wins when phase is `done`. Verify the two compose rather than clobber each other.
    - The section label currently always reads "THE ENTRY". In `done` there is no entry to author. Either hide the label in `done` alongside the group, or leave it — do NOT relabel it in this entry, since per-phase section copy belongs with the per-phase content slices. Hiding it is the cleaner default.
    - `mountDescFilePicker` builds and inserts the picker every open. When phase is `done`, either skip the mount entirely or mount-then-hide — skipping is cheaper and avoids a hidden manifest load, but confirm the sibling-walk anchors (`wireDescToggle`'s `pasteEntryPanel`/`createChipRow` scans) do not assume the picker's presence. Prefer skipping if safe.
    - Call `refreshViewerExpandedHeight()` after the switch runs, on both open and live-refresh — hiding the authoring group changes the panel's height, and an expanded viewer card below caches its body height.
    - EXTEND THE STRUCTURAL GUARD, and this is the load-bearing part of the entry: `DESC_PANEL_CHILD_SELECTORS` already lists every panel child. The existing test asserts each has a `grid-column`. Add a test that, for each terminal-phase config, asserts the authoring-group selectors resolve to `display: none` and the retained selectors do not — but since jsdom does not compute layout, assert instead on the `[hidden]` attribute state produced by `applyPhaseLayout(container, 'done')` versus a non-terminal phase. This is what catches a control that fails to hide in a phase the developer did not open by hand.
    - Confirm behavior across ALL phases, not just `done` and `idea` — run `applyPhaseLayout` for each of the eight `derivePhase` return values in a test and assert the group's visibility matches the spec. A defect that only appears in, say, `mockup` is invisible to manual testing of one or two states.
  - Out of scope: Any new per-phase CONTENT — the REVIEW "what shipped + open in TODO.md" face and the STUCK failure face are the next two slices and are not built here. Relabeling the section per phase. Fusing the row and panel into one card (option C). The mobile description-editor modal. `derivePhase`, the phase vocabulary, the rail's own rendering, the row badge, and the file picker's internal behavior. The filter pills and the paste chip.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 962d9a28-b18e-44cc-a510-e05cd5792a1e -->

- [x] **[HIGH]** Desktop: render the open item in a detail pane instead of expanding the row inline — Completed: 2026-07-26
  - Type: feature
  - Description: On desktop, opening a task mounts `#descSibling` into `#mainList` as a sibling of the row, so the panel pushes every row below it down and off screen — opening an item near the top of a long queue scrolls the rest of the queue away. Split the task pane at desktop widths into a queue column and a detail column, and RELOCATE the existing `#descSibling` into the detail column rather than rebuilding it, so the rail's rows stay collapsed and fully readable while one item is open. This is a host change, not a rewrite: the panel keeps its element, its children, its handlers, and every sync function that drives it. Model it on `placeChatContent()` in `claudeSheet.js`, which already relocates the chat surface between the mobile sheet and `#desktopChatPane` by breakpoint. Mobile keeps inline expansion exactly as it is.
  - Behavior: At viewports 1024px and wider, `#mainSec` splits into a queue column on the left and a detail pane on the right (the chat pane, when open, remains a third column to the right of both). Clicking a row's description chevron opens that item in the detail pane and marks the row visually selected; the row itself does not expand and no rows shift position. Opening a different row moves the panel to that item and clears the previous selection. Closing returns the pane to an empty state naming what it is waiting for. The panel's contents — phase rail, section label, ASKING and STUCK blocks, entry textarea, file picker, Generate, Inject, Discuss, and the phase-switch visibility logic — behave identically to today. Below 1024px, everything is unchanged: the panel mounts inline as a sibling of the row.
  - Implementation notes:
    - `#mainSec` is already a flex child of `#mainSplit` beside `#desktopChatPane`, so this split is NESTED: `#mainSplit` → [`#mainSec` → [queue | detail], `#desktopChatPane`]. Add the detail column inside `#mainSec` at `min-width: 1024px`; do not add a third `#mainSplit` child, which would put detail and chat in competition for the same flex space.
    - Write `placeDescPanel(descSibling, toDoChild)` mirroring `placeChatContent()`: resolve the target host by `window.innerWidth > MOBILE_MAX_WIDTH`, no-op when the panel already lives there, and move it with `appendChild` so handlers, scroll position, and in-flight state survive. `descSibling.id` is a fixed `"descSibling"`, so only one can be in the document at a time — that constraint already matches a single detail pane, but confirm the open path evicts any previously mounted panel rather than relying on it.
    - THE CRITICAL FUNCTION IS `openDescSiblingFor(toDoChild)` (line ~262). It resolves a row's panel by walking forward from the row past `createChipRow` and `pasteEntryPanel` siblings — which is meaningless once the panel lives in a pane. It is called by `syncAskingPanel`, `syncStuckPanel`, and the live-refresh sweep around line 958, so if it silently returns null in pane mode, the ASKING block, the STUCK block, and every live repaint stop working with no visible error. Give it a pane-mode branch that resolves the panel from the detail host and verifies it belongs to the passed row. Add a test asserting it returns the panel in BOTH modes.
    - The same sibling-walk assumption appears at lines ~2050, ~2106, ~2638, and ~2960 (the `descAnchor` / `descNode` / `scan` loops). Audit every one and gate it on mode; report each in the PR body.
    - `refreshViewerExpandedHeight()` exists because mounting into `#mainList` shifts rows below, including the viewer card whose body height is cached. In pane mode the panel no longer occupies list space, so those calls become no-ops for the list — but the pane has its own scroll. Do not delete the calls; make them mode-aware, and confirm the viewer card still measures correctly with the panel out of the list.
    - Track the selected row explicitly (a class or `data-selected` on `#toDoChild`) rather than inferring it from where the panel sits. `addAllToDo_DOM` and `reorderToDoDOM` rebuild rows on every sort, filter, and project switch — reapply selection after a rebuild, and clear the pane if the selected task no longer exists.
    - The detail pane needs an empty state. When nothing is open it should say so rather than showing a blank column, since an empty column reads as a rendering failure.
    - The panel's children were recently given explicit `grid-column` placement inside a `grid-template-columns: 14px 1fr 14px` grid, and the phase rail carries a `max-width`. Verify the panel renders
  <!-- id: eb16a5f3-db67-48bb-9a3f-2b826c9c19ac -->

- [x] **[MEDIUM]** Desktop: give the detail pane the flexible width, fix the queue rail and chat pane — Completed: 2026-07-26
  - Type: bug
  - Description: At wide viewports the desktop layout allocates width backwards. `#desktopChatPane` is `flex: 0 0 40%`, so at 1440px it takes ~576px; `#mainSec` gets the remaining 60% and splits it `minmax(0, 1fr) minmax(320px, 38%)`, giving the queue ~535px and the detail pane ~330px. The detail pane — which holds the phase rail, the entry text, the file picker, and the action stack — ends up the narrowest column on screen, while the chat pane and a queue of truncated one-line titles hold nearly 1100px between them. The reviewed layout inverts this: a fixed ~308px queue rail, a fixed ~352px chat pane, and the detail pane absorbing everything left over. Fix both splits together — correcting only `#mainSec` still leaves chat consuming 40% at every width.
  - Behavior: At desktop widths the queue occupies a fixed-width rail on the left, the chat pane (when docked) occupies a fixed-width column on the right, and the detail pane fills all remaining space — so widening the window grows the detail pane and nothing else. The detail pane is the widest column at typical desktop sizes. Queue rows truncate their titles on one line. Collapsing the chat pane hands its width to the detail pane. At the narrowest docked width the layout stays usable rather than crushing the detail column.
  - Implementation notes:
    - `#desktopChatPane`: change `flex: 0 0 40%` to a fixed basis around `0 0 360px`, and set `#mainSec` to `flex: 1 1 auto; min-width: 0` so it absorbs the remainder instead of being pinned to 60%. Keep `min-width: 0` on both so neither forces horizontal overflow.
    - `#mainSec`: change `grid-template-columns: minmax(0, 1fr) minmax(320px, 38%)` to `minmax(260px, 308px) minmax(0, 1fr)` — queue rail first at a fixed-ish width, detail flexible. Update the comment above it, which currently describes `#mainBar` as taking the 1fr track.
    - THE COLLAPSE PATH IS THE RISK: `body.chatPaneCollapsed` and the pane's collapse logic were tuned against the 40% basis, and the stylesheet documents hard-edged alignment box-shadows on `#desktopChatPane` matched to the task pane's sub-band. Verify the collapsed state still yields zero width and that the alignment shadows still line up at the new width; if a shadow offset is expressed relative to the pane's size, it will need adjusting.
    - Check the tight case at 1024px with chat docked: 1024 − 360 = 664 for `#mainSec`, minus a 308px rail leaves ~356px of detail. Confirm the phase rail, entry textarea, and action stack are usable there; if not, let the queue rail shrink toward its 260px lower bound in a narrower media block rather than capping the detail.
    - Queue rows now sit in a narrower fixed rail. Confirm `#toDoInput` and `.toDoTitleDisplay` ellipsize on one line rather than wrapping — `#toDoChild` has a fixed `height: var(--item-h)` and `overflow: clip`, so a wrapped title clips rather than expanding the row. Verify the phase badge and trailing copy control still fit beside a truncated title.
    - The TODO.md viewer card also lives in the queue column and will be noticeably narrower. Check its tab row (Rendered / Raw markdown), its action strip (Run backlog, Redeploy, sync, overflow, chevron), and its rendered body at the rail width — that strip is five controls wide and is the most likely thing to wrap or clip.
    - Re-verify the description panel at the new detail width: its three-column grid, the phase rail's `max-width`, and the file picker list. This panel has produced five layout defects; check rather than assume.
  - Out of scope: The detail pane's contents and its empty state. The chat pane's contents, tabs, and scoping. Merging the Tasks and Agent tabs. The usage meters. Mobile layout at any width.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 2ccac7dc-cc83-489d-b113-c7f2d8c8118a -->

- [x] **[MEDIUM]** TODO.md viewer: header controls clip in the narrow queue rail — Completed: 2026-07-26
  - Type: bug
  - Description: The viewer card's action controls are cut off now that the queue column is a fixed ~308px rail. `.todoMdViewerHeader` is `display: flex; flex-wrap: wrap`, so the header itself can wrap — but `.todoMdViewerMeta`, which holds the synced label, Run backlog, Redeploy, the sync chip, the overflow chip, and the chevron, is a nested flex row with no `flex-wrap` and `margin-left: auto`. Its children cannot break, so the whole group overflows as a single unit and is clipped rather than wrapping onto a second line. The card was laid out when the queue column was roughly 535px wide; at 308px the group no longer fits.
  - Behavior: At the queue rail's width, every viewer control remains visible and reachable — the Rendered / Raw markdown tabs, the synced timestamp, Run backlog, Redeploy, sync, overflow, and the expand chevron — wrapping onto additional lines as needed rather than clipping. Wrapped rows align to the card's left edge rather than being pushed right by a stale auto margin. Controls keep their existing sizes, order, and hit areas; the 36×36 chip convention is preserved. At wider widths, where the group still fits on one line, the header renders exactly as it does today with the meta group right-aligned.
  - Implementation notes:
    - Add `flex-wrap: wrap` to `.todoMdViewerMeta` and give it a row-gap so wrapped lines are not flush against each other. This alone fixes the clipping; everything below is about making the wrapped result read correctly.
    - `margin-left: auto` pushes the group right, which is correct on one line and wrong once it wraps — a wrapped group ends up right-aligned against the card edge while the tabs sit left. Drop the auto margin at rail widths (a `max-width` media block, or `justify-content` on the header instead of the auto margin) so wrapped rows start at the left edge.
    - `.todoMdViewerSynced` carries `white-space: nowrap` and a full relative timestamp. Confirm it does not force a wide line on its own — if "synced just now" is fine but "synced 3 days ago" is not, consider letting that one label shrink or truncate rather than reflowing the whole group.
    - Do NOT solve this by moving controls into the existing overflow menu. That is a behavioral change to which actions are one tap versus two, and it should be a deliberate decision rather than a side effect of a layout fix.
    - Do NOT reduce the chips below the 36×36 convention or shrink the Run backlog and Redeploy pills. Those are primary actions and the convention is established across the app.
    - Check the card in its OTHER host: `#todoMdViewerMobileSheet` uses the same `.todoMdViewerCard` markup at a different width with its own `flex: 1 1 auto; min-height: 0` treatment. Verify the wrap changes do not alter the mobile sheet's header, and scope them to the `#mainList` host if they do.
    - The card's expanded body height is cached and read by `refreshViewerExpandedHeight()`. A header that wraps to two lines changes the card's total height — confirm the cached measurement is taken after the wrap settles, or an expanded card will be short by one header row.
    - Verify at the tightest case: 1024px viewport with the chat pane docked, where the queue rail is at its narrowest permitted width.
  - Out of scope: Which controls live in the header versus the overflow menu. The viewer's tabs, its rendered and raw bodies, the per-entry pills, and the Acknowledge flow. The card's position in the queue column. Moving the viewer into the detail pane. The queue rail's width, which is correct as landed. Mobile sheet behavior beyond verifying no regression.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 56e3eaa0-b733-4dec-9d5a-1e0e66add52b -->

- [x] **[MEDIUM]** Desktop queue rail: task titles clip mid-word instead of ellipsizing — Completed: 2026-07-26
  - Type: bug
  - Description: In the desktop queue rail, task titles are cut off mid-word with no ellipsis — a row reads "fix column propor" with no indication text is missing. The desktop row renders its title in `#toDoInput`, an `<input>`, which clips at its content box by specification and cannot show an ellipsis; the stylesheet's own comment notes this. `.toDoTitleDisplay`, the span that can ellipsize, is `display: none` and only revealed inside the ≤420px media block, where the row already swaps between a read span and an edit input via `data-mobile-read` / `data-mobile-edit`. This was tolerable when the task column was ~535px and most titles fit; at the 308px rail almost every title clips. Adopt the same read/edit swap at desktop widths so an unfocused title ellipsizes and focusing it still gives a real input for renaming.
  - Behavior: In the desktop queue rail, an unfocused committed row shows its title as text truncated with a trailing ellipsis on one line. Clicking or focusing the title swaps to the editable input at the same position and size with the caret placed where clicked, so renaming works exactly as it does today; blurring or committing swaps back to the truncated display. The blank placeholder row always shows its input, never the display span. Row height, the phase badge, and the trailing controls are unchanged. Mobile (≤420px) keeps its existing two-line clamp behavior untouched, as does the 421–1023px range unless it exhibits the same clipping at its own widths.
  - Implementation notes:
    - Reuse the existing swap rather than inventing one. Grep `data-mobile-read` and `data-mobile-edit` in `toDoRow.js` and `style.css` — the read/edit toggle, its focus handling, and its caret placement already exist and are tested. Generalise those attribute names off the `mobile` prefix if they now apply at all widths, updating every selector and reference; do not add a parallel desktop-only mechanism.
    - Use single-line `text-overflow: ellipsis` on desktop, NOT the mobile two-line clamp. `#toDoChild` has a fixed `height: var(--item-h)` and `overflow: clip`, so a second line would be clipped rather than expanding the row. Matching the mockup's two-line rail rows would require changing desktop row height and is a separate decision.
    - The display span needs `flex: 1 1 0; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis` to truncate inside a flex row — `min-width: 0` is the one that is easy to miss and without it the span forces its max-content width and pushes the trailing controls out of the row.
    - Preserve the rename path completely: `commitTitle`, `exitTitleEditMode`, the `titleRenameHandledByEnter` blur guard, and the Escape-to-revert behavior all currently hang off the input. Confirm every one still fires when the input is reached through the swap rather than being permanently present.
    - This interacts with the row-focus-selects-row entry if that lands first or concurrently: focusing a row will both select it into the detail pane AND swap the title into edit mode. Decide deliberately whether a row click should enter rename immediately or require a second click on the title, and state the choice in the PR body. Entering rename on every selection would make arrowing through the queue put a caret in each title.
    - Keep the input at `font-size: 16px` or larger wherever iOS Safari can reach it — the 421–1023px range includes tablets, and the existing comment about avoiding focus auto-zoom applies to any width a touch device can hit.
    - Verify the phase badge and trailing controls still sit correctly beside a truncated span, and that a long title no longer pushes them off the rail.
  - Out of scope: Two-line titles in the rail and any change to desktop row height. The mobile ≤420px clamp behavior. The rail's width. The TODO.md viewer card's header wrapping. The detail pane and its contents. Merging the Tasks and Agent tabs.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 70fe7bcd-74d6-4490-a4a0-74861b7c555a -->

- [x] **[HIGH]** Desktop queue rail: title display span renders before the checkbox — Completed: 2026-07-26
  - Type: bug
  - Description: The read-mode title span added for desktop ellipsizing is inserted at the wrong position in the row. An unfocused committed row renders as title → checkbox → phase badge → controls, while the same row in edit mode renders correctly as checkbox → phase badge → title input. So focusing and blurring a row visibly reorders its contents, and the queue reads as two different layouts depending on which row has focus.
  - Behavior: The read-mode title span occupies exactly the same position in the row as the edit-mode input — after the completion checkbox and the phase badge, before the trailing controls — so swapping between read and edit changes only the element type, never the layout. Every row in the rail reads with the same left-to-right order regardless of focus state.
  - Implementation notes:
    - Insert the span at the input's position rather than prepending to the row. If the current code uses `toDoChild.prepend(...)` or `insertBefore(span, toDoChild.firstChild)`, replace it with an insert relative to `#toDoInput` — `toDoChild.insertBefore(span, toDoInput)` — so the two are always adjacent and the swap is purely a visibility toggle between siblings.
    - Prefer building the span ONCE at row construction next to the input and toggling visibility, over creating and inserting it on each blur. A create-and-insert path on every blur is how the file picker ended up duplicating on reopen, and rows are rebuilt constantly by `addAllToDo_DOM` and `reorderToDoDOM`.
    - Verify the mobile ≤420px path is unaffected — it already renders the span correctly, so the fix must not move it there. If the desktop and mobile inserts share a code path, confirm both positions after the change.
    - The span carries `flex: 1 1 0; min-width: 0`, matching the input, so it must sit in the same flex position for the row's sizing to be identical in both states. Verify the phase badge and trailing controls do not shift horizontally when a row gains or loses focus.
    - Add a test asserting the span and the input are adjacent siblings in the row, and that the span's index among the row's children matches the input's — this is a DOM-order assertion, not a layout one, so it works in jsdom.
  - Out of scope: The ellipsis truncation itself, which is correct. The read/edit swap's focus handling, caret placement, and rename path. Row height, the phase badge, and the trailing controls. The rail's width. Row focus opening the detail pane, which is a separate unbuilt entry.
  - File: `toDoList_main/src/toDoRow.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 88183626-5346-4dc2-b7e3-2ffc3fc7dfbc -->

- [x] **[MEDIUM]** Desktop: selecting a row opens it in the detail pane — Completed: 2026-07-26
  - Type: feature
  - Description: On desktop the detail pane only opens when the description chevron is clicked, even though the row already has a click handler that selects it — `toDoRow.js`'s committed-row branch (~line 1498) adds `.todo-active`, deactivates every other row, sets `data-title-edit`, and focuses the title input for one-click editing. Selection is therefore already happening; it simply does not drive the pane. Have that same branch open the row in the detail pane, and add a keyboard path so arrowing through the queue moves the pane with it. This is a small addition to an existing handler, not a new interaction model.
  - Behavior: At desktop widths, clicking a committed row opens it in the detail pane and marks it active, in addition to the one-click title editing it already does. Moving keyboard focus into a committed row — via tab or the existing arrow-key navigation — also opens it in the pane, but does NOT enter title-edit mode, so arrowing down the queue walks the detail pane without dropping a caret into every title. Opening a different row moves the pane and the active marker. The chevron keeps working exactly as it does. The blank placeholder row and the compose row never open the pane. Re-selecting the already-open row is a no-op rather than a re-render. Controls with their own action — the checkbox, the delete control, the phase badge, the copy chip — do not change what the pane shows. Mobile is untouched: the existing tap-to-read branch stays as is.
  - Implementation notes:
    - Reuse the chevron's open path; do not write a second one. The mobile branch in this very handler already does `descToggle.click()` for exactly this reason. In the desktop committed-row branch, if the panel is not already open for this row, trigger the same toggle — that guarantees `placeDescPanel`, `openDescSiblingFor`, the ASKING/STUCK sync, and the phase-switch layout all run identically regardless of what initiated the open.
    - Guard against re-entrancy and redundant work: check whether this row's panel is already the one mounted in `#descDetailPane` before triggering, so clicking an open row does nothing. Without that guard a click on the open row will toggle it CLOSED, which is the most likely bug here.
    - Extract the open into a named helper called from both the click branch and the new focus path, so the two cannot drift.
    - For keyboard: add a delegated `focusin` listener on `#mainList` that opens the focused row's panel WITHOUT setting `data-title-edit` and without calling `toDoInput.focus()`. The click path keeps one-click editing; the focus path must not, or arrow navigation becomes rename-on-every-step. Register the delegation once behind a module-level flag — `main.js` evaluates twice across the four webpack entry bundles, and a double-bound delegated listener on a shared container is exactly where that does damage.
    - `focusin` and `click` will both fire for a mouse click on the title. The open must be idempotent so one interaction results in one open.
    - Gate everything on pane mode, resolved the same way `syncDetailPaneForViewport` and `placeChatContent` do (`window.innerWidth > MOBILE_MAX_WIDTH`), so one definition governs whether the pane exists and whether row-select applies. When the pane is not mounted the new code must no-op rather than throw.
    - Exclusions resolved by walking up from the event target: `data-original-blank` rows, the compose row, the completion checkbox, the delete control,
  <!-- id: 400571b0-bef5-4955-9cfa-26e146e431a9 -->

- [x] **[HIGH]** TODO.md viewer: entry text crushes to one character per line in the narrow rail — Completed: 2026-07-26
  - Type: bug
  - Description: In the viewer's rendered body, an entry's text renders one character per line — a vertical column of glyphs. `.todoMdViewerCheckRow` is `display: flex` with no `flex-wrap`, holding checkbox → label → Revert → Acknowledge → delete. The label (`.todoMdViewerCheckText`) has `flex: 1 1 auto; word-break: break-word` and no minimum width, and `word-break: break-word` removes its min-content floor — so when the action buttons cannot fit on one line, flex shrink compresses the label toward zero instead of wrapping anything. At the old ~535px task column the buttons fit and the bug was invisible; in the 308px queue rail they do not.
  - Behavior: An entry's text reads as normal wrapped prose at the rail's width, occupying the available line and wrapping to further lines as needed. When the per-entry action buttons cannot fit alongside it, they wrap onto their own line below the text rather than squeezing it. The text never compresses narrower than a readable minimum. Checkbox alignment, the amber review edge, the strikethrough on completed entries, and every button's size, order, and behavior are unchanged. At wider widths, where text and buttons fit on one line, the row renders as it does today.
  - Implementation notes:
    - TWO changes are required and neither works alone. Add `flex-wrap: wrap` to `.todoMdViewerCheckRow`, AND give `.todoMdViewerCheckText` a real minimum width (`min-width: 14ch` or similar). Wrap alone will NOT fix it: the browser prefers shrinking a flex item over wrapping the line, and with no min-width the label shrinks to nothing, so the wrap never triggers. The min-width is what forces the buttons onto a second line.
    - Do not remove `word-break: break-word`. It is there so a long unbroken path or URL in an entry does not overflow the container. Keep it and add the floor.
    - Consider `flex-basis: 100%` on the label at rail widths as an alternative to the min-width, which would always give the text its own line with buttons below. Pick one approach; do not apply both, since together they will force a two-line layout even in wide hosts where one line is correct.
    - Add a row-gap alongside the existing `gap: 8px` so wrapped button rows are not flush against the text line.
    - `align-items: flex-start` on the row means the checkbox aligns to the text's first line — verify that still holds once the buttons wrap, and that the checkbox does not end up misaligned against a multi-line label.
    - Check the row's OTHER variants after the change: `--done` (strikethrough), `--review` (amber left border with `padding-left: 8px`), and rows at `indent > 0` for nested sub-bullets, which have their own indentation and may compound the width squeeze.
    - Check the card's OTHER host: `#todoMdViewerMobileSheet` renders the same markup at a different width. Verify the wrap changes read correctly there and scope them to the `#mainList` host if they do not.
    - The card's expanded body height is cached and read by `refreshViewerExpandedHeight()`. Rows that now wrap to two lines make the body taller — confirm the measurement is taken after the wrap settles, or an expanded card will be short.
    - Verify at the tightest case: 1024px viewport with the chat pane docked, where the queue rail is at its narrowest permitted width, on an entry that has all three action buttons present.
    - The regression test must assert the OUTCOME, not the declarations — a stylesheet-text assertion would pass with the bug present, which is how the last several layout defects in this app shipped. If the test environment cannot compute layout, say so plainly in the PR body.
  - Out of scope: Which per-entry actions exist and what they do. The Acknowledge flow, Revert, Run this entry, and the delete control. The viewer header's own wrapping, already fixed. The card's position in the queue rail, and the rail's width. Moving the viewer into the detail pane.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 2ae3cf7f-22df-4f30-a999-1a29ea4acec3 -->

- [x] **[MEDIUM]** Desktop rows: drop the manual status text, amber the in-progress stripe, move the control into the pane — Completed: 2026-07-26
  - Type: feature
  - Description: Desktop task rows show the manual status as a text badge (ACTIVE / IN PROGRESS / IDEA), which competes with the derived phase words the same element renders for blocked states and spends rail width on a value that is already conveyed by the row's own treatment. Mobile already solved this: the badge is hidden except when it carries a blocked phase, manual status reads from the left-edge tab, and it is set from the description editor's MANUAL STATUS control. Bring desktop in line — hide the manual status text, recolour the IN PROGRESS stripe from purple to the amber mobile already uses, and give the detail pane a MANUAL STATUS control, since removing the badge removes the only way to set status on desktop.
  - Behavior: A desktop task row shows no text badge for ACTIVE, IN PROGRESS, or IDEA. Blocked phases (REVIEW, ASKING, DRAFTED, STUCK, MOCKUP) still render their amber word, so a word on a row continues to mean only one thing: act on this. Manual status remains visible through the row's existing treatments — IN PROGRESS as a left stripe now in amber (`#d9b86a`) rather than purple, IDEA as a muted background and muted title, ACTIVE as the default with no override. The detail pane gains a MANUAL STATUS segmented control at the bottom, below the action buttons, matching the mobile modal's placement and driving the same `setToDoStatus`. Setting status there updates the row's treatment live. Mobile is unchanged.
  - Implementation notes:
    - Hide the manual status text with CSS, keyed off the badge's existing `data-status` attribute, mirroring how the mobile block already gates it. Hide by default and expose only the blocked-phase values — do NOT enumerate the three manual values as exceptions, so a future phase word cannot be accidentally hidden. Add explicit `[hidden]`-style display guards where needed; this codebase repeatedly hits author-level `display` outranking the UA rule.
    - The badge is the desktop status-popover trigger via `wireStatusLabelDelegation` → `showStatusPopover`. Once the manual values are hidden the popover is unreachable for them, but the blocked-phase branches must keep routing (REVIEW to the viewer, ASKING/DRAFTED/STUCK to the description editor, MOCKUP to the Agent view). Verify the delegated listener still resolves those and that a hidden badge is not left as a dead keyboard stop — check `firstFocusableInTaskFilterBar` is unaffected and that the row's own focus order skips it.
    - Recolour `#toDoChild.todo-row--in_progress`'s `box-shadow: inset 3px 0 0 #9D93EE` to the amber token (`var(--text-warning)`, `#d9b86a`), matching the mobile edge tab. Keep the inset form so it never shifts row layout. Leave the lifted background as is.
    - Add the MANUAL STATUS control to `#descDetailPane`'s panel by reusing the mobile modal's implementation — grep how `modals.js` builds it from `STATUS_META` / `STATUS_ORDER` and share one builder rather than writing a second. Both hosts must stay in step, and this is the same extraction the file picker and phase rail already needed.
    - Place it BELOW the action buttons, as the modal does. That ordering exists so the derived phase rail and the manual status control are not adjacent and read as one thing — the distinction this entry is partly about.
    - Anything mounted into `#descSibling` needs explicit `grid-column` placement, and it must be added to `DESC_PANEL_CHILD_SELECTORS` or the structural guard will fail. Content that aligns with the textarea uses `grid-column: 2`; blocks with their own padding span `1 / -1`.
    - The control must NOT appear in the authoring group that `applyPhaseLayout` hides in the `done` phase — manual status stays settable on a completed task. Verify it is excluded from `DESC_AUTHORING_GROUP_SELECTORS`.
    - Setting status must repaint the row's treatment without a full rebuild. Grep how the popover's `setToDoStatus` path currently refreshes the row and reuse it.
    - Mount idempotently and re-mount on panel open — `#descSibling` children survive a close, which is how the file picker duplicated.
    - Test: desktop rows render no text for the three manual values; blocked phases still render their word and still route on tap; the in-progress stripe is amber; the pane's status control sets status and repaints the row; it remains visible in the `done` phase; and mobile rows and modal are unchanged.
  - Out of scope: The three manual status values themselves, `STATUS_META`, `STATUS_ORDER`, and `setToDoStatus`. The derived phase vocabulary and `derivePhase`. The mobile row badge and description-editor modal. The left-edge `::before` tab, which stays mobile-only — desktop keeps the inset box-shadow. The run glyph. The filter pills and the ACTIVE cycle pill, which reads manual status and is a separate surface.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/todoStatus.js`, `toDoList_main/src/modals.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 3ec5c491-e931-478e-9340-09733aa16264 -->

- [x] **[MEDIUM]** Collapse the tab bar to Stream and Structure — Completed: 2026-07-26
  - Type: feature
  - Description: The tab bar carries TASKS VIEW / AGENT / STRUCTURE, but the Agent board's blocked-on-you states — ASKING, DRAFTED, STUCK, MOCKUP — all reach the task row now, so the board is no longer a place work is discovered. Collapse the bar to STREAM and STRUCTURE. The Agent VIEW is not deleted: Dispatch (drafted → run), Retry (failed), Accept, and the mockup A/B/C selection still live only there, and removing the view would leave a generated draft with no way to dispatch it. It becomes a routed destination reached from the badges that need it, rather than a tab you navigate to.
  - Behavior: The tab bar shows two tabs — STREAM (the renamed tasks view) and STRUCTURE. STREAM is the default. The Agent view remains mounted and fully functional but has no tab; it is reached by tapping a DRAFTED, STUCK, or MOCKUP badge on a row, each of which opens the board scrolled to that task's card. Returning from the board goes back to STREAM. Every existing Agent-board behavior — buckets, Dispatch, Retry, Accept, the mockup flow, its realtime updates — is unchanged. Task rows, the description panel, and Structure are unchanged apart from the two badge routes noted below.
  - Implementation notes:
    - BADGE ROUTING IS THE LOAD-BEARING PART. Today MOCKUP routes to the Agent view, but DRAFTED and STUCK route to the description editor — and their actions (Dispatch, Retry) exist only on the board. Change both to route to the Agent view, anchored to that task's card. Without this the tab removal strands Dispatch and the pipeline cannot complete. ASKING keeps routing to the description panel (its Send control is there) and REVIEW keeps routing to the TODO.md viewer.
    - The description panel's STUCK reason block stays as it is — it is still reached via the chevron and is useful context. This entry changes where the BADGE goes, not what the panel shows.
    - Reuse the existing open-and-anchor entry point built for the MOCKUP badge rather than adding a second. Grep how `todoStatus.js` reaches the Agent view for `mockup` and route `drafted` and `stuck` through the identical call.
    - Rename the tasks tab's label to STREAM. Check whether the tab's identifier is used as a key for persisted state (last-active view, focus restoration) — if so, keep the internal key and change only the visible label, or write a migration; a renamed key would silently reset users to the default view.
    - Removing the AGENT pill touches more than the bar. `main.js` has AGENT-specific logic in at least five places: the working-dot indicator, the no-repo gate that disables the tab in lockstep with project switches (~2363, ~2679, ~3458), and the panel-ownership comment at ~3662. Audit each — the no-repo gating must now apply to the routed entry points instead, so a badge cannot open the board for a project with no routed repo.
    - Verify the view-switch machinery handles a view with no tab. `setView`-style code often derives the active view from which pill is pressed; the Agent view now has no pill to press, so its activation path must not depend on one. The same applies to `viewFocusNav.js` and any arrow-key navigation over the tab bar, which must now walk two pills rather than three.
    - Confirm the Agent view still receives its realtime subscription and initial load when it has never been opened in a session. If the board's queue-row load is triggered by its tab activation, a badge route into it must trigger the same load.
    - Provide a way back. Once the board is not a tab, the user needs an obvious return to STREAM — verify the existing back affordance covers it, or add one.
    - Test: the bar renders two tabs; STREAM is default; DRAFTED, STUCK, and MOCKUP badges each open the board anchored to the right card; the board's Dispatch, Retry, and mockup flows still work when reached that way; a project with no routed repo cannot be routed into the board; and arrow navigation over the tab bar handles two pills.
  - Out of scope: Porting Dispatch, Retry, Accept, or the mockup A/B/C flow onto the task row or description panel — the natural follow-up, and the thing that would let the Agent view be deleted rather than demoted. The Agent board's own rendering, buckets, and behavior. The phase vocabulary and `derivePhase`. The filter pills, the usage meters, and the phase word on every row. Structure's contents.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/todoStatus.js`, `toDoList_main/src/agentView.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 58e26831-1848-4aeb-941f-42fceec99bf7 -->

- [x] **[LOW]** Retire or relocate the orphaned agent working-dot indicator — Completed: 2026-07-26
  - Type: feature
  - Description: Collapsing the tab bar to Stream and Structure removed the AGENT desktop pill and mobile AGENT tab — the only two surfaces that painted the "agent is working" dot. The persistent working watch still toggles the `body.agentWorking` class on its 15s interval and on realtime `agent_queue` pushes, but nothing consumes that flag on screen anymore (its CSS rules were removed alongside the pill/tab). Decide the fate of the indicator: either relocate a "working" dot/pill onto a surviving surface (the STREAM pill, a sidebar project row, or reuse the Agent board header's status pill) so a live agent still reads as alive at a glance, or retire the watch and the `body.agentWorking` machinery entirely if the indicator is no longer wanted. The `agentDotBreathe` keyframes are shared with the Agent board header status pill, so keep them.
  - File: `toDoList_main/src/agentView.js`, `toDoList_main/src/main.js`, `toDoList_main/src/style.css`

- [x] **[MEDIUM]** Description panel: dispatch a draft and retry a failed run without leaving the row — Completed: 2026-07-26
  - Type: feature
  - Description: The tab merge left DRAFTED and STUCK badges routing to the Agent view, because Dispatch and Retry exist only there — so acting on a generated draft or a broken run still means leaving the list. Both are the same call: `dispatchDraft(row, draftText, row.entry_id)`, with Retry passing the existing entry id so `injectEntry` dedup-skips the already-present marker rather than appending a duplicate. Add that one action to the description panel for both phases and route the badges back to it, so the row becomes the place work is both discovered and acted on.
  - Behavior: A task whose phase is `drafted` shows a Dispatch action in its description panel, beneath the generated entry text. A task whose phase is `stuck` shows a Retry action beneath its existing failure-reason block. Both run the same dispatch, show a pending label while in flight, and surface a failure message inline without losing the panel's state. On success the phase moves on via the existing realtime subscription and the action disappears on the next repaint — nothing polls. DRAFTED and STUCK badges route back to the description panel rather than the Agent view. MOCKUP keeps routing to the board, since the A/B/C selection flow is not ported. The Agent board's own Dispatch and Retry are unchanged and keep working.
  - Implementation notes:
    - `dispatchDraft` is module-private in `agentView.js` (~line 1796) and chains `dispatchDraft → shipEntryForTodo → inject → dispatch → poll`, per the comment at ~967. Extract it into a shared module that both `agentView.js` and the row layer import; do not copy it, and do not import `agentView.js` from `toDoRow.js`. Switch the board over to the extracted version in the same PR so the two cannot drift.
    - Both actions pass `row.entry_id` — Retry MUST, or a retry appends a duplicate entry to TODO.md instead of reusing the marker. Verify the drafted path passes it too, matching the board.
    - The queue row and its `draft` text come from the shared queue-row store's per-todo lookup, already imported by the row layer for `derivePhase`. Do not add a second fetch.
    - Guard the empty case the board guards: no draft text means fail with a message rather than dispatching nothing.
    - Mount idempotently and re-mount on panel open — `#descSibling` children survive a close, which is how the file picker duplicated. Mirror `syncAskingPanel`'s shape: remove any existing action before mounting, and use the shared insertion anchor so the rail stays first.
    - Anything mounted into `#descSibling` needs explicit `grid-column` placement and must be added to `DESC_PANEL_CHILD_SELECTORS`, or the structural guard fails. Content aligning with the textarea uses `grid-column: 2`; blocks with their own padding span `1 / -1`.
    - These actions must NOT join `DESC_AUTHORING_GROUP_SELECTORS` — that group is hidden in the `done` phase, and these belong to `drafted` and `stuck`, which are never `done`. Confirm `applyPhaseLayout` leaves them alone.
    - Repaint on `onQueueChange` from the queue store so the action appears and disappears live as the phase changes, including while the panel is open. Reuse the existing subscription.
    - Call `refreshViewerExpandedHeight()` after mounting and removing — the panel's height changes and an expanded viewer card caches its body height.
    - Route the badges by changing only the `drafted` and `stuck` branches in `todoStatus.js` to open the description panel, the same destination `asking` already uses. Leave `mockup` and `accept` as they are.
    - Style both with the SpaceMono uppercase treatment the panel's other actions use; Dispatch reads as the dispatched action it is, Retry in the danger red already carrying the stuck state.
    - Test: a `drafted` task shows Dispatch and a `stuck` task shows Retry; both call the shared dispatch with the row's existing entry id; a missing draft fails with a message rather than dispatching; the action clears on the realtime phase change; neither is hidden by the `done` phase switch; the badges route to the panel; and the board's own actions still work.
  - Out of scope: The mockup A/B/C selection flow and the `needs_mockup` state, which stay board-only. The Accept action on the board. The Agent board's rendering, buckets, and behavior. `derivePhase` and the phase vocabulary. The ASKING answer path and the STUCK reason block, both correct as landed. Deleting the Agent view, which still owns the mockup flow.
  - File: `toDoList_main/src/agentView.js`, `toDoList_main/src/toDoRow.js`, `toDoList_main/src/todoStatus.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 9c3abe6e-f2f1-4c57-b2a9-ab5c5ee7a237 -->

- [x] **[HIGH]** Description editor modal: textarea overflows its body and covers the action buttons — Completed: 2026-07-26
  - Type: bug
  - Description: In the mobile description-editor modal, the entry textarea visually overlaps the action buttons beneath it — the REVIEW SHIPPED CHANGE button renders half-hidden behind the textarea's lower edge. `#descEditorModal` is capped at `max-height: 86vh` (92vh on mobile) and lays out as a flex column of body plus a sibling actions container. `#descEditorModalBody` is `flex: 1 1 auto; min-height: 0` but declares no `overflow`, and `#descEditorModalTextarea` inside it has `min-height: 180px` (220px in the mobile block). When the modal's total content exceeds the cap, the body cannot shrink below the textarea's floor, and with `overflow: visible` the textarea spills out of the body's box and paints over the actions below rather than scrolling. Adding the full-width REVIEW SHIPPED CHANGE row (`flex: 0 0 100%`) made the actions taller and pushed the modal past the cap on common phone heights.
  - Behavior: The modal never renders overlapping content at any viewport height. When the content exceeds the modal's cap, the body region scrolls internally while the action buttons and the MANUAL STATUS control stay reachable at the bottom. The phase rail, THE ENTRY label, and the textarea keep their current appearance and sizing; the textarea retains its own internal scroll for long entries. No button is ever obscured.
  - Implementation notes:
    - Add `overflow-y: auto` to `#descEditorModalBody`. It already has `min-height: 0`, which is the other half of making a flex child scrollable — without the overflow declaration the min-height only permits shrinking that the textarea's floor then blocks.
    - Verify the actions container and MANUAL STATUS stay pinned outside the scrolling region. `#descEditorModalActions` is a sibling of the body with its own `padding` and `border-top`, so it should already sit below the scroll area — confirm it does not get swept into it.
    - Re-check the textarea's `min-height` after the overflow fix. At 220px on a short phone, plus the rail, the label, six action controls including the full-width review row, and the status segments, the body may scroll on every open, which is worse than a slightly shorter textarea. Consider lowering the mobile floor; if you do, keep it comfortably above a single line and state the value chosen in the PR body.
    - The textarea already declares `overflow: auto`, so it has its own scroll. Confirm the nested scroll behaves sanely — the body should scroll the modal's content, the textarea should scroll its own text, and neither should trap the other on touch.
    - Check the auto-grow path. `descInput`-style auto-grow sets an inline height from `scrollHeight`; if the modal textarea does the same, an inline height can defeat `flex: 1 1 auto` and reintroduce the overflow even with the fix. Grep for where the modal textarea's height is set and confirm the two do not fight.
    - Verify with a task in each phase that adds a block above the textarea — ASKING (question plus answer input), STUCK (failure reason), and REVIEW (the full-width route button) — since those are the cases that push the modal past its cap. Test on a short viewport, not just a tall one.
    - Do not fix this by raising `max-height` past 92vh or by removing the review button's `flex: 0 0 100%`; both trade one overflow for another.
  - Out of scope: The REVIEW SHIPPED CHANGE button, its colour, its ordering, and its route. The phase rail, the MANUAL STATUS control, and the action buttons' own styling. The desktop `#descSibling` panel, which is a grid and unaffected. Dispatch and Retry, correct as landed. The file picker.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 9447ae11-5c7a-443d-bff7-2ea557ade723 -->

- [x] **[MEDIUM]** Detail pane: lead with the task title and its entry marker — Completed: 2026-07-26
  - Type: feature
  - Description: The desktop detail pane renders only the relocated `#descSibling` panel, which opens straight into the phase rail and THE ENTRY label. Nothing in the pane names the task being viewed, so in a master-detail layout the only way to know which item is open is to look back at which queue row is active. Add a header at the top of the pane carrying the task title as a heading and, beneath it, the entry marker with its provenance. The marker line has diagnostic value beyond matching the reviewed design: a task's `entryId` is currently invisible everywhere in the UI, and a task silently losing it orphans it from its TODO.md entry with no way to see that has happened.
  - Behavior: When a task is open in the detail pane, the pane shows its title as a heading above the panel, with a second line reading the entry marker in monospace at reduced emphasis. A task that has been injected shows its marker id; a task that has not shows a short "not yet injected" style note in place of an id rather than an empty line. The header updates when a different task is opened and clears with the pane's empty state when nothing is open. The title is display-only — renaming still happens on the queue row, which already has one-click editing — and it truncates with an ellipsis rather than wrapping to more than two lines. Mobile is unaffected: the description-editor modal already carries the title in its own header.
  - Implementation notes:
    - Mount the header in `#descDetailPane` as a SIBLING of the relocated panel, not as a child of `#descSibling`. A `#descSibling` child would travel with the panel to the mobile inline host and duplicate the modal's existing title, and it would need explicit `grid-column` placement plus a `DESC_PANEL_CHILD_SELECTORS` entry. Pane-owned keeps it desktop-only and out of that contract entirely.
    - Populate it from the same place the panel is placed. Grep `placeDescPanel` / `syncDetailPaneForViewport` and update the header in that path, so title and panel can never disagree about which task is open. Clear it wherever the empty state is restored.
    - The marker is `item.entryId`. Show the full id in monospace — a truncated id is useless for the diagnostic case, which is comparing it against a marker in TODO.md. Label the line so it reads as an identifier rather than decoration.
    - Provenance wording: the reviewed design says "ASSIGNED HERE", but that distinction (minted locally versus carried in from a paste) is not tracked on the task — do not invent a field for it. Use wording that reflects what is actually known: present versus not yet injected.
    - The title must stay in sync with a rename. Renaming happens on the row while the pane is open, so the header has to refresh — grep the commit-title path and update the header there, or listen to whatever event that path already fires. A stale title in the pane after a rename is the most likely defect here.
    - Rows are rebuilt by `addAllToDo_DOM` and `reorderToDoDOM` on every sort, filter, and project switch. Confirm the header survives a rebuild, and that it clears if the open task no longer exists after one.
    - Truncate with `-webkit-line-clamp: 2` plus `overflow: hidden`, and set `min-width: 0` on the header if it sits in a flex context — the pane is a flex column, and without it a long unbroken title can force horizontal overflow.
    - Add a DOM-order test asserting the header precedes `#descSibling` in the pane, and a test that opening a second task updates the header's text. Both are jsdom-checkable without layout.
  - Out of scope: Editing the title from the pane — rename stays on the row. Making the marker copyable, clickable, or repairable, which is a separate diagnostic feature. The phase rail, THE ENTRY label, and everything inside `#descSibling`. The WRITE / PASTE / GENERATE authoring mode strip, which exists on mobile and not on desktop — a genuine host-parity gap, and the natural next entry. The mobile modal's header. The pane's empty state copy.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/toDoRow.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: e0f5804a-862f-43bf-a401-1c80d8e08cc3 -->

- [x] **[MEDIUM]** Detail panel: WRITE / PASTE / GENERATE authoring mode strip — Completed: 2026-07-26
  - Type: feature
  - Description: The description panel stacks the entry textarea, the file picker, Generate, Inject, and Discuss as a flat list, so Generate reads as a peer of Inject rather than as an alternative way to produce the entry — and while triage runs, the panel shows a spinner on a button beside an empty textarea. Introduce a three-mode strip above the entry region: WRITE keeps today's textarea, PASTE offers a parse-with-report path for pasting an entry into an existing task, and GENERATE replaces the entry region with the run's in-flight state. This groups the three ways an entry gets written into one control and gives triage a surface of its own instead of a spinner on a button.
  - Behavior: A three-segment strip sits above the entry region, defaulting to WRITE. In WRITE the panel renders exactly as it does today — textarea, file picker trigger, and the action stack. In PASTE the textarea is replaced by a paste field with a Parse action; parsing reports which fields were recognised and fills the entry, then returns the strip to WRITE with the fields populated for review. In GENERATE the entry region is replaced by the triage in-flight state with a cancel action while a run is live, or by a short explanation and a dispatch action when idle; when the draft lands the strip returns to WRITE with the generated entry in the textarea. Switching modes never discards entry text. Inject, Discuss, and MANUAL STATUS stay visible in every mode. The `done` phase hides the whole authoring group including the strip, as it does today. Mobile is unchanged.
  - Implementation notes:
    - Reuse `parsePastedEntry` from `entryParse.js` for PASTE — do not write a second parser. Note it currently pairs with `commitEntryToActiveProject`, which CREATES a task; this path must instead write into the OPEN task's description through the existing `listLogic` description path, then dispatch the textarea's synthetic `input` event for auto-grow and call `refreshInjectButton`.
    - GENERATE mode drives the EXISTING generate path — the same dispatch, the same queue-row state, the same landing. Do not add a second trigger or a second pending store; the linked `agent_queue` row is already the state, and `derivePhase` already returns `drafted`. The mode's job is to render that state larger, not to own it.
    - The mode is transient view state, not persisted per task. Reset to WRITE whenever the panel opens — `#descSibling` children survive a close, so a stale mode would carry across tasks.
    - EVERY element mounted into `#descSibling` needs explicit `grid-column` placement and an entry in `DESC_PANEL_CHILD_SELECTORS`, or the structural guard fails. Content aligning with the textarea uses `grid-column: 2`; blocks with their own padding span `1 / -1`. This panel has produced twelve layout defects in a day — most from implicit placement and from mounting non-idempotently.
    - Mount idempotently: remove any existing strip and mode body before mounting, mirroring `syncAskingPanel`, and use the shared insertion anchor so the phase rail stays first and the ASKING/STUCK blocks stay directly after it.
    - Hide the inactive mode's body with the `[hidden]` attribute plus an explicit `[hidden] { display: none }` guard for each new element. Author-level `display` outranking the UA rule has bitten this codebase repeatedly — `.filePickPanel[hidden]` and the `claude*` family all carry the same guard.
    - The strip joins `DESC_AUTHORING_GROUP_SELECTORS` so `applyPhaseLayout` hides it in `done` along with the rest of the authoring group. The mode bodies must be hidden there too, not just the strip.
    - Call `refreshViewerExpandedHeight()` on every mode switch — the three bodies are different heights and an expanded viewer card caches its body measurement.
    - Build the strip in a shared module so the mobile modal can adopt it later without a second implementation, following the phase rail and file picker extractions. Do NOT wire it into the modal in this entry.
    - Keep the file picker trigger visible in WRITE mode only; in PASTE and GENERATE there is no textarea to insert a path into.
    - Test: the strip defaults to WRITE on every open; switching modes preserves entry text; PASTE parses into the open task rather than creating a new one; GENERATE reflects the linked queue row's state; the `done` phase hides strip and bodies; mounting twice leaves one strip; and every new element resolves a `grid-column`.
  - Out of scope: The spatial target canvas that renders the live UI as pickable regions — that needs elements to carry their own origin and is the largest unbuilt piece in the design. The before/after diff in the ACCEPT state, which needs diff rendering that does not exist. Wiring the strip into the mobile description-editor modal. The usage meters. Dispatch and Retry, correct as landed. The phase rail, MANUAL STATUS, and the compose row's paste chip.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/entryParse.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 792ef9a7-5ddd-42ab-b714-83960c17d0a0 -->

- [x] **[MEDIUM]** File picker: offer UI regions alongside file paths — Completed: 2026-07-26
  - Type: feature
  - Description: The target picker lists raw file paths, so writing a `- File:` line still means knowing which file owns the thing you want changed. The manifest already carries a `regions` array of `{ selector, label, file, line, files }`, and `structureView.js` builds a `regionsIndex` from it that powers "Find in code" — mapping a human-readable region label to its owning file. Surface those regions in the picker so a target can be chosen by what it is on screen ("Filter & sort strip") rather than by path, with the file resolved underneath. This is the prototype's ⌖ gesture, and it needs no runtime instrumentation: the index exists and is keyed by label.
  - Behavior: The picker lists the active repo's UI regions above its file paths, each showing its label with the owning file beneath it at reduced emphasis, followed by the plain file list under a separating label. Filtering matches region labels, region file paths, and plain file paths together. Selecting a region inserts its owning file into the entry's `- File:` line exactly as selecting a file does — same dedup, same insertion position, same backtick wrapping. A repo whose manifest predates regions, or declares none, shows only the file list with no empty section. Everything else is unchanged: the on-demand manifest load, the row cap, both hosts.
  - Implementation notes:
    - Read regions from the SAME `loadManifest` result the picker already consumes — the parsed `regions` array is on that object. Do NOT import `structureView.js` into the picker or reach into its module-level `regionsIndex`; the picker must not depend on the Structure view having been opened.
    - `regions` is `undefined` for older manifests and filtered to entries with a string `selector` on parse, so an entry may still lack `label` or `file`. Skip any region with no `file` — it cannot produce a `File:` line — and fall back to the selector as the display label when `label` is missing.
    - Regions resolve to a file that still needs the `srcRoot` prefix, same as plain files. Reuse the existing join rather than adding a second — a region path that does not match what entries already use is the bug this repeats.
    - Several regions can share one owning file. Do not deduplicate them into one row — the labels are the point — but the insertion must still dedup against the `File:` line, so picking two regions from the same file adds that path once.
    - Keep the row cap. Regions and files share one list, so count both against it and keep the "keep typing to narrow" message accurate.
    - Region rows are two-line (label over file) while file rows are one-line. `.filePickRow` currently sets `white-space: nowrap` and a height that assumes one line; a two-line row needs its own class rather than overriding the shared one. This list has already produced a width defect and a flex-shrink defect — give the new row type `flex-shrink: 0` and a real minimum height rather than relying on content.
    - Do not use `line` yet. It is in the record and would let a future entry write a more precise target, but the `File:` line takes paths only.
    - Both hosts share `filePicker.js`, so this lands in the modal and the detail panel together. Verify the two-line rows render correctly at the mobile modal's narrower width.
    - Test: a manifest with regions lists them above files with their owning file shown; filtering matches labels and paths; selecting a region inserts its prefixed file path; two regions sharing a file insert it once; a manifest with no regions renders only files with no section header; and a region lacking a file is skipped.
  - Out of scope: Rendering the running UI as a visual tappable canvas — this is the list form of the same gesture; the visual form is separate. Using `line` for a more precise target. Structure's UI lens, its "Find in code", and its `regionsIndex`. The manifest generator and the `manifest-*.yml` workflows. The picker's on-demand load, insertion position, and row cap logic.
  - File: `toDoList_main/src/filePicker.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 6e5bc77c-a14e-404f-88a2-101b91f05e64 -->

- [x] **[LOW]** File picker: lead region rows with the owning file, group by file — Completed: 2026-07-26
  - Type: bug
  - Description: Region rows in the picker lead with a label derived by `prettify()` from an element id, so the list reads "Add Glyph / Add Proj / Add Project" — three near-identical strings that describe DOM elements rather than places in the UI, and give no basis for choosing between them. The generator cannot do better; it has only the identifier. But the picker already knows each region's owning file, which is the thing actually being inserted into the `- File:` line and the thing that distinguishes these rows. Invert the row so the file leads and the label is supporting detail, and group regions under their owning file so a file with several regions reads as one entry rather than three lookalikes.
  - Behavior: The UI REGIONS section lists one row per owning FILE, showing the file path as the row's primary text with its region labels beneath as a comma-separated secondary line, truncated with an ellipsis when there are many. Selecting the row inserts that file exactly as a plain file row does. Filtering matches the file path and every label grouped under it, so typing a label still finds its row. A region whose file already appears in the section is folded into that row rather than adding another. Regions remain listed above the plain file list. A manifest with no regions is unchanged.
  - Implementation notes:
    - Group by resolved file path AFTER the `srcRoot` prefix is applied, not by the raw manifest value, so grouping and insertion agree on identity.
    - Sort labels within a row for stable ordering, and cap the visible labels with a "+N more" style suffix rather than letting the secondary line grow unbounded. Keep ALL labels in the filter-match text even when only some are displayed.
    - This changes the row's information hierarchy, not its structure — it stays the two-line row type added with the regions work. Keep its `flex-shrink: 0` and minimum height; this list has already produced a width defect and a flex-shrink defect.
    - The primary line is now a path, which is long and should ellipsize on one line rather than wrapping. The secondary label line can wrap to at most one additional line before truncating.
    - Grouping reduces the row count, so re-check the shared row cap: regions and files still count together, and the "keep typing to narrow" message must reflect the grouped count.
    - Dedup against the `File:` line is unchanged — a grouped row inserts one path, which was already the behavior when two regions shared a file.
    - Do not change `prettify()` or the manifest generator. The labels are faithful to the identifiers; the problem is which one leads the row.
    - Both hosts share `filePicker.js`. Verify the inverted row reads correctly at the mobile modal's narrower width, where a full path plus a label line is tight.
    - Test: two regions in one file produce one row; the row's primary text is the prefixed path; filtering by a label that is not displayed still matches its row; selecting inserts the path once; and the row cap counts grouped rows.
  - Out of scope: `prettify()`, `scanRegions`, and `gen-src-manifest.js` — the generator is doing what it can with an identifier. Structure's UI lens and its own label rendering. Using the region's `line` for a more precise target. The plain file list's rows. The picker's on-demand load and insertion position.
  - File: `toDoList_main/src/filePicker.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 5f0976f4-1e99-4c8d-97b3-88b141343bf6 -->

- [x] **[MEDIUM]** Move the STREAM / STRUCTURE pills into the header row and retire the view sub-band — Completed: 2026-07-26
  - Type: feature
  - Description: The view switcher sits in `#desktopViewSubBand`, a full-viewport-width band at `grid-row: 3` that costs a row of vertical height to hold two pills. Moving the pills into the top header row recovers that height, but the band cannot simply be deleted: `#desktopChatPane` carries `margin-top: -32px` to lift itself over the band's `min-height: 32px`, plus a z-index raise so its CHAT/RUNS tabs paint above the band's full-width background, plus a `margin-top: 16px` gap-fill. Four tests pin that relationship — `chatPaneSubHeaderAlign`, `navbarGapPaneBackground`, `twoPaneSubHeaderPolish`, and `subBandBackground`. A previous attempt at this correctly aborted because the entry declared the chat pane out of scope while the band's removal requires realigning it. This entry brings both into scope so they ship together.
  - Behavior: The top header row carries the project title with its count badge and caret, then the STREAM and STRUCTURE pills, then the existing header actions at the right edge. The view sub-band is gone and everything below moves up by its height. The chat pane's top edge aligns flush with the task pane's top edge — no negative lift, no compensating gap-fill, and no z-index workaround, since there is no longer a full-width band to paint over its tabs. Both pills keep their labels, active styling, no-repo markers, and arrow-key navigation. Mobile is unchanged: the band is already `display: none` below the breakpoint and the bottom tab bar carries the views.
  - Implementation notes:
    - Do the chat-pane decoupling FIRST, then remove the band, so the intermediate state is never a pane lifting over nothing. Remove `margin-top: -32px`, the `margin-top: 16px` gap-fill, and the z-index raise together — each exists only because of the band.
    - `position: relative` on `#desktopChatPane` must STAY. The comment says it also anchors the absolutely-placed collapse button; only the z-index raise it enables is being removed.
    - The four pinned tests must be UPDATED, not deleted. They currently assert a coupling that will no longer exist; rewrite each to assert the new invariant — that the chat pane has no negative top margin and that no `#desktopViewSubBand` rule remains — so the decoupling is pinned as deliberately as the coupling was. Deleting them would leave the realignment unguarded.
    - `#outerContainer` is a five-row grid and the band holds `grid-row: 3`. Removing it means one fewer row and every later `grid-row` index shifts. Grep EVERY `grid-row:` in the stylesheet before editing the template — a stale index silently places an element in the wrong band, which is the most likely way this breaks.
    - Move `#viewSwitcher` in `main.js` at construction, after the project title and before the header action group, so DOM order matches visual order for keyboard navigation. Do not position it from CSS.
    - `#viewSwitcher` has `margin-right: auto`. In the header row that will push the actions right — verify the header does not already use its own spacer or auto margin, since two auto margins split the free space and would separate the pills from the title.
    - The band carries `pointer-events: none` with `pointer-events: auto` on the switcher inside it, so the band never swallowed clicks. That pairing goes away with the band; confirm nothing else relied on it.
    - `main.js:452` resolves `#viewSwitcher .viewPill[tabindex="0"]`, and the group is entered via `viewSwitcherEntryPill()` with tabindex 0 / -1 on the two pills. Verify both still work after the move and that tab order through the header reaches the group in the right sequence.
    - Confirm the project dropdown still anchors correctly now that the title has a sibling to its right, and that the pills align to the title's optical centre without growing the header's height.
    - Verify the chat pane's collapse toggle and its alignment box-shadows still read correctly at the new top edge, and check the layout at 1024px with chat docked, which is the tightest case.
  - Out of scope: The filter pills at the top of the queue column, a separate change. The chat pane's width, contents, tabs, and scoping — only its top-edge alignment changes here. The header's action icons, project dropdown, and count badge. The mobile bottom tab bar and the mobile header. The detail pane and the queue column's internal layout.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/chatPaneSubHeaderAlign.test.js`, `toDoList_main/tests/navbarGapPaneBackground.test.js`, `toDoList_main/tests/twoPaneSubHeaderPolish.test.js`, `toDoList_main/tests/subBandBackground.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: e2b5fa7b-7821-415e-9c84-9507b9957aa1 -->

- [x] **[MEDIUM]** Header: render the project as a two-line block with a "PROJECT n OF m" eyebrow — Completed: 2026-07-27
  - Type: feature
  - Description: The desktop header renders the active project through the mobile project-header block — a single line of name plus a `20/466` count badge and a caret (`mobileProjCountBadge`, main.js:2623). The reviewed desktop layout uses a two-line block instead: a small uppercase monospace eyebrow reading "PROJECT n OF m" above the project name in larger purple monospace. Build that desktop treatment. The open/done counts already appear in the footer ("20 OPEN · 448 DONE"), so the inline badge is redundant on desktop and its information is not lost.
  - Behavior: At desktop widths the header's project control renders two stacked lines: an eyebrow reading "PROJECT n OF m" — n being the active project's 1-based position in the sidebar's order, m the total — in small uppercase monospace at muted emphasis, and beneath it the project name in the larger purple monospace treatment with its dropdown caret. The inline count badge is not shown on desktop. Both lines are part of the same control, so the whole block opens the project dropdown, and it keeps its current keyboard focus behavior and accessible name. The eyebrow updates when a project is added, deleted, reordered, or switched. Mobile is unchanged: it keeps the single-line name, count badge, and caret exactly as they are.
  - Implementation notes:
    - Derive n from the SAME ordered project list the sidebar renders, not from an independent sort. If the sidebar reorders (drag, rename, or a sort preference), the eyebrow must agree with what the user sees — grep how the sidebar builds its row order and read from that source.
    - Update the eyebrow wherever the project list changes, not only on switch: add, delete, rename-driven reorder, and any drag reorder. A stale "PROJECT 3 OF 7" after a delete is the most likely defect. Grep for the existing refresh path that keeps `mobileProjCountBadge` current and hang the eyebrow off the same one.
    - Do not restructure the shared control. Add the eyebrow as an element inside the existing header project control, gated to desktop, and hide the count badge at desktop widths — do not build a separate desktop-only control, or the dropdown wiring, focus handling, and accessible name all need duplicating.
    - The accessible name must stay meaningful. With two visible lines the control's name should still read as the project and its action, not as "PROJECT 1 OF 7 Task Management App" concatenated — set an explicit `aria-label` and mark the eyebrow `aria-hidden` if it would otherwise be announced as part of the button.
    - The header row now also holds the STREAM / STRUCTURE pills and the action icons. A two-line block is taller than the current single line, so verify the header's height, that the pills and icons stay vertically centred against the taller block, and that nothing below shifts — the sub-band was just removed to recover exactly this vertical space.
    - Long project names must truncate on one line with an ellipsis rather than wrapping into a third line, with `min-width: 0` on the block if it sits in a flex context.
    - Use the existing SpaceMono and purple accent tokens; no new colours.
    - Test: the eyebrow shows the active project's 1-based index and the total; it updates after add, delete, and switch; the count badge is absent at desktop widths and present on mobile; the block still opens the dropdown; and the accessible name is unchanged.
  - Out of scope: The footer's open/done counts. The project dropdown's contents and behavior. The sidebar's project rows and ordering. The STREAM / STRUCTURE pills and header action icons. The mobile project header, which keeps its single-line treatment. The usage meters, which have no data source.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 191ef979-16f0-405b-ab66-bfbc399b2e33 -->

- [x] **[LOW]** Header: restore bordered pills for STREAM / STRUCTURE and open up the project block — Completed: 2026-07-27
  - Type: bug
  - Description: Two header issues from the sub-band retirement. First, `#navBar .viewPill` (style.css:13520) overrides the base `.viewPill` treatment to `border: none; border-radius: 0` with an `::after` underline on the active tab — a tab-strip look that made sense when the pills sat in their own band, but now reads as flat text beside a bordered project block. The base rule already carries the reviewed treatment: a `0.5px` purple border, `6px` radius, and a filled accent background when active. Second, the two-line project block sits tight against the header's top edge and its neighbours, so the header reads cramped now that it holds a taller block plus the pills plus the action icons.
  - Behavior: STREAM and STRUCTURE render as bordered pills — a thin purple outline with rounded corners, the inactive one transparent with muted text, the active one filled with the accent tint, brighter text, and a solid border. No underline marker on the active pill. The project block has breathing room above, below, and to its right, so the eyebrow is not flush against the header's top edge and the pills do not crowd the project name. The header's overall height grows only as much as the added spacing requires, and the pills and action icons stay vertically centred against the taller project block.
  - Implementation notes:
    - Delete the `#navBar .viewPill` override block entirely — including its `:hover`, `.active`, and `.active::after` variants — rather than patching individual declarations. The base `.viewPill` and `.viewPill.active` rules are what the reviewed design specifies, and leaving a partial override is how the two treatments drift again.
    - The `::after` underline is a separate element in the cascade; confirm removing the rule leaves no residual pseudo-element, and check the STRUCTURE pill's `.agentNoRepoMarker` and the STREAM pill's `.agentWorkingMarker` still position correctly against a bordered pill — they were positioned against a borderless one.
    - Add horizontal spacing between the project block and the pill group, and vertical padding above and below the project block. Prefer padding on the header row or a gap on its flex container over margins on the block itself, so the pills and icons stay centred rather than being pushed by a one-sided margin.
    - `#viewSwitcher` carries `margin-right: auto`, which is what pushes the action icons to the right edge. Adding a gap must not introduce a second auto margin — verify the spacing comes from padding or `gap`, not from a competing auto.
    - The pills must stay vertically centred against a two-line block. If the header row uses `align-items: center` this is free; if it uses `stretch` or `baseline`, the pills will align to the eyebrow or the name rather than the block's centre.
    - Check the mobile breakpoint. The `#navBar .viewPill` override may be inside a desktop media block or may apply at all widths — if mobile relies on the flat tab look anywhere, scope the removal rather than deleting globally. Grep the enclosing media query before removing.
    - Verify at the narrowest desktop width with the chat pane docked, where the header holds the project block, both pills, and four action icons in the least space.
  - Out of scope: The project eyebrow's content and its index logic. The count badge's mobile behavior. The header action icons themselves. The mobile bottom tab bar. The queue column's filter controls and the detail pane.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 34fd6074-6195-4510-8793-6bc0678b083b -->

- [x] **[LOW]** Header: remove the pill chrome from the desktop project block — Completed: 2026-07-27
  - Type: bug
  - Description: At desktop widths `#mobileProjHeader` renders as a raised pill — `background: var(--bg-elevated)` plus a `0.5px solid var(--border-mid)` border and a radius. The reviewed layout has the project block as bare text sitting directly on the header background, with only the eyebrow and the name plus caret; the surrounding box makes it read as a control competing with the STREAM / STRUCTURE pills beside it rather than as the header's title. Strip the chrome at desktop only.
  - Behavior: At desktop widths the project block shows the "PROJECT n OF m" eyebrow above the project name and caret with no background fill, no border, and no rounded box — it sits flush on the header background. It remains a button: it still opens the project dropdown, still shows a hover affordance (a subtle text or caret colour shift rather than a background fill), and keeps its focus-visible outline for keyboard users. Its spacing from the header edge and from the pill group is unchanged. Mobile is unaffected — the mobile project header keeps its own treatment.
  - Implementation notes:
    - Remove `background`, `border`, and `border-radius` from the DESKTOP `#mobileProjHeader` rule (style.css ~13057, inside the desktop media block). Do not touch the mobile rule at ~12056, which uses the same id and needs its chrome.
    - The `transition: background 0.15s ease, border-color 0.15s ease` on that rule becomes dead once both properties are gone. Replace it with a transition on whatever the new hover affordance animates, or drop it — a transition on properties that never change is noise.
    - There is almost certainly a `:hover` and possibly a `:focus-visible` rule that sets a background or border colour on this block. Grep them in the same media block and update together, or hover will paint a background on an otherwise chromeless element. Keep a visible hover — a chromeless button with no hover feedback is not obviously clickable.
    - `focus-visible` must stay clearly visible. If the current focus ring relies on the border colour changing, it needs replacing with an actual outline, or keyboard users lose the indicator entirely.
    - `#mobileProjHeader[data-empty="true"]` at ~13086 handles the no-active-project case with its own contract ("no bare pill"). Check whether that rule assumes the pill chrome exists; if it hides the block by removing the background, it needs adjusting now that there is none.
    - Removing the padding is not required and probably should not happen — the block's `padding: 4px 10px 4px 12px` still governs its spacing from the header edge and from the pill group. Keep it unless it visibly misaligns the block's left edge against the queue column below.
    - Check the `--proj-accent` custom property referenced around ~12109. If the project's accent colour is expressed through the border or background at desktop, that signal disappears with the chrome — confirm whether it is used here and, if so, where it should surface instead.
  - Out of scope: The mobile project header's styling. The eyebrow's content and index logic. The STREAM / STRUCTURE pills, the header action icons, and the header's spacing and height. The project dropdown's contents and behavior.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: fb026cc1-e32c-4843-983c-183cb6f71333 -->

- [x] **[MEDIUM]** Desktop queue rail: ALL / IDEAS / RUNNING / DONE phase filters, mobile unchanged — Completed: 2026-07-27
  - Type: feature
  - Description: Add four always-visible phase filter pills above the compose row at DESKTOP widths only: ALL, IDEAS, RUNNING, DONE. Two need explicit definitions. RUNNING requires a new phase covering the `dispatched` and `running` queue states — accurate for agent-dispatched work and blind to backlog runs, which are per-file and cannot be attributed to a row. DONE means shipped-and-acknowledged (`PHASE.DONE`), which is NOT the checkbox-completed COMPLETED section below the list; a task can be phase-DONE while still unchecked. Mobile is deliberately unchanged: it keeps the ACTIVE/Ideas cycle pill filtering on manual status, the sort control, the blocked chip, and the per-row run glyphs. Four pills at the ~393px mobile width would be roughly 89px each, which is why the segmented control is already hidden there.
  - Behavior: At desktop widths the queue rail shows four pills above the compose row, each with a live count, exactly one active, the selection persisted. ALL shows every uncompleted task; IDEAS shows phase `none`; RUNNING shows tasks whose linked `agent_queue` row is `dispatched` or `running`; DONE shows phase `done`. Below the desktop breakpoint the rail renders exactly as it does today — cycle pill, sort control, blocked chip, unchanged. The blocked chip remains an independent overlay filter at both breakpoints. Switching between breakpoints in one session leaves each side's filter selection intact rather than one overwriting the other.
  - Implementation notes:
    - TWO FILTER STATES, TWO KEYS. Desktop filters on derived phase; mobile filters on manual status. They are different vocabularies and must not share `TASK_FILTER_KEY`, or resizing across the breakpoint writes a phase value into the status filter and blanks the list. Add a second `todoapp_`-prefixed key for the phase filter and leave the existing key's meaning untouched.
    - `applyTaskFilter` selects the predicate by breakpoint, resolved the same way `syncDetailPaneForViewport` and `placeChatContent` do (`window.innerWidth > MOBILE_MAX_WIDTH`), so one definition governs which control is visible and which predicate runs. Re-apply on resize so crossing the breakpoint swaps predicates rather than leaving a stale filtered set.
    - Keep the existing manual-status `FILTERS` array, `filterFor`, and `rowStatus` intact — they still drive mobile. Add the phase predicate alongside rather than replacing them.
    - `phase.js`: add `RUNNING: 'running'` to the frozen `PHASE` map and a branch in `derivePhase` returning it when the linked queue row is `dispatched` or `running`. Place it with the queue-derived checks above the `entryId` guard, ranked BELOW `ASKING`, `DRAFTED`, `STUCK`, and `MOCKUP` — those are blocked on the user and must keep winning — and above the marker-derived phases.
    - Do NOT add RUNNING to `PHASE_RAIL_ORDER`, `PHASE_RAIL_LABELS`, or `isBlockedPhase`. The rail resolves it to its underlying DRAFT stage like every other queue-derived state, and a run in flight is not waiting on the user, so it must not inflate the blocked chip or earn a row badge.
    - Verify adding RUNNING changes nothing mobile-visible: a running task previously resolved to DRAFT via its pending marker, and should still render identically on mobile rows and in the modal's rail.
    - Counts come from the FULL committed row set, matching the existing pill counts, not the visible subset.
    - Reuse `taskFilterHidden`, `isCommittedRow`, and the `EMPTY_MESSAGES` pattern — only the predicate and the control differ. Do not introduce a second hiding mechanism.
    - Layout: four pills at the ~308px rail leave roughly 70px each, so labels stay short and counts compact. Give them their own row above the compose row; the sort control almost certainly cannot share it at that width, so verify before deciding where sort goes on desktop. Check at the narrowest permitted rail width with the chat pane docked.
    - The desktop segmented control currently renders the three manual values. Either repurpose it as the four phase pills or remove it — do not leave a stale three-value control rendering on desktop beside the new one.
    - Repaint counts and membership on `TODO_RUN_STATUS_EVENT` and `onQueueChange`, reusing the subscriptions already wired for the blocked chip.
    - Test: `derivePhase` returns RUNNING for `dispatched` and `running`; blocked phases outrank it; it is excluded from `isBlockedPhase` and from the rail order; each desktop pill filters correctly; mobile still filters on manual status with its cycle pill; crossing the breakpoint preserves both selections; and counts match the full set.
  - Out of scope: The mobile cycle pill, sort control, blocked chip, and per-row run glyphs, all unchanged. The blocked chip's behavior at either breakpoint. The COMPLETED section and the `completed` checkbox. Manual status, `STATUS_META`, `STATUS_ORDER`, and the detail pane's MANUAL STATUS control. Giving RUNNING a row badge. Attributing backlog runs to individual rows. The usage meters.
  - File: `toDoList_main/src/phase.js`, `toDoList_main/src/taskFilter.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 889f254f-dcf5-4845-a991-b80987885989 -->

- [x] **[MEDIUM]** Queue row: selection stripe hides the manual status stripe — Completed: 2026-07-27
  - Type: bug
  - Description: Setting a task to IN PROGRESS from the detail pane appears to do nothing until another row is selected. The status write and the row repaint are both working — the conflict is in CSS. `#mainList #toDoChild.todo-detail-open` sets `box-shadow: inset 2px 0 0 0 var(--accent-dim)` for the selected row, and `#toDoChild.todo-row--in_progress` sets `box-shadow: inset 3px 0 0 #d9b86a` for the status stripe. `box-shadow` does not compose across rules — the winning declaration replaces the other entirely — and the detail-open selector has higher specificity, so an open row shows the selection stripe instead of its status stripe. Deselecting reveals the amber, which is why it appears only after clicking away.
  - Behavior: A selected row shows both its selection state and its manual status stripe at the same time. Changing status from the detail pane updates the open row's appearance immediately, with no need to select another row. An unselected row is unchanged. The IDEA muted-background treatment continues to coexist with selection.
  - Implementation notes:
    - Preferred fix: give the two treatments different properties so they cannot clobber each other. Move the SELECTION indicator off `box-shadow` — the selected row already has `background: var(--bg-hover)`, so an outline, a border, or a right-edge marker can carry selection while `box-shadow` stays reserved for status. Do NOT solve it by combining both into one comma-separated `box-shadow` in a `.todo-detail-open.todo-row--in_progress` compound rule: that requires a compound selector per status value, and every future status or phase treatment has to be added to each one.
    - If a compound approach is chosen anyway, it must cover all three manual statuses plus the default, and the PR body must say why the property-separation approach was rejected.
    - Check the interaction with `.todo-row--idea`, which uses `background` rather than `box-shadow`. `.todo-detail-open` also sets `background: var(--bg-hover)` and will override the idea muting on a selected row — verify whether that is acceptable or whether idea's muted treatment should survive selection too, and state the decision.
    - Verify the selected row is still clearly distinguishable from an unselected in-progress row after the change. Selection and status are different axes and both need to read at a glance.
    - The mobile row uses a `::before` left-edge tab rather than an inset box-shadow for status, and mobile has no detail-open selection state. Confirm mobile is unaffected and that any new selection treatment is scoped so it does not leak below the breakpoint.
    - Check `focus-visible` on the row as well — if the focus ring is also a box-shadow it will collide with whichever property selection ends up using.
    - Add a test asserting that `.todo-detail-open` and each `.todo-row--*` treatment use different CSS properties, so a future change cannot reintroduce the clobber. This is a stylesheet-structure assertion and works without layout computation.
  - Out of scope: The manual status write path, `setToDoStatus`, and `refreshTodoStatusUI`, all working correctly. The detail pane's MANUAL STATUS control. `derivePhase` and the phase badges. Row selection behavior itself. The filter pills.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 6ddef8f1-2bb4-4a93-ab57-ecfea2fd8398 -->

- [x] **[MEDIUM]** Desktop queue rail: give the phase filters their own row so labels stop truncating — Completed: 2026-07-27
  - Type: bug
  - Description: The four phase filter pills render as "ALL 18 / I… 18 / RU… 0" with DONE pushed out of view. `.taskPhaseFilters` is `flex: 1 1 auto` sharing `#taskFilterBar` with the SORT: STATUS control, and each `.taskPhaseFilterPill` is `flex: 1 1 0` with `text-overflow: ellipsis`. At the ~308px queue rail, after the sort control and gaps, each pill gets well under 60px — enough for two characters and a count. The truncation is working as written; the row simply cannot hold five controls at that width. Move the pills onto their own full-width row above the sort control.
  - Behavior: At desktop widths the queue rail shows the four phase pills on their own row spanning the rail's full width, with every label readable in full — ALL, IDEAS, RUNNING, DONE — each with its count. The sort control sits on the row beneath them, aligned as it is today. Neither row scrolls horizontally at the rail's normal width. Mobile is unchanged: its cycle pill, blocked chip, and sort keep sharing one row.
  - Implementation notes:
    - Put the pills in their own container row rather than making `#taskFilterBar` wrap. A wrapping flex would let the pills and sort reflow into arbitrary arrangements at intermediate widths; an explicit second row is predictable.
    - The pills stay `flex: 1 1 0` so they share the full rail width evenly. At ~308px minus padding, four pills plus gaps give roughly 70px each, which fits "RUNNING" at the current 10px uppercase type — verify against the longest label with its count rather than assuming, and reduce letter-spacing before reducing font-size if it is tight.
    - Keep `overflow-x: auto` with hidden scrollbars as the last-resort fallback, and keep the ellipsis on `.taskPhaseFilterLabel`. Both are correct safety nets; this change is about giving them enough width that they never engage.
    - Scope the new row to desktop. `.taskPhaseFilters` is already `display: none` by default and revealed in the desktop block — the row container needs the same gating so mobile's single-row bar is untouched.
    - The blocked chip currently sits in `#taskFilterBar` anchored right via `margin-left: auto` on the leftmost member of the right-hand cluster. Confirm which row it belongs on after the split — it is an overlay filter, not a phase, so it likely belongs with sort rather than with the phase pills. Whichever row it lands on must keep exactly one auto margin, or the cluster separates.
    - Verify at the narrowest permitted rail width with the chat pane docked, which is the tightest case, and confirm the added row does not push the compose row or the task list below the fold.
    - Check `firstFocusableInTaskFilterBar` and `taskFilterArrowTarget` in `taskFilter.js`, both exported and consumed by `viewFocusNav.js` and `main.js` — arrow-key navigation now walks two rows instead of one and must still enter and traverse the controls in a sensible order.
  - Out of scope: The phase vocabulary, the filters' predicates, and their counts. The sort control's own behavior and appearance. The blocked chip's behavior. Mobile's filter bar layout. The compose row and the task list.
  - File: `toDoList_main/src/taskFilter.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 42ddcd2e-2b3d-4931-a066-828ad35ea955 -->

- [x] **[LOW]** Detail pane: let the phase rail and mode strip span the pane's width — Completed: 2026-07-27
  - Type: bug
  - Description: The phase rail and the WRITE / PASTE / GENERATE strip both render compressed at the left of the detail pane while the pane itself is now the widest column on screen. `#descSibling .phaseRail` is capped at `max-width: 440px` with `justify-self: start` and fixed 52px nodes, so its connectors stay short and the four nodes cluster; `.descModeStrip` is `display: inline-flex`, so it shrinks to its content rather than spanning. The reviewed layout has both stretching across the pane — the rail reading as one left-to-right sequence with long connectors, and the three mode buttons as equal thirds of the content width.
  - Behavior: In the detail pane the phase rail spans the content column's full width, with its four nodes evenly distributed and long visible connectors between them. The mode strip spans the same width with WRITE, PASTE, and GENERATE as three equal segments. Both keep their current type, colours, states, and behavior — only their width and distribution change. The mobile description-editor modal is unchanged: its narrower non-grid host keeps the current compact treatment.
  - Implementation notes:
    - Raise or remove the rail's `max-width: 440px` in the `#descSibling` rule and let it fill `grid-column: 2`. If a cap is kept for very wide windows, set it high enough that the rail spans the pane at typical desktop sizes — the point is long connectors, not a centred cluster.
    - The rail's connectors must be the flexible element. `.phaseRailNode` is `flex: 0 0 auto` at a fixed 52px, which is correct; verify the segment between nodes is `flex: 1` so the extra width goes to the connectors rather than the nodes. If it is not, that is the actual fix and the max-width is secondary.
    - `.phaseRail` sets `justify-content: center` while the `#descSibling` rule sets `justify-self: start`. Once the rail fills its column those two no longer conflict, but confirm the nodes distribute evenly rather than bunching at one end.
    - Change `.descModeStrip` from `inline-flex` to `flex` in the `#descSibling` host only, and give the three buttons `flex: 1 1 0` with `min-width: 0` so they divide the width equally. Scope it to `#descSibling` — the modal's strip should keep its compact inline treatment at the narrower width.
    - The mode strip is a `radiogroup`-style control; equal-width segments must not break its keyboard navigation or its selected-state styling. Verify arrow-key movement between segments still works.
    - Both elements live in `#descSibling` and already carry `grid-column: 2`, so their placement is correct — this is width distribution inside that column, not a placement change.
    - Verify at the narrowest desktop case: 1024px viewport with the chat pane docked, where the detail column is at its tightest. The rail's four 52px nodes plus connectors and the three mode segments must still fit without overflow or truncating labels.
    - Call `refreshViewerExpandedHeight()` is not needed here — neither change affects the panel's height — but confirm the rail's height is unchanged, since a taller rail would shift everything below it.
  - Out of scope: The rail's node states, labels, colours, and inertness. The phase vocabulary and `derivePhase`. The mode strip's behavior, its three bodies, and their contents. The two-column body arrangement in the reviewed GENERATE state, which is a separate layout change. The entry textarea, file picker, action buttons, and MANUAL STATUS control. The mobile modal.
  - File: `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: cf4e495e-ac31-4ac6-b054-f24352653ebf -->

- [x] **[MEDIUM]** Detail panel: group the actions in a row and add a FILE readout — Completed: 2026-07-27
  - Type: feature
  - Description: Inject, Generate, and Discuss each carry `grid-column: 1 / -1` in `#descSibling`, so each spans the panel's full width as its own stacked bar — roughly 900px wide in the desktop detail pane. The reviewed layout never uses full-width action bars; its controls are sized to their labels and grouped horizontally. Wrap the three in a single actions row so they sit side by side at natural width. Add, beneath that row, a read-only `FILE` readout mirroring the paths on the entry's `- File:` line, so the target the entry will act on is visible without scrolling the textarea.
  - Behavior: The detail panel shows Inject, Generate, and Discuss on one row, each sized to its label rather than stretched, left-aligned in the content column, wrapping to a second line only if the pane is too narrow to hold them. Beneath the row sits a `FILE` label with the paths parsed from the entry's `- File:` line rendered in monospace at reduced emphasis; multiple comma-separated paths each render on their own line. When the entry has no `File:` line the readout shows a short note that no target is set rather than an empty block. The readout is read-only — it reflects the entry text and is never the source of truth — and it updates as the entry is edited, including when the file picker inserts a path. Every button keeps its current behavior, states, disabled handling, and ordering. The mobile modal is unchanged.
  - Implementation notes:
    - Wrap the three buttons in one container that itself takes `grid-column: 2` (the content column, aligning with the textarea and the rail), and remove the `grid-column: 1 / -1` from `.injectBtn`, `.generateBtn`, and `.discussBtn` inside `#descSibling`. Inside the wrapper use flex with `flex-wrap: wrap` and a gap; the buttons take their natural width. Do NOT give them `flex: 1` — equal thirds is just full-width stacking in a different shape.
    - Adding a wrapper changes the panel's child set: register it in `DESC_PANEL_CHILD_SELECTORS` or the structural guard fails, and confirm the guard's per-child `grid-column` assertion is satisfied by the wrapper rather than by the buttons now nested inside it.
    - The wrapper must be swept correctly by `applyPhaseLayout`. `DESC_AUTHORING_GROUP_SELECTORS` currently lists `.injectBtn` and `.generateBtn` individually, and hiding them inside a visible wrapper would leave an empty row in the `done` phase. Decide whether the wrapper joins the group or whether it collapses when all its children are hidden, and state the choice in the PR body — Discuss is NOT in the authoring group, so the wrapper cannot simply be hidden wholesale.
    - `refreshInjectButton` hides Inject via inline `style.display` when the description is empty, while `applyPhaseLayout` uses the `hidden` attribute. Those two gates already coexist uneasily; verify the wrapper's flex layout looks right in all four combinations (inject shown/hidden × generate shown/hidden) rather than only the common one.
    - `syncGenerateControl` dims Inject during a run. Confirm the disabled treatment still reads correctly on a natural-width button — it was tuned against a full-width bar.
    - Parse the readout from the SAME tolerant `- File:` matcher the picker's insertion logic uses — leading whitespace, `- File:`, backtick-wrapped comma-separated paths. Import it rather than writing a second matcher; a readout that disagrees with what the picker inserts is worse than no readout.
    - Recompute the readout on the same signals that persist the description — the textarea's existing input/blur handlers and the picker's insertion path. Do not poll, and do not recompute on every keystroke if that proves expensive; debounce if needed.
    - The readout is a `#descSibling` child: `grid-column: 2`, registered in the selector list, mounted idempotently and re-mounted on panel open, since the panel's children survive a close.
    - Call `refreshViewerExpandedHeight()` after the readout mounts or changes height — a two-path readout is taller than a one-path one, and an expanded viewer card caches its body height.
    - Verify at 1024px with the chat pane docked, where the detail column is tightest: the three buttons should wrap rather than overflow, and the readout's long paths should ellipsize on one line each rather than forcing horizontal scroll.
  - Out of scope: The two-column body from the reviewed GENERATE state — the target picker stays a toggle in its current position, so there is no second column. Storing a target on the task; the entry's `File:` line remains the single source of truth. The buttons' own behavior, labels, and glyphs. The file picker's list, filtering, and insertion. The phase rail, the mode strip, the textarea, and MANUAL STATUS. The mobile description-editor modal.
  - File: `toDoList_main/src/toDoRow.js`, `toDoList_main/src/filePicker.js`, `toDoList_main/src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 06a63327-2fab-42c1-a2bc-6ae410c067fe -->
