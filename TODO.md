# TODO LIST

- [x] **[MEDIUM]** Update CLAUDE.md — Part 1 of 3: system overview, repo allowlist, three context modes — Completed: 2026-06-02
  - Type: feature
  - Description: The `CLAUDE.md` file at repo root is the agent's primary architectural reference and gets read during every Claude run. It has not been updated since the in-app Claude assistant was built. This is the first of three documentation entries that together update CLAUDE.md. Add these three sections (preserving any existing CLAUDE.md content above them â append, do not replace, unless existing content directly conflicts). DO NOT invent details â when uncertain about a specific value, write `(see inject.js)` rather than hallucinating; the doc's job is to point to truth, not to duplicate it. REQUIRED SECTIONS: (1) "System overview" â describe the pipeline at high level: user opens the Claude sheet in the PWA, authors a TODO entry through chat with Sonnet, the entry injects into TODO.md via the Cloudflare worker (`todo-injector-worker`), dispatch fires `claude-run.yml`, the agent (Opus) implements the change, opens a PR, auto-merges, deploys. Name each layer explicitly. Mention both the conversational planner role (Sonnet, billed to Console) and the agentic builder role (Opus, via Max plan). (2) "Repos and allowlist" â explain that `ALLOWED_TARGETS` in the worker defines the set of repos the system can act on (currently `rsterenchak/toDoList_TOP` and `rsterenchak/matchingGame-test`). Document the recipe for adding a third: (a) add `{ repo, filePath }` to `ALLOWED_TARGETS` in the worker, (b) ensure the GitHub PAT has Contents:write and Actions:read+write scope on the new repo, (c) add a `scripts/gen-src-manifest.js` (or `.cjs` for ESM repos) and a deploy.yml step in the new repo so its manifest is published, (d) `npm run deploy` the worker. Note: use `.cjs` extension when the repo's package.json has `"type": "module"`. (3) "Three context modes" â document the layered context system: (a) ACTIVE REPO reframe (sent via `body.repo`; switches conversation frame; cheap), (b) attached files (sent via `body.attach_files`; loads real source content; bounded at 5 files / 40KB each / 80KB total), (c) iterate seed (sent via `body.entry_id` on turn 1 only; loads diff + sliced post-merge code from a merged PR). State the typical input token cost for each mode (reframe ~1.7k, attach ~5-25k, iterate ~12-20k) and which UI surface invokes each. Acceptance: after merge, grep CLAUDE.md for `ALLOWED_TARGETS`, `attach_files`, `iterate seed`, `todo-injector-worker`, `claude-run.yml` â all five terms must appear. Each section should be 2-5 paragraphs, prose with occasional bullet points where they clarify; no need for headers beyond the section titles.
  - File: `CLAUDE.md`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 0459f9dd-4341-4180-88b3-4f5f84f4a7be -->

- [x] **[MEDIUM]** Update CLAUDE.md — Part 2 of 3: key files, hard rollback, worker location and routes — Completed: 2026-06-02
  - Type: feature
  - Description: Second of three documentation entries updating CLAUDE.md. Append these sections to whatever already exists in CLAUDE.md (do not remove or rewrite earlier content). DO NOT invent details — when uncertain, point at the source file rather than hallucinate. REQUIRED SECTIONS: (4) "Key files in this repo" — `toDoList_main/src/claudeSheet.js` (the in-app Claude assistant sheet — chat, author flow, Runs tab, iterate, layout-inspector wiring, file-attach picker, workspace pill), `toDoList_main/src/inject.js` (worker calls — inject, dispatch, chat, status poll, entry-id minting), `toDoList_main/src/layoutInspect.js` (serializes an element's live computed layout for the inspector), `toDoList_main/src/main.js` (DOM/UI/event wiring, very large — grep with offset/limit), `toDoList_main/src/listLogic.js` (data model — ALL mutations route through here), `toDoList_main/src/style.css` (all styling + breakpoints), `toDoList_main/scripts/gen-src-manifest.js` (manifest generator run by deploy.yml on each deploy). One descriptive sentence each. (5) "Hard rollback" — if a shipped change is bad in a way fix-forward via iterate cannot quickly cure: open the offending PR in the GitHub mobile app or web, tap Revert, merge the revert PR, deploy.yml runs automatically, rollback ships in ~2 minutes. State explicitly that there is no in-app revert button by design (~95% fix-forward rate makes an unused safety button decay; for the ~5% case, use this manual path). IMPORTANT WARNING to add: when reverting via GitHub mobile, revert ONLY the original feature PR, never a revert PR. Revert PRs share titles with their originals ("Revert '[Claude] feature: X'"), and reverting a revert re-applies the original change — easy to do by mistake at a glance. (7) "Worker location and routes" — `todo-injector-worker` is a SEPARATE repo (not part of toDoList_TOP). It's deployed via `npm run deploy` (Wrangler). Document the routes the worker exposes: `inject` (write to TODO.md), `dispatch` (start claude-run.yml workflow), `status` (poll a workflow run by correlation_id), `read` (read TODO.md), `chat` (Sonnet proxy; accepts `messages`, `entry_id`, `attach_files`, `repo`, `telemetry` fields), `resolve` (find a merged PR by `<!-- id: ... -->` marker in its body). Note that SYSTEM_PROMPT and ITERATE_PREAMBLE live in the worker, NOT in this repo — so changes to chat behavior require editing and redeploying the worker, not the app. Acceptance: after merge, grep CLAUDE.md for `claudeSheet.js`, `Revert`, `todo-injector-worker`, `wrangler`, and at least three of the six route names above.
  - File: `CLAUDE.md`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Update CLAUDE.md — Part 3 of 3: instrumentation, operating lessons, cross-cutting verification discipline — Completed: 2026-06-02
  - Type: feature
  - Description: Third of three documentation entries updating CLAUDE.md. Append these final two sections to CLAUDE.md, preserving earlier content. DO NOT invent details. REQUIRED SECTIONS: (6) "Instrumentation & operating lessons" — document the verification habits the build relies on: (a) `npx wrangler tail` to see `chat usage { iterate_seed: true|false, input_tokens, ... }` per turn; a healthy iterate-seed turn is ~12-20k tokens, follow-ups ~1-2k; (b) DevTools console snippet for service worker state: `navigator.serviceWorker.getRegistration().then(r => console.log({waiting: !!r.waiting, installing: !!r.installing, active: r.active?.scriptURL}))` (in raw form so it's copy-pasteable); (c) view-source on live HTML to verify content-hashed bundle filenames (proves SW revisions on each deploy); (d) probe-injection into `todoapp_claudeRuns` localStorage for testing reconcile logic. State the principle that earned each: "Green Shipped status + a changelog entry is NOT proof a fix worked — only behavior on real data is. Always instrument, never trust the surface." Mention specifically that retroactive promotion and run dedup both have regression tests because the no-op pattern shipped once and we don't trust ourselves to catch it by code review alone. (8) "Cross-cutting verification discipline for structural UI changes" — when describing a UI move/relocate/restructure in chat, the system prompt now instructs the chat agent to lead with proactive enumeration of cross-cutting concerns (listeners, state reads, paired UI elements, ARIA wiring) BEFORE drafting an entry. User's role in that flow: verify the enumeration is complete, add anything Sonnet missed from local knowledge, confirm. Outcome: defensive entries with explicit acceptance criteria. Briefly document the failure modes this defends against — early structural-UI moves (workspace pill, file picker) broke load-bearing flows like injection because their entries didn't name what behaviors depended on the moved element. Subsequent structural moves shipped clean once the cross-cutting clause produced enumerations like "tapping the pill must still open the workspace menu; selecting a repo must still update activeChatRepo; the chat → inject flow must still send body.repo correctly." Acceptance: after merge, grep CLAUDE.md for `wrangler tail`, `cross-cutting`, `regression test`, and `proactive enumeration` — all four must appear.
  - File: `CLAUDE.md`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 867daff7-8baf-4d95-91a3-d4a24bbafe40 -->

- [x] **[LOW]** Render the Inject & run card from user-pasted entries, not only Sonnet-emitted ones — Completed: 2026-06-01
  - Type: feature
  - Description: The chat surface currently creates a "drafted entry" card with an Inject & run button only when an ASSISTANT reply contains a fenced ```md block. When the user pastes a pre-drafted entry directly into the composer as a USER message, no card appears because the extractor only scans assistant turns. This forces the user to either re-prompt Sonnet to re-emit the entry in fenced form (extra round trip, can truncate on long entries) or paste into TODO.md manually. Update the chat surface so user messages containing a fenced ```md entry block ALSO trigger the draft-card render path. Specifically: when `appendMessageBubble('user', text)` runs, after the bubble is in the DOM, scan `text` with the same `extractDraftedEntry` function used for assistant replies; if a valid entry is found, call `renderDraftedEntryCard(draft)` the same way the assistant path does. The card renders below the user message, and tapping Inject & run goes through the existing `shipDraftedEntry` flow. Cross-cutting concerns: (a) the existing assistant-reply path must continue working unchanged — both paths can produce cards independently; (b) if the user paste contains MULTIPLE ```md blocks, only the first should render a card (matching how assistant replies are handled); (c) the entry-id mint on Inject & run must still work — the entry's parsed text is what matters, not which message it came from; (d) if the user paste is also part of a longer message (e.g. "here's an entry: ```md ... ``` thoughts?"), the card still renders cleanly below the bubble; (e) the existing `extractDraftedEntry` regex must NOT be modified — both code paths share it, so changes there could affect assistant-reply behavior. Tests must cover: a user-pasted entry produces a card with Inject button; the card uses the same shipDraftedEntry path on tap; an assistant-emitted entry still produces a card unchanged.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/tests/claudeSheet.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: ff646576-fed1-41ab-ac58-14ee9ccef254 -->

- [x] **[MEDIUM]** Render a "📎 Attach X?" suggestion chip below assistant messages when the worker returns suggested_files — Completed: 2026-06-01
  - Type: feature
  - Description: The worker's chat response now includes a `suggested_files: [<paths>]` field — populated when Sonnet's reply contains need-language ("I'd need to see X", "can you paste", "I don't have access to") AND the named files exist in the active repo's manifest, AND the user did NOT manually attach files, AND the conversation is not in iterate mode. This is the worker side of a one-tap context-attachment workflow ("Lever 4"). Implement the app side: render the suggestion as a small chip below the assistant message, accept on tap by adding to a separate `suggestedAttachedFiles` array (so the worker can apply its 20KB suggestion-cap instead of the 40KB manual-attach cap), and pipe that array as a new `suggested_attach_files` field on subsequent chat requests. Implementation pieces: (1) In `inject.js` `chatWithWorker`, parse the `suggested_files` array from the worker response and return it alongside the reply — extend the return shape from `reply` (string) to `{reply, suggestedFiles}` (object), and update every call site (`requestAssistantReply` is the only caller in `claudeSheet.js`) to handle the new shape; (2) Add a module-level `suggestedAttachedFiles = []` array in `claudeSheet.js`, alongside the existing `attachedFiles` array; (3) In `requestAssistantReply`, after rendering the assistant bubble and the existing draft-card / inspect-button rendering, if the returned `suggestedFiles.length > 0` call a new `renderSuggestedFilesRow(suggestedFiles)` function that appends a chip row to `#claudeChatSurface` — same parent the assistant bubble appends to so it visually pairs with the reply; (4) Each chip displays "📎 Attach `<basename>`?" using `fileBasename(path)` for display, with the full path stored in `dataset.path`. On tap, call a new `acceptSuggestedFile(path)` function that adds path to `suggestedAttachedFiles`, replaces the chip's content with "✓ Attached `<basename>`" (or removes the chip and adds a regular chip to the composer area, your choice — the simpler removeand-replace is fine), and re-renders any composer-side chip display so the user can see the file is now attached; (5) Add a small "×" dismiss button on each suggestion chip that, when tapped, removes that chip from the DOM without adding the path to `suggestedAttachedFiles` — for cases where the user doesn't want the suggested file; (6) In `chatWithWorker`, when the request payload is being assembled, add `suggested_attach_files: suggestedAttachedFiles.slice()` alongside `attach_files: attachedFiles.slice()` — passing both arrays as separate fields so the worker applies the correct cap to each; (7) The new `clearAttachments()` function in `claudeSheet.js` (called on +New and on workspace switch via the existing reset path) should ALSO clear `suggestedAttachedFiles = []` alongside `attachedFiles = []`, otherwise stale suggestion-attachments persist across conversations and accumulate token cost silently. Cross-cutting concerns the move must preserve: (a) Manual attach via the picker continues to work identically — `attachedFiles` array, picker UI, paperclip button, and chip rendering in the composer area are all unchanged; (b) `suggestedAttachedFiles` is a separate channel — its files travel under `suggested_attach_files` in the request body and get the 20KB cap on the worker side, never co-mingled with manual-attached files; (c) The worker's trigger discipline ("no suggestion when user manually attached") means once a suggestion is accepted, the worker stops surfacing MORE suggestions on top of it — the app doesn't need to enforce this client-side, but it should respect the worker's `suggested_files: []` response on those turns (i.e., don't accidentally re-render a stale chip from a prior turn's response); (d) The new `renderSuggestedFilesRow` function must NOT depend on parent traversal from a specific composer parent — it queries `#claudeChatSurface` directly so it survives any future structural moves of the composer; (e) The existing `chatWithWorker` calls from the iterate seed path (turn-1 with `entryId`) and from the inspect-attach path (`sendInspectTurn`) must continue working — they don't currently use suggestions, and shouldn't break when the new return-shape lands. Add tests in `claudeSheet.test.js`: (1) A worker response with `suggested_files: ["claudeSheet.js"]` produces exactly one chip in the chat surface with text "📎 Attach `claudeSheet.js`?"; (2) Tapping the chip adds `"claudeSheet.js"` to `suggestedAttachedFiles` and removes/updates the chip's visible state; (3) The next `chatWithWorker` call includes `suggested_attach_files: ["claudeSheet.js"]` in its body (mock the fetch and inspect the request payload); (4) Tapping the dismiss "×" removes the chip WITHOUT adding to `suggestedAttachedFiles`; (5) Calling `clearAttachments()` empties both `attachedFiles` and `suggestedAttachedFiles`; (6) An existing assistant-reply path with no `suggested_files` field still renders normally without any suggestion chips appearing — back-compat check. Visual styling: the suggestion chip should look distinct from regular attach chips — slightly smaller, with the paperclip + question-mark glyph, and use the existing `claudePill` / `claudeAttachChip` styling vocabulary so it feels native. Reference: the existing manual-attach chip rendering in `renderAttachChips()` is the right styling sibling to copy from. The suggestion chip parent should be classed `.claudeSuggestionRow` and the chip itself `.claudeSuggestionChip` so CSS can target them specifically without affecting the manual-attach chips.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/inject.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/claudeSheet.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 876bd8e3-8c21-4524-ac74-c91a67d9333d -->

- [x] **[LOW]** Move suggestion chips from below the assistant message into the composer area, above the input bar — Completed: 2026-06-01
  - Type: feature
  - Description: A prior entry rendered suggestion chips (from worker's `suggested_files` response) as a row below the assistant message that prompted them. While this works, it places the chip far from where the user is about to type — forcing a scroll-up to accept. Move the suggestion chip into the existing composer chip area (the chip-intro row that displays manual-attach chips), so all attach-related chips live in one place above the input bar. Implementation: (1) Remove the `renderSuggestedFilesRow` call from `requestAssistantReply` (or wherever the chip-below-message logic currently lives) — chips no longer append to `#claudeChatSurface`; (2) Add suggestion chips to the existing chip-intro area where manual-attach chips render via `renderAttachIntro` or `renderAttachChips` — extend whichever function handles chip display to ALSO read from `suggestedAttachedFiles` and render those chips alongside manual-attach chips; (3) Visually differentiate suggestion-attached chips from manual-attach chips so users can tell them apart — small ✦ prefix on suggestion chips, or a distinct border color, or a slightly different background. Use the existing `claudeAttachChip` styling vocabulary as the sibling pattern to copy; the suggestion variant should be `claudeAttachChip--suggested` for CSS targeting. (4) The chip should still be one-tap-accept (clicking it confirms the suggestion) and dismissable (a small × removes without accepting). After acceptance, the chip transitions to look like a regular manual-attach chip — visually integrated rather than distinct, so the user sees "yes, this file is attached" without further visual noise. Cross-cutting concerns: (a) The accept tap still adds the path to `suggestedAttachedFiles` (not to `attachedFiles`) so the worker continues to apply the 20KB cap; the visual transformation is purely cosmetic and shouldn't change which array holds the file; (b) The dismiss tap still removes from suggestedAttachedFiles only, not from attachedFiles; (c) Removing a manual-attach chip via its existing × should NOT affect suggestion chips and vice versa — the two arrays remain functionally separate; (d) When the chip-intro renders, the order should be manual-attach chips first, then suggestion chips, so the user-curated set takes visual precedence; (e) `clearAttachments()` still clears both arrays — no change to reset logic. Add a test in `claudeSheet.test.js`: a worker response with `suggested_files: ["claudeSheet.js"]` produces exactly one chip in the chip-intro area (not below the assistant message); the chip is visually marked with the suggestion variant; tapping it adds to `suggestedAttachedFiles`; tapping the × removes from suggestedAttachedFiles only.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/claudeSheet.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: ef2fe21e-e89e-43b4-b832-63e4eed3cae1 -->

- [x] **[LOW]** Consolidate composer chip-area rendering into a single function handling all chip types — Completed: 2026-06-02
  - Type: feature
  - Description: After the suggestion-chip-move entry lands, the composer area renders chips from two sources: manual-attached files (in `attachedFiles`) and suggestion-attached files (in `suggestedAttachedFiles`). Currently these may be rendered by separate functions or by a function that branches internally on the array source. This entry consolidates them into a single rendering function so future chip types (file attachments, layout snapshots, paste-detected entries — anything chip-shaped) have a clean home, and so the composer chip-area's behavior is easier to reason about as one unit. This is purely a refactor — no user-visible change. Implementation: (1) Identify whichever function currently renders the chip-intro area (likely `renderAttachIntro` or `renderAttachChips` after the suggestion move lands — check the exact function after the prior entry merges, since structure may shift). Rename it to `renderComposerChipArea` (or keep its current name if it's already unified-shaped). (2) The function should read from both `attachedFiles` and `suggestedAttachedFiles`, produce chips in a defined order (manual-attach chips first, then suggestion chips — same order as the prior entry's behavior), and clear and re-populate the chip-area DOM container in a single pass. (3) All callers that currently invoke chip rendering (after add, after remove, after accept-suggestion, after dismiss-suggestion, after clearAttachments) should call the single consolidated function instead of any older per-array variants. (4) Inline any single-array helper functions (e.g. `renderAttachChips` if it exists separately) into the consolidated function; remove the now-unused helpers from the file. (5) The chip *element-builder* (the function that builds an individual chip DOM element with its × button) can stay separate from the *area renderer* — that's a useful internal split. The consolidation is at the AREA level, not the per-chip level. (6) Each chip element should still encode its source via a data attribute (`data-source="manual"` or `data-source="suggestion"`) so the dismiss-handler can route to the correct array. Cross-cutting concerns the refactor must preserve EXACTLY (this is a refactor — behavior must not change): (a) Manual-attach chips still appear when files are added via the picker, still display the existing styling, still respond to the × dismiss button by removing from `attachedFiles`; (b) Suggestion-attach chips still appear when a suggestion is accepted, still display the suggestion variant styling (`claudeAttachChip--suggested` or whatever the prior entry shipped), still respond to their × by removing from `suggestedAttachedFiles`; (c) The chip ordering (manual first, then suggestion) is preserved; (d) Empty state — when both arrays are empty, the chip-intro area renders empty exactly as it does today (or hides entirely if that's the current behavior); (e) `clearAttachments()` still clears both arrays and triggers a re-render that shows the empty state; (f) `+ New` and workspace-switch still reset both arrays and the chip area renders empty afterward; (g) The function that sends chat requests (`chatWithWorker` or wherever the request body is assembled) STILL reads from `attachedFiles` and `suggestedAttachedFiles` as separate request-body fields — the consolidation is in rendering only, NOT in state; (h) The 20KB suggestion-attach cap and 40KB manual-attach cap on the worker side still apply correctly — the data path is unchanged. Tests in `claudeSheet.test.js` must pass without modification — if any existing test breaks because it asserted against the old function name, update only the function-name references, NEVER the behavioral assertions. Add a new test that asserts the unified rendering: with `attachedFiles = ["foo.js"]` and `suggestedAttachedFiles = ["bar.js"]`, the chip area renders exactly 2 chips, in order: manual chip for foo.js first, suggestion chip for bar.js second, each with the appropriate source data attribute. Add another test: tapping the × on the manual chip removes from `attachedFiles` only (not from `suggestedAttachedFiles`); tapping × on the suggestion chip removes from `suggestedAttachedFiles` only.
  - File: `toDoList_main/src/claudeSheet.js`, `toDoList_main/tests/claudeSheet.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 32cd39b6-627c-4c29-885d-3b2d2db296d4 -->
