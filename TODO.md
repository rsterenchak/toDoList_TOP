# TODO List

## Bugs

- [ ] **[MEDIUM]** Add first-run welcome carousel for mobile new users
  - Description: First-time mobile users currently land on a near-empty Void screen (ghost mascot + "Welcome." + a single "+ New project" button) with no orientation to projects, todos, gestures, music, settings, or the slide-out menu. Add a full-screen carousel intro that runs the first time the app loads on a touch device with no saved data. Proposed cards: (1) "Welcome to Void" — ghost mascot, short tagline, three cards then you're in; (2) "Projects & todos" — explain projects as buckets, show the tap-to-check / tap-date-to-reschedule / tap-▾-to-expand gestures with small inline screenshots or icons; (3) "Menu, music & settings" — show the ≡ icon position and call out that projects, the music toggle, and the settings toggle all live inside the slide-out menu; (4) "You're set" — closer card that drops the user into the seeded "Getting started" sample project (same `todoapp_sampleSeeded` seeding as the desktop tour entry, so the targets exist when the user lands). Each card has a centered illustration, headline, one short paragraph of body copy, pagination dots, a "Next ›" pill button at the bottom, and a "Skip" link in the top-right corner that completes the flow immediately. Card transitions use a horizontal swipe (touch) or a Next button tap; swipe gestures are wired through the same `touchstart`/`touchmove`/`touchend` pattern used elsewhere in `main.js`. Persist a `todoapp_onboardingComplete` flag in `localStorage` so the carousel never auto-runs again. Detection: trigger on first load when `(pointer: coarse) AND viewport width < 768px` and no completion flag is set; desktop continues to use the existing spotlight tour entry. Expose a "Replay welcome carousel" entry inside the settings toggle menu (in the slide-out menu, alongside the existing View / Appearance sections) — replay re-runs the carousel but does not re-seed the sample project (only first run does). Constraints: no new dependencies (build the carousel as a single fixed-position container with translateX-shifted card slides in vanilla JS), close on Escape (for users with attached keyboards) and on the explicit Skip link only — no backdrop click since the carousel fills the viewport, and mobile inputs inside callouts (if any) need `font-size: 16px+` to avoid iOS Safari auto-zoom. Out of scope: desktop changes (already covered in the desktop spotlight tour entry), tablet-specific layout (use desktop flow at ≥ 768px). The carousel UI and replay wiring live in `main.js` / `style.css`; the `todoapp_onboardingComplete` flag check and viewport/pointer detection happen in `index.js`; sample-project seeding is shared with the desktop entry in `listLogic.js`.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/index.js`, `toDoList_main/src/style.css`, `toDoList_main/src/listLogic.js`
  - Completed: YYYY-MM-DD (PR #<number>)

## Features

- [x] **[MEDIUM]** Add first-run spotlight coachmark tour for new users (desktop)
  - Description: First-time users currently land on a near-empty Void screen (ghost + "Welcome." + a single "+ New project" button) and have to discover the rest of the app on their own — projects, due-date pills, expand chevron, Pomodoro, theme toggle, sidebar. Replace that cold start with a dismissible multi-step coachmark tour that runs the first time the app loads with no saved projects. Each step dims the rest of the UI with a full-screen overlay, cuts out a single highlighted element, and anchors a small purple-bordered callout next to it (`STEP N OF M`, one sentence of guidance, Skip / pagination dots / Next ›). Proposed steps: (1) the sidebar "+" project button, (2) the todo input row, (3) the due-date pill on the first todo row, (4) the expand-description chevron, (5) the Pomodoro/music navbar surface. The tour advances on Next or when the user actually interacts with the highlighted element (clicks +, types in the input, etc.) and ends on the last step with a "You're set" closer that drops them into a normal first project. Persists a `todoapp_onboardingComplete` flag in localStorage so it never auto-runs again; expose a "Replay welcome tour" entry in the sidebar settings panel for re-triggering. Constraints: no new dependencies (build the overlay + cutout + callout in vanilla JS — a fixed full-viewport `<div>` with a `clip-path` or four-rect mask around the target's `getBoundingClientRect`), close on Escape and on backdrop click outside the callout, recompute target geometry on `resize` / `scroll`, and keep the overlay above all existing popovers/modals. Mobile is intentionally out of scope for this entry — a follow-up will adapt the flow for narrow viewports.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/index.js`, `toDoList_main/src/style.css`, `toDoList_main/src/listLogic.js`
  - Completed: 2026-05-18

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
