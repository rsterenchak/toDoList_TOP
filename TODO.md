# TODO LIST

- [ ] **[HIGH]** Register the injection points the app lost when the Agent view was severed
  - Type: bug
  - Description: Flagging a task queues a `triaging` row and never fires `claude-triage.yml`, so Generate has been silently broken since `main.js` was severed from `agentView.js`. The row layer dispatches through `fireTriageSweep(projectName)` in `agentQueueStore.js`, which delegates to a `_triageDispatcher` registered by `setTriageDispatcher` — whose only caller is `agentView.js`, which nothing loads any more. So `_triageDispatcher` is null, `fireTriageSweep` resolves to null, `dispatchTriage` is never called, and `startSweepTracking` never runs, which is also why the nav dot never lights for a sweep. An audit found three further registrars in the same state: `setTriageInFlight`, also called only from the severed view, and `setItemPhaseResolver` and `setBlockedItemResolver` in `taskFilter.js`, which have NO caller in any module. This entry closes the class rather than one instance.
  - Behavior: Flagging a task from the row's Generate button or the ASKING answer path dispatches a real `claude-triage.yml` run, the nav working dot lights while it is in flight and clears when it settles, and the queue row leaves `triaging` normally. The task filter pills and the blocked chip resolve each task's phase and blocked state through their intended path. No feature gains or loses capability — this restores wiring that was silently lost.
  - Implementation notes:
    - Register a boardless triage dispatcher from `agentWiring.js`, the module that already loads on boot. Reproduce what `agentView.js` did: resolve the project id, mint a correlation id, call `dispatchTriage`, then `startSweepTracking`. Use only acyclic imports — `dispatchTriage` and `mintEntryId` from `inject.js`, `resolveDispatchTarget` from `dispatchDraft.js`, and `startSweepTracking` / `setTriageDispatcher` from the store. Do NOT import `agentView.js`, and verify no cycle forms.
    - `setTriageInFlight` is in the same state. Either register it from `agentWiring.js` alongside the dispatcher, or establish that it is genuinely board-only and remove it. Say which and why.
    - `setItemPhaseResolver` and `setBlockedItemResolver` have no caller anywhere. Determine what they were meant to inject, and whether the phase filter pills and the blocked chip are currently working through a fallback or are quietly broken. Then either wire them from the module that owns the resolution, or delete them as dead API. Report which, with evidence for the filters' current behavior.
    - Leave `agentView.js`'s own registrations in place. It still self-registers when a test mounts the board, and the deletion sequence removes them with the file.
    - THE EXISTING TESTS CANNOT VALIDATE ANY OF THIS. The triage-dispatch tests import `agentView.js` for its side effects, so they exercise a wired state the app never reaches — which is exactly why this shipped. Add tests that assert each registration happens through the boot path WITHOUT mounting the board.
    - Manual verification is required before this is done: flag a task in the app, confirm a real `claude-triage.yml` run appears in Actions, confirm the nav dot lights and clears, and click through the ALL / IDEAS / RUNNING / DONE pills and the blocked chip to confirm their counts are correct. Report all four results.
    - Audit for any remaining registrar whose only caller is `agentView.js` or which has no caller at all, and report the list even if empty. This is the fourth regression from the same severing; the goal is that it is the last.
  - Out of scope: Deleting `agentView.js`, its CSS, or any test file. Retiring the board test files. The trackers' cadence and settle logic. `derivePhase`, the phase vocabulary, and the queue pipeline's semantics.
  - File: `toDoList_main/src/agentWiring.js`, `toDoList_main/src/agentQueueStore.js`, `toDoList_main/src/taskFilter.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 34d5b79a-f145-4541-a327-ed0f650cc97d -->
