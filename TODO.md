# TODO List

## Bugs

- [x] **[HIGH]** Skip saveToStorage in sortCompletedToBottom when the sort is a noop
  - Description: Switching to a different project in the sidebar causes the Drive sync indicator to flip amber within ~10 seconds and triggers an auto-push to Drive, despite the user only changing their view — no todo was added, removed, edited, or reordered. Live stack trace captured the exact path: clicking a project fires `addToDos_restore (toDoRow.js:1417)` → `sortCompletedToBottom (listLogic.js:482)` → `saveToStorage (listLogic.js:82)` → bumps `lastLocalMutationAt = now` → dispatches `driveSyncStateChanged` → `scheduleAutoSync` → 10s debounce → `performAutoSync` → `exportTodosToDrive` → unnecessary Drive upload of unchanged content. Root design problem: `sortCompletedToBottom` writes to storage unconditionally on every call, even when the array order is already correct. From a render path (restore-from-storage, switch-project rebuild) the data isn't actually changing — the sort is just defensive normalization against data that came off disk already sorted. From a user-action path (checking off a todo) the sort IS a real reorder and should persist. Earlier work threaded `{ fromSync: true }` through the import path so render-time sorts during import wouldn't bump the mutation timestamp; that fix doesn't help here because project-switch also goes through `addToDos_restore` and the flag isn't passed (and threading the flag through every render path is fragile — it's an audit miss waiting to happen on the next render entry point). Fix at the source instead: make `sortCompletedToBottom` a no-op when the sort doesn't change order. In `listLogic.js`, modify `sortCompletedToBottom`: compute the sorted array as today, but BEFORE writing it back to the data model and calling `saveToStorage`, compare the new order against the existing order. If they're identical (same items, same positions), skip both the assignment and the save. If they differ, proceed as today. Comparison should be O(n) — walk both arrays once and check each position's identity (reference equality on the item objects is sufficient; we're checking whether positions moved, not whether contents changed). The `opts` parameter from the earlier `fromSync` work stays intact and forwards to `saveToStorage` when a save IS warranted, so the post-import path still passes `{ fromSync: true }` correctly through its own legitimate sort. Practical effect: render-time sorts against already-sorted data become silent no-ops; user-action sorts (checking off a todo, importing data that wasn't pre-sorted) still write and bump the mutation timestamp. The indicator stops flipping amber on project switch and auto-sync stops issuing pointless Drive uploads. Acceptance criteria: (1) the monkey-patched `Storage.setItem` diagnostic shows zero `todoapp_` writes when switching between projects whose data is already correctly sorted; (2) checking off a todo (a real reorder) still writes and bumps `lastLocalMutationAt` and the indicator correctly flips amber; (3) importing a backup whose data is NOT pre-sorted (mix of completed and incomplete items in arbitrary order) still triggers exactly one sort-driven write per project at the end of the import pipeline; (4) auto-sync no longer fires after a project switch — the `driveSyncStateChanged` event doesn't dispatch because no mutation occurred. Tests: in `listLogic.test.js`, pin three cases: (a) `sortCompletedToBottom` against an already-sorted project writes nothing — assert no `saveToStorage` call via a spy and `lastLocalMutationAt` is unchanged; (b) `sortCompletedToBottom` against an unsorted project writes once and reorders the array correctly; (c) `sortCompletedToBottom({ fromSync: true })` against an unsorted project writes once but does NOT bump `lastLocalMutationAt` (the fromSync semantics are preserved). In `driveAutoSync.test.js`, add a regression: simulate a project-switch render by calling `addToDos_restore` for each row of a pre-sorted project, then assert `_autoSyncArmed` did not transition to a pending-sync state and `scheduleAutoSync` was not called. Out of scope: extending the static-scan test from the earlier saveToStorage-callers work to enforce a "no-op when unchanged" contract on every caller — that's a useful generalization but adds complexity beyond this entry; flagged as a separate LOW follow-up. Changelog: add a 1.1 entry "Fixed: switching to another project no longer triggers an unnecessary sync to Drive (was caused by a defensive sort step that persisted even when nothing actually changed)."
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/src/changelog.js`, `toDoList_main/tests/listLogic.test.js`, `toDoList_main/tests/driveAutoSync.test.js`
  - Completed: 2026-05-23

- [ ] **[LOW]** Extend saveToStorage static-scan test to enforce noop-when-unchanged or fromSync contract
  - Description: The "switching projects triggers an unnecessary Drive sync" bug was the third instance of the same underlying class of regression — a non-user-action code path reaches `saveToStorage` and unconditionally bumps `lastLocalMutationAt`, which fools the auto-sync indicator into thinking real data changed. Prior fixes have addressed each instance individually: the post-import `replaceAllProjects` chain via `{ fromSync: true }` opts; the post-import row-rebuild chain (`restoreFromStorage` → `addToDos_restore` → `sortCompletedToBottom`) via the same flag; and most recently, `sortCompletedToBottom` itself learning to skip the write when the sort is a noop. An earlier static-scan test (`saveToStorageCallers.test.js`) enforces that every direct caller of `saveToStorage` in `listLogic.js` either accepts an `opts` parameter and forwards it, or carries a `// @user-mutation-only` annotation. That test catches the most common failure mode (a new caller forgets to thread the flag) but doesn't catch the failure mode that bit us this time: a caller that has the flag plumbed correctly but is invoked from a render path that legitimately can't pass the flag (because the render entry point doesn't know whether it's a real mutation or just a redraw). Extend the static-scan test to also enforce a noop-when-unchanged contract on callers that are invoked from both render and mutation paths. Specifically: for each caller of `saveToStorage` in `listLogic.js`, classify the function as one of three categories — `user-mutation-only` (always a real mutation, no defensive check needed), `sync-safe` (accepts `opts` and forwards), or `defensive-normalize` (called from both render and mutation paths, must skip the write when no observable change occurred). Add a `// @category: ...` annotation requirement above each `saveToStorage` caller; the static-scan test parses the annotation and asserts the function body matches: `user-mutation-only` calls `saveToStorage()` with no opts; `sync-safe` accepts `opts` and forwards via `saveToStorage(opts)`; `defensive-normalize` contains a guard expression (regex match for an `if (...changed...)` or `if (...!==...)` pattern wrapping the `saveToStorage` call) AND accepts `opts` for the cases where a real change DID occur. The annotation is the source of truth — when a developer adds a new caller, they pick a category; the test verifies the function body matches the chosen category. Baseline annotations to add for current callers (verify against actual source — function names may differ slightly): `addProject` → user-mutation-only; `removeProjectByTitle` → user-mutation-only; `editProjectTitle` → user-mutation-only; `addToDo` → user-mutation-only; `removeToDoByTitle` → user-mutation-only; `editToDo` → user-mutation-only; `toggleCompleted` → user-mutation-only; `reorderProjects` → user-mutation-only; `reorderToDos` → user-mutation-only; `replaceAllProjects` → sync-safe; `sortCompletedToBottom` → defensive-normalize (per the recent fix). Implementation notes: the static-scan test reads `listLogic.js` source as a string, finds every `saveToStorage(` call site, walks up to the containing function declaration, extracts the `// @category:` annotation from the line above the function, and matches the function body against the category's required shape. Annotations missing or mismatched fail CI. Out of scope: refactoring `saveToStorage` into separate `saveToStorageForUserMutation` and `saveToStorageForSync` exports — that would eliminate the need for the flag-passing pattern entirely but is a much larger change with no immediate behavior benefit. Acceptance criteria: (1) running the test suite passes with the current source as the baseline; (2) deliberately introducing a new direct caller of `saveToStorage` without an annotation fails CI with a clear error message naming the file, line, and the three valid category values; (3) deliberately changing a `defensive-normalize` function's body to remove the guard expression while keeping its annotation fails CI; (4) adding a `// @category: user-mutation-only` annotation to a function whose body forwards `opts` (i.e. the body looks sync-safe but is annotated user-only) fails CI. Tests: extend `saveToStorageCallers.test.js` with the annotation-parsing and body-shape-matching logic; add a fixture-based unit test for the parser itself (small synthetic source strings exercising each category and each failure mode). Changelog: not needed — this is internal tooling and has no user-visible effect.
  - File: `toDoList_main/src/listLogic.js`, `toDoList_main/tests/saveToStorageCallers.test.js`
  - Completed: YYYY-MM-DD (PR #<number>)

## Features

- [x] **[MEDIUM]** Remove LOCAL JSON export/import and add Drive sync-state indicator to ghost icon + DRIVE section header
  - Description: The LOCAL JSON export/import path is no longer used — remove it entirely and replace the footer's last-exported label with a Drive-sync-state indicator surfaced on the ghost icon and the ghost menu's DRIVE section header. Detect "behind" by comparing the local `lastDriveExportedAt` timestamp (already persisted by the existing Drive export flow) against the most recent Drive file's `modifiedTime` via `queryLatestDriveFile`. State has four values: `never` (no `lastDriveExportedAt` and/or no Drive file), `synced` (local timestamp >= Drive `modifiedTime`), `behind` (Drive `modifiedTime` newer than local — implies another device pushed), and `unknown` (Drive query failed, network offline, or OAuth not yet granted). The Drive query runs on app load (one shot, silent — never triggers an OAuth consent prompt; if no cached token exists, state stays `unknown`) and again each time the ghost menu opens. Render the indicator two places: (1) a small status icon overlaid on the ghost icon in the header — `ti-cloud-check` green for synced, `ti-cloud-up` amber for behind, `ti-cloud-off` muted gray for never/unknown — and (2) the same icon + glyph next to the DRIVE section header inside the popover menu, with a short tooltip on hover/long-press giving the full message ("Up to date — last synced 5m ago" / "Drive is newer than local — pull to update" / "Not synced to Drive yet"). Clicking the ghost-icon indicator opens the ghost menu (existing behavior — no separate action). Removal scope: delete the LOCAL section heading and its two menu rows (Export, Import) plus the hidden `importFileInput`, `exportTodosToFile`, `importFromFile`, and the LOCAL ghost-menu wiring in `main.js`; delete the footer's "NEVER EXPORTED" / "EXPORTED Xm ago" label, the `lastExportedAt` localStorage key writer, and the relative-time render call wired to it; keep `exportImport.js` intact — DRIVE import depends on `importTodosFromString` and DRIVE export reuses `buildExportPayload`/`buildBaseExportFilename` from there. Update `changelog.js` with a 1.1 entry noting the LOCAL JSON paths were removed and the Drive sync indicator was added. Update relevant tests: the LOCAL-row assertions in `driveExport.test.js` ("the four data rows render in LOCAL-then-DRIVE order") need to be rewritten for the new two-row DRIVE-only menu, and the footer-export-label tests (likely in `main.js` or a dedicated test file) should be removed.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/src/driveImport.js`, `toDoList_main/src/driveAuth.js`, `toDoList_main/src/prefs.js`, `toDoList_main/src/changelog.js`, `toDoList_main/tests/driveExport.test.js`
  - Completed: 2026-05-23
     
- [x] **[MEDIUM]** Add slide-out fade completion animation to todo row checkboxes — Completed: 2026-05-22
  - Description: Clicking the row checkbox to mark a todo complete currently produces no completion animation — the row's "done" styling (strikethrough title, muted color) applies instantly, which reads as no feedback at all and makes the interaction feel flat. Add a CSS-driven slide-out fade animation (ANIM-D from the mockup round): on click, the row briefly translates ~40px to the right while fading to ~40% opacity and the title strikethrough draws in, then settles back to its final position in the standard "done" appearance. Total duration ~280ms. The animation runs in parallel with the underlying data mutation — `listLogic.toggleToDo` (or whatever the existing toggle path is) fires immediately on click without waiting on the animation, so the persisted state and the visual completion stay in sync even if the user navigates away mid-animation. Wire by adding a transient class (e.g., `.todoCompleting`) to the row element on the click handler that triggers the toggle from open → done; the class drives a CSS `@keyframes` rule that animates `transform: translateX()` and `opacity` from 0 → 40px / 100% → 40% across the first ~140ms, then back to 0 / 100% across the remaining ~140ms while the row's actual `.done` class lands. Remove the transient class via an `animationend` listener so subsequent renders don't re-fire it. Animation applies in three places — the project view `#mainList` rows, the `.todayRow.todoRowCard` rows in the Today view, and the same row style in the Calendar day-detail panel — all three already share the row markup, so a single CSS rule scoped to `.todoCompleting` covers all three surfaces. Animation only fires on the open → done transition, NOT on done → open (un-completing); un-completing applies its end state instantly so users who misclick and immediately fix it don't watch the row dance backwards. Honor `prefers-reduced-motion: reduce` — wrap the keyframes block in `@media (prefers-reduced-motion: no-preference)` so the animation is skipped entirely for users who've opted out, and the row just snaps to its done state as it does today. Test pins: clicking an open row's checkbox adds `.todoCompleting` and removes it after `animationend`; clicking a done row's checkbox does NOT add the class; with `prefers-reduced-motion: reduce` matched, the keyframes rule is not in the active stylesheet (assert via media query gate or by checking computed `animation-name` on `.todoCompleting` resolves to `none`); the data layer's toggle method fires synchronously on click regardless of animation state.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/tests/todoCompletionAnimation.test.js` (new)
  - Completed: 2026-05-22

- [x] **[MEDIUM]** Add "Export to Google Drive" option to ghost menu — Completed: 2026-05-22
  - Description: Add a new "Export to Drive" menu item directly below the existing "Export JSON" row in the ghost popover menu. Clicking it triggers a Google OAuth 2.0 flow (via Google Identity Services, using PKCE) prompting the user to sign in and authorize the app with the non-sensitive `drive.file` scope, then uploads the same JSON payload that the existing Export JSON produces to the user's Drive root via a multipart POST to `https://www.googleapis.com/upload/drive/v3/files`. On success, show a toast matching the existing toast styling with an "Open in Drive" link to the uploaded file's `webViewLink`. Re-prompt OAuth on each session — do not persist the access token to `localStorage`. Reuse the existing export filename pattern (`todoapp-export-YYYY-MM-DD.json`) and the same JSON serialization path so both export methods produce byte-identical files.
  - Behavior:
    1. User opens ghost popover menu, sees "Export to Drive" as a new row below "Export JSON".
    2. First click in a session opens Google sign-in popup; subsequent clicks in the same session reuse the in-memory token until it expires (~1 hour), at which point re-auth runs silently if possible, otherwise re-prompts.
    3. While the upload is in flight, the menu row shows a subtle loading state (text dim + spinner) and is disabled to prevent double-uploads.
    4. On success: toast "Exported to Drive" with "Open in Drive" link (opens `webViewLink` in a new tab).
    5. On failure (denied consent, network error, API error): toast with a clear error message; no partial state left behind.
  - Implementation notes:
    - Requires a Google Cloud project with an OAuth 2.0 Client ID configured for the GitHub Pages origin. Document the setup steps in a comment block at the top of the new driveExport module so a fresh fork knows what to provision.
    - Use the Google Identity Services library loaded lazily on first click via a `<script>` tag injection (mirrors the lazy-load pattern from the music player's YouTube IFrame API), not bundled with Webpack — avoids a hard dependency on a Google CDN at build time.
    - Use the `drive.file` scope only (not `drive`) so the app stays in the non-sensitive scope tier and avoids Google's security review process.
    - Extract the JSON serialization logic from the existing Export JSON handler into a shared helper so both code paths produce identical output.
    - Build the OAuth client ID into a module-level constant; do not hardcode in `main.js`.
  - Out of scope: folder picker (always dumps to Drive root), token persistence across sessions, two-way sync, import-from-Drive (separate future entry), accounts/multi-user.
  - File: `toDoList_main/src/main.js`, `toDoList_main/src/style.css`, `toDoList_main/src/driveExport.js`
  - Completed: YYYY-MM-DD (PR #<number>)

## In Progress

- [x] Fix API rate limiting issue
  - Completed: 2024-04-15
